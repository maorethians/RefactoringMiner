{
  "repo": "bazelbuild/bazel",
  "number": 20853,
  "pr_number": 20853,
  "title": "Force output checking for incremental run commands without the bytes.",
  "user": "tjgq",
  "created_at": "2024-01-11T15:48:02Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [
    "team-Remote-Exec"
  ],
  "base_commit": "68e2eb5438fa51eacfd91d7ab845f0640cbb1570",
  "merge_commit_sha": "836846bb24bb240f50c40a67a653a8e9b7b0c6d4",
  "commits": 1,
  "issue_events": [
    "review_requested",
    "head_ref_force_pushed",
    "head_ref_force_pushed",
    "head_ref_force_pushed",
    "labeled",
    "labeled",
    "mentioned",
    "subscribed",
    "closed",
    "unlabeled",
    "referenced",
    "referenced",
    "mentioned",
    "subscribed",
    "referenced",
    "referenced",
    "referenced",
    "head_ref_deleted",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed"
  ],
  "url": "https://github.com/bazelbuild/bazel/pull/20853",
  "timeline": [
    {
      "type": "description",
      "created_at": "2024-01-11T15:48:02Z",
      "user": "tjgq",
      "body": "When building without the bytes, outputs are only materialized when either (1) an action prefetches them, or (2) they've been explicitly requested. When both --remote_download_minimal and --noexperimental_check_output_files are set, this presents a problem for a build command followed by a run command for the same target: the build command won't download the outputs and the run command won't check for their presence, proceeding without them. A non-incremental run command works, because the outputs are considered top-level even in minimal mode.\r\n\r\nThis is only a problem for a run command because it exists outside of action execution, and doesn't get a chance to prefetch inputs; it would have been better to implement the run command by injecting a specially crafted action into the build graph, but that will have to wait for another day. The present fix might theoretically slow things down if output checking is truly unnecessary, but I doubt that would cause a significant regression in practice.\r\n\r\nFixes #20843."
    },
    {
      "type": "commit",
      "date": "2024-01-11T16:15:25Z",
      "sha": "5bc9620f51d254f771ff3167616be7e168f2b3c2",
      "message": "Force output checking for incremental run commands without the bytes.\n\nWhen building without the bytes, outputs are only materialized when either\n(1) an action prefetches them, or (2) they've been explicitly requested. When\nboth --remote_download_minimal and --noexperimental_check_output_files are\nset, this presents a problem for a build command followed by a run command\nfor the same target: the build command won't download the outputs and the run\ncommand won't check for their presence, proceeding without them. A\nnon-incremental run command works, because the outputs are considered\ntop-level even in minimal mode.\n\nThis is only a problem for a run command because it exists outside of action\nexecution, and doesn't get a chance to prefetch inputs; it would have been\nbetter to implement the run command by injecting a specially crafted action\ninto the build graph, but that will have to wait for another day. The present\nfix might theoretically slow things down if output checking is truly\nunnecessary, but I doubt that would cause a significant regression in\npractice.\n\nFixes #20843.",
      "author": "Tiago Quelhas",
      "author_email": "tjgq@google.com",
      "author_raw_date": "2024-01-11T15:32:05Z",
      "author_date": "2024-01-11T15:32:05Z",
      "committer": "Tiago Quelhas",
      "committer_email": "tjgq@google.com",
      "raw_date": "2024-01-11T16:15:25Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/buildtool/ExecutionTool.java\n@@ -519,11 +519,15 @@ private ModifiedFileSet startBuildAndDetermineModifiedOutputFiles(\n         startLocalOutputBuild();\n       }\n     }\n-    if (!request.getPackageOptions().checkOutputFiles\n-        // Do not skip invalidation in case the output tree is empty -- this can happen\n-        // after it's cleaned or corrupted.\n-        && !modifiedOutputFiles.treatEverythingAsDeleted()) {\n-      modifiedOutputFiles = ModifiedFileSet.NOTHING_MODIFIED;\n+    if (!request.getPackageOptions().checkOutputFiles) {\n+      // Do not skip output invalidation in the following cases:\n+      // 1. If the output tree is empty: this can happen after it's cleaned or corrupted.\n+      // 2. For a run command: so that outputs are downloaded even if they were previously built\n+      //    with --remote_download_minimal. See https://github.com/bazelbuild/bazel/issues/20843.\n+      if (!modifiedOutputFiles.treatEverythingAsDeleted() && !request.getCommandName()\n+          .equals(\"run\")) {\n+        return ModifiedFileSet.NOTHING_MODIFIED;\n+      }\n     }\n     return modifiedOutputFiles;\n   }\nsrc/test/shell/bazel/remote/build_without_the_bytes_test.sh\n@@ -2210,4 +2210,42 @@ EOF\n     //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n }\n \n+function test_incremental_run_command_with_no_check_output_files() {\n+  # Regression test for https://github.com/bazelbuild/bazel/issues/20843.\n+  cat > BUILD <<'EOF'\n+genrule(\n+  name = \"gen\",\n+  outs = [\"out.txt\"],\n+  cmd = \"touch $@\",\n+)\n+sh_binary(\n+  name = \"foo\",\n+  srcs = [\"foo.sh\"],\n+  data = [\"out.txt\"],\n+)\n+EOF\n+  cat > foo.sh <<'EOF'\n+#!/bin/bash\n+if ! [[ -f \"$0.runfiles/_main/out.txt\" ]]; then\n+  echo \"runfile $0.runfiles/_main/out.txt not found\" 1>&2\n+  exit 1\n+fi\n+EOF\n+  chmod +x foo.sh\n+\n+  CACHEDIR=$(mktemp -d)\n+  FLAGS=(--disk_cache=\"$CACHEDIR\" --remote_download_minimal --noexperimental_check_output_files)\n+\n+  # Populate the disk cache.\n+  bazel build \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n+\n+  # Clean build. No outputs are considered top-level, so nothing is downloaded.\n+  bazel clean \"${FLAGS[@]}\"\n+  bazel build \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n+\n+  # Incremental run. Even though output checking is disabled, invalidation must\n+  # must still occur to force them to be downloaded.\n+  bazel run \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to run //:foo\"\n+}\n+\n run_suite \"Build without the Bytes tests\"",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/buildtool/ExecutionTool.java",
          "patch": "@@ -519,11 +519,15 @@ private ModifiedFileSet startBuildAndDetermineModifiedOutputFiles(\n         startLocalOutputBuild();\n       }\n     }\n-    if (!request.getPackageOptions().checkOutputFiles\n-        // Do not skip invalidation in case the output tree is empty -- this can happen\n-        // after it's cleaned or corrupted.\n-        && !modifiedOutputFiles.treatEverythingAsDeleted()) {\n-      modifiedOutputFiles = ModifiedFileSet.NOTHING_MODIFIED;\n+    if (!request.getPackageOptions().checkOutputFiles) {\n+      // Do not skip output invalidation in the following cases:\n+      // 1. If the output tree is empty: this can happen after it's cleaned or corrupted.\n+      // 2. For a run command: so that outputs are downloaded even if they were previously built\n+      //    with --remote_download_minimal. See https://github.com/bazelbuild/bazel/issues/20843.\n+      if (!modifiedOutputFiles.treatEverythingAsDeleted() && !request.getCommandName()\n+          .equals(\"run\")) {\n+        return ModifiedFileSet.NOTHING_MODIFIED;\n+      }\n     }\n     return modifiedOutputFiles;\n   }"
        },
        {
          "file": "src/test/shell/bazel/remote/build_without_the_bytes_test.sh",
          "patch": "@@ -2210,4 +2210,42 @@ EOF\n     //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n }\n \n+function test_incremental_run_command_with_no_check_output_files() {\n+  # Regression test for https://github.com/bazelbuild/bazel/issues/20843.\n+  cat > BUILD <<'EOF'\n+genrule(\n+  name = \"gen\",\n+  outs = [\"out.txt\"],\n+  cmd = \"touch $@\",\n+)\n+sh_binary(\n+  name = \"foo\",\n+  srcs = [\"foo.sh\"],\n+  data = [\"out.txt\"],\n+)\n+EOF\n+  cat > foo.sh <<'EOF'\n+#!/bin/bash\n+if ! [[ -f \"$0.runfiles/_main/out.txt\" ]]; then\n+  echo \"runfile $0.runfiles/_main/out.txt not found\" 1>&2\n+  exit 1\n+fi\n+EOF\n+  chmod +x foo.sh\n+\n+  CACHEDIR=$(mktemp -d)\n+  FLAGS=(--disk_cache=\"$CACHEDIR\" --remote_download_minimal --noexperimental_check_output_files)\n+\n+  # Populate the disk cache.\n+  bazel build \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n+\n+  # Clean build. No outputs are considered top-level, so nothing is downloaded.\n+  bazel clean \"${FLAGS[@]}\"\n+  bazel build \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to build //:foo\"\n+\n+  # Incremental run. Even though output checking is disabled, invalidation must\n+  # must still occur to force them to be downloaded.\n+  bazel run \"${FLAGS[@]}\" //:foo >& $TEST_log || fail \"Failed to run //:foo\"\n+}\n+\n run_suite \"Build without the Bytes tests\""
        }
      ]
    },
    {
      "type": "review",
      "id": 1815974732,
      "created_at": "2024-01-11T16:18:22Z",
      "user": "tjgq",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 1449096265,
      "created_at": "2024-01-11T16:18:22Z",
      "user": "tjgq",
      "body": "I don't think I can make the check narrower, because the remote options can change between builds without invalidating the Skyframe state. (But maybe you can see a way.)",
      "path": "src/main/java/com/google/devtools/build/lib/buildtool/ExecutionTool.java",
      "diff_hunk": "@@ -519,11 +519,15 @@ private ModifiedFileSet startBuildAndDetermineModifiedOutputFiles(\n         startLocalOutputBuild();\n       }\n     }\n-    if (!request.getPackageOptions().checkOutputFiles\n-        // Do not skip invalidation in case the output tree is empty -- this can happen\n-        // after it's cleaned or corrupted.\n-        && !modifiedOutputFiles.treatEverythingAsDeleted()) {\n-      modifiedOutputFiles = ModifiedFileSet.NOTHING_MODIFIED;\n+    if (!request.getPackageOptions().checkOutputFiles) {\n+      // Do not skip output invalidation in the following cases:\n+      // 1. If the output tree is empty: this can happen after it's cleaned or corrupted.\n+      // 2. For a run command: so that outputs are downloaded even if they were previously built\n+      //    with --remote_download_minimal. See https://github.com/bazelbuild/bazel/issues/20843.\n+      if (!modifiedOutputFiles.treatEverythingAsDeleted() && !request.getCommandName()\n+          .equals(\"run\")) {",
      "start_line": 527,
      "original_start_line": 527,
      "start_side": "RIGHT",
      "line": 528,
      "original_line": 528,
      "side": "RIGHT",
      "original_position": 15,
      "position": 15,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 1817577043,
      "created_at": "2024-01-12T07:43:44Z",
      "user": "coeuvre",
      "state": "APPROVED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 1889586622,
      "created_at": "2024-01-12T16:16:49Z",
      "user": "tjgq",
      "body": "@bazel-io fork 7.0.1"
    },
    {
      "type": "comment",
      "id": 1904969050,
      "created_at": "2024-01-22T22:51:26Z",
      "user": "iancha1992",
      "body": "@bazel-io fork 7.1.0"
    },
    {
      "type": "comment",
      "id": 2373335429,
      "created_at": "2024-09-25T07:55:45Z",
      "user": "tomdegoede",
      "body": "@tjgq This change slows down our bazel run invocations by 5 seconds compared to bazel build. I think your theoretical case of switching remote options between builds will be very uncommon in practice, where these settings are pinned in the .bazelrc. Could you reconsider narrowing down the check to also return NOTHING_MODIFIED when remote download is set to all? This would allow us to speed up common dev tasks by 5 seconds. "
    },
    {
      "type": "comment",
      "id": 2457476835,
      "created_at": "2024-11-05T15:28:41Z",
      "user": "tjgq",
      "body": "> I think your theoretical case of switching remote options between builds will be very uncommon in practice, where these settings are pinned in the .bazelrc\r\n\r\nSwitching options is not the problem; the problem is a `bazel build` followed by a `bazel run`, even if the output mode hasn't changed. The test case added by this PR demonstrates this.\r\n\r\n> Could you reconsider narrowing down the check to also return NOTHING_MODIFIED when remote download is set to all? This would allow us to speed up common dev tasks by 5 seconds.\r\n\r\nTo be absolutely clear, the condition for `NOTHING_MODIFIED` isn't whether `--remote_download_all` is set in the *current* invocation; the condition for a complete fix is whether the *previous* invocation either set `--remote_download_all`, or it set `--remote_download_toplevel` *and* the `bazel run` target was toplevel. It can definitely be done, but it requires keeping this additional state somewhere, so it's not a localized fix.\r\n\r\n(Switching between output modes might be infrequent, but we don't have the luxury of saying \"we'll be correct unless you happen to do something unusual\".)\r\n\r\nAs mentioned in the PR description, there's also the alternative of reimplementing `bazel run` as a specifically crafted build action, but I think that's more difficult (though more principled)."
    },
    {
      "type": "comment",
      "id": 2457584095,
      "created_at": "2024-11-05T16:05:37Z",
      "user": "tjgq",
      "body": "> To be absolutely clear, the condition for NOTHING_MODIFIED isn't whether --remote_download_all is set in the current invocation; the condition for a complete fix is whether the previous invocation either set --remote_download_all, or it set --remote_download_toplevel and the bazel run target was toplevel. It can definitely be done, but it requires keeping this additional state somewhere, so it's not a localized fix.\r\n\r\nIt turns out that I'm wrong, and the fix is much simpler than anticipated: the changes in 4c42edb2e8b2e8169da294591f3b45d369432f14 also made this workaround unnecessary, so we can literally revert this PR and fix the performance issue while preserving correctness (if the integration test is to be believed). I'll send it in soon."
    },
    {
      "type": "comment",
      "id": 2458965927,
      "created_at": "2024-11-06T08:16:29Z",
      "user": "tomdegoede",
      "body": "Amazing! Thanks @tjgq :)"
    }
  ]
}