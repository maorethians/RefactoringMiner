{
  "repo": "bazelbuild/bazel",
  "number": 7239,
  "pr_number": 7239,
  "title": "remote: verify hash of download blobs. fixes #7169",
  "user": "buchgr",
  "created_at": "2019-01-24T14:53:44Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [
    "cla: yes"
  ],
  "base_commit": "52446a12b7435e850347dda144566280625cf498",
  "merge_commit_sha": "cac89e8063e9f7213293ed0f71c4555bce67406f",
  "commits": 1,
  "issue_events": [
    "mentioned",
    "subscribed",
    "assigned",
    "assigned",
    "review_requested",
    "review_requested",
    "labeled",
    "head_ref_force_pushed",
    "closed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "referenced"
  ],
  "url": "https://github.com/bazelbuild/bazel/pull/7239",
  "timeline": [
    {
      "type": "description",
      "created_at": "2019-01-24T14:53:44Z",
      "user": "buchgr",
      "body": "FYI @bayareabear"
    },
    {
      "type": "commit",
      "date": "2019-01-24T16:27:41Z",
      "sha": "aa929d9954368ab2ac21a729175b71761928f208",
      "message": "remote: verify hash of download blobs. fixes #7169",
      "author": "Jakob Buchgraber",
      "author_email": "buchgr@google.com",
      "author_raw_date": "2019-01-24T14:52:03Z",
      "author_date": "2019-01-24T14:52:03Z",
      "committer": "Jakob Buchgraber",
      "committer_email": "buchgr@google.com",
      "raw_date": "2019-01-24T16:27:41Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/AbstractRemoteActionCache.java\n@@ -28,6 +28,7 @@\n import com.google.common.annotations.VisibleForTesting;\n import com.google.common.base.Preconditions;\n import com.google.common.collect.Iterables;\n+import com.google.common.hash.HashingOutputStream;\n import com.google.common.util.concurrent.FutureCallback;\n import com.google.common.util.concurrent.Futures;\n import com.google.common.util.concurrent.ListenableFuture;\n@@ -130,13 +131,19 @@ public ListenableFuture<byte[]> downloadBlob(Digest digest) {\n       return EMPTY_BYTES;\n     }\n     ByteArrayOutputStream bOut = new ByteArrayOutputStream((int) digest.getSizeBytes());\n+    HashingOutputStream hashOut = digestUtil.newHashingOutputStream(bOut);\n     SettableFuture<byte[]> outerF = SettableFuture.create();\n     Futures.addCallback(\n-        downloadBlob(digest, bOut),\n+        downloadBlob(digest, hashOut),\n         new FutureCallback<Void>() {\n           @Override\n           public void onSuccess(Void aVoid) {\n-            outerF.set(bOut.toByteArray());\n+            try {\n+              verifyContents(digest, hashOut);\n+              outerF.set(bOut.toByteArray());\n+            } catch (IOException e) {\n+              outerF.setException(e);\n+            }\n           }\n \n           @Override\n@@ -681,6 +688,16 @@ private void illegalOutput(Path what) throws ExecException, IOException {\n     }\n   }\n \n+  protected void verifyContents(Digest expected, HashingOutputStream actual) throws IOException {\n+    String expectedHash = expected.getHash();\n+    String actualHash = DigestUtil.hashCodeToString(actual.hash());\n+    if (!expectedHash.equals(actualHash)) {\n+      String msg = String.format(\"Download an output failed, because the expected hash\"\n+          + \"'%s' did not match the received hash '%s'.\", expectedHash, actualHash);\n+      throw new IOException(msg);\n+    }\n+  }\n+\n   /** Release resources associated with the cache. The cache may not be used after calling this. */\n   @Override\n   public abstract void close();\nsrc/main/java/com/google/devtools/build/lib/remote/GrpcRemoteCache.java\n@@ -287,21 +287,12 @@ public void onError(Throwable t) {\n \n               @Override\n               public void onCompleted() {\n-                String expectedHash = digest.getHash();\n-                String actualHash = DigestUtil.hashCodeToString(hashOut.hash());\n-                if (!expectedHash.equals(actualHash)) {\n-                  String msg =\n-                      String.format(\n-                          \"Expected hash '%s' does not match received hash '%s'.\",\n-                          expectedHash, actualHash);\n-                  outerF.setException(new IOException(msg));\n-                } else {\n-                  try {\n-                    out.flush();\n-                    outerF.set(null);\n-                  } catch (IOException e) {\n-                    outerF.setException(e);\n-                  }\n+                try {\n+                  verifyContents(digest, hashOut);\n+                  out.flush();\n+                  outerF.set(null);\n+                } catch (IOException e) {\n+                  outerF.setException(e);\n                 }\n               }\n             });\nsrc/main/java/com/google/devtools/build/lib/remote/SimpleBlobStoreActionCache.java\n@@ -21,6 +21,7 @@\n import build.bazel.remote.execution.v2.Directory;\n import build.bazel.remote.execution.v2.DirectoryNode;\n import build.bazel.remote.execution.v2.FileNode;\n+import com.google.common.hash.HashingOutputStream;\n import com.google.common.util.concurrent.FutureCallback;\n import com.google.common.util.concurrent.Futures;\n import com.google.common.util.concurrent.ListenableFuture;\n@@ -225,13 +226,15 @@ public void close() {\n   @Override\n   protected ListenableFuture<Void> downloadBlob(Digest digest, OutputStream out) {\n     SettableFuture<Void> outerF = SettableFuture.create();\n+    HashingOutputStream hashOut = digestUtil.newHashingOutputStream(out);\n     Futures.addCallback(\n-        blobStore.get(digest.getHash(), out),\n+        blobStore.get(digest.getHash(), hashOut),\n         new FutureCallback<Boolean>() {\n           @Override\n           public void onSuccess(Boolean found) {\n             if (found) {\n               try {\n+                verifyContents(digest, hashOut);\n                 out.flush();\n                 outerF.set(null);\n               } catch (IOException e) {\nsrc/test/java/com/google/devtools/build/lib/remote/SimpleBlobStoreActionCacheTest.java\n@@ -15,6 +15,7 @@\n \n import static com.google.common.truth.Truth.assertThat;\n import static com.google.devtools.build.lib.remote.util.Utils.getFromFuture;\n+import static com.google.devtools.build.lib.testutil.MoreAsserts.assertThrows;\n import static java.nio.charset.StandardCharsets.UTF_8;\n \n import build.bazel.remote.execution.v2.Action;\n@@ -42,6 +43,7 @@\n import com.google.devtools.build.lib.vfs.inmemoryfs.InMemoryFileSystem;\n import com.google.devtools.common.options.Options;\n import io.grpc.Context;\n+import java.io.IOException;\n import java.util.concurrent.ConcurrentHashMap;\n import java.util.concurrent.ConcurrentMap;\n import java.util.concurrent.Executors;\n@@ -449,4 +451,24 @@ public void testUploadDirectoryNested() throws Exception {\n     assertThat(map.keySet())\n         .containsExactly(wobbleDigest.getHash(), quxDigest.getHash(), barDigest.getHash());\n   }\n+\n+  @Test\n+  public void testDownloadFailsOnDigestMismatch() {\n+    // Test that the download fails when a blob/file has a different content hash than expected.\n+\n+    final ConcurrentMap<String, byte[]> map = new ConcurrentHashMap<>();\n+    Digest digest = DIGEST_UTIL.computeAsUtf8(\"hello\");\n+    // Store content that doesn't match its digest\n+    map.put(digest.getHash(), \"world\".getBytes(Charsets.UTF_8));\n+    final SimpleBlobStoreActionCache client = newClient(map);\n+\n+    IOException e =\n+        assertThrows(IOException.class, () -> getFromFuture(client.downloadBlob(digest)));\n+    assertThat(e).hasMessageThat().contains(digest.getHash());\n+\n+    e = assertThrows(IOException.class,\n+        () -> getFromFuture(client.downloadFile(fs.getPath(\"/exec/root/foo\"), digest)));\n+    assertThat(e).hasMessageThat().contains(digest.getHash());\n+  }\n+\n }",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/AbstractRemoteActionCache.java",
          "patch": "@@ -28,6 +28,7 @@\n import com.google.common.annotations.VisibleForTesting;\n import com.google.common.base.Preconditions;\n import com.google.common.collect.Iterables;\n+import com.google.common.hash.HashingOutputStream;\n import com.google.common.util.concurrent.FutureCallback;\n import com.google.common.util.concurrent.Futures;\n import com.google.common.util.concurrent.ListenableFuture;\n@@ -130,13 +131,19 @@ public ListenableFuture<byte[]> downloadBlob(Digest digest) {\n       return EMPTY_BYTES;\n     }\n     ByteArrayOutputStream bOut = new ByteArrayOutputStream((int) digest.getSizeBytes());\n+    HashingOutputStream hashOut = digestUtil.newHashingOutputStream(bOut);\n     SettableFuture<byte[]> outerF = SettableFuture.create();\n     Futures.addCallback(\n-        downloadBlob(digest, bOut),\n+        downloadBlob(digest, hashOut),\n         new FutureCallback<Void>() {\n           @Override\n           public void onSuccess(Void aVoid) {\n-            outerF.set(bOut.toByteArray());\n+            try {\n+              verifyContents(digest, hashOut);\n+              outerF.set(bOut.toByteArray());\n+            } catch (IOException e) {\n+              outerF.setException(e);\n+            }\n           }\n \n           @Override\n@@ -681,6 +688,16 @@ private void illegalOutput(Path what) throws ExecException, IOException {\n     }\n   }\n \n+  protected void verifyContents(Digest expected, HashingOutputStream actual) throws IOException {\n+    String expectedHash = expected.getHash();\n+    String actualHash = DigestUtil.hashCodeToString(actual.hash());\n+    if (!expectedHash.equals(actualHash)) {\n+      String msg = String.format(\"Download an output failed, because the expected hash\"\n+          + \"'%s' did not match the received hash '%s'.\", expectedHash, actualHash);\n+      throw new IOException(msg);\n+    }\n+  }\n+\n   /** Release resources associated with the cache. The cache may not be used after calling this. */\n   @Override\n   public abstract void close();"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/GrpcRemoteCache.java",
          "patch": "@@ -287,21 +287,12 @@ public void onError(Throwable t) {\n \n               @Override\n               public void onCompleted() {\n-                String expectedHash = digest.getHash();\n-                String actualHash = DigestUtil.hashCodeToString(hashOut.hash());\n-                if (!expectedHash.equals(actualHash)) {\n-                  String msg =\n-                      String.format(\n-                          \"Expected hash '%s' does not match received hash '%s'.\",\n-                          expectedHash, actualHash);\n-                  outerF.setException(new IOException(msg));\n-                } else {\n-                  try {\n-                    out.flush();\n-                    outerF.set(null);\n-                  } catch (IOException e) {\n-                    outerF.setException(e);\n-                  }\n+                try {\n+                  verifyContents(digest, hashOut);\n+                  out.flush();\n+                  outerF.set(null);\n+                } catch (IOException e) {\n+                  outerF.setException(e);\n                 }\n               }\n             });"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/SimpleBlobStoreActionCache.java",
          "patch": "@@ -21,6 +21,7 @@\n import build.bazel.remote.execution.v2.Directory;\n import build.bazel.remote.execution.v2.DirectoryNode;\n import build.bazel.remote.execution.v2.FileNode;\n+import com.google.common.hash.HashingOutputStream;\n import com.google.common.util.concurrent.FutureCallback;\n import com.google.common.util.concurrent.Futures;\n import com.google.common.util.concurrent.ListenableFuture;\n@@ -225,13 +226,15 @@ public void close() {\n   @Override\n   protected ListenableFuture<Void> downloadBlob(Digest digest, OutputStream out) {\n     SettableFuture<Void> outerF = SettableFuture.create();\n+    HashingOutputStream hashOut = digestUtil.newHashingOutputStream(out);\n     Futures.addCallback(\n-        blobStore.get(digest.getHash(), out),\n+        blobStore.get(digest.getHash(), hashOut),\n         new FutureCallback<Boolean>() {\n           @Override\n           public void onSuccess(Boolean found) {\n             if (found) {\n               try {\n+                verifyContents(digest, hashOut);\n                 out.flush();\n                 outerF.set(null);\n               } catch (IOException e) {"
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/SimpleBlobStoreActionCacheTest.java",
          "patch": "@@ -15,6 +15,7 @@\n \n import static com.google.common.truth.Truth.assertThat;\n import static com.google.devtools.build.lib.remote.util.Utils.getFromFuture;\n+import static com.google.devtools.build.lib.testutil.MoreAsserts.assertThrows;\n import static java.nio.charset.StandardCharsets.UTF_8;\n \n import build.bazel.remote.execution.v2.Action;\n@@ -42,6 +43,7 @@\n import com.google.devtools.build.lib.vfs.inmemoryfs.InMemoryFileSystem;\n import com.google.devtools.common.options.Options;\n import io.grpc.Context;\n+import java.io.IOException;\n import java.util.concurrent.ConcurrentHashMap;\n import java.util.concurrent.ConcurrentMap;\n import java.util.concurrent.Executors;\n@@ -449,4 +451,24 @@ public void testUploadDirectoryNested() throws Exception {\n     assertThat(map.keySet())\n         .containsExactly(wobbleDigest.getHash(), quxDigest.getHash(), barDigest.getHash());\n   }\n+\n+  @Test\n+  public void testDownloadFailsOnDigestMismatch() {\n+    // Test that the download fails when a blob/file has a different content hash than expected.\n+\n+    final ConcurrentMap<String, byte[]> map = new ConcurrentHashMap<>();\n+    Digest digest = DIGEST_UTIL.computeAsUtf8(\"hello\");\n+    // Store content that doesn't match its digest\n+    map.put(digest.getHash(), \"world\".getBytes(Charsets.UTF_8));\n+    final SimpleBlobStoreActionCache client = newClient(map);\n+\n+    IOException e =\n+        assertThrows(IOException.class, () -> getFromFuture(client.downloadBlob(digest)));\n+    assertThat(e).hasMessageThat().contains(digest.getHash());\n+\n+    e = assertThrows(IOException.class,\n+        () -> getFromFuture(client.downloadFile(fs.getPath(\"/exec/root/foo\"), digest)));\n+    assertThat(e).hasMessageThat().contains(digest.getHash());\n+  }\n+\n }"
        }
      ]
    },
    {
      "type": "review",
      "id": 196125747,
      "created_at": "2019-01-24T16:29:47Z",
      "user": "ulfjack",
      "state": "APPROVED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 457284016,
      "created_at": "2019-01-24T17:28:17Z",
      "user": "bayareabear",
      "body": "Thank you!"
    },
    {
      "type": "review_comment",
      "id": 250835740,
      "created_at": "2019-01-25T01:06:22Z",
      "user": "benjaminp",
      "body": "Doesn't this result in double hashing, since both concrete `downloadBlob` implementations now verify?",
      "path": "src/main/java/com/google/devtools/build/lib/remote/AbstractRemoteActionCache.java",
      "diff_hunk": "@@ -130,13 +131,19 @@ abstract void upload(\n       return EMPTY_BYTES;\n     }\n     ByteArrayOutputStream bOut = new ByteArrayOutputStream((int) digest.getSizeBytes());\n+    HashingOutputStream hashOut = digestUtil.newHashingOutputStream(bOut);\n     SettableFuture<byte[]> outerF = SettableFuture.create();\n     Futures.addCallback(\n-        downloadBlob(digest, bOut),\n+        downloadBlob(digest, hashOut),",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 137,
      "original_line": 137,
      "side": "RIGHT",
      "original_position": 16,
      "position": 16,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 196322368,
      "created_at": "2019-01-25T01:12:01Z",
      "user": "benjaminp",
      "state": "COMMENTED",
      "body": "I would appreciate some way to turn this off. Cryptographic hashing is pretty slow. Our internal action cache implementation verifies CAS hashes match, so it's not particularly useful for us to have the client verify it, too."
    },
    {
      "type": "comment",
      "id": 457722655,
      "created_at": "2019-01-25T20:48:59Z",
      "user": "ittaiz",
      "body": "I’m sorry if this question is really dumb but why do we need a cryptographic hash here? Is it because we don’t want someone to maliciously poison the cache?\r\nReason I’m asking is that for our use case what we’re mainly concerned about is network issues."
    },
    {
      "type": "comment",
      "id": 457783227,
      "created_at": "2019-01-26T00:46:54Z",
      "user": "buchgr",
      "body": "@ittaiz it's not about malicious intent but simply about verifying that what we got is what we requested. Pretty much any http server can serve as a remote cache for Bazel and there's plenty of opportunity for files to get corrupted and this is a simple defensive measure."
    },
    {
      "type": "comment",
      "id": 457849318,
      "created_at": "2019-01-26T17:29:54Z",
      "user": "ittaiz",
      "body": "I definitely agree. My question was why we need a *cryptographic* hash?"
    },
    {
      "type": "comment",
      "id": 457850496,
      "created_at": "2019-01-26T17:45:14Z",
      "user": "philwo",
      "body": "@ittaiz Because the cache is a content-addressable storage system, which means we're deduplicating data purely based on their hash. If you don't want to protect against insiders poisoning your cache with malicious artifacts, you'd still want to minimize the chance of accidental collisions, because that would break your build and cache.\r\n\r\nAs an example, ZFS, which also supports deduplication, only allows you to do it based on the checksum when you use cryptographic hashes, e.g. SHA256, SHA512, Skein or Edon-R. Other hashes are not seen as safe enough and require a byte-for-byte comparison in case of a collision, which ZFS can do, but a CAS cannot."
    },
    {
      "type": "comment",
      "id": 457851474,
      "created_at": "2019-01-26T17:57:14Z",
      "user": "ittaiz",
      "body": "👍🏽 thanks for the explanation\r\n@benjaminp aren’t you concerned about network issues corrupting the outputs?"
    },
    {
      "type": "comment",
      "id": 457873959,
      "created_at": "2019-01-26T23:07:13Z",
      "user": "benjaminp",
      "body": "> @benjaminp aren’t you concerned about network issues corrupting the outputs?\r\n\r\nTCP/IP has checksums, so not particularly, no. #7169, which prompted this change, involved manually corrupting the cache."
    },
    {
      "type": "comment",
      "id": 457887523,
      "created_at": "2019-01-27T04:10:34Z",
      "user": "ittaiz",
      "body": "I see, thanks!\nOn Sun, 27 Jan 2019 at 1:07 Benjamin Peterson <notifications@github.com>\nwrote:\n\n> @benjaminp <https://github.com/benjaminp> aren’t you concerned about\n> network issues corrupting the outputs?\n>\n> TCP/IP has checksums, so not particularly, no. #7169\n> <https://github.com/bazelbuild/bazel/issues/7169>, which prompted this\n> change, involved manually corrupting the cache.\n>\n> —\n> You are receiving this because you were mentioned.\n>\n>\n> Reply to this email directly, view it on GitHub\n> <https://github.com/bazelbuild/bazel/pull/7239#issuecomment-457873959>,\n> or mute the thread\n> <https://github.com/notifications/unsubscribe-auth/ABUIF5uDV28qUOR2tHgCXzvvunQkVxEvks5vHN-lgaJpZM4aRCIo>\n> .\n>\n"
    },
    {
      "type": "comment",
      "id": 458210106,
      "created_at": "2019-01-28T16:51:25Z",
      "user": "buchgr",
      "body": "@ittaiz we use crypto hashes because the protocol requires us to use crypto hashes. The network is not a concern for corruption - it's bugs in the backend software that's concering. "
    }
  ]
}