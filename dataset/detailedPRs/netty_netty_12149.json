{
  "repo": "netty/netty",
  "number": 12149,
  "pr_number": 12149,
  "title": "Fix race when handling delegating tasks in ReferenceCountedOpenSslEngine",
  "user": "normanmaurer",
  "created_at": "2022-03-08T10:50:39Z",
  "merged_at": "2022-03-08T21:24:35Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "1a84c738497887387ff60d70d49632391a015346",
  "merge_commit_sha": "1df38d2c7be98e11fa13c7ee52aadbfbea7669e6",
  "commits": 1,
  "issue_events": [
    "review_requested",
    "mentioned",
    "subscribed",
    "milestoned",
    "referenced",
    "merged",
    "closed",
    "head_ref_deleted",
    "referenced",
    "referenced",
    "referenced"
  ],
  "url": "https://github.com/netty/netty/pull/12149",
  "timeline": [
    {
      "type": "commit",
      "date": "2022-03-08T10:46:26Z",
      "sha": "50d80766d5f0a43fcc02aea6ee1b2f0b108e01ea",
      "message": "Fix race when handling delegating tasks in ReferenceCountedOpenSslEngine\n\nMotivation:\n\nDue incorrect handling of delegating tasks in ReferenceCountedOpenSslEngine it was possible to observe handshake timeouts / failures.\n\nModification:\n\n- Only reset needsTask variable after we actually ran the task.\n- Add missing synchronized as otherwise we might end up calling native code concurrently which is not safe.\n- Change how we excute delegating tasks in our SSLEngineTest. This change would result in timeouts / failures before the fix.\n\nResult:\n\nFixes https://github.com/netty/netty/issues/12139",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2022-03-08T10:46:26Z",
      "author_date": "2022-03-08T10:46:26Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2022-03-08T10:46:26Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ReferenceCountedOpenSslEngine.java\n@@ -1456,13 +1456,7 @@ private class TaskDecorator<R extends Runnable> implements Runnable {\n \n         @Override\n         public void run() {\n-            if (isDestroyed()) {\n-                // The engine was destroyed in the meantime, just return.\n-                return;\n-            }\n-            // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n-            needTask = false;\n-            task.run();\n+            runAndResetNeedTask(task);\n         }\n     }\n \n@@ -1475,19 +1469,22 @@ private final class AsyncTaskDecorator extends TaskDecorator<AsyncTask> implemen\n         public void run(final Runnable runnable) {\n             if (isDestroyed()) {\n                 // The engine was destroyed in the meantime, just return.\n-                runnable.run();\n                 return;\n             }\n-            task.runAsync(new Runnable() {\n-                @Override\n-                public void run() {\n-                    // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n-                    // This needs to be done before we run the completion runnable, since that might\n-                    // query the handshake status.\n-                    needTask = false;\n-                    runnable.run();\n-                }\n-            });\n+            task.runAsync(new TaskDecorator<Runnable>(runnable));\n+        }\n+    }\n+\n+    private synchronized void runAndResetNeedTask(Runnable task) {\n+        try {\n+            if (isDestroyed()) {\n+                // The engine was destroyed in the meantime, just return.\n+                return;\n+            }\n+            task.run();\n+        } finally {\n+            // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n+            needTask = false;\n         }\n     }\n \nhandler/src/test/java/io/netty/handler/ssl/SSLEngineTest.java\n@@ -1712,7 +1712,7 @@ private static boolean isHandshakeFinished(SSLEngineResult result) {\n         return result.getHandshakeStatus() == SSLEngineResult.HandshakeStatus.FINISHED;\n     }\n \n-    private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngine engine) throws Exception {\n+    private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngine engine) {\n         if (result.getHandshakeStatus() == SSLEngineResult.HandshakeStatus.NEED_TASK) {\n             for (;;) {\n                 Runnable task = engine.getDelegatedTask();\n@@ -1722,7 +1722,7 @@ private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngi\n                 if (!delegate) {\n                     task.run();\n                 } else {\n-                    delegatingExecutor.submit(task).get();\n+                    delegatingExecutor.execute(task);\n                 }\n             }\n         }",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ReferenceCountedOpenSslEngine.java",
          "patch": "@@ -1456,13 +1456,7 @@ private class TaskDecorator<R extends Runnable> implements Runnable {\n \n         @Override\n         public void run() {\n-            if (isDestroyed()) {\n-                // The engine was destroyed in the meantime, just return.\n-                return;\n-            }\n-            // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n-            needTask = false;\n-            task.run();\n+            runAndResetNeedTask(task);\n         }\n     }\n \n@@ -1475,19 +1469,22 @@ private final class AsyncTaskDecorator extends TaskDecorator<AsyncTask> implemen\n         public void run(final Runnable runnable) {\n             if (isDestroyed()) {\n                 // The engine was destroyed in the meantime, just return.\n-                runnable.run();\n                 return;\n             }\n-            task.runAsync(new Runnable() {\n-                @Override\n-                public void run() {\n-                    // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n-                    // This needs to be done before we run the completion runnable, since that might\n-                    // query the handshake status.\n-                    needTask = false;\n-                    runnable.run();\n-                }\n-            });\n+            task.runAsync(new TaskDecorator<Runnable>(runnable));\n+        }\n+    }\n+\n+    private synchronized void runAndResetNeedTask(Runnable task) {\n+        try {\n+            if (isDestroyed()) {\n+                // The engine was destroyed in the meantime, just return.\n+                return;\n+            }\n+            task.run();\n+        } finally {\n+            // The task was run, reset needTask to false so getHandshakeStatus() returns the correct value.\n+            needTask = false;\n         }\n     }\n "
        },
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/SSLEngineTest.java",
          "patch": "@@ -1712,7 +1712,7 @@ private static boolean isHandshakeFinished(SSLEngineResult result) {\n         return result.getHandshakeStatus() == SSLEngineResult.HandshakeStatus.FINISHED;\n     }\n \n-    private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngine engine) throws Exception {\n+    private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngine engine) {\n         if (result.getHandshakeStatus() == SSLEngineResult.HandshakeStatus.NEED_TASK) {\n             for (;;) {\n                 Runnable task = engine.getDelegatedTask();\n@@ -1722,7 +1722,7 @@ private void runDelegatedTasks(boolean delegate, SSLEngineResult result, SSLEngi\n                 if (!delegate) {\n                     task.run();\n                 } else {\n-                    delegatingExecutor.submit(task).get();\n+                    delegatingExecutor.execute(task);\n                 }\n             }\n         }"
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2022-03-08T10:50:39Z",
      "user": "normanmaurer",
      "body": "Motivation:\r\n\r\nDue incorrect handling of delegating tasks in ReferenceCountedOpenSslEngine it was possible to observe handshake timeouts / failures.\r\n\r\nModification:\r\n\r\n- Only reset needsTask variable after we actually ran the task.\r\n- Add missing synchronized as otherwise we might end up calling native code concurrently which is not safe.\r\n- Change how we excute delegating tasks in our SSLEngineTest. This change would result in timeouts / failures before the fix.\r\n\r\nResult:\r\n\r\nFixes https://github.com/netty/netty/issues/12139"
    },
    {
      "type": "comment",
      "id": 1061825959,
      "created_at": "2022-03-08T14:18:09Z",
      "user": "normanmaurer",
      "body": "@chrisvest I opened a PR against 4.1 and main to proof that for both cases all works. "
    },
    {
      "type": "review",
      "id": 903600436,
      "created_at": "2022-03-08T21:23:35Z",
      "user": "chrisvest",
      "state": "APPROVED",
      "body": ""
    },
    {
      "type": "review",
      "id": 903628069,
      "created_at": "2022-03-08T21:53:04Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 822104104,
      "created_at": "2022-03-08T21:53:04Z",
      "user": "johnou",
      "body": "@normanmaurer intentionally not running the runnable in this refactor if the engine is destroyed?",
      "path": "handler/src/main/java/io/netty/handler/ssl/ReferenceCountedOpenSslEngine.java",
      "diff_hunk": "@@ -1475,19 +1469,22 @@ public void run() {\n         public void run(final Runnable runnable) {\n             if (isDestroyed()) {\n                 // The engine was destroyed in the meantime, just return.\n-                runnable.run();",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 1478,
      "original_line": 1478,
      "side": "LEFT",
      "original_position": 19,
      "position": 19,
      "subject_type": "line",
      "reply": [
        {
          "id": 822295973,
          "user": "normanmaurer",
          "body": "Yes as the SSL object is already destroyed",
          "created_at": "2022-03-09T05:22:19Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 903876086,
      "created_at": "2022-03-09T05:22:19Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    }
  ]
}