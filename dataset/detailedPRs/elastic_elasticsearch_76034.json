{
  "repo": "elastic/elasticsearch",
  "number": 76034,
  "pr_number": 76034,
  "title": "[Transform] fix potential deadlock when using stop_at_checkpoint",
  "user": "hendrikmuhs",
  "created_at": "2021-08-03T14:07:03Z",
  "merged_at": "2021-08-06T14:07:49Z",
  "merged": true,
  "state": "closed",
  "labels": [
    ">bug",
    ":ml/Transform",
    "Team:ML",
    "auto-backport",
    "v7.15.0",
    "v8.0.0-alpha2"
  ],
  "base_commit": "d85acf48ae80cc5a3d5fdb514c8d64681cbf6955",
  "merge_commit_sha": "4f22f437ee50cacb94b37b457be1da0b8ba0e8ce",
  "commits": 5,
  "issue_events": [
    "labeled",
    "labeled",
    "labeled",
    "labeled",
    "labeled",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "labeled",
    "mentioned",
    "subscribed",
    "renamed",
    "labeled",
    "review_requested",
    "unlabeled",
    "labeled",
    "head_ref_force_pushed",
    "unlabeled",
    "head_ref_force_pushed"
  ],
  "url": "https://github.com/elastic/elasticsearch/pull/76034",
  "timeline": [
    {
      "type": "description",
      "created_at": "2021-08-03T14:07:03Z",
      "user": "hendrikmuhs",
      "body": "When `_stop` gets called with `stop_at_checkpoint=true` and at the same time a transform got triggered internally or externally a race condition could lead to a deadlock of `5s`. The change fixes the situation where 2 lock objects could lock one another.\r\n\r\nfixes #75846"
    },
    {
      "type": "comment",
      "id": 891877082,
      "created_at": "2021-08-03T14:07:07Z",
      "user": "elasticmachine",
      "body": "Pinging @elastic/ml-core (Team:ML)"
    },
    {
      "type": "review",
      "id": 721266091,
      "created_at": "2021-08-03T14:23:54Z",
      "user": "benwtrent",
      "state": "COMMENTED",
      "body": "It might be good to bump test logging up to debug? I am wondering if this will really fix the issue.\r\n\r\nThe listener would have gotten an error when trying to wait for stop at checkpoint on a timeout. So, I am not sure increasing the timeout here fixes it. Did the logs in the issue show that the stop call failed?\r\n\r\nIt seems like race condition where the task is removed from cluster state before its previous state is fully "
    },
    {
      "type": "comment",
      "id": 891915054,
      "created_at": "2021-08-03T14:54:55Z",
      "user": "hendrikmuhs",
      "body": "> It might be good to bump test logging up to debug?\r\n\r\nIt is already on debug. \r\n\r\n> I am wondering if this will really fix the issue.\r\n\r\nIt does not fix the issue (this PR _relates_ only to the issue)\r\n\r\n> \r\n> The listener would have gotten an error when trying to wait for stop at checkpoint on a timeout. So, I am not sure increasing the timeout here fixes it. Did the logs in the issue show that the stop call failed?\r\n\r\nyes:\r\n```\r\n[2021-08-02T05:07:00,126][ERROR][o.e.x.t.t.TransformIndexer] [javaRestTest-2] [transform-wait-for-checkpoint] Timed out (5s) waiting for transform state to be stored.\r\n```\r\n\r\nOf course, this could also be a race condition where the listener is not called. If the problem appears again with the `10s` timeout it is unlikely a timeout again, but a race.\r\n\r\n> \r\n> It seems like race condition where the task is removed from cluster state before its previous state is fully\r\n\r\nThis PR does _not_ fix #75846, I am still investigating. I see a new task created before the old one has been completely removed. But this happens after the timeout or race condition above.\r\n\r\nI hope to get more info when it happens again, the `cause` in stop was missing. Next time we should get the full stack trace."
    },
    {
      "type": "review",
      "id": 721311354,
      "created_at": "2021-08-03T14:59:38Z",
      "user": "benwtrent",
      "state": "APPROVED",
      "body": "Cool, didn't know the test was already on debug :)\r\n\r\n++"
    },
    {
      "type": "comment",
      "id": 891939183,
      "created_at": "2021-08-03T15:23:47Z",
      "user": "hendrikmuhs",
      "body": "as for the race, I think here is one:\r\n\r\n```\r\n[2021-08-02T05:06:55,168][DEBUG][o.e.x.t.t.ClientTransformIndexer] [javaRestTest-2] [transform-wait-for-checkpoint] Updated state document from [{seqNo=9,primaryTerm=1,index=.transform-internal-007}] to [{seqNo=10,primaryTerm=1,index=.transform-internal-007}]\r\n[2021-08-02T05:07:00,126][ERROR][o.e.x.t.t.TransformIndexer] [javaRestTest-2] [transform-wait-for-checkpoint] Timed out (5s) waiting for transform state to be stored.\r\n```\r\n\r\nIt's suspicious that the time between the 2 log lines is roughly `5s`. I see a `synchronized` method and a countdown latch, this sounds like a deadly mixture. But I still don't see the issue, will add more debug logging."
    },
    {
      "type": "comment",
      "id": 891996221,
      "created_at": "2021-08-03T16:39:41Z",
      "user": "benwtrent",
      "body": "@hendrikmuhs I am wondering if we will see that delay be `10s` after this PR :D. I think there is a thread dead-lock there. The fact that its SOO close to 5 seconds is very suspicious. "
    },
    {
      "type": "comment",
      "id": 892448199,
      "created_at": "2021-08-04T07:55:13Z",
      "user": "hendrikmuhs",
      "body": "Found it:\r\n\r\n| Thread 1 (transport of stop)                   | Thread 2 (task/scheduler)                        |\r\n|------------------------------------------------|--------------------------------------------------|\r\n| **(s)** task->stop_at_checkpoint               |                                                  |\r\n|                                                | **(s)** task->triggered() (**lock A**)           |\r\n| **(s)** indexer->save_state (**lock B**)       |                                                  |\r\n|                                                | **(s)** indexer->triggered (**wait for lock B**) |\r\n| **(s)** task->shutdown() (**wait for lock A**) |                                                  |\r\n|                                                |                                                  |\r\n| _timeout_ (**unlock B**)                       |                                                  |\r\n|                                                | **(s)** indexer->triggered (**lock B**)          |\r\n\r\n\r\nThe problem are 2 locks of the task and the indexer that can lock one another. \r\n\r\nInteresting is the `task->stop_at_checkpoint` which is `synchronized` but as it moves the execution of\r\nthe thread:\r\n\r\n```\r\n        // move the call to the generic thread pool, so we do not block the network thread\r\n        getThreadPool().executor(ThreadPool.Names.GENERIC)\r\n            .execute(() -> { getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener); });\r\n```\r\n\r\nit unlocks the _task_ (lock A) and therefore allows the trigger originating from the scheduler to acquire lock A."
    },
    {
      "type": "comment",
      "id": 892703392,
      "created_at": "2021-08-04T14:24:18Z",
      "user": "hendrikmuhs",
      "body": "I incorporated the new findings ans inspected all code paths where the indexer thread gets locked 1st and 2nd the task. With that change:\r\n\r\n - all calls either lock the task or the indexer\r\n - if it's required to lock both, it locks the task 1st and the indexer 2nd\r\n \t- it might call task->indexer->task, however as locks are re-entrant, that's not a problem"
    },
    {
      "type": "commit",
      "date": "2021-08-05T12:07:36Z",
      "sha": "bd8d8d4696dce47aef5eb4eb4f1065cbe98acc36",
      "message": "increase timeout for persisting stop at checkpoint\n\nrelates #75846",
      "author": "Hendrik Muhs",
      "author_email": "hendrik.muhs@elastic.co",
      "author_raw_date": "2021-08-03T14:03:49Z",
      "author_date": "2021-08-03T14:03:49Z",
      "committer": "Hendrik Muhs",
      "committer_email": "hendrik.muhs@elastic.co",
      "raw_date": "2021-08-05T12:07:36Z",
      "diff_text": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/action/TransportStopTransformAction.java\n@@ -256,6 +256,7 @@ protected void taskOperation(Request request, TransformTask transformTask, Actio\n                     new ElasticsearchStatusException(\n                         \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n                         RestStatus.CONFLICT,\n+                        e,\n                         transformTask.getTransformId(),\n                         transformTask.getState().shouldStopAtNextCheckpoint(),\n                         request.isWaitForCheckpoint()\nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java\n@@ -61,7 +61,7 @@\n \n public abstract class TransformIndexer extends AsyncTwoPhaseIndexer<TransformIndexerPosition, TransformIndexerStats> {\n \n-    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 5;\n+    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 10;\n \n     /**\n      * RunState is an internal (non-persisted) state that controls the internal logic\n@@ -696,7 +696,7 @@ private synchronized boolean addSetStopAtCheckpointListener(\n             // because save state is async we need to block the call until state is persisted, so that the job can not\n             // be triggered (ensured by synchronized)\n             CountDownLatch latch = new CountDownLatch(1);\n-            logger.debug(\"[{}] persisiting stop at checkpoint\", getJobId());\n+            logger.debug(\"[{}] persisting stop at checkpoint\", getJobId());\n \n             doSaveState(IndexerState.STARTED, getPosition(), () -> { latch.countDown(); });\n \nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformTask.java\n@@ -311,6 +311,11 @@ public synchronized void setShouldStopAtCheckpoint(\n             return;\n         }\n \n+        if (context.shouldStopAtCheckpoint() == shouldStopAtCheckpoint) {\n+            shouldStopAtCheckpointListener.onResponse(null);\n+            return;\n+        }\n+\n         // move the call to the generic thread pool, so we do not block the network thread\n         getThreadPool().executor(ThreadPool.Names.GENERIC)\n             .execute(() -> { getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener); });",
      "diff": [
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/action/TransportStopTransformAction.java",
          "patch": "@@ -256,6 +256,7 @@ protected void taskOperation(Request request, TransformTask transformTask, Actio\n                     new ElasticsearchStatusException(\n                         \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n                         RestStatus.CONFLICT,\n+                        e,\n                         transformTask.getTransformId(),\n                         transformTask.getState().shouldStopAtNextCheckpoint(),\n                         request.isWaitForCheckpoint()"
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
          "patch": "@@ -61,7 +61,7 @@\n \n public abstract class TransformIndexer extends AsyncTwoPhaseIndexer<TransformIndexerPosition, TransformIndexerStats> {\n \n-    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 5;\n+    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 10;\n \n     /**\n      * RunState is an internal (non-persisted) state that controls the internal logic\n@@ -696,7 +696,7 @@ private synchronized boolean addSetStopAtCheckpointListener(\n             // because save state is async we need to block the call until state is persisted, so that the job can not\n             // be triggered (ensured by synchronized)\n             CountDownLatch latch = new CountDownLatch(1);\n-            logger.debug(\"[{}] persisiting stop at checkpoint\", getJobId());\n+            logger.debug(\"[{}] persisting stop at checkpoint\", getJobId());\n \n             doSaveState(IndexerState.STARTED, getPosition(), () -> { latch.countDown(); });\n "
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformTask.java",
          "patch": "@@ -311,6 +311,11 @@ public synchronized void setShouldStopAtCheckpoint(\n             return;\n         }\n \n+        if (context.shouldStopAtCheckpoint() == shouldStopAtCheckpoint) {\n+            shouldStopAtCheckpointListener.onResponse(null);\n+            return;\n+        }\n+\n         // move the call to the generic thread pool, so we do not block the network thread\n         getThreadPool().executor(ThreadPool.Names.GENERIC)\n             .execute(() -> { getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener); });"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2021-08-05T12:07:36Z",
      "sha": "ba0a9717ce9d3c058c0bdd8017a518f8936ce048",
      "message": "switch the threadpool in the transport, not the task to avoid a potential\ndeadlock",
      "author": "Hendrik Muhs",
      "author_email": "hendrik.muhs@elastic.co",
      "author_raw_date": "2021-08-04T14:19:35Z",
      "author_date": "2021-08-04T14:19:35Z",
      "committer": "Hendrik Muhs",
      "committer_email": "hendrik.muhs@elastic.co",
      "raw_date": "2021-08-05T12:07:36Z",
      "diff_text": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/action/TransportStopTransformAction.java\n@@ -244,25 +244,28 @@ protected void taskOperation(Request request, TransformTask transformTask, Actio\n         }\n \n         if (ids.contains(transformTask.getTransformId())) {\n-            transformTask.setShouldStopAtCheckpoint(request.isWaitForCheckpoint(), ActionListener.wrap(r -> {\n-                try {\n-                    transformTask.stop(request.isForce(), request.isWaitForCheckpoint());\n-                    listener.onResponse(new Response(true));\n-                } catch (ElasticsearchException ex) {\n-                    listener.onFailure(ex);\n-                }\n-            },\n-                e -> listener.onFailure(\n-                    new ElasticsearchStatusException(\n-                        \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n-                        RestStatus.CONFLICT,\n-                        e,\n-                        transformTask.getTransformId(),\n-                        transformTask.getState().shouldStopAtNextCheckpoint(),\n-                        request.isWaitForCheckpoint()\n+            // move the call to the generic thread pool, so we do not block the network thread\n+            threadPool.executor(ThreadPool.Names.GENERIC).execute(() -> {\n+                transformTask.setShouldStopAtCheckpoint(request.isWaitForCheckpoint(), ActionListener.wrap(r -> {\n+                    try {\n+                        transformTask.stop(request.isForce(), request.isWaitForCheckpoint());\n+                        listener.onResponse(new Response(true));\n+                    } catch (ElasticsearchException ex) {\n+                        listener.onFailure(ex);\n+                    }\n+                },\n+                    e -> listener.onFailure(\n+                        new ElasticsearchStatusException(\n+                            \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n+                            RestStatus.CONFLICT,\n+                            e,\n+                            transformTask.getTransformId(),\n+                            transformTask.getState().shouldStopAtNextCheckpoint(),\n+                            request.isWaitForCheckpoint()\n+                        )\n                     )\n-                )\n-            ));\n+                ));\n+            });\n         } else {\n             listener.onFailure(\n                 new RuntimeException(\nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java\n@@ -362,6 +362,12 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         }));\n     }\n \n+    /**\n+     * Runs the persistent part of state storage\n+     *\n+     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n+     * the lock for TransformTask has been acquired, too. See gh#75846\n+     */\n     private void doSaveState(TransformState state, ActionListener<Void> listener) {\n         // This could be `null` but the putOrUpdateTransformStoredDoc handles that case just fine\n         SeqNoPrimaryTermAndIndex seqNoPrimaryTermAndIndex = getSeqNoPrimaryTermAndIndex();\nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java\n@@ -61,7 +61,7 @@\n \n public abstract class TransformIndexer extends AsyncTwoPhaseIndexer<TransformIndexerPosition, TransformIndexerStats> {\n \n-    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 10;\n+    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 5;\n \n     /**\n      * RunState is an internal (non-persisted) state that controls the internal logic\n@@ -755,7 +755,18 @@ synchronized void stopAndMaybeSaveState() {\n         }\n     }\n \n-    synchronized void handleFailure(Exception e) {\n+    /**\n+     * Checks the given exception and handles the error based on it.\n+     *\n+     * In case the error is permanent or the number for failures exceed the number of retries, sets the indexer\n+     * to `FAILED`.\n+     *\n+     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n+     * the lock for TransformTask has been acquired, too. See gh#75846\n+     *\n+     * (Note: originally this method was synchronized, which is not necessary)\n+     */\n+    void handleFailure(Exception e) {\n         logger.warn(new ParameterizedMessage(\"[{}] transform encountered an exception: \", getJobId()), e);\n         Throwable unwrappedException = ExceptionsHelper.findSearchExceptionRootCause(e);\n \nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformTask.java\n@@ -300,6 +300,8 @@ public synchronized void setShouldStopAtCheckpoint(\n         boolean shouldStopAtCheckpoint,\n         ActionListener<Void> shouldStopAtCheckpointListener\n     ) {\n+        // this should be called from the generic threadpool\n+        assert Thread.currentThread().getName().contains(ThreadPool.Names.GENERIC);\n         logger.debug(\n             \"[{}] attempted to set task to stop at checkpoint [{}] with state [{}]\",\n             getTransformId(),\n@@ -316,9 +318,7 @@ public synchronized void setShouldStopAtCheckpoint(\n             return;\n         }\n \n-        // move the call to the generic thread pool, so we do not block the network thread\n-        getThreadPool().executor(ThreadPool.Names.GENERIC)\n-            .execute(() -> { getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener); });\n+        getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener);\n     }\n \n     public synchronized void stop(boolean force, boolean shouldStopAtCheckpoint) {",
      "diff": [
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/action/TransportStopTransformAction.java",
          "patch": "@@ -244,25 +244,28 @@ protected void taskOperation(Request request, TransformTask transformTask, Actio\n         }\n \n         if (ids.contains(transformTask.getTransformId())) {\n-            transformTask.setShouldStopAtCheckpoint(request.isWaitForCheckpoint(), ActionListener.wrap(r -> {\n-                try {\n-                    transformTask.stop(request.isForce(), request.isWaitForCheckpoint());\n-                    listener.onResponse(new Response(true));\n-                } catch (ElasticsearchException ex) {\n-                    listener.onFailure(ex);\n-                }\n-            },\n-                e -> listener.onFailure(\n-                    new ElasticsearchStatusException(\n-                        \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n-                        RestStatus.CONFLICT,\n-                        e,\n-                        transformTask.getTransformId(),\n-                        transformTask.getState().shouldStopAtNextCheckpoint(),\n-                        request.isWaitForCheckpoint()\n+            // move the call to the generic thread pool, so we do not block the network thread\n+            threadPool.executor(ThreadPool.Names.GENERIC).execute(() -> {\n+                transformTask.setShouldStopAtCheckpoint(request.isWaitForCheckpoint(), ActionListener.wrap(r -> {\n+                    try {\n+                        transformTask.stop(request.isForce(), request.isWaitForCheckpoint());\n+                        listener.onResponse(new Response(true));\n+                    } catch (ElasticsearchException ex) {\n+                        listener.onFailure(ex);\n+                    }\n+                },\n+                    e -> listener.onFailure(\n+                        new ElasticsearchStatusException(\n+                            \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n+                            RestStatus.CONFLICT,\n+                            e,\n+                            transformTask.getTransformId(),\n+                            transformTask.getState().shouldStopAtNextCheckpoint(),\n+                            request.isWaitForCheckpoint()\n+                        )\n                     )\n-                )\n-            ));\n+                ));\n+            });\n         } else {\n             listener.onFailure(\n                 new RuntimeException("
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java",
          "patch": "@@ -362,6 +362,12 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         }));\n     }\n \n+    /**\n+     * Runs the persistent part of state storage\n+     *\n+     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n+     * the lock for TransformTask has been acquired, too. See gh#75846\n+     */\n     private void doSaveState(TransformState state, ActionListener<Void> listener) {\n         // This could be `null` but the putOrUpdateTransformStoredDoc handles that case just fine\n         SeqNoPrimaryTermAndIndex seqNoPrimaryTermAndIndex = getSeqNoPrimaryTermAndIndex();"
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
          "patch": "@@ -61,7 +61,7 @@\n \n public abstract class TransformIndexer extends AsyncTwoPhaseIndexer<TransformIndexerPosition, TransformIndexerStats> {\n \n-    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 10;\n+    private static final int PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC = 5;\n \n     /**\n      * RunState is an internal (non-persisted) state that controls the internal logic\n@@ -755,7 +755,18 @@ synchronized void stopAndMaybeSaveState() {\n         }\n     }\n \n-    synchronized void handleFailure(Exception e) {\n+    /**\n+     * Checks the given exception and handles the error based on it.\n+     *\n+     * In case the error is permanent or the number for failures exceed the number of retries, sets the indexer\n+     * to `FAILED`.\n+     *\n+     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n+     * the lock for TransformTask has been acquired, too. See gh#75846\n+     *\n+     * (Note: originally this method was synchronized, which is not necessary)\n+     */\n+    void handleFailure(Exception e) {\n         logger.warn(new ParameterizedMessage(\"[{}] transform encountered an exception: \", getJobId()), e);\n         Throwable unwrappedException = ExceptionsHelper.findSearchExceptionRootCause(e);\n "
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformTask.java",
          "patch": "@@ -300,6 +300,8 @@ public synchronized void setShouldStopAtCheckpoint(\n         boolean shouldStopAtCheckpoint,\n         ActionListener<Void> shouldStopAtCheckpointListener\n     ) {\n+        // this should be called from the generic threadpool\n+        assert Thread.currentThread().getName().contains(ThreadPool.Names.GENERIC);\n         logger.debug(\n             \"[{}] attempted to set task to stop at checkpoint [{}] with state [{}]\",\n             getTransformId(),\n@@ -316,9 +318,7 @@ public synchronized void setShouldStopAtCheckpoint(\n             return;\n         }\n \n-        // move the call to the generic thread pool, so we do not block the network thread\n-        getThreadPool().executor(ThreadPool.Names.GENERIC)\n-            .execute(() -> { getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener); });\n+        getIndexer().setStopAtCheckpoint(shouldStopAtCheckpoint, shouldStopAtCheckpointListener);\n     }\n \n     public synchronized void stop(boolean force, boolean shouldStopAtCheckpoint) {"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2021-08-05T12:07:36Z",
      "sha": "b54c04351d9d86d09bb1ef13095385448fcdda5a",
      "message": "re-factor state persistence when stop at checkpoint is called so that\nshutdown is called with the thread that holds the locks.",
      "author": "Hendrik Muhs",
      "author_email": "hendrik.muhs@elastic.co",
      "author_raw_date": "2021-08-05T10:04:28Z",
      "author_date": "2021-08-05T10:04:28Z",
      "committer": "Hendrik Muhs",
      "committer_email": "hendrik.muhs@elastic.co",
      "raw_date": "2021-08-05T12:07:36Z",
      "diff_text": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java\n@@ -337,7 +337,7 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n \n         // we might need to call the save state listeners, but do not want to stop rolling\n-        doSaveState(state, ActionListener.wrap(r -> {\n+        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n             try {\n                 if (saveStateListenersAtTheMomentOfCalling != null) {\n                     ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n@@ -362,13 +362,19 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         }));\n     }\n \n+    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n+        persistState(state, ActionListener.runBefore(listener, () -> {\n+            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+        }));\n+    }\n+\n     /**\n-     * Runs the persistent part of state storage\n-     *\n-     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n-     * the lock for TransformTask has been acquired, too. See gh#75846\n+     * Runs the persistence part of state storage\n      */\n-    private void doSaveState(TransformState state, ActionListener<Void> listener) {\n+    @Override\n+    protected void persistState(TransformState state, ActionListener<Void> listener) {\n         // This could be `null` but the putOrUpdateTransformStoredDoc handles that case just fine\n         SeqNoPrimaryTermAndIndex seqNoPrimaryTermAndIndex = getSeqNoPrimaryTermAndIndex();\n \n@@ -380,10 +386,6 @@ private void doSaveState(TransformState state, ActionListener<Void> listener) {\n             seqNoPrimaryTermAndIndex,\n             ActionListener.wrap(r -> {\n                 updateSeqNoPrimaryTermAndIndex(seqNoPrimaryTermAndIndex, r);\n-                // for auto stop shutdown the task\n-                if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                    context.shutdown();\n-                }\n \n                 // Only do this clean up once, if it succeeded, no reason to do the query again.\n                 if (oldStatsCleanedUp.compareAndSet(false, true)) {\n@@ -428,9 +430,6 @@ private void doSaveState(TransformState state, ActionListener<Void> listener) {\n                     logger.error(new ParameterizedMessage(\"[{}] updating stats of transform failed.\", transformConfig.getId()), statsExc);\n                     auditor.warning(getJobId(), \"Failure updating stats of transform: \" + statsExc.getMessage());\n                 }\n-                if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                    context.shutdown();\n-                }\n                 listener.onFailure(statsExc);\n             })\n         );\nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java\n@@ -39,6 +39,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n import org.elasticsearch.xpack.core.transform.transforms.TransformProgress;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.core.transform.utils.ExceptionsHelper;\n import org.elasticsearch.xpack.transform.TransformServices;\n@@ -164,6 +165,8 @@ public TransformIndexer(\n \n     abstract void refreshDestinationIndex(ActionListener<RefreshResponse> responseListener);\n \n+    abstract void persistState(TransformState state, ActionListener<Void> listener);\n+\n     public int getPageSize() {\n         return pageSize;\n     }\n@@ -691,14 +694,36 @@ private synchronized boolean addSetStopAtCheckpointListener(\n \n         // in case the indexer isn't running, respond immediately\n         if (state == IndexerState.STARTED && context.shouldStopAtCheckpoint() != shouldStopAtCheckpoint) {\n-            context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            IndexerState newIndexerState = IndexerState.STARTED;\n+            TransformTaskState newtaskState = context.getTaskState();\n+\n+            // check if the transform is at a checkpoint, if so, we will stop it below\n+            // otherwise we set shouldStopAtCheckpoint and wait\n+            if (shouldStopAtCheckpoint && initialRun()) {\n+                newIndexerState = IndexerState.STOPPED;\n+                newtaskState = TransformTaskState.STOPPED;\n+                logger.debug(\"[{}] transform is at a checkpoint, initiating stop.\", transformConfig.getId());\n+            } else {\n+                context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            }\n+\n+            final TransformState newTransformState = new TransformState(\n+                newtaskState,\n+                newIndexerState,\n+                getPosition(),\n+                context.getCheckpoint(),\n+                context.getStateReason(),\n+                getProgress(),\n+                null,\n+                shouldStopAtCheckpoint\n+            );\n \n             // because save state is async we need to block the call until state is persisted, so that the job can not\n             // be triggered (ensured by synchronized)\n             CountDownLatch latch = new CountDownLatch(1);\n             logger.debug(\"[{}] persisting stop at checkpoint\", getJobId());\n \n-            doSaveState(IndexerState.STARTED, getPosition(), () -> { latch.countDown(); });\n+            persistState(newTransformState, ActionListener.wrap(() -> latch.countDown()));\n \n             if (latch.await(PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC, TimeUnit.SECONDS) == false) {\n                 logger.error(\n@@ -709,6 +734,12 @@ private synchronized boolean addSetStopAtCheckpointListener(\n                     )\n                 );\n             }\n+\n+            // stop the transform if the decision was to stop it above\n+            if (newtaskState.equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+\n             return false;\n         }\n \nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerFailureHandlingTests.java\n@@ -45,6 +45,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformConfig;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.transform.Transform;\n import org.elasticsearch.xpack.transform.TransformServices;\n@@ -271,6 +272,11 @@ void refreshDestinationIndex(ActionListener<RefreshResponse> responseListener) {\n         void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListener) {\n             fieldMappingsListener.onResponse(Collections.emptyMap());\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java\n@@ -256,6 +256,11 @@ public TransformState getPersistedState() {\n         void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListener) {\n             fieldMappingsListener.onResponse(Collections.emptyMap());\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerTests.java\n@@ -41,6 +41,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformConfig;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.transform.TransformServices;\n import org.elasticsearch.xpack.transform.checkpoint.CheckpointProvider;\n@@ -266,6 +267,11 @@ public boolean waitingForNextSearch() {\n         public int getDeleteByQueryCallCount() {\n             return deleteByQueryCallCount;\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before",
      "diff": [
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java",
          "patch": "@@ -337,7 +337,7 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n \n         // we might need to call the save state listeners, but do not want to stop rolling\n-        doSaveState(state, ActionListener.wrap(r -> {\n+        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n             try {\n                 if (saveStateListenersAtTheMomentOfCalling != null) {\n                     ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n@@ -362,13 +362,19 @@ protected void doSaveState(IndexerState indexerState, TransformIndexerPosition p\n         }));\n     }\n \n+    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n+        persistState(state, ActionListener.runBefore(listener, () -> {\n+            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+        }));\n+    }\n+\n     /**\n-     * Runs the persistent part of state storage\n-     *\n-     * Important: Might call into TransformTask, this should _only_ be called with an acquired indexer lock if and only if\n-     * the lock for TransformTask has been acquired, too. See gh#75846\n+     * Runs the persistence part of state storage\n      */\n-    private void doSaveState(TransformState state, ActionListener<Void> listener) {\n+    @Override\n+    protected void persistState(TransformState state, ActionListener<Void> listener) {\n         // This could be `null` but the putOrUpdateTransformStoredDoc handles that case just fine\n         SeqNoPrimaryTermAndIndex seqNoPrimaryTermAndIndex = getSeqNoPrimaryTermAndIndex();\n \n@@ -380,10 +386,6 @@ private void doSaveState(TransformState state, ActionListener<Void> listener) {\n             seqNoPrimaryTermAndIndex,\n             ActionListener.wrap(r -> {\n                 updateSeqNoPrimaryTermAndIndex(seqNoPrimaryTermAndIndex, r);\n-                // for auto stop shutdown the task\n-                if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                    context.shutdown();\n-                }\n \n                 // Only do this clean up once, if it succeeded, no reason to do the query again.\n                 if (oldStatsCleanedUp.compareAndSet(false, true)) {\n@@ -428,9 +430,6 @@ private void doSaveState(TransformState state, ActionListener<Void> listener) {\n                     logger.error(new ParameterizedMessage(\"[{}] updating stats of transform failed.\", transformConfig.getId()), statsExc);\n                     auditor.warning(getJobId(), \"Failure updating stats of transform: \" + statsExc.getMessage());\n                 }\n-                if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                    context.shutdown();\n-                }\n                 listener.onFailure(statsExc);\n             })\n         );"
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
          "patch": "@@ -39,6 +39,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n import org.elasticsearch.xpack.core.transform.transforms.TransformProgress;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.core.transform.utils.ExceptionsHelper;\n import org.elasticsearch.xpack.transform.TransformServices;\n@@ -164,6 +165,8 @@ public TransformIndexer(\n \n     abstract void refreshDestinationIndex(ActionListener<RefreshResponse> responseListener);\n \n+    abstract void persistState(TransformState state, ActionListener<Void> listener);\n+\n     public int getPageSize() {\n         return pageSize;\n     }\n@@ -691,14 +694,36 @@ private synchronized boolean addSetStopAtCheckpointListener(\n \n         // in case the indexer isn't running, respond immediately\n         if (state == IndexerState.STARTED && context.shouldStopAtCheckpoint() != shouldStopAtCheckpoint) {\n-            context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            IndexerState newIndexerState = IndexerState.STARTED;\n+            TransformTaskState newtaskState = context.getTaskState();\n+\n+            // check if the transform is at a checkpoint, if so, we will stop it below\n+            // otherwise we set shouldStopAtCheckpoint and wait\n+            if (shouldStopAtCheckpoint && initialRun()) {\n+                newIndexerState = IndexerState.STOPPED;\n+                newtaskState = TransformTaskState.STOPPED;\n+                logger.debug(\"[{}] transform is at a checkpoint, initiating stop.\", transformConfig.getId());\n+            } else {\n+                context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            }\n+\n+            final TransformState newTransformState = new TransformState(\n+                newtaskState,\n+                newIndexerState,\n+                getPosition(),\n+                context.getCheckpoint(),\n+                context.getStateReason(),\n+                getProgress(),\n+                null,\n+                shouldStopAtCheckpoint\n+            );\n \n             // because save state is async we need to block the call until state is persisted, so that the job can not\n             // be triggered (ensured by synchronized)\n             CountDownLatch latch = new CountDownLatch(1);\n             logger.debug(\"[{}] persisting stop at checkpoint\", getJobId());\n \n-            doSaveState(IndexerState.STARTED, getPosition(), () -> { latch.countDown(); });\n+            persistState(newTransformState, ActionListener.wrap(() -> latch.countDown()));\n \n             if (latch.await(PERSIST_STOP_AT_CHECKPOINT_TIMEOUT_SEC, TimeUnit.SECONDS) == false) {\n                 logger.error(\n@@ -709,6 +734,12 @@ private synchronized boolean addSetStopAtCheckpointListener(\n                     )\n                 );\n             }\n+\n+            // stop the transform if the decision was to stop it above\n+            if (newtaskState.equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+\n             return false;\n         }\n "
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerFailureHandlingTests.java",
          "patch": "@@ -45,6 +45,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformConfig;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.transform.Transform;\n import org.elasticsearch.xpack.transform.TransformServices;\n@@ -271,6 +272,11 @@ void refreshDestinationIndex(ActionListener<RefreshResponse> responseListener) {\n         void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListener) {\n             fieldMappingsListener.onResponse(Collections.emptyMap());\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java",
          "patch": "@@ -256,6 +256,11 @@ public TransformState getPersistedState() {\n         void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListener) {\n             fieldMappingsListener.onResponse(Collections.emptyMap());\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerTests.java",
          "patch": "@@ -41,6 +41,7 @@\n import org.elasticsearch.xpack.core.transform.transforms.TransformConfig;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerPosition;\n import org.elasticsearch.xpack.core.transform.transforms.TransformIndexerStats;\n+import org.elasticsearch.xpack.core.transform.transforms.TransformState;\n import org.elasticsearch.xpack.core.transform.transforms.TransformTaskState;\n import org.elasticsearch.xpack.transform.TransformServices;\n import org.elasticsearch.xpack.transform.checkpoint.CheckpointProvider;\n@@ -266,6 +267,11 @@ public boolean waitingForNextSearch() {\n         public int getDeleteByQueryCallCount() {\n             return deleteByQueryCallCount;\n         }\n+\n+        @Override\n+        void persistState(TransformState state, ActionListener<Void> listener) {\n+            listener.onResponse(null);\n+        }\n     }\n \n     @Before"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2021-08-05T12:07:36Z",
      "sha": "e2860ccb64fb4fbee76f12f0207cc2024edd93fd",
      "message": "add testcase to differ between indexer in the middle of the checkpoint or\nat a checkpoint",
      "author": "Hendrik Muhs",
      "author_email": "hendrik.muhs@elastic.co",
      "author_raw_date": "2021-08-05T11:47:27Z",
      "author_date": "2021-08-05T11:47:27Z",
      "committer": "Hendrik Muhs",
      "committer_email": "hendrik.muhs@elastic.co",
      "raw_date": "2021-08-05T12:07:36Z",
      "diff_text": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java\n@@ -682,7 +682,6 @@ private synchronized boolean addSetStopAtCheckpointListener(\n         boolean shouldStopAtCheckpoint,\n         ActionListener<Void> shouldStopAtCheckpointListener\n     ) throws InterruptedException {\n-\n         // in case the indexer is already shutting down\n         if (indexerThreadShuttingDown) {\n             context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n@@ -697,8 +696,9 @@ private synchronized boolean addSetStopAtCheckpointListener(\n             IndexerState newIndexerState = IndexerState.STARTED;\n             TransformTaskState newtaskState = context.getTaskState();\n \n-            // check if the transform is at a checkpoint, if so, we will stop it below\n-            // otherwise we set shouldStopAtCheckpoint and wait\n+            // check if the transform is at a checkpoint, if so, we will shortcut and stop it below\n+            // otherwise we set shouldStopAtCheckpoint, for this case the transform needs to get\n+            // triggered, complete the checkpoint and stop\n             if (shouldStopAtCheckpoint && initialRun()) {\n                 newIndexerState = IndexerState.STOPPED;\n                 newtaskState = TransformTaskState.STOPPED;\n@@ -715,7 +715,7 @@ private synchronized boolean addSetStopAtCheckpointListener(\n                 context.getStateReason(),\n                 getProgress(),\n                 null,\n-                shouldStopAtCheckpoint\n+                newIndexerState == IndexerState.STARTED\n             );\n \n             // because save state is async we need to block the call until state is persisted, so that the job can not\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java\n@@ -259,6 +259,7 @@ void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListene\n \n         @Override\n         void persistState(TransformState state, ActionListener<Void> listener) {\n+            persistedState = state;\n             listener.onResponse(null);\n         }\n     }\n@@ -307,6 +308,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                new TransformIndexerPosition(Collections.singletonMap(\"afterkey\", \"value\"), Collections.emptyMap()),\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -322,6 +324,27 @@ public void testStopAtCheckpoint() throws Exception {\n             }\n         }\n \n+        // test the case that the indexer is at a checkpoint already\n+        {\n+            AtomicReference<IndexerState> stateRef = new AtomicReference<>(IndexerState.STARTED);\n+            TransformContext context = new TransformContext(TransformTaskState.STARTED, \"\", 0, mock(TransformContext.Listener.class));\n+            final MockedTransformIndexer indexer = createMockIndexer(\n+                config,\n+                stateRef,\n+                null,\n+                threadPool,\n+                auditor,\n+                null,\n+                new TransformIndexerStats(),\n+                context\n+            );\n+            assertResponse(listener -> setStopAtCheckpoint(indexer, true, listener));\n+            assertEquals(0, indexer.getSaveStateListenerCallCount());\n+            // shouldStopAtCheckpoint should not be set, the indexer was started, however at a checkpoint\n+            assertFalse(context.shouldStopAtCheckpoint());\n+            assertFalse(indexer.getPersistedState().shouldStopAtNextCheckpoint());\n+        }\n+\n         // lets test a running indexer\n         AtomicReference<IndexerState> state = new AtomicReference<>(IndexerState.STARTED);\n         {\n@@ -332,6 +355,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -361,6 +385,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -391,6 +416,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -434,6 +460,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -509,6 +536,7 @@ public void testStopAtCheckpointForThrottledTransform() throws Exception {\n             null,\n             threadPool,\n             auditor,\n+            null,\n             new TransformIndexerStats(),\n             context\n         );\n@@ -602,6 +630,7 @@ private MockedTransformIndexer createMockIndexer(\n         Consumer<String> failureConsumer,\n         ThreadPool threadPool,\n         TransformAuditor auditor,\n+        TransformIndexerPosition initialPosition,\n         TransformIndexerStats jobStats,\n         TransformContext context\n     ) {\n@@ -620,7 +649,7 @@ private MockedTransformIndexer createMockIndexer(\n             checkpointProvider,\n             config,\n             state,\n-            null,\n+            initialPosition,\n             jobStats,\n             context\n         );",
      "diff": [
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
          "patch": "@@ -682,7 +682,6 @@ private synchronized boolean addSetStopAtCheckpointListener(\n         boolean shouldStopAtCheckpoint,\n         ActionListener<Void> shouldStopAtCheckpointListener\n     ) throws InterruptedException {\n-\n         // in case the indexer is already shutting down\n         if (indexerThreadShuttingDown) {\n             context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n@@ -697,8 +696,9 @@ private synchronized boolean addSetStopAtCheckpointListener(\n             IndexerState newIndexerState = IndexerState.STARTED;\n             TransformTaskState newtaskState = context.getTaskState();\n \n-            // check if the transform is at a checkpoint, if so, we will stop it below\n-            // otherwise we set shouldStopAtCheckpoint and wait\n+            // check if the transform is at a checkpoint, if so, we will shortcut and stop it below\n+            // otherwise we set shouldStopAtCheckpoint, for this case the transform needs to get\n+            // triggered, complete the checkpoint and stop\n             if (shouldStopAtCheckpoint && initialRun()) {\n                 newIndexerState = IndexerState.STOPPED;\n                 newtaskState = TransformTaskState.STOPPED;\n@@ -715,7 +715,7 @@ private synchronized boolean addSetStopAtCheckpointListener(\n                 context.getStateReason(),\n                 getProgress(),\n                 null,\n-                shouldStopAtCheckpoint\n+                newIndexerState == IndexerState.STARTED\n             );\n \n             // because save state is async we need to block the call until state is persisted, so that the job can not"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java",
          "patch": "@@ -259,6 +259,7 @@ void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListene\n \n         @Override\n         void persistState(TransformState state, ActionListener<Void> listener) {\n+            persistedState = state;\n             listener.onResponse(null);\n         }\n     }\n@@ -307,6 +308,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                new TransformIndexerPosition(Collections.singletonMap(\"afterkey\", \"value\"), Collections.emptyMap()),\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -322,6 +324,27 @@ public void testStopAtCheckpoint() throws Exception {\n             }\n         }\n \n+        // test the case that the indexer is at a checkpoint already\n+        {\n+            AtomicReference<IndexerState> stateRef = new AtomicReference<>(IndexerState.STARTED);\n+            TransformContext context = new TransformContext(TransformTaskState.STARTED, \"\", 0, mock(TransformContext.Listener.class));\n+            final MockedTransformIndexer indexer = createMockIndexer(\n+                config,\n+                stateRef,\n+                null,\n+                threadPool,\n+                auditor,\n+                null,\n+                new TransformIndexerStats(),\n+                context\n+            );\n+            assertResponse(listener -> setStopAtCheckpoint(indexer, true, listener));\n+            assertEquals(0, indexer.getSaveStateListenerCallCount());\n+            // shouldStopAtCheckpoint should not be set, the indexer was started, however at a checkpoint\n+            assertFalse(context.shouldStopAtCheckpoint());\n+            assertFalse(indexer.getPersistedState().shouldStopAtNextCheckpoint());\n+        }\n+\n         // lets test a running indexer\n         AtomicReference<IndexerState> state = new AtomicReference<>(IndexerState.STARTED);\n         {\n@@ -332,6 +355,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -361,6 +385,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -391,6 +416,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -434,6 +460,7 @@ public void testStopAtCheckpoint() throws Exception {\n                 null,\n                 threadPool,\n                 auditor,\n+                null,\n                 new TransformIndexerStats(),\n                 context\n             );\n@@ -509,6 +536,7 @@ public void testStopAtCheckpointForThrottledTransform() throws Exception {\n             null,\n             threadPool,\n             auditor,\n+            null,\n             new TransformIndexerStats(),\n             context\n         );\n@@ -602,6 +630,7 @@ private MockedTransformIndexer createMockIndexer(\n         Consumer<String> failureConsumer,\n         ThreadPool threadPool,\n         TransformAuditor auditor,\n+        TransformIndexerPosition initialPosition,\n         TransformIndexerStats jobStats,\n         TransformContext context\n     ) {\n@@ -620,7 +649,7 @@ private MockedTransformIndexer createMockIndexer(\n             checkpointProvider,\n             config,\n             state,\n-            null,\n+            initialPosition,\n             jobStats,\n             context\n         );"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2021-08-05T12:07:36Z",
      "sha": "ffeec6ebabee28088fbdf031ba89049f2b44bc80",
      "message": "move doSaveState into indexer",
      "author": "Hendrik Muhs",
      "author_email": "hendrik.muhs@elastic.co",
      "author_raw_date": "2021-08-05T12:07:18Z",
      "author_date": "2021-08-05T12:07:18Z",
      "committer": "Hendrik Muhs",
      "committer_email": "hendrik.muhs@elastic.co",
      "raw_date": "2021-08-05T12:07:36Z",
      "diff_text": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java\n@@ -56,7 +56,6 @@\n import org.elasticsearch.xpack.transform.transforms.pivot.SchemaUtil;\n import org.elasticsearch.xpack.transform.utils.ExceptionRootCauseFinder;\n \n-import java.util.Collection;\n import java.util.LinkedHashMap;\n import java.util.Map;\n import java.util.Map.Entry;\n@@ -257,119 +256,6 @@ void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListene\n         );\n     }\n \n-    @Override\n-    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {\n-        if (context.getTaskState() == TransformTaskState.FAILED) {\n-            logger.debug(\"[{}] attempted to save state and stats while failed.\", getJobId());\n-            // If we are failed, we should call next to allow failure handling to occur if necessary.\n-            next.run();\n-            return;\n-        }\n-        if (indexerState.equals(IndexerState.ABORTING)) {\n-            // If we're aborting, just invoke `next` (which is likely an onFailure handler)\n-            next.run();\n-            return;\n-        }\n-\n-        // getting the listeners that registered till now, in theory a new listener could sneak in between this line\n-        // and the next, however this is benign: we would store `shouldStopAtCheckpoint = true` twice which is ok\n-        Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n-        boolean shouldStopAtCheckpoint = context.shouldStopAtCheckpoint();\n-\n-        // If we should stop at the next checkpoint, are STARTED, and with `initialRun()` we are in one of two states\n-        // 1. We have just called `onFinish` completing our request, but `shouldStopAtCheckpoint` was set to `true` before our check\n-        // there and now\n-        // 2. We are on the very first run of a NEW checkpoint and got here either through a failure, or the very first save state call.\n-        //\n-        // In either case, we should stop so that we guarantee a consistent state and that there are no partially completed checkpoints\n-        if (shouldStopAtCheckpoint && initialRun() && indexerState.equals(IndexerState.STARTED)) {\n-            indexerState = IndexerState.STOPPED;\n-            auditor.info(transformConfig.getId(), \"Transform is no longer in the middle of a checkpoint, initiating stop.\");\n-            logger.info(\"[{}] transform is no longer in the middle of a checkpoint, initiating stop.\", transformConfig.getId());\n-        }\n-\n-        // This means that the indexer was triggered to discover changes, found none, and exited early.\n-        // If the state is `STOPPED` this means that TransformTask#stop was called while we were checking for changes.\n-        // Allow the stop call path to continue\n-        if (hasSourceChanged == false && indexerState.equals(IndexerState.STOPPED) == false) {\n-            if (saveStateListenersAtTheMomentOfCalling != null) {\n-                ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n-            }\n-            next.run();\n-            return;\n-        }\n-\n-        TransformTaskState taskState = context.getTaskState();\n-\n-        if (indexerState.equals(IndexerState.STARTED) && context.getCheckpoint() == 1 && this.isContinuous() == false) {\n-            // set both to stopped so they are persisted as such\n-            indexerState = IndexerState.STOPPED;\n-\n-            auditor.info(transformConfig.getId(), \"Transform finished indexing all data, initiating stop\");\n-            logger.info(\"[{}] transform finished indexing all data, initiating stop.\", transformConfig.getId());\n-        }\n-\n-        // If we are `STOPPED` on a `doSaveState` call, that indicates we transitioned to `STOPPED` from `STOPPING`\n-        // OR we called `doSaveState` manually as the indexer was not actively running.\n-        // Since we save the state to an index, we should make sure that our task state is in parity with the indexer state\n-        if (indexerState.equals(IndexerState.STOPPED)) {\n-            // If we are going to stop after the state is saved, we should NOT persist `shouldStopAtCheckpoint: true` as this may\n-            // cause problems if the task starts up again.\n-            // Additionally, we don't have to worry about inconsistency with the ClusterState (if it is persisted there) as the\n-            // when we stop, we mark the task as complete and that state goes away.\n-            shouldStopAtCheckpoint = false;\n-\n-            // We don't want adjust the stored taskState because as soon as it is `STOPPED` a user could call\n-            // .start again.\n-            taskState = TransformTaskState.STOPPED;\n-        }\n-\n-        final TransformState state = new TransformState(\n-            taskState,\n-            indexerState,\n-            position,\n-            context.getCheckpoint(),\n-            context.getStateReason(),\n-            getProgress(),\n-            null,\n-            shouldStopAtCheckpoint\n-        );\n-        logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n-\n-        // we might need to call the save state listeners, but do not want to stop rolling\n-        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n-                }\n-            } catch (Exception onResponseException) {\n-                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n-                logger.warn(msg, onResponseException);\n-            } finally {\n-                next.run();\n-            }\n-        }, e -> {\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    ActionListener.onFailure(saveStateListenersAtTheMomentOfCalling, e);\n-                }\n-            } catch (Exception onFailureException) {\n-                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n-                logger.warn(msg, onFailureException);\n-            } finally {\n-                next.run();\n-            }\n-        }));\n-    }\n-\n-    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n-        persistState(state, ActionListener.runBefore(listener, () -> {\n-            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                context.shutdown();\n-            }\n-        }));\n-    }\n-\n     /**\n      * Runs the persistence part of state storage\n      */\nx-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java\n@@ -19,6 +19,7 @@\n import org.elasticsearch.action.search.SearchResponse;\n import org.elasticsearch.action.support.IndicesOptions;\n import org.elasticsearch.common.breaker.CircuitBreakingException;\n+import org.elasticsearch.common.logging.LoggerMessageFormat;\n import org.elasticsearch.common.util.CollectionUtils;\n import org.elasticsearch.core.Tuple;\n import org.elasticsearch.index.query.BoolQueryBuilder;\n@@ -642,6 +643,119 @@ protected void onAbort() {\n         context.shutdown();\n     }\n \n+    @Override\n+    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {\n+        if (context.getTaskState() == TransformTaskState.FAILED) {\n+            logger.debug(\"[{}] attempted to save state and stats while failed.\", getJobId());\n+            // If we are failed, we should call next to allow failure handling to occur if necessary.\n+            next.run();\n+            return;\n+        }\n+        if (indexerState.equals(IndexerState.ABORTING)) {\n+            // If we're aborting, just invoke `next` (which is likely an onFailure handler)\n+            next.run();\n+            return;\n+        }\n+\n+        // getting the listeners that registered till now, in theory a new listener could sneak in between this line\n+        // and the next, however this is benign: we would store `shouldStopAtCheckpoint = true` twice which is ok\n+        Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n+        boolean shouldStopAtCheckpoint = context.shouldStopAtCheckpoint();\n+\n+        // If we should stop at the next checkpoint, are STARTED, and with `initialRun()` we are in one of two states\n+        // 1. We have just called `onFinish` completing our request, but `shouldStopAtCheckpoint` was set to `true` before our check\n+        // there and now\n+        // 2. We are on the very first run of a NEW checkpoint and got here either through a failure, or the very first save state call.\n+        //\n+        // In either case, we should stop so that we guarantee a consistent state and that there are no partially completed checkpoints\n+        if (shouldStopAtCheckpoint && initialRun() && indexerState.equals(IndexerState.STARTED)) {\n+            indexerState = IndexerState.STOPPED;\n+            auditor.info(transformConfig.getId(), \"Transform is no longer in the middle of a checkpoint, initiating stop.\");\n+            logger.info(\"[{}] transform is no longer in the middle of a checkpoint, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // This means that the indexer was triggered to discover changes, found none, and exited early.\n+        // If the state is `STOPPED` this means that TransformTask#stop was called while we were checking for changes.\n+        // Allow the stop call path to continue\n+        if (hasSourceChanged == false && indexerState.equals(IndexerState.STOPPED) == false) {\n+            if (saveStateListenersAtTheMomentOfCalling != null) {\n+                ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n+            }\n+            next.run();\n+            return;\n+        }\n+\n+        TransformTaskState taskState = context.getTaskState();\n+\n+        if (indexerState.equals(IndexerState.STARTED) && context.getCheckpoint() == 1 && this.isContinuous() == false) {\n+            // set both to stopped so they are persisted as such\n+            indexerState = IndexerState.STOPPED;\n+\n+            auditor.info(transformConfig.getId(), \"Transform finished indexing all data, initiating stop\");\n+            logger.info(\"[{}] transform finished indexing all data, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // If we are `STOPPED` on a `doSaveState` call, that indicates we transitioned to `STOPPED` from `STOPPING`\n+        // OR we called `doSaveState` manually as the indexer was not actively running.\n+        // Since we save the state to an index, we should make sure that our task state is in parity with the indexer state\n+        if (indexerState.equals(IndexerState.STOPPED)) {\n+            // If we are going to stop after the state is saved, we should NOT persist `shouldStopAtCheckpoint: true` as this may\n+            // cause problems if the task starts up again.\n+            // Additionally, we don't have to worry about inconsistency with the ClusterState (if it is persisted there) as the\n+            // when we stop, we mark the task as complete and that state goes away.\n+            shouldStopAtCheckpoint = false;\n+\n+            // We don't want adjust the stored taskState because as soon as it is `STOPPED` a user could call\n+            // .start again.\n+            taskState = TransformTaskState.STOPPED;\n+        }\n+\n+        final TransformState state = new TransformState(\n+            taskState,\n+            indexerState,\n+            position,\n+            context.getCheckpoint(),\n+            context.getStateReason(),\n+            getProgress(),\n+            null,\n+            shouldStopAtCheckpoint\n+        );\n+        logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n+\n+        // we might need to call the save state listeners, but do not want to stop rolling\n+        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n+                }\n+            } catch (Exception onResponseException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onResponseException);\n+            } finally {\n+                next.run();\n+            }\n+        }, e -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onFailure(saveStateListenersAtTheMomentOfCalling, e);\n+                }\n+            } catch (Exception onFailureException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onFailureException);\n+            } finally {\n+                next.run();\n+            }\n+        }));\n+    }\n+\n+    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n+        persistState(state, ActionListener.runBefore(listener, () -> {\n+            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+        }));\n+    }\n+\n     /**\n      * Let the indexer stop at the next checkpoint and call the listener after the flag has been persisted in state.\n      *\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerFailureHandlingTests.java\n@@ -198,7 +198,7 @@ protected void doNextBulk(BulkRequest request, ActionListener<BulkResponse> next\n         @Override\n         protected void doSaveState(IndexerState state, TransformIndexerPosition position, Runnable next) {\n             assert state == IndexerState.STARTED || state == IndexerState.INDEXING || state == IndexerState.STOPPED;\n-            next.run();\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java\n@@ -205,28 +205,11 @@ protected void doNextBulk(BulkRequest request, ActionListener<BulkResponse> next\n \n         @Override\n         protected void doSaveState(IndexerState state, TransformIndexerPosition position, Runnable next) {\n-            persistedState = new TransformState(\n-                context.getTaskState(),\n-                state,\n-                position,\n-                context.getCheckpoint(),\n-                context.getStateReason(),\n-                getProgress(),\n-                null,\n-                context.shouldStopAtCheckpoint()\n-            );\n-\n-            Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    saveStateListenerCallCount += saveStateListenersAtTheMomentOfCalling.size();\n-                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n-                }\n-            } catch (Exception onResponseException) {\n-                fail(\"failed to call save state listeners\");\n-            } finally {\n-                next.run();\n-            }\n+            Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.get();\n+            saveStateListenerCallCount += (saveStateListenersAtTheMomentOfCalling != null)\n+                ? saveStateListenersAtTheMomentOfCalling.size()\n+                : 0;\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override\nx-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerTests.java\n@@ -239,7 +239,7 @@ protected void doSaveState(IndexerState state, TransformIndexerPosition position\n             assert state == IndexerState.STARTED || state == IndexerState.INDEXING || state == IndexerState.STOPPED;\n \n             assertTrue(saveStateInProgress.compareAndSet(true, false));\n-            next.run();\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override",
      "diff": [
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java",
          "patch": "@@ -56,7 +56,6 @@\n import org.elasticsearch.xpack.transform.transforms.pivot.SchemaUtil;\n import org.elasticsearch.xpack.transform.utils.ExceptionRootCauseFinder;\n \n-import java.util.Collection;\n import java.util.LinkedHashMap;\n import java.util.Map;\n import java.util.Map.Entry;\n@@ -257,119 +256,6 @@ void doGetFieldMappings(ActionListener<Map<String, String>> fieldMappingsListene\n         );\n     }\n \n-    @Override\n-    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {\n-        if (context.getTaskState() == TransformTaskState.FAILED) {\n-            logger.debug(\"[{}] attempted to save state and stats while failed.\", getJobId());\n-            // If we are failed, we should call next to allow failure handling to occur if necessary.\n-            next.run();\n-            return;\n-        }\n-        if (indexerState.equals(IndexerState.ABORTING)) {\n-            // If we're aborting, just invoke `next` (which is likely an onFailure handler)\n-            next.run();\n-            return;\n-        }\n-\n-        // getting the listeners that registered till now, in theory a new listener could sneak in between this line\n-        // and the next, however this is benign: we would store `shouldStopAtCheckpoint = true` twice which is ok\n-        Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n-        boolean shouldStopAtCheckpoint = context.shouldStopAtCheckpoint();\n-\n-        // If we should stop at the next checkpoint, are STARTED, and with `initialRun()` we are in one of two states\n-        // 1. We have just called `onFinish` completing our request, but `shouldStopAtCheckpoint` was set to `true` before our check\n-        // there and now\n-        // 2. We are on the very first run of a NEW checkpoint and got here either through a failure, or the very first save state call.\n-        //\n-        // In either case, we should stop so that we guarantee a consistent state and that there are no partially completed checkpoints\n-        if (shouldStopAtCheckpoint && initialRun() && indexerState.equals(IndexerState.STARTED)) {\n-            indexerState = IndexerState.STOPPED;\n-            auditor.info(transformConfig.getId(), \"Transform is no longer in the middle of a checkpoint, initiating stop.\");\n-            logger.info(\"[{}] transform is no longer in the middle of a checkpoint, initiating stop.\", transformConfig.getId());\n-        }\n-\n-        // This means that the indexer was triggered to discover changes, found none, and exited early.\n-        // If the state is `STOPPED` this means that TransformTask#stop was called while we were checking for changes.\n-        // Allow the stop call path to continue\n-        if (hasSourceChanged == false && indexerState.equals(IndexerState.STOPPED) == false) {\n-            if (saveStateListenersAtTheMomentOfCalling != null) {\n-                ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n-            }\n-            next.run();\n-            return;\n-        }\n-\n-        TransformTaskState taskState = context.getTaskState();\n-\n-        if (indexerState.equals(IndexerState.STARTED) && context.getCheckpoint() == 1 && this.isContinuous() == false) {\n-            // set both to stopped so they are persisted as such\n-            indexerState = IndexerState.STOPPED;\n-\n-            auditor.info(transformConfig.getId(), \"Transform finished indexing all data, initiating stop\");\n-            logger.info(\"[{}] transform finished indexing all data, initiating stop.\", transformConfig.getId());\n-        }\n-\n-        // If we are `STOPPED` on a `doSaveState` call, that indicates we transitioned to `STOPPED` from `STOPPING`\n-        // OR we called `doSaveState` manually as the indexer was not actively running.\n-        // Since we save the state to an index, we should make sure that our task state is in parity with the indexer state\n-        if (indexerState.equals(IndexerState.STOPPED)) {\n-            // If we are going to stop after the state is saved, we should NOT persist `shouldStopAtCheckpoint: true` as this may\n-            // cause problems if the task starts up again.\n-            // Additionally, we don't have to worry about inconsistency with the ClusterState (if it is persisted there) as the\n-            // when we stop, we mark the task as complete and that state goes away.\n-            shouldStopAtCheckpoint = false;\n-\n-            // We don't want adjust the stored taskState because as soon as it is `STOPPED` a user could call\n-            // .start again.\n-            taskState = TransformTaskState.STOPPED;\n-        }\n-\n-        final TransformState state = new TransformState(\n-            taskState,\n-            indexerState,\n-            position,\n-            context.getCheckpoint(),\n-            context.getStateReason(),\n-            getProgress(),\n-            null,\n-            shouldStopAtCheckpoint\n-        );\n-        logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n-\n-        // we might need to call the save state listeners, but do not want to stop rolling\n-        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n-                }\n-            } catch (Exception onResponseException) {\n-                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n-                logger.warn(msg, onResponseException);\n-            } finally {\n-                next.run();\n-            }\n-        }, e -> {\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    ActionListener.onFailure(saveStateListenersAtTheMomentOfCalling, e);\n-                }\n-            } catch (Exception onFailureException) {\n-                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n-                logger.warn(msg, onFailureException);\n-            } finally {\n-                next.run();\n-            }\n-        }));\n-    }\n-\n-    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n-        persistState(state, ActionListener.runBefore(listener, () -> {\n-            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n-                context.shutdown();\n-            }\n-        }));\n-    }\n-\n     /**\n      * Runs the persistence part of state storage\n      */"
        },
        {
          "file": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
          "patch": "@@ -19,6 +19,7 @@\n import org.elasticsearch.action.search.SearchResponse;\n import org.elasticsearch.action.support.IndicesOptions;\n import org.elasticsearch.common.breaker.CircuitBreakingException;\n+import org.elasticsearch.common.logging.LoggerMessageFormat;\n import org.elasticsearch.common.util.CollectionUtils;\n import org.elasticsearch.core.Tuple;\n import org.elasticsearch.index.query.BoolQueryBuilder;\n@@ -642,6 +643,119 @@ protected void onAbort() {\n         context.shutdown();\n     }\n \n+    @Override\n+    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {\n+        if (context.getTaskState() == TransformTaskState.FAILED) {\n+            logger.debug(\"[{}] attempted to save state and stats while failed.\", getJobId());\n+            // If we are failed, we should call next to allow failure handling to occur if necessary.\n+            next.run();\n+            return;\n+        }\n+        if (indexerState.equals(IndexerState.ABORTING)) {\n+            // If we're aborting, just invoke `next` (which is likely an onFailure handler)\n+            next.run();\n+            return;\n+        }\n+\n+        // getting the listeners that registered till now, in theory a new listener could sneak in between this line\n+        // and the next, however this is benign: we would store `shouldStopAtCheckpoint = true` twice which is ok\n+        Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n+        boolean shouldStopAtCheckpoint = context.shouldStopAtCheckpoint();\n+\n+        // If we should stop at the next checkpoint, are STARTED, and with `initialRun()` we are in one of two states\n+        // 1. We have just called `onFinish` completing our request, but `shouldStopAtCheckpoint` was set to `true` before our check\n+        // there and now\n+        // 2. We are on the very first run of a NEW checkpoint and got here either through a failure, or the very first save state call.\n+        //\n+        // In either case, we should stop so that we guarantee a consistent state and that there are no partially completed checkpoints\n+        if (shouldStopAtCheckpoint && initialRun() && indexerState.equals(IndexerState.STARTED)) {\n+            indexerState = IndexerState.STOPPED;\n+            auditor.info(transformConfig.getId(), \"Transform is no longer in the middle of a checkpoint, initiating stop.\");\n+            logger.info(\"[{}] transform is no longer in the middle of a checkpoint, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // This means that the indexer was triggered to discover changes, found none, and exited early.\n+        // If the state is `STOPPED` this means that TransformTask#stop was called while we were checking for changes.\n+        // Allow the stop call path to continue\n+        if (hasSourceChanged == false && indexerState.equals(IndexerState.STOPPED) == false) {\n+            if (saveStateListenersAtTheMomentOfCalling != null) {\n+                ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n+            }\n+            next.run();\n+            return;\n+        }\n+\n+        TransformTaskState taskState = context.getTaskState();\n+\n+        if (indexerState.equals(IndexerState.STARTED) && context.getCheckpoint() == 1 && this.isContinuous() == false) {\n+            // set both to stopped so they are persisted as such\n+            indexerState = IndexerState.STOPPED;\n+\n+            auditor.info(transformConfig.getId(), \"Transform finished indexing all data, initiating stop\");\n+            logger.info(\"[{}] transform finished indexing all data, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // If we are `STOPPED` on a `doSaveState` call, that indicates we transitioned to `STOPPED` from `STOPPING`\n+        // OR we called `doSaveState` manually as the indexer was not actively running.\n+        // Since we save the state to an index, we should make sure that our task state is in parity with the indexer state\n+        if (indexerState.equals(IndexerState.STOPPED)) {\n+            // If we are going to stop after the state is saved, we should NOT persist `shouldStopAtCheckpoint: true` as this may\n+            // cause problems if the task starts up again.\n+            // Additionally, we don't have to worry about inconsistency with the ClusterState (if it is persisted there) as the\n+            // when we stop, we mark the task as complete and that state goes away.\n+            shouldStopAtCheckpoint = false;\n+\n+            // We don't want adjust the stored taskState because as soon as it is `STOPPED` a user could call\n+            // .start again.\n+            taskState = TransformTaskState.STOPPED;\n+        }\n+\n+        final TransformState state = new TransformState(\n+            taskState,\n+            indexerState,\n+            position,\n+            context.getCheckpoint(),\n+            context.getStateReason(),\n+            getProgress(),\n+            null,\n+            shouldStopAtCheckpoint\n+        );\n+        logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n+\n+        // we might need to call the save state listeners, but do not want to stop rolling\n+        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n+                }\n+            } catch (Exception onResponseException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onResponseException);\n+            } finally {\n+                next.run();\n+            }\n+        }, e -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onFailure(saveStateListenersAtTheMomentOfCalling, e);\n+                }\n+            } catch (Exception onFailureException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onFailureException);\n+            } finally {\n+                next.run();\n+            }\n+        }));\n+    }\n+\n+    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {\n+        persistState(state, ActionListener.runBefore(listener, () -> {\n+            if (state.getTaskState().equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();\n+            }\n+        }));\n+    }\n+\n     /**\n      * Let the indexer stop at the next checkpoint and call the listener after the flag has been persisted in state.\n      *"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerFailureHandlingTests.java",
          "patch": "@@ -198,7 +198,7 @@ protected void doNextBulk(BulkRequest request, ActionListener<BulkResponse> next\n         @Override\n         protected void doSaveState(IndexerState state, TransformIndexerPosition position, Runnable next) {\n             assert state == IndexerState.STARTED || state == IndexerState.INDEXING || state == IndexerState.STOPPED;\n-            next.run();\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerStateTests.java",
          "patch": "@@ -205,28 +205,11 @@ protected void doNextBulk(BulkRequest request, ActionListener<BulkResponse> next\n \n         @Override\n         protected void doSaveState(IndexerState state, TransformIndexerPosition position, Runnable next) {\n-            persistedState = new TransformState(\n-                context.getTaskState(),\n-                state,\n-                position,\n-                context.getCheckpoint(),\n-                context.getStateReason(),\n-                getProgress(),\n-                null,\n-                context.shouldStopAtCheckpoint()\n-            );\n-\n-            Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n-            try {\n-                if (saveStateListenersAtTheMomentOfCalling != null) {\n-                    saveStateListenerCallCount += saveStateListenersAtTheMomentOfCalling.size();\n-                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n-                }\n-            } catch (Exception onResponseException) {\n-                fail(\"failed to call save state listeners\");\n-            } finally {\n-                next.run();\n-            }\n+            Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.get();\n+            saveStateListenerCallCount += (saveStateListenersAtTheMomentOfCalling != null)\n+                ? saveStateListenersAtTheMomentOfCalling.size()\n+                : 0;\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override"
        },
        {
          "file": "x-pack/plugin/transform/src/test/java/org/elasticsearch/xpack/transform/transforms/TransformIndexerTests.java",
          "patch": "@@ -239,7 +239,7 @@ protected void doSaveState(IndexerState state, TransformIndexerPosition position\n             assert state == IndexerState.STARTED || state == IndexerState.INDEXING || state == IndexerState.STOPPED;\n \n             assertTrue(saveStateInProgress.compareAndSet(true, false));\n-            next.run();\n+            super.doSaveState(state, position, next);\n         }\n \n         @Override"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 683412544,
      "created_at": "2021-08-05T12:38:23Z",
      "user": "hendrikmuhs",
      "body": "the thread switch is the only change, otherwise the code below is unchanged.\r\n\r\nBy moving the thread switch, the `transformTask.setShouldStopAtCheckpoint` call does not unlock the class lock of the task.",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/action/TransportStopTransformAction.java",
      "diff_hunk": "@@ -244,24 +244,28 @@ protected void taskOperation(Request request, TransformTask transformTask, Actio\n         }\n \n         if (ids.contains(transformTask.getTransformId())) {\n-            transformTask.setShouldStopAtCheckpoint(request.isWaitForCheckpoint(), ActionListener.wrap(r -> {\n-                try {\n-                    transformTask.stop(request.isForce(), request.isWaitForCheckpoint());\n-                    listener.onResponse(new Response(true));\n-                } catch (ElasticsearchException ex) {\n-                    listener.onFailure(ex);\n-                }\n-            },\n-                e -> listener.onFailure(\n-                    new ElasticsearchStatusException(\n-                        \"Failed to update transform task [{}] state value should_stop_at_checkpoint from [{}] to [{}]\",\n-                        RestStatus.CONFLICT,\n-                        transformTask.getTransformId(),\n-                        transformTask.getState().shouldStopAtNextCheckpoint(),\n-                        request.isWaitForCheckpoint()\n+            // move the call to the generic thread pool, so we do not block the network thread\n+            threadPool.executor(ThreadPool.Names.GENERIC).execute(() -> {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 248,
      "original_line": 248,
      "side": "RIGHT",
      "original_position": 20,
      "position": 20,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683413145,
      "created_at": "2021-08-05T12:39:11Z",
      "user": "hendrikmuhs",
      "body": "this is now wrapped in `persistStateWithAutoStop`",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/ClientTransformIndexer.java",
      "diff_hunk": "@@ -374,10 +272,6 @@ private void doSaveState(TransformState state, ActionListener<Void> listener) {\n             seqNoPrimaryTermAndIndex,\n             ActionListener.wrap(r -> {\n                 updateSeqNoPrimaryTermAndIndex(seqNoPrimaryTermAndIndex, r);\n-                // for auto stop shutdown the task\n-                if (state.getTaskState().equals(TransformTaskState.STOPPED)) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 378,
      "original_line": 378,
      "side": "LEFT",
      "original_position": 130,
      "position": 130,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683413585,
      "created_at": "2021-08-05T12:39:51Z",
      "user": "hendrikmuhs",
      "body": "this method has been moved from ClientTransformIndexer without changes",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
      "diff_hunk": "@@ -639,6 +643,119 @@ protected void onAbort() {\n         context.shutdown();\n     }\n \n+    @Override\n+    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 647,
      "original_line": 647,
      "side": "RIGHT",
      "original_position": 30,
      "position": 30,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683414306,
      "created_at": "2021-08-05T12:40:50Z",
      "user": "hendrikmuhs",
      "body": "to fix the deadlock we don't initiate the shutdown in this place.",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
      "diff_hunk": "@@ -639,6 +643,119 @@ protected void onAbort() {\n         context.shutdown();\n     }\n \n+    @Override\n+    protected void doSaveState(IndexerState indexerState, TransformIndexerPosition position, Runnable next) {\n+        if (context.getTaskState() == TransformTaskState.FAILED) {\n+            logger.debug(\"[{}] attempted to save state and stats while failed.\", getJobId());\n+            // If we are failed, we should call next to allow failure handling to occur if necessary.\n+            next.run();\n+            return;\n+        }\n+        if (indexerState.equals(IndexerState.ABORTING)) {\n+            // If we're aborting, just invoke `next` (which is likely an onFailure handler)\n+            next.run();\n+            return;\n+        }\n+\n+        // getting the listeners that registered till now, in theory a new listener could sneak in between this line\n+        // and the next, however this is benign: we would store `shouldStopAtCheckpoint = true` twice which is ok\n+        Collection<ActionListener<Void>> saveStateListenersAtTheMomentOfCalling = saveStateListeners.getAndSet(null);\n+        boolean shouldStopAtCheckpoint = context.shouldStopAtCheckpoint();\n+\n+        // If we should stop at the next checkpoint, are STARTED, and with `initialRun()` we are in one of two states\n+        // 1. We have just called `onFinish` completing our request, but `shouldStopAtCheckpoint` was set to `true` before our check\n+        // there and now\n+        // 2. We are on the very first run of a NEW checkpoint and got here either through a failure, or the very first save state call.\n+        //\n+        // In either case, we should stop so that we guarantee a consistent state and that there are no partially completed checkpoints\n+        if (shouldStopAtCheckpoint && initialRun() && indexerState.equals(IndexerState.STARTED)) {\n+            indexerState = IndexerState.STOPPED;\n+            auditor.info(transformConfig.getId(), \"Transform is no longer in the middle of a checkpoint, initiating stop.\");\n+            logger.info(\"[{}] transform is no longer in the middle of a checkpoint, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // This means that the indexer was triggered to discover changes, found none, and exited early.\n+        // If the state is `STOPPED` this means that TransformTask#stop was called while we were checking for changes.\n+        // Allow the stop call path to continue\n+        if (hasSourceChanged == false && indexerState.equals(IndexerState.STOPPED) == false) {\n+            if (saveStateListenersAtTheMomentOfCalling != null) {\n+                ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, null);\n+            }\n+            next.run();\n+            return;\n+        }\n+\n+        TransformTaskState taskState = context.getTaskState();\n+\n+        if (indexerState.equals(IndexerState.STARTED) && context.getCheckpoint() == 1 && this.isContinuous() == false) {\n+            // set both to stopped so they are persisted as such\n+            indexerState = IndexerState.STOPPED;\n+\n+            auditor.info(transformConfig.getId(), \"Transform finished indexing all data, initiating stop\");\n+            logger.info(\"[{}] transform finished indexing all data, initiating stop.\", transformConfig.getId());\n+        }\n+\n+        // If we are `STOPPED` on a `doSaveState` call, that indicates we transitioned to `STOPPED` from `STOPPING`\n+        // OR we called `doSaveState` manually as the indexer was not actively running.\n+        // Since we save the state to an index, we should make sure that our task state is in parity with the indexer state\n+        if (indexerState.equals(IndexerState.STOPPED)) {\n+            // If we are going to stop after the state is saved, we should NOT persist `shouldStopAtCheckpoint: true` as this may\n+            // cause problems if the task starts up again.\n+            // Additionally, we don't have to worry about inconsistency with the ClusterState (if it is persisted there) as the\n+            // when we stop, we mark the task as complete and that state goes away.\n+            shouldStopAtCheckpoint = false;\n+\n+            // We don't want adjust the stored taskState because as soon as it is `STOPPED` a user could call\n+            // .start again.\n+            taskState = TransformTaskState.STOPPED;\n+        }\n+\n+        final TransformState state = new TransformState(\n+            taskState,\n+            indexerState,\n+            position,\n+            context.getCheckpoint(),\n+            context.getStateReason(),\n+            getProgress(),\n+            null,\n+            shouldStopAtCheckpoint\n+        );\n+        logger.debug(\"[{}] updating persistent state of transform to [{}].\", transformConfig.getId(), state.toString());\n+\n+        // we might need to call the save state listeners, but do not want to stop rolling\n+        persistStateWithAutoStop(state, ActionListener.wrap(r -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onResponse(saveStateListenersAtTheMomentOfCalling, r);\n+                }\n+            } catch (Exception onResponseException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onResponseException);\n+            } finally {\n+                next.run();\n+            }\n+        }, e -> {\n+            try {\n+                if (saveStateListenersAtTheMomentOfCalling != null) {\n+                    ActionListener.onFailure(saveStateListenersAtTheMomentOfCalling, e);\n+                }\n+            } catch (Exception onFailureException) {\n+                String msg = LoggerMessageFormat.format(\"[{}] failed notifying saveState listeners, ignoring.\", getJobId());\n+                logger.warn(msg, onFailureException);\n+            } finally {\n+                next.run();\n+            }\n+        }));\n+    }\n+\n+    private void persistStateWithAutoStop(TransformState state, ActionListener<Void> listener) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 751,
      "original_line": 751,
      "side": "RIGHT",
      "original_position": 134,
      "position": 134,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683415723,
      "created_at": "2021-08-05T12:42:43Z",
      "user": "hendrikmuhs",
      "body": "instead of calling `doSaveState` we call persist, which simplifies a lot, because `doSaveState` makes a lot of other checks that aren't necessary for this case.",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
      "diff_hunk": "@@ -691,14 +807,37 @@ private synchronized boolean addSetStopAtCheckpointListener(\n \n         // in case the indexer isn't running, respond immediately\n         if (state == IndexerState.STARTED && context.shouldStopAtCheckpoint() != shouldStopAtCheckpoint) {\n-            context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            IndexerState newIndexerState = IndexerState.STARTED;\n+            TransformTaskState newtaskState = context.getTaskState();\n+\n+            // check if the transform is at a checkpoint, if so, we will shortcut and stop it below\n+            // otherwise we set shouldStopAtCheckpoint, for this case the transform needs to get\n+            // triggered, complete the checkpoint and stop\n+            if (shouldStopAtCheckpoint && initialRun()) {\n+                newIndexerState = IndexerState.STOPPED;\n+                newtaskState = TransformTaskState.STOPPED;\n+                logger.debug(\"[{}] transform is at a checkpoint, initiating stop.\", transformConfig.getId());\n+            } else {\n+                context.setShouldStopAtCheckpoint(shouldStopAtCheckpoint);\n+            }\n+\n+            final TransformState newTransformState = new TransformState(\n+                newtaskState,\n+                newIndexerState,\n+                getPosition(),\n+                context.getCheckpoint(),\n+                context.getStateReason(),\n+                getProgress(),\n+                null,\n+                newIndexerState == IndexerState.STARTED\n+            );\n \n             // because save state is async we need to block the call until state is persisted, so that the job can not\n             // be triggered (ensured by synchronized)\n             CountDownLatch latch = new CountDownLatch(1);\n-            logger.debug(\"[{}] persisiting stop at checkpoint\", getJobId());\n+            logger.debug(\"[{}] persisting stop at checkpoint\", getJobId());\n \n-            doSaveState(IndexerState.STARTED, getPosition(), () -> { latch.countDown(); });\n+            persistState(newTransformState, ActionListener.wrap(() -> latch.countDown()));",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 840,
      "original_line": 840,
      "side": "RIGHT",
      "original_position": 190,
      "position": 190,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683416733,
      "created_at": "2021-08-05T12:44:01Z",
      "user": "hendrikmuhs",
      "body": "the shutdown now happens in the thread that locked the task and the indexer. shutdown locks the task once more.",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformIndexer.java",
      "diff_hunk": "@@ -709,6 +848,12 @@ private synchronized boolean addSetStopAtCheckpointListener(\n                     )\n                 );\n             }\n+\n+            // stop the transform if the decision was to stop it above\n+            if (newtaskState.equals(TransformTaskState.STOPPED)) {\n+                context.shutdown();",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 854,
      "original_line": 854,
      "side": "RIGHT",
      "original_position": 201,
      "position": 201,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 683418236,
      "created_at": "2021-08-05T12:46:05Z",
      "user": "hendrikmuhs",
      "body": "this is the origin of the deadlock, by moving to another thread we unlocked the task (this method is `synchronized`).",
      "path": "x-pack/plugin/transform/src/main/java/org/elasticsearch/xpack/transform/transforms/TransformTask.java",
      "diff_hunk": "@@ -311,9 +313,12 @@ public synchronized void setShouldStopAtCheckpoint(\n             return;\n         }\n \n-        // move the call to the generic thread pool, so we do not block the network thread\n-        getThreadPool().executor(ThreadPool.Names.GENERIC)",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 315,
      "original_line": 315,
      "side": "LEFT",
      "original_position": 14,
      "position": 14,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 723300889,
      "created_at": "2021-08-05T12:47:20Z",
      "user": "hendrikmuhs",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 723364100,
      "created_at": "2021-08-05T13:36:42Z",
      "user": "benwtrent",
      "state": "APPROVED",
      "body": "moving the persistence + stop into the top level action and not calling `doSaveState` will probably address the issue.\r\n\r\nI would like to have this PR running in CI for a bit to catch anything else lingering for sure but I think it looks ok. "
    },
    {
      "type": "comment",
      "id": 894288422,
      "created_at": "2021-08-06T14:10:34Z",
      "user": "elasticsearchmachine",
      "body": "## 💚 Backport successful\n\n| Status | Branch | Result |\n|:------:|:------:|:------:|\n| ✅ |  [7.x](https://github.com/elastic/elasticsearch/pull/76211)  | [<img src=\"https://img.shields.io/github/pulls/detail/state/elastic/elasticsearch/76211\">](https://github.com/elastic/elasticsearch/pull/76211) |"
    }
  ]
}