{
  "repo": "netty/netty",
  "number": 10363,
  "pr_number": 10363,
  "title": "Ensure ApplicationProtocolNegotiationHandler does handle handshake fa…",
  "user": "normanmaurer",
  "created_at": "2020-06-22T14:26:24Z",
  "merged_at": "2020-06-24T06:42:09Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "48938e2d99d51c8226f60b045b9a7a4520ed65da",
  "merge_commit_sha": "f3356c298939ade98a12263a23d05f5b21eae472",
  "commits": 7,
  "issue_events": [
    "review_requested",
    "review_requested",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "milestoned",
    "referenced",
    "merged",
    "closed",
    "head_ref_deleted",
    "referenced",
    "referenced",
    "referenced"
  ],
  "url": "https://github.com/netty/netty/pull/10363",
  "timeline": [
    {
      "type": "commit",
      "date": "2020-06-22T14:23:41Z",
      "sha": "57c8833e6e2d32064dc5b2af6ce2cee48cb724d0",
      "message": "Ensure ApplicationProtocolNegotiationHandler does handle handshake failures completely\n\nMotivation:\n\nWhen ApplicationProtocolNegotiationHandler is in the pipeline we should expect that its handshakeFailure(...) method will be able to completly handle the handshake error. At the moment this is not the case as it only handled SslHandshakeCompletionEvent but not the exceptionCaught(...) that is also triggered in this case\n\nModifications:\n\n- Call handshakeFailure(...) in exceptionCaught and so fix double notification.\n- Add testcases\n\nResult:\n\nFixes https://github.com/netty/netty/issues/10342",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-22T14:23:41Z",
      "author_date": "2020-06-22T14:23:41Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-22T14:23:41Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java\n@@ -20,10 +20,13 @@\n import io.netty.channel.ChannelInboundHandlerAdapter;\n import io.netty.channel.ChannelInitializer;\n import io.netty.channel.ChannelPipeline;\n+import io.netty.handler.codec.DecoderException;\n import io.netty.util.internal.ObjectUtil;\n import io.netty.util.internal.logging.InternalLogger;\n import io.netty.util.internal.logging.InternalLoggerFactory;\n \n+import javax.net.ssl.SSLException;\n+\n /**\n  * Configures a {@link ChannelPipeline} depending on the application-level protocol negotiation result of\n  * {@link SslHandler}.  For example, you could configure your HTTP pipeline depending on the result of ALPN:\n@@ -78,30 +81,33 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n \n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n-        if (evt instanceof SslHandshakeCompletionEvent) {\n-\n-            try {\n-                SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n-                if (handshakeEvent.isSuccess()) {\n-                    SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n-                    if (sslHandler == null) {\n-                        throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n-                                + \"application-level protocol negotiation)\");\n-                    }\n-                    String protocol = sslHandler.applicationProtocol();\n-                    configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n-                } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n-                }\n-            } catch (Throwable cause) {\n-                exceptionCaught(ctx, cause);\n-            } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+        SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n+        try {\n+            if (handshakeEvent.isSuccess()) {\n+                SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n+                if (sslHandler == null) {\n+                    throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n+                            + \"application-level protocol negotiation)\");\n                 }\n+                String protocol = sslHandler.applicationProtocol();\n+                configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n+            } else {\n+                // if the event is not produced because of an successful handshake we will receive the same exception\n+                // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n+                // which exception we propergate down the ChannelPipeline.\n+                //\n+                // See https://github.com/netty/netty/issues/10342\n+            }\n+        } catch (Throwable cause) {\n+            exceptionCaught(ctx, cause);\n+        } finally {\n+            ChannelPipeline pipeline = ctx.pipeline();\n+            // Handshake failures are handled in exceptionCaught(...).\n+            if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n+                pipeline.remove(this);\n             }\n         }\n+\n         ctx.fireUserEventTriggered(evt);\n     }\n \n@@ -125,6 +131,18 @@ protected void handshakeFailure(ChannelHandlerContext ctx, Throwable cause) thro\n \n     @Override\n     public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {\n+        Throwable wrapped;\n+        if (cause instanceof DecoderException && ((wrapped = cause.getCause()) instanceof SSLException)) {\n+            try {\n+                handshakeFailure(ctx, wrapped);\n+                return;\n+            } finally {\n+                ChannelPipeline pipeline = ctx.pipeline();\n+                if (pipeline.context(this) != null) {\n+                    pipeline.remove(this);\n+                }\n+            }\n+        }\n         logger.warn(\"{} Failed to select the application-level protocol:\", ctx.channel(), cause);\n         ctx.fireExceptionCaught(cause);\n         ctx.close();\nhandler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java\n@@ -0,0 +1,93 @@\n+/*\n+ * Copyright 2020 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   http://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+package io.netty.handler.ssl;\n+\n+import io.netty.channel.ChannelHandler;\n+import io.netty.channel.ChannelHandlerContext;\n+import io.netty.channel.embedded.EmbeddedChannel;\n+import io.netty.handler.codec.DecoderException;\n+import org.junit.Test;\n+\n+import javax.net.ssl.SSLContext;\n+import javax.net.ssl.SSLHandshakeException;\n+import java.security.NoSuchAlgorithmException;\n+import java.util.concurrent.atomic.AtomicBoolean;\n+\n+import static org.junit.Assert.assertEquals;\n+import static org.junit.Assert.assertFalse;\n+import static org.junit.Assert.assertNull;\n+import static org.junit.Assert.assertTrue;\n+import static org.junit.Assert.fail;\n+\n+\n+public class ApplicationProtocolNegotiationHandlerTest {\n+\n+    @Test\n+    public void testHandshakeFailure() {\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n+                fail();\n+            }\n+        };\n+\n+        EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n+        SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n+        channel.pipeline().fireUserEventTriggered(completionEvent);\n+        channel.pipeline().fireExceptionCaught(new DecoderException(exception));\n+        assertNull(channel.pipeline().context(alpnHandler));\n+        assertFalse(channel.finishAndReleaseAll());\n+    }\n+\n+    @Test\n+    public void testHandshakeSuccess() throws NoSuchAlgorithmException {\n+        final AtomicBoolean configureCalled = new AtomicBoolean(false);\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) throws Exception {\n+                configureCalled.set(true);\n+                assertEquals(ApplicationProtocolNames.HTTP_1_1, protocol);\n+            }\n+        };\n+\n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(SSLContext.getDefault().createSSLEngine()),\n+                alpnHandler);\n+\n+        channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n+        assertNull(channel.pipeline().context(alpnHandler));\n+        // Should produce the close_notify messages\n+        assertTrue(channel.finishAndReleaseAll());\n+        assertTrue(configureCalled.get());\n+    }\n+\n+    @Test(expected = IllegalStateException.class)\n+    public void testHandshakeSuccessButNoSslHandler() {\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n+                fail();\n+            }\n+        };\n+        EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        try {\n+            channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n+        } finally {\n+            assertNull(channel.pipeline().context(alpnHandler));\n+            assertFalse(channel.finishAndReleaseAll());\n+        }\n+    }\n+}",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
          "patch": "@@ -20,10 +20,13 @@\n import io.netty.channel.ChannelInboundHandlerAdapter;\n import io.netty.channel.ChannelInitializer;\n import io.netty.channel.ChannelPipeline;\n+import io.netty.handler.codec.DecoderException;\n import io.netty.util.internal.ObjectUtil;\n import io.netty.util.internal.logging.InternalLogger;\n import io.netty.util.internal.logging.InternalLoggerFactory;\n \n+import javax.net.ssl.SSLException;\n+\n /**\n  * Configures a {@link ChannelPipeline} depending on the application-level protocol negotiation result of\n  * {@link SslHandler}.  For example, you could configure your HTTP pipeline depending on the result of ALPN:\n@@ -78,30 +81,33 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n \n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n-        if (evt instanceof SslHandshakeCompletionEvent) {\n-\n-            try {\n-                SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n-                if (handshakeEvent.isSuccess()) {\n-                    SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n-                    if (sslHandler == null) {\n-                        throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n-                                + \"application-level protocol negotiation)\");\n-                    }\n-                    String protocol = sslHandler.applicationProtocol();\n-                    configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n-                } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n-                }\n-            } catch (Throwable cause) {\n-                exceptionCaught(ctx, cause);\n-            } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+        SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n+        try {\n+            if (handshakeEvent.isSuccess()) {\n+                SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n+                if (sslHandler == null) {\n+                    throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n+                            + \"application-level protocol negotiation)\");\n                 }\n+                String protocol = sslHandler.applicationProtocol();\n+                configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n+            } else {\n+                // if the event is not produced because of an successful handshake we will receive the same exception\n+                // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n+                // which exception we propergate down the ChannelPipeline.\n+                //\n+                // See https://github.com/netty/netty/issues/10342\n+            }\n+        } catch (Throwable cause) {\n+            exceptionCaught(ctx, cause);\n+        } finally {\n+            ChannelPipeline pipeline = ctx.pipeline();\n+            // Handshake failures are handled in exceptionCaught(...).\n+            if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n+                pipeline.remove(this);\n             }\n         }\n+\n         ctx.fireUserEventTriggered(evt);\n     }\n \n@@ -125,6 +131,18 @@ protected void handshakeFailure(ChannelHandlerContext ctx, Throwable cause) thro\n \n     @Override\n     public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {\n+        Throwable wrapped;\n+        if (cause instanceof DecoderException && ((wrapped = cause.getCause()) instanceof SSLException)) {\n+            try {\n+                handshakeFailure(ctx, wrapped);\n+                return;\n+            } finally {\n+                ChannelPipeline pipeline = ctx.pipeline();\n+                if (pipeline.context(this) != null) {\n+                    pipeline.remove(this);\n+                }\n+            }\n+        }\n         logger.warn(\"{} Failed to select the application-level protocol:\", ctx.channel(), cause);\n         ctx.fireExceptionCaught(cause);\n         ctx.close();"
        },
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java",
          "patch": "@@ -0,0 +1,93 @@\n+/*\n+ * Copyright 2020 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   http://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+package io.netty.handler.ssl;\n+\n+import io.netty.channel.ChannelHandler;\n+import io.netty.channel.ChannelHandlerContext;\n+import io.netty.channel.embedded.EmbeddedChannel;\n+import io.netty.handler.codec.DecoderException;\n+import org.junit.Test;\n+\n+import javax.net.ssl.SSLContext;\n+import javax.net.ssl.SSLHandshakeException;\n+import java.security.NoSuchAlgorithmException;\n+import java.util.concurrent.atomic.AtomicBoolean;\n+\n+import static org.junit.Assert.assertEquals;\n+import static org.junit.Assert.assertFalse;\n+import static org.junit.Assert.assertNull;\n+import static org.junit.Assert.assertTrue;\n+import static org.junit.Assert.fail;\n+\n+\n+public class ApplicationProtocolNegotiationHandlerTest {\n+\n+    @Test\n+    public void testHandshakeFailure() {\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n+                fail();\n+            }\n+        };\n+\n+        EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n+        SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n+        channel.pipeline().fireUserEventTriggered(completionEvent);\n+        channel.pipeline().fireExceptionCaught(new DecoderException(exception));\n+        assertNull(channel.pipeline().context(alpnHandler));\n+        assertFalse(channel.finishAndReleaseAll());\n+    }\n+\n+    @Test\n+    public void testHandshakeSuccess() throws NoSuchAlgorithmException {\n+        final AtomicBoolean configureCalled = new AtomicBoolean(false);\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) throws Exception {\n+                configureCalled.set(true);\n+                assertEquals(ApplicationProtocolNames.HTTP_1_1, protocol);\n+            }\n+        };\n+\n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(SSLContext.getDefault().createSSLEngine()),\n+                alpnHandler);\n+\n+        channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n+        assertNull(channel.pipeline().context(alpnHandler));\n+        // Should produce the close_notify messages\n+        assertTrue(channel.finishAndReleaseAll());\n+        assertTrue(configureCalled.get());\n+    }\n+\n+    @Test(expected = IllegalStateException.class)\n+    public void testHandshakeSuccessButNoSslHandler() {\n+        ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n+            @Override\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n+                fail();\n+            }\n+        };\n+        EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        try {\n+            channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n+        } finally {\n+            assertNull(channel.pipeline().context(alpnHandler));\n+            assertFalse(channel.finishAndReleaseAll());\n+        }\n+    }\n+}"
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2020-06-22T14:26:24Z",
      "user": "normanmaurer",
      "body": "…ilures completely\r\n\r\nMotivation:\r\n\r\nWhen ApplicationProtocolNegotiationHandler is in the pipeline we should expect that its handshakeFailure(...) method will be able to completly handle the handshake error. At the moment this is not the case as it only handled SslHandshakeCompletionEvent but not the exceptionCaught(...) that is also triggered in this case\r\n\r\nModifications:\r\n\r\n- Call handshakeFailure(...) in exceptionCaught and so fix double notification.\r\n- Add testcases\r\n\r\nResult:\r\n\r\nFixes https://github.com/netty/netty/issues/10342"
    },
    {
      "type": "comment",
      "id": 647553786,
      "created_at": "2020-06-22T14:27:09Z",
      "user": "normanmaurer",
      "body": "/cc @hyperxpro "
    },
    {
      "type": "review",
      "id": 435066952,
      "created_at": "2020-06-22T16:01:53Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 443666347,
      "created_at": "2020-06-22T16:01:53Z",
      "user": "johnou",
      "body": "possible class cast exception?",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -78,30 +81,33 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n \n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n-        if (evt instanceof SslHandshakeCompletionEvent) {\n-\n-            try {\n-                SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n-                if (handshakeEvent.isSuccess()) {\n-                    SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n-                    if (sslHandler == null) {\n-                        throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n-                                + \"application-level protocol negotiation)\");\n-                    }\n-                    String protocol = sslHandler.applicationProtocol();\n-                    configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n-                } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n-                }\n-            } catch (Throwable cause) {\n-                exceptionCaught(ctx, cause);\n-            } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+        SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 84,
      "side": "RIGHT",
      "original_position": 39,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "commit",
      "date": "2020-06-22T16:05:57Z",
      "sha": "e774406455530a0406146d5c20ecee80f02364cb",
      "message": "Fix possible ClassCastException",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-22T16:05:57Z",
      "author_date": "2020-06-22T16:05:57Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-22T16:05:57Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java\n@@ -81,30 +81,32 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n \n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n-        SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n-        try {\n-            if (handshakeEvent.isSuccess()) {\n-                SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n-                if (sslHandler == null) {\n-                    throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n-                            + \"application-level protocol negotiation)\");\n+        if (evt instanceof SslHandshakeCompletionEvent) {\n+            SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n+            try {\n+                if (handshakeEvent.isSuccess()) {\n+                    SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n+                    if (sslHandler == null) {\n+                        throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n+                                + \"application-level protocol negotiation)\");\n+                    }\n+                    String protocol = sslHandler.applicationProtocol();\n+                    configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n+                } else {\n+                    // if the event is not produced because of an successful handshake we will receive the same exception\n+                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n+                    // which exception we propergate down the ChannelPipeline.\n+                    //\n+                    // See https://github.com/netty/netty/issues/10342\n+                }\n+            } catch (Throwable cause) {\n+                exceptionCaught(ctx, cause);\n+            } finally {\n+                ChannelPipeline pipeline = ctx.pipeline();\n+                // Handshake failures are handled in exceptionCaught(...).\n+                if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n+                    pipeline.remove(this);\n                 }\n-                String protocol = sslHandler.applicationProtocol();\n-                configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n-            } else {\n-                // if the event is not produced because of an successful handshake we will receive the same exception\n-                // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n-                // which exception we propergate down the ChannelPipeline.\n-                //\n-                // See https://github.com/netty/netty/issues/10342\n-            }\n-        } catch (Throwable cause) {\n-            exceptionCaught(ctx, cause);\n-        } finally {\n-            ChannelPipeline pipeline = ctx.pipeline();\n-            // Handshake failures are handled in exceptionCaught(...).\n-            if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n-                pipeline.remove(this);\n             }\n         }\n ",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
          "patch": "@@ -81,30 +81,32 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n \n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n-        SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n-        try {\n-            if (handshakeEvent.isSuccess()) {\n-                SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n-                if (sslHandler == null) {\n-                    throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n-                            + \"application-level protocol negotiation)\");\n+        if (evt instanceof SslHandshakeCompletionEvent) {\n+            SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n+            try {\n+                if (handshakeEvent.isSuccess()) {\n+                    SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n+                    if (sslHandler == null) {\n+                        throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n+                                + \"application-level protocol negotiation)\");\n+                    }\n+                    String protocol = sslHandler.applicationProtocol();\n+                    configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n+                } else {\n+                    // if the event is not produced because of an successful handshake we will receive the same exception\n+                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n+                    // which exception we propergate down the ChannelPipeline.\n+                    //\n+                    // See https://github.com/netty/netty/issues/10342\n+                }\n+            } catch (Throwable cause) {\n+                exceptionCaught(ctx, cause);\n+            } finally {\n+                ChannelPipeline pipeline = ctx.pipeline();\n+                // Handshake failures are handled in exceptionCaught(...).\n+                if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n+                    pipeline.remove(this);\n                 }\n-                String protocol = sslHandler.applicationProtocol();\n-                configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n-            } else {\n-                // if the event is not produced because of an successful handshake we will receive the same exception\n-                // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n-                // which exception we propergate down the ChannelPipeline.\n-                //\n-                // See https://github.com/netty/netty/issues/10342\n-            }\n-        } catch (Throwable cause) {\n-            exceptionCaught(ctx, cause);\n-        } finally {\n-            ChannelPipeline pipeline = ctx.pipeline();\n-            // Handshake failures are handled in exceptionCaught(...).\n-            if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n-                pipeline.remove(this);\n             }\n         }\n "
        }
      ]
    },
    {
      "type": "review",
      "id": 435077844,
      "created_at": "2020-06-22T16:15:05Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 443674631,
      "created_at": "2020-06-22T16:15:05Z",
      "user": "johnou",
      "body": "propagate ",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -91,17 +93,23 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n+                    // if the event is not produced because of an successful handshake we will receive the same exception\n+                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n+                    // which exception we propergate down the ChannelPipeline.",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 98,
      "side": "RIGHT",
      "original_position": 32,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 435078719,
      "created_at": "2020-06-22T16:16:13Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 443675275,
      "created_at": "2020-06-22T16:16:13Z",
      "user": "johnou",
      "body": "cannot be reused?",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -125,6 +133,18 @@ protected void handshakeFailure(ChannelHandlerContext ctx, Throwable cause) thro\n \n     @Override\n     public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {\n+        Throwable wrapped;\n+        if (cause instanceof DecoderException && ((wrapped = cause.getCause()) instanceof SSLException)) {\n+            try {\n+                handshakeFailure(ctx, wrapped);\n+                return;\n+            } finally {\n+                ChannelPipeline pipeline = ctx.pipeline();\n+                if (pipeline.context(this) != null) {\n+                    pipeline.remove(this);",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 144,
      "side": "RIGHT",
      "original_position": 63,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 435149597,
      "created_at": "2020-06-22T17:52:30Z",
      "user": "carl-mastrangelo",
      "state": "APPROVED",
      "body": "LGTM"
    },
    {
      "type": "review_comment",
      "id": 443762344,
      "created_at": "2020-06-22T18:56:01Z",
      "user": "hyperxpro",
      "body": "This line is exceeding 120 characters' length threshold.",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -91,17 +93,23 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n+                    // if the event is not produced because of an successful handshake we will receive the same exception",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 96,
      "side": "RIGHT",
      "original_position": 30,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 435191618,
      "created_at": "2020-06-22T18:56:02Z",
      "user": "hyperxpro",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 647712038,
      "created_at": "2020-06-22T18:56:24Z",
      "user": "hyperxpro",
      "body": "All good! Thanks for the fix. :-D"
    },
    {
      "type": "commit",
      "date": "2020-06-23T07:38:16Z",
      "sha": "995de7e86111d0c6daeb2ebd3f2a5de268147504",
      "message": "Address comments",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-23T07:38:16Z",
      "author_date": "2020-06-23T07:38:16Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-23T07:38:16Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java\n@@ -93,26 +93,31 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    // if the event is not produced because of an successful handshake we will receive the same exception\n-                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n-                    // which exception we propergate down the ChannelPipeline.\n+                    // if the event is not produced because of an successful handshake we will receive the same\n+                    // exception in exceptionCaught(...) and handle it there. This will allow us more fine-grained\n+                    // control over which exception we propagate down the ChannelPipeline.\n                     //\n                     // See https://github.com/netty/netty/issues/10342\n                 }\n             } catch (Throwable cause) {\n                 exceptionCaught(ctx, cause);\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n                 // Handshake failures are handled in exceptionCaught(...).\n-                if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+                if (handshakeEvent.isSuccess()) {\n+                    removeSelfIfPresent(ctx);\n                 }\n             }\n         }\n \n         ctx.fireUserEventTriggered(evt);\n     }\n \n+    private void removeSelfIfPresent(ChannelHandlerContext ctx) {\n+        ChannelPipeline pipeline = ctx.pipeline();\n+        if (pipeline.context(this) != null) {\n+            pipeline.remove(this);\n+        }\n+    }\n     /**\n      * Invoked on successful initial SSL/TLS handshake. Implement this method to configure your pipeline\n      * for the negotiated application-level protocol.\n@@ -139,10 +144,7 @@ public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws E\n                 handshakeFailure(ctx, wrapped);\n                 return;\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n-                }\n+                removeSelfIfPresent(ctx);\n             }\n         }\n         logger.warn(\"{} Failed to select the application-level protocol:\", ctx.channel(), cause);",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
          "patch": "@@ -93,26 +93,31 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    // if the event is not produced because of an successful handshake we will receive the same exception\n-                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n-                    // which exception we propergate down the ChannelPipeline.\n+                    // if the event is not produced because of an successful handshake we will receive the same\n+                    // exception in exceptionCaught(...) and handle it there. This will allow us more fine-grained\n+                    // control over which exception we propagate down the ChannelPipeline.\n                     //\n                     // See https://github.com/netty/netty/issues/10342\n                 }\n             } catch (Throwable cause) {\n                 exceptionCaught(ctx, cause);\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n                 // Handshake failures are handled in exceptionCaught(...).\n-                if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+                if (handshakeEvent.isSuccess()) {\n+                    removeSelfIfPresent(ctx);\n                 }\n             }\n         }\n \n         ctx.fireUserEventTriggered(evt);\n     }\n \n+    private void removeSelfIfPresent(ChannelHandlerContext ctx) {\n+        ChannelPipeline pipeline = ctx.pipeline();\n+        if (pipeline.context(this) != null) {\n+            pipeline.remove(this);\n+        }\n+    }\n     /**\n      * Invoked on successful initial SSL/TLS handshake. Implement this method to configure your pipeline\n      * for the negotiated application-level protocol.\n@@ -139,10 +144,7 @@ public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws E\n                 handshakeFailure(ctx, wrapped);\n                 return;\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n-                }\n+                removeSelfIfPresent(ctx);\n             }\n         }\n         logger.warn(\"{} Failed to select the application-level protocol:\", ctx.channel(), cause);"
        }
      ]
    },
    {
      "type": "comment",
      "id": 647995056,
      "created_at": "2020-06-23T08:32:42Z",
      "user": "normanmaurer",
      "body": "@netty-bot test this please "
    },
    {
      "type": "commit",
      "date": "2020-06-23T08:40:29Z",
      "sha": "216a065462f6165d9903f3c81fc61f58515129db",
      "message": "cleanup",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-23T08:40:29Z",
      "author_date": "2020-06-23T08:40:29Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-23T08:40:29Z",
      "diff_text": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java\n@@ -58,7 +58,7 @@ public void testHandshakeSuccess() throws NoSuchAlgorithmException {\n         final AtomicBoolean configureCalled = new AtomicBoolean(false);\n         ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n             @Override\n-            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) throws Exception {\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n                 configureCalled.set(true);\n                 assertEquals(ApplicationProtocolNames.HTTP_1_1, protocol);\n             }",
      "diff": [
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java",
          "patch": "@@ -58,7 +58,7 @@ public void testHandshakeSuccess() throws NoSuchAlgorithmException {\n         final AtomicBoolean configureCalled = new AtomicBoolean(false);\n         ChannelHandler alpnHandler = new ApplicationProtocolNegotiationHandler(ApplicationProtocolNames.HTTP_1_1) {\n             @Override\n-            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) throws Exception {\n+            protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n                 configureCalled.set(true);\n                 assertEquals(ApplicationProtocolNames.HTTP_1_1, protocol);\n             }"
        }
      ]
    },
    {
      "type": "review",
      "id": 435615037,
      "created_at": "2020-06-23T09:40:37Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 444096417,
      "created_at": "2020-06-23T09:40:37Z",
      "user": "johnou",
      "body": "static?",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -93,26 +93,31 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    // if the event is not produced because of an successful handshake we will receive the same exception\n-                    // in exceptionCaught(...) and handle it there. This will allow us more fine-granted control over\n-                    // which exception we propergate down the ChannelPipeline.\n+                    // if the event is not produced because of an successful handshake we will receive the same\n+                    // exception in exceptionCaught(...) and handle it there. This will allow us more fine-grained\n+                    // control over which exception we propagate down the ChannelPipeline.\n                     //\n                     // See https://github.com/netty/netty/issues/10342\n                 }\n             } catch (Throwable cause) {\n                 exceptionCaught(ctx, cause);\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n                 // Handshake failures are handled in exceptionCaught(...).\n-                if (handshakeEvent.isSuccess() && pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+                if (handshakeEvent.isSuccess()) {\n+                    removeSelfIfPresent(ctx);\n                 }\n             }\n         }\n \n         ctx.fireUserEventTriggered(evt);\n     }\n \n+    private void removeSelfIfPresent(ChannelHandlerContext ctx) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 115,
      "original_line": 115,
      "side": "RIGHT",
      "original_position": 29,
      "position": 52,
      "subject_type": "line",
      "reply": [
        {
          "id": 444098071,
          "user": "normanmaurer",
          "body": "nope ... it uses `this`",
          "created_at": "2020-06-23T09:43:31Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 435617187,
      "created_at": "2020-06-23T09:43:31Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2020-06-23T09:47:09Z",
      "sha": "88de2549e97a0a49d4f9ca138196e89940092cce",
      "message": "Test fix",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-23T09:47:09Z",
      "author_date": "2020-06-23T09:47:09Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-23T09:47:09Z",
      "diff_text": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java\n@@ -45,6 +45,8 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n         };\n \n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        channel.runPendingTasks();\n+\n         SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n         SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n         channel.pipeline().fireUserEventTriggered(completionEvent);\n@@ -64,8 +66,9 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n \n-        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(SSLContext.getDefault().createSSLEngine()),\n-                alpnHandler);\n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(\n+                SSLContext.getDefault().createSSLEngine()), alpnHandler);\n+        channel.runPendingTasks();\n \n         channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         assertNull(channel.pipeline().context(alpnHandler));\n@@ -83,6 +86,7 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        channel.runPendingTasks();\n         try {\n             channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         } finally {",
      "diff": [
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java",
          "patch": "@@ -45,6 +45,8 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n         };\n \n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        channel.runPendingTasks();\n+\n         SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n         SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n         channel.pipeline().fireUserEventTriggered(completionEvent);\n@@ -64,8 +66,9 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n \n-        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(SSLContext.getDefault().createSSLEngine()),\n-                alpnHandler);\n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(\n+                SSLContext.getDefault().createSSLEngine()), alpnHandler);\n+        channel.runPendingTasks();\n \n         channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         assertNull(channel.pipeline().context(alpnHandler));\n@@ -83,6 +86,7 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n+        channel.runPendingTasks();\n         try {\n             channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         } finally {"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2020-06-23T10:57:15Z",
      "sha": "c41a6a63cc271eedb690898b82d77c30bf61f155",
      "message": "Fix test for java11 and later",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-23T10:57:15Z",
      "author_date": "2020-06-23T10:57:15Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-23T10:57:15Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java\n@@ -87,6 +87,7 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                 if (handshakeEvent.isSuccess()) {\n                     SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n                     if (sslHandler == null) {\n+                        System.err.println(ctx.pipeline().toString());\n                         throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n                                 + \"application-level protocol negotiation)\");\n                     }\nhandler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java\n@@ -22,6 +22,7 @@\n import org.junit.Test;\n \n import javax.net.ssl.SSLContext;\n+import javax.net.ssl.SSLEngine;\n import javax.net.ssl.SSLHandshakeException;\n import java.security.NoSuchAlgorithmException;\n import java.util.concurrent.atomic.AtomicBoolean;\n@@ -45,8 +46,6 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n         };\n \n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n-        channel.runPendingTasks();\n-\n         SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n         SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n         channel.pipeline().fireUserEventTriggered(completionEvent);\n@@ -66,10 +65,10 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n \n-        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(\n-                SSLContext.getDefault().createSSLEngine()), alpnHandler);\n-        channel.runPendingTasks();\n+        SSLEngine engine = SSLContext.getDefault().createSSLEngine();\n+        engine.setUseClientMode(false);\n \n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(engine), alpnHandler);\n         channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         assertNull(channel.pipeline().context(alpnHandler));\n         // Should produce the close_notify messages\n@@ -86,7 +85,6 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n-        channel.runPendingTasks();\n         try {\n             channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         } finally {",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
          "patch": "@@ -87,6 +87,7 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                 if (handshakeEvent.isSuccess()) {\n                     SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n                     if (sslHandler == null) {\n+                        System.err.println(ctx.pipeline().toString());\n                         throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n                                 + \"application-level protocol negotiation)\");\n                     }"
        },
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandlerTest.java",
          "patch": "@@ -22,6 +22,7 @@\n import org.junit.Test;\n \n import javax.net.ssl.SSLContext;\n+import javax.net.ssl.SSLEngine;\n import javax.net.ssl.SSLHandshakeException;\n import java.security.NoSuchAlgorithmException;\n import java.util.concurrent.atomic.AtomicBoolean;\n@@ -45,8 +46,6 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n         };\n \n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n-        channel.runPendingTasks();\n-\n         SSLHandshakeException exception = new SSLHandshakeException(\"error\");\n         SslHandshakeCompletionEvent completionEvent = new SslHandshakeCompletionEvent(exception);\n         channel.pipeline().fireUserEventTriggered(completionEvent);\n@@ -66,10 +65,10 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n \n-        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(\n-                SSLContext.getDefault().createSSLEngine()), alpnHandler);\n-        channel.runPendingTasks();\n+        SSLEngine engine = SSLContext.getDefault().createSSLEngine();\n+        engine.setUseClientMode(false);\n \n+        EmbeddedChannel channel = new EmbeddedChannel(new SslHandler(engine), alpnHandler);\n         channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         assertNull(channel.pipeline().context(alpnHandler));\n         // Should produce the close_notify messages\n@@ -86,7 +85,6 @@ protected void configurePipeline(ChannelHandlerContext ctx, String protocol) {\n             }\n         };\n         EmbeddedChannel channel = new EmbeddedChannel(alpnHandler);\n-        channel.runPendingTasks();\n         try {\n             channel.pipeline().fireUserEventTriggered(SslHandshakeCompletionEvent.SUCCESS);\n         } finally {"
        }
      ]
    },
    {
      "type": "review",
      "id": 435703151,
      "created_at": "2020-06-23T11:50:13Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 444163775,
      "created_at": "2020-06-23T11:50:13Z",
      "user": "johnou",
      "body": ":eye: ",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -79,32 +82,43 @@ protected ApplicationProtocolNegotiationHandler(String fallbackProtocol) {\n     @Override\n     public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n         if (evt instanceof SslHandshakeCompletionEvent) {\n-\n+            SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n             try {\n-                SslHandshakeCompletionEvent handshakeEvent = (SslHandshakeCompletionEvent) evt;\n                 if (handshakeEvent.isSuccess()) {\n                     SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n                     if (sslHandler == null) {\n+                        System.err.println(ctx.pipeline().toString());",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 90,
      "side": "RIGHT",
      "original_position": 25,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "commit",
      "date": "2020-06-23T11:54:50Z",
      "sha": "181aa62ea7d3180feccca3328c67af86492bf953",
      "message": "Remove debug cruft",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-06-23T11:54:50Z",
      "author_date": "2020-06-23T11:54:50Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-06-23T11:54:50Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java\n@@ -87,7 +87,6 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                 if (handshakeEvent.isSuccess()) {\n                     SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n                     if (sslHandler == null) {\n-                        System.err.println(ctx.pipeline().toString());\n                         throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n                                 + \"application-level protocol negotiation)\");\n                     }",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
          "patch": "@@ -87,7 +87,6 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                 if (handshakeEvent.isSuccess()) {\n                     SslHandler sslHandler = ctx.pipeline().get(SslHandler.class);\n                     if (sslHandler == null) {\n-                        System.err.println(ctx.pipeline().toString());\n                         throw new IllegalStateException(\"cannot find an SslHandler in the pipeline (required for \"\n                                 + \"application-level protocol negotiation)\");\n                     }"
        }
      ]
    },
    {
      "type": "review",
      "id": 436701494,
      "created_at": "2020-06-24T14:27:26Z",
      "user": "johnou",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 444934722,
      "created_at": "2020-06-24T14:27:26Z",
      "user": "johnou",
      "body": "@normanmaurer tbh I would remove the isSuccess check, I think it would be safer to always attempt to remove the handler from the pipeline, what if an exception is not thrown but handshake event is false?",
      "path": "handler/src/main/java/io/netty/handler/ssl/ApplicationProtocolNegotiationHandler.java",
      "diff_hunk": "@@ -91,20 +93,31 @@ public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exc\n                     String protocol = sslHandler.applicationProtocol();\n                     configurePipeline(ctx, protocol != null ? protocol : fallbackProtocol);\n                 } else {\n-                    handshakeFailure(ctx, handshakeEvent.cause());\n+                    // if the event is not produced because of an successful handshake we will receive the same\n+                    // exception in exceptionCaught(...) and handle it there. This will allow us more fine-grained\n+                    // control over which exception we propagate down the ChannelPipeline.\n+                    //\n+                    // See https://github.com/netty/netty/issues/10342\n                 }\n             } catch (Throwable cause) {\n                 exceptionCaught(ctx, cause);\n             } finally {\n-                ChannelPipeline pipeline = ctx.pipeline();\n-                if (pipeline.context(this) != null) {\n-                    pipeline.remove(this);\n+                // Handshake failures are handled in exceptionCaught(...).\n+                if (handshakeEvent.isSuccess()) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 106,
      "original_line": 106,
      "side": "RIGHT",
      "original_position": 43,
      "position": 43,
      "subject_type": "line",
      "reply": [
        {
          "id": 444940534,
          "user": "normanmaurer",
          "body": "thats impossible I think",
          "created_at": "2020-06-24T14:34:44Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 436708830,
      "created_at": "2020-06-24T14:34:44Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    }
  ]
}