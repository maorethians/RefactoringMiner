{
  "repo": "netty/netty",
  "number": 13558,
  "pr_number": 13558,
  "title": "[codec-mqtt] Add reason codes inventory",
  "user": "andsel",
  "created_at": "2023-08-18T12:11:43Z",
  "merged_at": "2023-08-28T16:44:13Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "64c898307de06a49f365093bb823bd4b7966c3d3",
  "merge_commit_sha": "bcaad8a5f0654103a982904ad9545c1c7614c09d",
  "commits": 13,
  "issue_events": [
    "review_requested",
    "renamed",
    "mentioned",
    "subscribed",
    "review_requested",
    "review_requested",
    "review_requested",
    "referenced",
    "merged",
    "closed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed"
  ],
  "url": "https://github.com/netty/netty/pull/13558",
  "timeline": [
    {
      "type": "commit",
      "date": "2023-08-18T12:03:57Z",
      "sha": "1a73764217adc8d4e1454f08829f618b6770ac1e",
      "message": "Added missed reason codes inventories",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-18T12:03:21Z",
      "author_date": "2023-08-18T12:03:21Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-18T12:03:57Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte)0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte)0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte)0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte)0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte)0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte)0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte)0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte)0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte)0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte)0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte)0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte)0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte)0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte)0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte)0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte)0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte)0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte)0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum UnsubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91);\n+\n+        private static final UnsubAck[] VALUES;\n+\n+        static {\n+            UnsubAck[] values = values();\n+            VALUES = new UnsubAck[7];\n+            for (UnsubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        UnsubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static UnsubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+}",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte)0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte)0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte)0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte)0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte)0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte)0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte)0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte)0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte)0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte)0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte)0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte)0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte)0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte)0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte)0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte)0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte)0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte)0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum UnsubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91);\n+\n+        private static final UnsubAck[] VALUES;\n+\n+        static {\n+            UnsubAck[] values = values();\n+            VALUES = new UnsubAck[7];\n+            for (UnsubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        UnsubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static UnsubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+}"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-18T12:08:24Z",
      "sha": "8ac63b9d077b6d6e509752d04dcab32689bc7019",
      "message": "Added trivial test to proove that reverse mapping works",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-18T12:08:24Z",
      "author_date": "2023-08-18T12:08:24Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-18T12:08:24Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -32,26 +32,26 @@ public enum Disconnect {\n         NOT_AUTHORIZED((byte) 0x87), //sent by: server\n         SERVER_BUSY((byte) 0x89), //sent by: server\n         SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n-\t    KEEP_ALIVE_TIMEOUT((byte)0x8D), //sent by: Server\n-\t    SESSION_TAKEN_OVER((byte)0x8E), //sent by: Server\n-\t    TOPIC_FILTER_INVALID((byte)0x8F), //sent by: Server\n-\t    TOPIC_NAME_INVALID((byte)0x90), //sent by: Client or Server\n-\t    RECEIVE_MAXIMUM_EXCEEDED((byte)0x93), //sent by: Client or Server\n-\t    TOPIC_ALIAS_INVALID((byte)0x94), //sent by: Client or Server\n-\t    PACKET_TOO_LARGE((byte)0x95), //sent by: Client or Server\n-\t    MESSAGE_RATE_TOO_HIGH((byte)0x96), //sent by: Client or Server\n-\t    QUOTA_EXCEEDED((byte)0x97), //sent by: Client or Server\n-\t    ADMINISTRATIVE_ACTION((byte)0x98), //sent by: Client or Server\n-\t    PAYLOAD_FORMAT_INVALID((byte)0x99), //sent by: Client or Server\n-\t    RETAIN_NOT_SUPPORTED((byte)0x9A), //sent by: Server\n-\t    QOS_NOT_SUPPORTED((byte)0x9B), //sent by: Server\n-\t    USE_ANOTHER_SERVER((byte)0x9C), //sent by: Server\n-\t    SERVER_MOVED((byte)0x9D), //sent by: Server\n-\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0x9E), //sent by: Server\n-\t    CONNECTION_RATE_EXCEEDED((byte)0x9F), //sent by: Server\n-\t    MAXIMUM_CONNECT_TIME((byte)0xA0), //sent by: Server\n-\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte)0xA1), //sent by: Server\n-\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0xA2); //sent by: Server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n \n         protected static final Disconnect[] VALUES;\n \ncodec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -0,0 +1,14 @@\n+package io.netty.handler.codec.mqtt;\n+\n+import org.junit.jupiter.api.Test;\n+\n+import static org.junit.jupiter.api.Assertions.*;\n+\n+class MqttReasonCodesTest {\n+\n+    @Test\n+    public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n+        assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n+                \"0xA2 must match 'wildcard subscriptions not supported'\");\n+    }\n+}\n\\ No newline at end of file",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -32,26 +32,26 @@ public enum Disconnect {\n         NOT_AUTHORIZED((byte) 0x87), //sent by: server\n         SERVER_BUSY((byte) 0x89), //sent by: server\n         SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n-\t    KEEP_ALIVE_TIMEOUT((byte)0x8D), //sent by: Server\n-\t    SESSION_TAKEN_OVER((byte)0x8E), //sent by: Server\n-\t    TOPIC_FILTER_INVALID((byte)0x8F), //sent by: Server\n-\t    TOPIC_NAME_INVALID((byte)0x90), //sent by: Client or Server\n-\t    RECEIVE_MAXIMUM_EXCEEDED((byte)0x93), //sent by: Client or Server\n-\t    TOPIC_ALIAS_INVALID((byte)0x94), //sent by: Client or Server\n-\t    PACKET_TOO_LARGE((byte)0x95), //sent by: Client or Server\n-\t    MESSAGE_RATE_TOO_HIGH((byte)0x96), //sent by: Client or Server\n-\t    QUOTA_EXCEEDED((byte)0x97), //sent by: Client or Server\n-\t    ADMINISTRATIVE_ACTION((byte)0x98), //sent by: Client or Server\n-\t    PAYLOAD_FORMAT_INVALID((byte)0x99), //sent by: Client or Server\n-\t    RETAIN_NOT_SUPPORTED((byte)0x9A), //sent by: Server\n-\t    QOS_NOT_SUPPORTED((byte)0x9B), //sent by: Server\n-\t    USE_ANOTHER_SERVER((byte)0x9C), //sent by: Server\n-\t    SERVER_MOVED((byte)0x9D), //sent by: Server\n-\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0x9E), //sent by: Server\n-\t    CONNECTION_RATE_EXCEEDED((byte)0x9F), //sent by: Server\n-\t    MAXIMUM_CONNECT_TIME((byte)0xA0), //sent by: Server\n-\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte)0xA1), //sent by: Server\n-\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte)0xA2); //sent by: Server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n \n         protected static final Disconnect[] VALUES;\n "
        },
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -0,0 +1,14 @@\n+package io.netty.handler.codec.mqtt;\n+\n+import org.junit.jupiter.api.Test;\n+\n+import static org.junit.jupiter.api.Assertions.*;\n+\n+class MqttReasonCodesTest {\n+\n+    @Test\n+    public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n+        assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n+                \"0xA2 must match 'wildcard subscriptions not supported'\");\n+    }\n+}\n\\ No newline at end of file"
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2023-08-18T12:11:43Z",
      "user": "andsel",
      "body": "Motivation:\r\n\r\nDespite all the Connect reason codes was inventoried into the `MqttConnectReturnCode` the same wasn't done for the rest of MQTT messages. This would simplify the consumption of the messages, having a predetermined list of result codes as the one defined in the specification.\r\n\r\nModification:\r\n\r\nIntroduces a class which contains all the enums to cover Disconnet, PubSub etc reason codes.\r\n\r\nResult:\r\n\r\nFixes #13557 . \r\n\r\n"
    },
    {
      "type": "review_comment",
      "id": 1298381247,
      "created_at": "2023-08-18T12:14:24Z",
      "user": "normanmaurer",
      "body": "missing license header. ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 17,
      "original_line": 1,
      "side": "RIGHT",
      "original_position": 1,
      "position": 17,
      "subject_type": "line",
      "reply": [
        {
          "id": 1299156156,
          "user": "andsel",
          "body": "Fixed by 3e237515cf650ef2aabd144e414da1f1eeb2fa08",
          "created_at": "2023-08-19T08:31:32Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 1298383216,
      "created_at": "2023-08-18T12:16:41Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n    private static <E> E valueOfHelper(byte b, E[] values) {\r\n        try {\r\n            return values[b & 0xFF];\r\n        } catch (ArrayIndexOutOfBoundsException e) {\r\n            throw new IllegalArgumentException(\"unknown reason code: \" + b);\r\n        }\r\n    }\r\n```",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }",
      "start_line": 8,
      "original_start_line": 8,
      "start_side": "RIGHT",
      "line": null,
      "original_line": 20,
      "side": "RIGHT",
      "original_position": 20,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298384072,
      "created_at": "2023-08-18T12:17:40Z",
      "user": "normanmaurer",
      "body": "add javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 93,
      "original_line": 74,
      "side": "RIGHT",
      "original_position": 74,
      "position": 93,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298384170,
      "created_at": "2023-08-18T12:17:45Z",
      "user": "normanmaurer",
      "body": "add javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 101,
      "original_line": 78,
      "side": "RIGHT",
      "original_position": 78,
      "position": 101,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298384653,
      "created_at": "2023-08-18T12:18:19Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 143,
      "original_line": 110,
      "side": "RIGHT",
      "original_position": 110,
      "position": 143,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298384748,
      "created_at": "2023-08-18T12:18:24Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 135,
      "original_line": 106,
      "side": "RIGHT",
      "original_position": 106,
      "position": 135,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385265,
      "created_at": "2023-08-18T12:18:56Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n   enum Disconnect {\r\n```",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 41,
      "original_line": 25,
      "side": "RIGHT",
      "original_position": 25,
      "position": 41,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385437,
      "created_at": "2023-08-18T12:19:09Z",
      "user": "normanmaurer",
      "body": "add javadocs ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 83,
      "side": "RIGHT",
      "original_position": 83,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385695,
      "created_at": "2023-08-18T12:19:29Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 183,
      "original_line": 144,
      "side": "RIGHT",
      "original_position": 144,
      "position": 183,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385757,
      "created_at": "2023-08-18T12:19:33Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 191,
      "original_line": 148,
      "side": "RIGHT",
      "original_position": 148,
      "position": 191,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385912,
      "created_at": "2023-08-18T12:19:44Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 239,
      "original_line": 186,
      "side": "RIGHT",
      "original_position": 186,
      "position": 239,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298385990,
      "created_at": "2023-08-18T12:19:49Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 231,
      "original_line": 182,
      "side": "RIGHT",
      "original_position": 182,
      "position": 231,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386082,
      "created_at": "2023-08-18T12:19:56Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 153,
      "side": "RIGHT",
      "original_position": 153,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386218,
      "created_at": "2023-08-18T12:20:06Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 272,
      "original_line": 214,
      "side": "RIGHT",
      "original_position": 214,
      "position": 272,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386288,
      "created_at": "2023-08-18T12:20:11Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 280,
      "original_line": 218,
      "side": "RIGHT",
      "original_position": 218,
      "position": 280,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386385,
      "created_at": "2023-08-18T12:20:18Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 223,
      "side": "RIGHT",
      "original_position": 223,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386601,
      "created_at": "2023-08-18T12:20:32Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 313,
      "original_line": 245,
      "side": "RIGHT",
      "original_position": 245,
      "position": 313,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386719,
      "created_at": "2023-08-18T12:20:38Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 321,
      "original_line": 249,
      "side": "RIGHT",
      "original_position": 249,
      "position": 321,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386779,
      "created_at": "2023-08-18T12:20:43Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 254,
      "side": "RIGHT",
      "original_position": 254,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386878,
      "created_at": "2023-08-18T12:20:50Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 372,
      "original_line": 290,
      "side": "RIGHT",
      "original_position": 290,
      "position": 372,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298386964,
      "created_at": "2023-08-18T12:20:55Z",
      "user": "normanmaurer",
      "body": "javadoc",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 364,
      "original_line": 286,
      "side": "RIGHT",
      "original_position": 286,
      "position": 364,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298387021,
      "created_at": "2023-08-18T12:21:00Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum UnsubAck {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 295,
      "side": "RIGHT",
      "original_position": 295,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298387118,
      "created_at": "2023-08-18T12:21:07Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum UnsubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91);\n+\n+        private static final UnsubAck[] VALUES;\n+\n+        static {\n+            UnsubAck[] values = values();\n+            VALUES = new UnsubAck[7];\n+            for (UnsubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        UnsubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 410,
      "original_line": 322,
      "side": "RIGHT",
      "original_position": 322,
      "position": 410,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298387198,
      "created_at": "2023-08-18T12:21:12Z",
      "user": "normanmaurer",
      "body": "javadoc ",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Disconnect(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Disconnect valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum Auth {\n+        SUCCESS((byte) 0x00), //sent by: Server\n+        CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n+        REAUTHENTICATE((byte) 0x19); //sent by: Client\n+\n+        private static final Auth[] VALUES;\n+\n+        static {\n+            Auth[] values = values();\n+            VALUES = new Auth[3];\n+            for (Auth code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        Auth(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static Auth valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+\n+        private static final PubAck[] VALUES;\n+\n+        static {\n+            PubAck[] values = values();\n+            VALUES = new PubAck[9];\n+            for (PubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubRec {\n+        SUCCESS((byte) 0x00),\n+        NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_NAME_INVALID((byte) 0x90),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n+\n+        private static final PubRec[] VALUES;\n+\n+        static {\n+            PubRec[] values = values();\n+            VALUES = new PubRec[9];\n+            for (PubRec code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRec(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRec valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+\n+    }\n+\n+    enum PubRel {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubRel[] VALUES;\n+\n+        static {\n+            PubRel[] values = values();\n+            VALUES = new PubRel[2];\n+            for (PubRel code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubRel(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubRel valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum PubComp {\n+        SUCCESS((byte) 0x00),\n+        PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n+\n+        private static final PubComp[] VALUES;\n+\n+        static {\n+            PubComp[] values = values();\n+            VALUES = new PubComp[2];\n+            for (PubComp code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        PubComp(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static PubComp valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum SubAck {\n+        GRANTED_QOS_0((byte) 0x00),\n+        GRANTED_QOS_1((byte) 0x01),\n+        GRANTED_QOS_2((byte) 0x02),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n+        QUOTA_EXCEEDED((byte) 0x97),\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E),\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1),\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2);\n+\n+        private static final SubAck[] VALUES;\n+\n+        static {\n+            SubAck[] values = values();\n+            VALUES = new SubAck[12];\n+            for (SubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        SubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static SubAck valueOf(byte b) {\n+            return valueOfHelper(b, VALUES);\n+        }\n+    }\n+\n+    enum UnsubAck {\n+        SUCCESS((byte) 0x00),\n+        NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n+        UNSPECIFIED_ERROR((byte) 0x80),\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83),\n+        NOT_AUTHORIZED((byte) 0x87),\n+        TOPIC_FILTER_INVALID((byte) 0x8F),\n+        PACKET_IDENTIFIER_IN_USE((byte) 0x91);\n+\n+        private static final UnsubAck[] VALUES;\n+\n+        static {\n+            UnsubAck[] values = values();\n+            VALUES = new UnsubAck[7];\n+            for (UnsubAck code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]\n+            }\n+        }\n+\n+        private final byte byteValue;\n+\n+        UnsubAck(byte byteValue) {\n+            this.byteValue = byteValue;\n+        }\n+\n+        public byte byteValue() {\n+            return byteValue;\n+        }\n+\n+        public static UnsubAck valueOf(byte b) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 418,
      "original_line": 326,
      "side": "RIGHT",
      "original_position": 326,
      "position": 418,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 1298387284,
      "created_at": "2023-08-18T12:21:19Z",
      "user": "normanmaurer",
      "body": "license header ",
      "path": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
      "diff_hunk": "@@ -0,0 +1,14 @@\n+package io.netty.handler.codec.mqtt;",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 17,
      "original_line": 1,
      "side": "RIGHT",
      "original_position": 1,
      "position": 17,
      "subject_type": "line",
      "reply": [
        {
          "id": 1299156177,
          "user": "andsel",
          "body": "Fixed by 3e237515cf650ef2aabd144e414da1f1eeb2fa08",
          "created_at": "2023-08-19T08:31:43Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 1298387411,
      "created_at": "2023-08-18T12:21:27Z",
      "user": "normanmaurer",
      "body": "missing EOL",
      "path": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
      "diff_hunk": "@@ -0,0 +1,14 @@\n+package io.netty.handler.codec.mqtt;\n+\n+import org.junit.jupiter.api.Test;\n+\n+import static org.junit.jupiter.api.Assertions.*;\n+\n+class MqttReasonCodesTest {\n+\n+    @Test\n+    public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n+        assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n+                \"0xA2 must match 'wildcard subscriptions not supported'\");\n+    }\n+}",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 14,
      "side": "RIGHT",
      "original_position": 14,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 1584458919,
      "created_at": "2023-08-18T12:21:34Z",
      "user": "normanmaurer",
      "state": "CHANGES_REQUESTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1584751900,
      "created_at": "2023-08-18T15:08:42Z",
      "user": "hyperxpro",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 1298564003,
      "created_at": "2023-08-18T15:08:42Z",
      "user": "hyperxpro",
      "body": "From where is this coming from `[java/index-out-of-bounds]`?",
      "path": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
      "diff_hunk": "@@ -0,0 +1,330 @@\n+package io.netty.handler.codec.mqtt;\n+\n+public final class MqttReasonCodes {\n+\n+    /**\n+     * @return the corresponding enum value to the hex value.\n+     * */\n+    private static <E> E valueOfHelper(byte b, E[] values) {\n+        final int unsignedByte = b & 0xFF;\n+        E reasonCode = null;\n+        try {\n+            reasonCode = values[unsignedByte];\n+        } catch (ArrayIndexOutOfBoundsException ignored) {\n+            // no op\n+        }\n+        if (reasonCode == null) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n+        }\n+        return reasonCode;\n+    }\n+\n+    /**\n+     * Return Code of MQTT Disconnect message.\n+     */\n+    public enum Disconnect {\n+        NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n+        DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n+        UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n+        MALFORMED_PACKET((byte) 0x81), //sent by: client or server\n+        PROTOCOL_ERROR((byte) 0x82), //sent by: client or server\n+        IMPLEMENTATION_SPECIFIC_ERROR((byte) 0x83), //sent by: client or server\n+        NOT_AUTHORIZED((byte) 0x87), //sent by: server\n+        SERVER_BUSY((byte) 0x89), //sent by: server\n+        SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n+\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n+\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+\n+        protected static final Disconnect[] VALUES;\n+\n+        static {\n+            Disconnect[] values = values();\n+            VALUES = new Disconnect[28];\n+            for (Disconnect code : values) {\n+                final int unsignedByte = code.byteValue & 0xFF;\n+                // Suppress a warning about out of bounds access since the enum contains only correct values\n+                VALUES[unsignedByte] = code;    //  [java/index-out-of-bounds]",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 80,
      "original_line": 64,
      "side": "RIGHT",
      "original_position": 64,
      "position": 80,
      "subject_type": "line",
      "reply": [
        {
          "id": 1299155337,
          "user": "andsel",
          "body": "This comes from code that inspired this:\r\nhttps://github.com/netty/netty/blob/64c898307de06a49f365093bb823bd4b7966c3d3/codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttConnectReturnCode.java#L61\r\n\r\nwhich was originated by LGTM analysis (I suppose) #10689 (related #13157)",
          "created_at": "2023-08-19T08:25:31Z"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-19T08:09:06Z",
      "sha": "6c239bfc9c11c71d813c2ee19c9825d83829c2a3",
      "message": "Apply suggestions from code review\n\nCo-authored-by: Norman Maurer <norman_maurer@apple.com>",
      "author": "Andrea Selva",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T08:09:06Z",
      "author_date": "2023-08-19T08:09:06Z",
      "committer": "GitHub",
      "committer_email": "noreply@github.com",
      "raw_date": "2023-08-19T08:09:06Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -6,23 +6,17 @@ public final class MqttReasonCodes {\n      * @return the corresponding enum value to the hex value.\n      * */\n     private static <E> E valueOfHelper(byte b, E[] values) {\n-        final int unsignedByte = b & 0xFF;\n-        E reasonCode = null;\n         try {\n-            reasonCode = values[unsignedByte];\n-        } catch (ArrayIndexOutOfBoundsException ignored) {\n-            // no op\n+            return values[b & 0xFF];\n+        } catch (ArrayIndexOutOfBoundsException e) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + b);\n         }\n-        if (reasonCode == null) {\n-            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n-        }\n-        return reasonCode;\n     }\n \n     /**\n      * Return Code of MQTT Disconnect message.\n      */\n-    public enum Disconnect {\n+   enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -6,23 +6,17 @@ public final class MqttReasonCodes {\n      * @return the corresponding enum value to the hex value.\n      * */\n     private static <E> E valueOfHelper(byte b, E[] values) {\n-        final int unsignedByte = b & 0xFF;\n-        E reasonCode = null;\n         try {\n-            reasonCode = values[unsignedByte];\n-        } catch (ArrayIndexOutOfBoundsException ignored) {\n-            // no op\n+            return values[b & 0xFF];\n+        } catch (ArrayIndexOutOfBoundsException e) {\n+            throw new IllegalArgumentException(\"unknown reason code: \" + b);\n         }\n-        if (reasonCode == null) {\n-            throw new IllegalArgumentException(\"unknown reason code: \" + unsignedByte);\n-        }\n-        return reasonCode;\n     }\n \n     /**\n      * Return Code of MQTT Disconnect message.\n      */\n-    public enum Disconnect {\n+   enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-19T08:20:58Z",
      "sha": "3e237515cf650ef2aabd144e414da1f1eeb2fa08",
      "message": "Added license headers",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T08:20:58Z",
      "author_date": "2023-08-19T08:20:58Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-19T08:20:58Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -1,3 +1,19 @@\n+/*\n+ * Copyright 2014 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   https://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+\n package io.netty.handler.codec.mqtt;\n \n public final class MqttReasonCodes {\ncodec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -1,3 +1,19 @@\n+/*\n+ * Copyright 2014 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   https://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+\n package io.netty.handler.codec.mqtt;\n \n import org.junit.jupiter.api.Test;",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -1,3 +1,19 @@\n+/*\n+ * Copyright 2014 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   https://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+\n package io.netty.handler.codec.mqtt;\n \n public final class MqttReasonCodes {"
        },
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -1,3 +1,19 @@\n+/*\n+ * Copyright 2014 The Netty Project\n+ *\n+ * The Netty Project licenses this file to you under the Apache License,\n+ * version 2.0 (the \"License\"); you may not use this file except in compliance\n+ * with the License. You may obtain a copy of the License at:\n+ *\n+ *   https://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS, WITHOUT\n+ * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the\n+ * License for the specific language governing permissions and limitations\n+ * under the License.\n+ */\n+\n package io.netty.handler.codec.mqtt;\n \n import org.junit.jupiter.api.Test;"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-19T08:21:49Z",
      "sha": "e9d6c43b1b655c64d89a1ce7422d1701cd522bb6",
      "message": "Added EOL to test file",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T08:21:49Z",
      "author_date": "2023-08-19T08:21:49Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-19T08:21:49Z",
      "diff_text": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -27,4 +27,4 @@ public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n         assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n-}\n\\ No newline at end of file\n+}",
      "diff": [
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -27,4 +27,4 @@ public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n         assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n-}\n\\ No newline at end of file\n+}"
        }
      ]
    },
    {
      "type": "review",
      "id": 1585613414,
      "created_at": "2023-08-19T08:25:32Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2023-08-19T08:30:31Z",
      "sha": "bf4d68b0191c78439fec78fd1e04e764c5c10012",
      "message": "Added javadocs",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T08:30:31Z",
      "author_date": "2023-08-19T08:30:31Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-19T08:30:31Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -16,6 +16,9 @@\n \n package io.netty.handler.codec.mqtt;\n \n+/**\n+ * Provides a set of enumeration that exposes standard MQTT 5 reason codes used by various messages.\n+ * */\n public final class MqttReasonCodes {\n \n     /**\n@@ -30,9 +33,9 @@ private static <E> E valueOfHelper(byte b, E[] values) {\n     }\n \n     /**\n-     * Return Code of MQTT Disconnect message.\n+     * Reason codes for MQTT Disconnect message.\n      */\n-   enum Disconnect {\n+    enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n@@ -90,6 +93,9 @@ public static Disconnect valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT Auth message.\n+     */\n     enum Auth {\n         SUCCESS((byte) 0x00), //sent by: Server\n         CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n@@ -122,6 +128,9 @@ public static Auth valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubAck message.\n+     */\n     enum PubAck {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n@@ -160,6 +169,9 @@ public static PubAck valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubRec message.\n+     */\n     enum PubRec {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n@@ -199,6 +211,9 @@ public static PubRec valueOf(byte b) {\n \n     }\n \n+    /**\n+     * Reason codes for MQTT PubRel message.\n+     */\n     enum PubRel {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n@@ -230,6 +245,9 @@ public static PubRel valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubComp message.\n+     */\n     enum PubComp {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n@@ -261,6 +279,9 @@ public static PubComp valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT SubAck message.\n+     */\n     enum SubAck {\n         GRANTED_QOS_0((byte) 0x00),\n         GRANTED_QOS_1((byte) 0x01),\n@@ -302,6 +323,9 @@ public static SubAck valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT UnsubAck message.\n+     */\n     enum UnsubAck {\n         SUCCESS((byte) 0x00),\n         NO_SUBSCRIPTION_EXISTED((byte) 0x11),",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -16,6 +16,9 @@\n \n package io.netty.handler.codec.mqtt;\n \n+/**\n+ * Provides a set of enumeration that exposes standard MQTT 5 reason codes used by various messages.\n+ * */\n public final class MqttReasonCodes {\n \n     /**\n@@ -30,9 +33,9 @@ private static <E> E valueOfHelper(byte b, E[] values) {\n     }\n \n     /**\n-     * Return Code of MQTT Disconnect message.\n+     * Reason codes for MQTT Disconnect message.\n      */\n-   enum Disconnect {\n+    enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n@@ -90,6 +93,9 @@ public static Disconnect valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT Auth message.\n+     */\n     enum Auth {\n         SUCCESS((byte) 0x00), //sent by: Server\n         CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n@@ -122,6 +128,9 @@ public static Auth valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubAck message.\n+     */\n     enum PubAck {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n@@ -160,6 +169,9 @@ public static PubAck valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubRec message.\n+     */\n     enum PubRec {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n@@ -199,6 +211,9 @@ public static PubRec valueOf(byte b) {\n \n     }\n \n+    /**\n+     * Reason codes for MQTT PubRel message.\n+     */\n     enum PubRel {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n@@ -230,6 +245,9 @@ public static PubRel valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT PubComp message.\n+     */\n     enum PubComp {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n@@ -261,6 +279,9 @@ public static PubComp valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT SubAck message.\n+     */\n     enum SubAck {\n         GRANTED_QOS_0((byte) 0x00),\n         GRANTED_QOS_1((byte) 0x01),\n@@ -302,6 +323,9 @@ public static SubAck valueOf(byte b) {\n         }\n     }\n \n+    /**\n+     * Reason codes for MQTT UnsubAck message.\n+     */\n     enum UnsubAck {\n         SUCCESS((byte) 0x00),\n         NO_SUBSCRIPTION_EXISTED((byte) 0x11),"
        }
      ]
    },
    {
      "type": "review",
      "id": 1585614278,
      "created_at": "2023-08-19T08:31:32Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614290,
      "created_at": "2023-08-19T08:31:43Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614340,
      "created_at": "2023-08-19T08:32:19Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614352,
      "created_at": "2023-08-19T08:32:26Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614365,
      "created_at": "2023-08-19T08:32:33Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614376,
      "created_at": "2023-08-19T08:32:41Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614391,
      "created_at": "2023-08-19T08:32:47Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614402,
      "created_at": "2023-08-19T08:32:56Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614408,
      "created_at": "2023-08-19T08:33:00Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614419,
      "created_at": "2023-08-19T08:33:08Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614573,
      "created_at": "2023-08-19T08:33:23Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614579,
      "created_at": "2023-08-19T08:33:27Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585614857,
      "created_at": "2023-08-19T08:34:51Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2023-08-19T08:39:53Z",
      "sha": "783bfe90f5de2bba464d27886fb57ad5e709d83a",
      "message": "Added javadoc to methods",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T08:39:53Z",
      "author_date": "2023-08-19T08:39:53Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-19T08:39:53Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -84,10 +84,17 @@ enum Disconnect {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static Disconnect valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -119,10 +126,17 @@ enum Auth {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static Auth valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -160,10 +174,17 @@ enum PubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -201,10 +222,17 @@ enum PubRec {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubRec valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -236,10 +264,17 @@ enum PubRel {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubRel valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -270,10 +305,17 @@ enum PubComp {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubComp valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -314,10 +356,17 @@ enum SubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static SubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -353,10 +402,17 @@ enum UnsubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static UnsubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -84,10 +84,17 @@ enum Disconnect {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static Disconnect valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -119,10 +126,17 @@ enum Auth {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static Auth valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -160,10 +174,17 @@ enum PubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -201,10 +222,17 @@ enum PubRec {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubRec valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -236,10 +264,17 @@ enum PubRel {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubRel valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -270,10 +305,17 @@ enum PubComp {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static PubComp valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -314,10 +356,17 @@ enum SubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static SubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n@@ -353,10 +402,17 @@ enum UnsubAck {\n             this.byteValue = byteValue;\n         }\n \n+        /**\n+         * @return the value number corresponding to the constant.\n+         * */\n         public byte byteValue() {\n             return byteValue;\n         }\n \n+        /**\n+         * @param b the number to decode.\n+         * @return the enum value corresponding to the number.\n+         * */\n         public static UnsubAck valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }"
        }
      ]
    },
    {
      "type": "review",
      "id": 1585615456,
      "created_at": "2023-08-19T08:40:44Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615466,
      "created_at": "2023-08-19T08:40:52Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615480,
      "created_at": "2023-08-19T08:41:01Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615487,
      "created_at": "2023-08-19T08:41:08Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615511,
      "created_at": "2023-08-19T08:41:25Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615592,
      "created_at": "2023-08-19T08:42:03Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615622,
      "created_at": "2023-08-19T08:42:22Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615630,
      "created_at": "2023-08-19T08:42:30Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585615997,
      "created_at": "2023-08-19T08:45:13Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585616036,
      "created_at": "2023-08-19T08:45:31Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2023-08-19T09:15:05Z",
      "sha": "541fbafba997c0006a375db7f5a0d460ec8a6214",
      "message": "Fixed style errors",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T09:15:05Z",
      "author_date": "2023-08-19T09:15:05Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-19T09:15:05Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -45,26 +45,26 @@ enum Disconnect {\n         NOT_AUTHORIZED((byte) 0x87), //sent by: server\n         SERVER_BUSY((byte) 0x89), //sent by: server\n         SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n-\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n-\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n-\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n-\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n-\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n-\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n-\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n-\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n-\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n-\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n-\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n-\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n-\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n-\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n-\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n-\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n-\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n-\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n-\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n-\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+        KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+        SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+        TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+        TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+        RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+        TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+        PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+        MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+        QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+        ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+        RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+        QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+        USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+        SERVER_MOVED((byte) 0x9D), //sent by: Server\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+        CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+        MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n \n         protected static final Disconnect[] VALUES;\n \n@@ -154,7 +154,7 @@ enum PubAck {\n         TOPIC_NAME_INVALID((byte) 0x90),\n         PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n         QUOTA_EXCEEDED((byte) 0x97),\n-        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n \n         private static final PubAck[] VALUES;\n \n@@ -236,7 +236,6 @@ public byte byteValue() {\n         public static PubRec valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n-\n     }\n \n     /**\ncodec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -18,13 +18,14 @@\n \n import org.junit.jupiter.api.Test;\n \n-import static org.junit.jupiter.api.Assertions.*;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.Disconnect;\n+import static org.junit.jupiter.api.Assertions.assertEquals;\n \n class MqttReasonCodesTest {\n \n     @Test\n     public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n-        assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n+        assertEquals(Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n }",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -45,26 +45,26 @@ enum Disconnect {\n         NOT_AUTHORIZED((byte) 0x87), //sent by: server\n         SERVER_BUSY((byte) 0x89), //sent by: server\n         SERVER_SHUTTING_DOWN((byte) 0x8B), //sent by: server\n-\t    KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n-\t    SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n-\t    TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n-\t    TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n-\t    RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n-\t    TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n-\t    PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n-\t    MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n-\t    QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n-\t    ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n-\t    PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n-\t    RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n-\t    QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n-\t    USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n-\t    SERVER_MOVED((byte) 0x9D), //sent by: Server\n-\t    SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n-\t    CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n-\t    MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n-\t    SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n-\t    WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n+        KEEP_ALIVE_TIMEOUT((byte) 0x8D), //sent by: Server\n+        SESSION_TAKEN_OVER((byte) 0x8E), //sent by: Server\n+        TOPIC_FILTER_INVALID((byte) 0x8F), //sent by: Server\n+        TOPIC_NAME_INVALID((byte) 0x90), //sent by: Client or Server\n+        RECEIVE_MAXIMUM_EXCEEDED((byte) 0x93), //sent by: Client or Server\n+        TOPIC_ALIAS_INVALID((byte) 0x94), //sent by: Client or Server\n+        PACKET_TOO_LARGE((byte) 0x95), //sent by: Client or Server\n+        MESSAGE_RATE_TOO_HIGH((byte) 0x96), //sent by: Client or Server\n+        QUOTA_EXCEEDED((byte) 0x97), //sent by: Client or Server\n+        ADMINISTRATIVE_ACTION((byte) 0x98), //sent by: Client or Server\n+        PAYLOAD_FORMAT_INVALID((byte) 0x99), //sent by: Client or Server\n+        RETAIN_NOT_SUPPORTED((byte) 0x9A), //sent by: Server\n+        QOS_NOT_SUPPORTED((byte) 0x9B), //sent by: Server\n+        USE_ANOTHER_SERVER((byte) 0x9C), //sent by: Server\n+        SERVER_MOVED((byte) 0x9D), //sent by: Server\n+        SHARED_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0x9E), //sent by: Server\n+        CONNECTION_RATE_EXCEEDED((byte) 0x9F), //sent by: Server\n+        MAXIMUM_CONNECT_TIME((byte) 0xA0), //sent by: Server\n+        SUBSCRIPTION_IDENTIFIERS_NOT_SUPPORTED((byte) 0xA1), //sent by: Server\n+        WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED((byte) 0xA2); //sent by: Server\n \n         protected static final Disconnect[] VALUES;\n \n@@ -154,7 +154,7 @@ enum PubAck {\n         TOPIC_NAME_INVALID((byte) 0x90),\n         PACKET_IDENTIFIER_IN_USE((byte) 0x91),\n         QUOTA_EXCEEDED((byte) 0x97),\n-        PAYLOAD_FORMAT_INVALID((byte) 0x99); \n+        PAYLOAD_FORMAT_INVALID((byte) 0x99);\n \n         private static final PubAck[] VALUES;\n \n@@ -236,7 +236,6 @@ public byte byteValue() {\n         public static PubRec valueOf(byte b) {\n             return valueOfHelper(b, VALUES);\n         }\n-\n     }\n \n     /**"
        },
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -18,13 +18,14 @@\n \n import org.junit.jupiter.api.Test;\n \n-import static org.junit.jupiter.api.Assertions.*;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.Disconnect;\n+import static org.junit.jupiter.api.Assertions.assertEquals;\n \n class MqttReasonCodesTest {\n \n     @Test\n     public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n-        assertEquals(MqttReasonCodes.Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, MqttReasonCodes.Disconnect.valueOf((byte) 0xA2),\n+        assertEquals(Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n }"
        }
      ]
    },
    {
      "type": "review",
      "id": 1585619680,
      "created_at": "2023-08-19T09:25:11Z",
      "user": "hyperxpro",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585619717,
      "created_at": "2023-08-19T09:27:08Z",
      "user": "hyperxpro",
      "state": "CHANGES_REQUESTED",
      "body": "Also, remove LGTM ignore statements."
    },
    {
      "type": "commit",
      "date": "2023-08-19T09:29:10Z",
      "sha": "b9ec0c1c471f3d7b4a5e16ca0b7df321726244f0",
      "message": "Fixed copyright year in newly added files\n\nCo-authored-by: Aayush Atharva <hyperx.pro@outlook.com>",
      "author": "Andrea Selva",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-19T09:29:10Z",
      "author_date": "2023-08-19T09:29:10Z",
      "committer": "GitHub",
      "committer_email": "noreply@github.com",
      "raw_date": "2023-08-19T09:29:10Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -1,5 +1,5 @@\n /*\n- * Copyright 2014 The Netty Project\n+ * Copyright 2023 The Netty Project\n  *\n  * The Netty Project licenses this file to you under the Apache License,\n  * version 2.0 (the \"License\"); you may not use this file except in compliance\ncodec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -1,5 +1,5 @@\n /*\n- * Copyright 2014 The Netty Project\n+ * Copyright 2023 The Netty Project\n  *\n  * The Netty Project licenses this file to you under the Apache License,\n  * version 2.0 (the \"License\"); you may not use this file except in compliance",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -1,5 +1,5 @@\n /*\n- * Copyright 2014 The Netty Project\n+ * Copyright 2023 The Netty Project\n  *\n  * The Netty Project licenses this file to you under the Apache License,\n  * version 2.0 (the \"License\"); you may not use this file except in compliance"
        },
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -1,5 +1,5 @@\n /*\n- * Copyright 2014 The Netty Project\n+ * Copyright 2023 The Netty Project\n  *\n  * The Netty Project licenses this file to you under the Apache License,\n  * version 2.0 (the \"License\"); you may not use this file except in compliance"
        }
      ]
    },
    {
      "type": "review",
      "id": 1585620040,
      "created_at": "2023-08-19T09:29:42Z",
      "user": "andsel",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 1585689387,
      "created_at": "2023-08-19T16:41:36Z",
      "user": "hyperxpro",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2023-08-23T17:03:05Z",
      "sha": "8ac7d530e764348efeb69f24b985027a29d44589",
      "message": "Fixed sizing of sparse arrays for each constant and added test to check all the constants for all the enums",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-23T17:03:05Z",
      "author_date": "2023-08-23T17:03:05Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-23T17:03:05Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -70,7 +70,7 @@ enum Disconnect {\n \n         static {\n             Disconnect[] values = values();\n-            VALUES = new Disconnect[28];\n+            VALUES = new Disconnect[163];\n             for (Disconnect code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -112,7 +112,7 @@ enum Auth {\n \n         static {\n             Auth[] values = values();\n-            VALUES = new Auth[3];\n+            VALUES = new Auth[26];\n             for (Auth code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -160,7 +160,7 @@ enum PubAck {\n \n         static {\n             PubAck[] values = values();\n-            VALUES = new PubAck[9];\n+            VALUES = new PubAck[154];\n             for (PubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -208,7 +208,7 @@ enum PubRec {\n \n         static {\n             PubRec[] values = values();\n-            VALUES = new PubRec[9];\n+            VALUES = new PubRec[154];\n             for (PubRec code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -249,7 +249,7 @@ enum PubRel {\n \n         static {\n             PubRel[] values = values();\n-            VALUES = new PubRel[2];\n+            VALUES = new PubRel[147];\n             for (PubRel code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -290,7 +290,7 @@ enum PubComp {\n \n         static {\n             PubComp[] values = values();\n-            VALUES = new PubComp[2];\n+            VALUES = new PubComp[147];\n             for (PubComp code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -341,7 +341,7 @@ enum SubAck {\n \n         static {\n             SubAck[] values = values();\n-            VALUES = new SubAck[12];\n+            VALUES = new SubAck[163];\n             for (SubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -387,7 +387,7 @@ enum UnsubAck {\n \n         static {\n             UnsubAck[] values = values();\n-            VALUES = new UnsubAck[7];\n+            VALUES = new UnsubAck[146];\n             for (UnsubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\ncodec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -18,14 +18,90 @@\n \n import org.junit.jupiter.api.Test;\n \n+import java.util.Arrays;\n+import java.util.stream.Collectors;\n+\n import static io.netty.handler.codec.mqtt.MqttReasonCodes.Disconnect;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.Auth;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubAck;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubRec;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubRel;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubComp;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.SubAck;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.UnsubAck;\n import static org.junit.jupiter.api.Assertions.assertEquals;\n \n+// ./mvnw -B -ntp --file pom.xml verify -pl codec-mqtt\n+\n class MqttReasonCodesTest {\n \n     @Test\n     public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n         assertEquals(Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n+\n+    @Test\n+    public void testDisconnectReasonCodesCorrectlyMapToConstant() {\n+        for (Disconnect reasonCode : Disconnect.values()) {\n+            assertEquals(reasonCode, Disconnect.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"Disconnect hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testAuthReasonCodesCorrectlyMapToConstant() {\n+        for (Auth reasonCode : Auth.values()) {\n+            assertEquals(reasonCode, Auth.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"Auth hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubAckReasonCodesCorrectlyMapToConstant() {\n+        for (PubAck reasonCode : PubAck.values()) {\n+            assertEquals(reasonCode, PubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubAck hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubRecReasonCodesCorrectlyMapToConstant() {\n+        for (PubRec reasonCode : PubRec.values()) {\n+            assertEquals(reasonCode, PubRec.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubRec hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubRelReasonCodesCorrectlyMapToConstant() {\n+        for (PubRel reasonCode : PubRel.values()) {\n+            assertEquals(reasonCode, PubRel.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubRel hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubCompReasonCodesCorrectlyMapToConstant() {\n+        for (PubComp reasonCode : PubComp.values()) {\n+            assertEquals(reasonCode, PubComp.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubComp hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testSubAckReasonCodesCorrectlyMapToConstant() {\n+        for (SubAck reasonCode : SubAck.values()) {\n+            assertEquals(reasonCode, SubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"SubAck hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testUnsubAckReasonCodesCorrectlyMapToConstant() {\n+        for (UnsubAck reasonCode : UnsubAck.values()) {\n+            assertEquals(reasonCode, UnsubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"UnsubAck hex doesn't match the proper constant\");\n+        }\n+    }\n }",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -70,7 +70,7 @@ enum Disconnect {\n \n         static {\n             Disconnect[] values = values();\n-            VALUES = new Disconnect[28];\n+            VALUES = new Disconnect[163];\n             for (Disconnect code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -112,7 +112,7 @@ enum Auth {\n \n         static {\n             Auth[] values = values();\n-            VALUES = new Auth[3];\n+            VALUES = new Auth[26];\n             for (Auth code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -160,7 +160,7 @@ enum PubAck {\n \n         static {\n             PubAck[] values = values();\n-            VALUES = new PubAck[9];\n+            VALUES = new PubAck[154];\n             for (PubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -208,7 +208,7 @@ enum PubRec {\n \n         static {\n             PubRec[] values = values();\n-            VALUES = new PubRec[9];\n+            VALUES = new PubRec[154];\n             for (PubRec code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -249,7 +249,7 @@ enum PubRel {\n \n         static {\n             PubRel[] values = values();\n-            VALUES = new PubRel[2];\n+            VALUES = new PubRel[147];\n             for (PubRel code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -290,7 +290,7 @@ enum PubComp {\n \n         static {\n             PubComp[] values = values();\n-            VALUES = new PubComp[2];\n+            VALUES = new PubComp[147];\n             for (PubComp code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -341,7 +341,7 @@ enum SubAck {\n \n         static {\n             SubAck[] values = values();\n-            VALUES = new SubAck[12];\n+            VALUES = new SubAck[163];\n             for (SubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values\n@@ -387,7 +387,7 @@ enum UnsubAck {\n \n         static {\n             UnsubAck[] values = values();\n-            VALUES = new UnsubAck[7];\n+            VALUES = new UnsubAck[146];\n             for (UnsubAck code : values) {\n                 final int unsignedByte = code.byteValue & 0xFF;\n                 // Suppress a warning about out of bounds access since the enum contains only correct values"
        },
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -18,14 +18,90 @@\n \n import org.junit.jupiter.api.Test;\n \n+import java.util.Arrays;\n+import java.util.stream.Collectors;\n+\n import static io.netty.handler.codec.mqtt.MqttReasonCodes.Disconnect;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.Auth;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubAck;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubRec;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubRel;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.PubComp;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.SubAck;\n+import static io.netty.handler.codec.mqtt.MqttReasonCodes.UnsubAck;\n import static org.junit.jupiter.api.Assertions.assertEquals;\n \n+// ./mvnw -B -ntp --file pom.xml verify -pl codec-mqtt\n+\n class MqttReasonCodesTest {\n \n     @Test\n     public void givenADisconnectReasonCodeTheCorrectEnumerationValueIsReturned() {\n         assertEquals(Disconnect.WILDCARD_SUBSCRIPTIONS_NOT_SUPPORTED, Disconnect.valueOf((byte) 0xA2),\n                 \"0xA2 must match 'wildcard subscriptions not supported'\");\n     }\n+\n+    @Test\n+    public void testDisconnectReasonCodesCorrectlyMapToConstant() {\n+        for (Disconnect reasonCode : Disconnect.values()) {\n+            assertEquals(reasonCode, Disconnect.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"Disconnect hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testAuthReasonCodesCorrectlyMapToConstant() {\n+        for (Auth reasonCode : Auth.values()) {\n+            assertEquals(reasonCode, Auth.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"Auth hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubAckReasonCodesCorrectlyMapToConstant() {\n+        for (PubAck reasonCode : PubAck.values()) {\n+            assertEquals(reasonCode, PubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubAck hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubRecReasonCodesCorrectlyMapToConstant() {\n+        for (PubRec reasonCode : PubRec.values()) {\n+            assertEquals(reasonCode, PubRec.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubRec hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubRelReasonCodesCorrectlyMapToConstant() {\n+        for (PubRel reasonCode : PubRel.values()) {\n+            assertEquals(reasonCode, PubRel.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubRel hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testPubCompReasonCodesCorrectlyMapToConstant() {\n+        for (PubComp reasonCode : PubComp.values()) {\n+            assertEquals(reasonCode, PubComp.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"PubComp hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testSubAckReasonCodesCorrectlyMapToConstant() {\n+        for (SubAck reasonCode : SubAck.values()) {\n+            assertEquals(reasonCode, SubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"SubAck hex doesn't match the proper constant\");\n+        }\n+    }\n+\n+    @Test\n+    public void testUnsubAckReasonCodesCorrectlyMapToConstant() {\n+        for (UnsubAck reasonCode : UnsubAck.values()) {\n+            assertEquals(reasonCode, UnsubAck.valueOf((byte) (reasonCode.byteValue() & 0xFF)),\n+                    \"UnsubAck hex doesn't match the proper constant\");\n+        }\n+    }\n }"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-23T17:06:57Z",
      "sha": "0be12de9a2aa30debe8a831b85b4302ff4bd768f",
      "message": "Added private constructor for class that contains all the enums",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-23T17:06:57Z",
      "author_date": "2023-08-23T17:06:57Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-23T17:06:57Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -21,6 +21,9 @@\n  * */\n public final class MqttReasonCodes {\n \n+    private MqttReasonCodes() {\n+    }\n+\n     /**\n      * @return the corresponding enum value to the hex value.\n      * */",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -21,6 +21,9 @@\n  * */\n public final class MqttReasonCodes {\n \n+    private MqttReasonCodes() {\n+    }\n+\n     /**\n      * @return the corresponding enum value to the hex value.\n      * */"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2023-08-23T17:46:10Z",
      "sha": "75f337f98d8037310ebb1b840eab36e6dbfe6ee4",
      "message": "Spread usage of return codes enums in MqttSubAckPayload and MqttUnsubAckPayload",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-23T17:46:10Z",
      "author_date": "2023-08-23T17:46:10Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-23T17:46:10Z",
      "diff_text": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java\n@@ -38,7 +38,7 @@ private static <E> E valueOfHelper(byte b, E[] values) {\n     /**\n      * Reason codes for MQTT Disconnect message.\n      */\n-    enum Disconnect {\n+    public enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n@@ -106,7 +106,7 @@ public static Disconnect valueOf(byte b) {\n     /**\n      * Reason codes for MQTT Auth message.\n      */\n-    enum Auth {\n+    public enum Auth {\n         SUCCESS((byte) 0x00), //sent by: Server\n         CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n         REAUTHENTICATE((byte) 0x19); //sent by: Client\n@@ -148,7 +148,7 @@ public static Auth valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubAck message.\n      */\n-    enum PubAck {\n+    public enum PubAck {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n         UNSPECIFIED_ERROR((byte) 0x80),\n@@ -196,7 +196,7 @@ public static PubAck valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubRec message.\n      */\n-    enum PubRec {\n+    public enum PubRec {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n         UNSPECIFIED_ERROR((byte) 0x80),\n@@ -244,7 +244,7 @@ public static PubRec valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubRel message.\n      */\n-    enum PubRel {\n+    public enum PubRel {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n \n@@ -285,7 +285,7 @@ public static PubRel valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubComp message.\n      */\n-    enum PubComp {\n+    public enum PubComp {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n \n@@ -326,7 +326,7 @@ public static PubComp valueOf(byte b) {\n     /**\n      * Reason codes for MQTT SubAck message.\n      */\n-    enum SubAck {\n+    public enum SubAck {\n         GRANTED_QOS_0((byte) 0x00),\n         GRANTED_QOS_1((byte) 0x01),\n         GRANTED_QOS_2((byte) 0x02),\n@@ -377,7 +377,7 @@ public static SubAck valueOf(byte b) {\n     /**\n      * Reason codes for MQTT UnsubAck message.\n      */\n-    enum UnsubAck {\n+    public enum UnsubAck {\n         SUCCESS((byte) 0x00),\n         NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n         UNSPECIFIED_ERROR((byte) 0x80),\ncodec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttSubAckPayload.java\n@@ -19,7 +19,9 @@\n import io.netty.util.internal.ObjectUtil;\n import io.netty.util.internal.StringUtil;\n \n+import java.sql.Array;\n import java.util.ArrayList;\n+import java.util.Arrays;\n import java.util.Collections;\n import java.util.List;\n \n@@ -28,43 +30,63 @@\n  */\n public class MqttSubAckPayload {\n \n-    private final List<Integer> reasonCodes;\n+    private final List<MqttReasonCodes.SubAck> reasonCodes;\n \n     public MqttSubAckPayload(int... reasonCodes) {\n         ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n \n-        List<Integer> list = new ArrayList<Integer>(reasonCodes.length);\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>(reasonCodes.length);\n         for (int v: reasonCodes) {\n-            list.add(v);\n+            list.add(MqttReasonCodes.SubAck.valueOf((byte) (v & 0xFF)));\n         }\n         this.reasonCodes = Collections.unmodifiableList(list);\n     }\n \n+    public MqttSubAckPayload(MqttReasonCodes.SubAck... reasonCodes) {\n+        ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n+\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>(reasonCodes.length);\n+        list.addAll(Arrays.asList(reasonCodes));\n+        this.reasonCodes = Collections.unmodifiableList(list);\n+    }\n+\n     public MqttSubAckPayload(Iterable<Integer> reasonCodes) {\n         ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n-        List<Integer> list = new ArrayList<Integer>();\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>();\n         for (Integer v: reasonCodes) {\n             if (v == null) {\n                 break;\n             }\n-            list.add(v);\n+            list.add(MqttReasonCodes.SubAck.valueOf(v.byteValue()));\n         }\n         this.reasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public List<Integer> grantedQoSLevels() {\n         List<Integer> qosLevels = new ArrayList<Integer>(reasonCodes.size());\n-        for (int code: reasonCodes) {\n-            if (code > MqttQoS.EXACTLY_ONCE.value()) {\n+        for (MqttReasonCodes.SubAck code: reasonCodes) {\n+            if ((code.byteValue() & 0xFF) > MqttQoS.EXACTLY_ONCE.value()) {\n                 qosLevels.add(MqttQoS.FAILURE.value());\n             } else {\n-                qosLevels.add(code);\n+                qosLevels.add(code.byteValue() & 0xFF);\n             }\n         }\n         return qosLevels;\n     }\n \n     public List<Integer> reasonCodes() {\n+        return typedReasonCodesToOrdinal();\n+    }\n+\n+    private List<Integer> typedReasonCodesToOrdinal() {\n+        List<Integer> intCodes = new ArrayList<Integer>(reasonCodes.size());\n+        for (MqttReasonCodes.SubAck code: reasonCodes) {\n+            intCodes.add(code.byteValue() & 0xFF);\n+        }\n+        return intCodes;\n+    }\n+\n+    public List<MqttReasonCodes.SubAck> typedReasonCodes() {\n         return reasonCodes;\n     }\n \ncodec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttUnsubAckPayload.java\n@@ -27,7 +27,7 @@\n  */\n public final class MqttUnsubAckPayload {\n \n-    private final List<Short> unsubscribeReasonCodes;\n+    private final List<MqttReasonCodes.UnsubAck> unsubscribeReasonCodes;\n \n     private static final MqttUnsubAckPayload EMPTY = new MqttUnsubAckPayload();\n \n@@ -42,25 +42,37 @@ public static MqttUnsubAckPayload withEmptyDefaults(MqttUnsubAckPayload payload)\n     public MqttUnsubAckPayload(short... unsubscribeReasonCodes) {\n         ObjectUtil.checkNotNull(unsubscribeReasonCodes, \"unsubscribeReasonCodes\");\n \n-        List<Short> list = new ArrayList<Short>(unsubscribeReasonCodes.length);\n+        List<MqttReasonCodes.UnsubAck> list = new ArrayList<MqttReasonCodes.UnsubAck>(unsubscribeReasonCodes.length);\n         for (Short v: unsubscribeReasonCodes) {\n-            list.add(v);\n+            list.add(MqttReasonCodes.UnsubAck.valueOf((byte) (v & 0xFF)));\n         }\n         this.unsubscribeReasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public MqttUnsubAckPayload(Iterable<Short> unsubscribeReasonCodes) {\n         ObjectUtil.checkNotNull(unsubscribeReasonCodes, \"unsubscribeReasonCodes\");\n \n-        List<Short> list = new ArrayList<Short>();\n+        List<MqttReasonCodes.UnsubAck> list = new ArrayList<MqttReasonCodes.UnsubAck>();\n         for (Short v: unsubscribeReasonCodes) {\n             ObjectUtil.checkNotNull(v, \"unsubscribeReasonCode\");\n-            list.add(v);\n+            list.add(MqttReasonCodes.UnsubAck.valueOf(v.byteValue()));\n         }\n         this.unsubscribeReasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public List<Short> unsubscribeReasonCodes() {\n+        return typedReasonCodesToOrdinal();\n+    }\n+\n+    private List<Short> typedReasonCodesToOrdinal() {\n+        List<Short> codes = new ArrayList<Short>(unsubscribeReasonCodes.size());\n+        for (MqttReasonCodes.UnsubAck code: unsubscribeReasonCodes) {\n+            codes.add((short) (code.byteValue() & 0xFF));\n+        }\n+        return codes;\n+    }\n+\n+    public List<MqttReasonCodes.UnsubAck> typedReasonCodes() {\n         return unsubscribeReasonCodes;\n     }\n ",
      "diff": [
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttReasonCodes.java",
          "patch": "@@ -38,7 +38,7 @@ private static <E> E valueOfHelper(byte b, E[] values) {\n     /**\n      * Reason codes for MQTT Disconnect message.\n      */\n-    enum Disconnect {\n+    public enum Disconnect {\n         NORMAL_DISCONNECT((byte) 0x00), //sent by: client or server\n         DISCONNECT_WITH_WILL_MESSAGE((byte) 0x04), //sent by: client\n         UNSPECIFIED_ERROR((byte) 0x80), //sent by: client or server\n@@ -106,7 +106,7 @@ public static Disconnect valueOf(byte b) {\n     /**\n      * Reason codes for MQTT Auth message.\n      */\n-    enum Auth {\n+    public enum Auth {\n         SUCCESS((byte) 0x00), //sent by: Server\n         CONTINUE_AUTHENTICATION((byte) 0x18), //sent by: Client or Server\n         REAUTHENTICATE((byte) 0x19); //sent by: Client\n@@ -148,7 +148,7 @@ public static Auth valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubAck message.\n      */\n-    enum PubAck {\n+    public enum PubAck {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n         UNSPECIFIED_ERROR((byte) 0x80),\n@@ -196,7 +196,7 @@ public static PubAck valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubRec message.\n      */\n-    enum PubRec {\n+    public enum PubRec {\n         SUCCESS((byte) 0x00),\n         NO_MATCHING_SUBSCRIBERS((byte) 0x10),\n         UNSPECIFIED_ERROR((byte) 0x80),\n@@ -244,7 +244,7 @@ public static PubRec valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubRel message.\n      */\n-    enum PubRel {\n+    public enum PubRel {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n \n@@ -285,7 +285,7 @@ public static PubRel valueOf(byte b) {\n     /**\n      * Reason codes for MQTT PubComp message.\n      */\n-    enum PubComp {\n+    public enum PubComp {\n         SUCCESS((byte) 0x00),\n         PACKET_IDENTIFIER_NOT_FOUND((byte) 0x92);\n \n@@ -326,7 +326,7 @@ public static PubComp valueOf(byte b) {\n     /**\n      * Reason codes for MQTT SubAck message.\n      */\n-    enum SubAck {\n+    public enum SubAck {\n         GRANTED_QOS_0((byte) 0x00),\n         GRANTED_QOS_1((byte) 0x01),\n         GRANTED_QOS_2((byte) 0x02),\n@@ -377,7 +377,7 @@ public static SubAck valueOf(byte b) {\n     /**\n      * Reason codes for MQTT UnsubAck message.\n      */\n-    enum UnsubAck {\n+    public enum UnsubAck {\n         SUCCESS((byte) 0x00),\n         NO_SUBSCRIPTION_EXISTED((byte) 0x11),\n         UNSPECIFIED_ERROR((byte) 0x80),"
        },
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttSubAckPayload.java",
          "patch": "@@ -19,7 +19,9 @@\n import io.netty.util.internal.ObjectUtil;\n import io.netty.util.internal.StringUtil;\n \n+import java.sql.Array;\n import java.util.ArrayList;\n+import java.util.Arrays;\n import java.util.Collections;\n import java.util.List;\n \n@@ -28,43 +30,63 @@\n  */\n public class MqttSubAckPayload {\n \n-    private final List<Integer> reasonCodes;\n+    private final List<MqttReasonCodes.SubAck> reasonCodes;\n \n     public MqttSubAckPayload(int... reasonCodes) {\n         ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n \n-        List<Integer> list = new ArrayList<Integer>(reasonCodes.length);\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>(reasonCodes.length);\n         for (int v: reasonCodes) {\n-            list.add(v);\n+            list.add(MqttReasonCodes.SubAck.valueOf((byte) (v & 0xFF)));\n         }\n         this.reasonCodes = Collections.unmodifiableList(list);\n     }\n \n+    public MqttSubAckPayload(MqttReasonCodes.SubAck... reasonCodes) {\n+        ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n+\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>(reasonCodes.length);\n+        list.addAll(Arrays.asList(reasonCodes));\n+        this.reasonCodes = Collections.unmodifiableList(list);\n+    }\n+\n     public MqttSubAckPayload(Iterable<Integer> reasonCodes) {\n         ObjectUtil.checkNotNull(reasonCodes, \"reasonCodes\");\n-        List<Integer> list = new ArrayList<Integer>();\n+        List<MqttReasonCodes.SubAck> list = new ArrayList<MqttReasonCodes.SubAck>();\n         for (Integer v: reasonCodes) {\n             if (v == null) {\n                 break;\n             }\n-            list.add(v);\n+            list.add(MqttReasonCodes.SubAck.valueOf(v.byteValue()));\n         }\n         this.reasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public List<Integer> grantedQoSLevels() {\n         List<Integer> qosLevels = new ArrayList<Integer>(reasonCodes.size());\n-        for (int code: reasonCodes) {\n-            if (code > MqttQoS.EXACTLY_ONCE.value()) {\n+        for (MqttReasonCodes.SubAck code: reasonCodes) {\n+            if ((code.byteValue() & 0xFF) > MqttQoS.EXACTLY_ONCE.value()) {\n                 qosLevels.add(MqttQoS.FAILURE.value());\n             } else {\n-                qosLevels.add(code);\n+                qosLevels.add(code.byteValue() & 0xFF);\n             }\n         }\n         return qosLevels;\n     }\n \n     public List<Integer> reasonCodes() {\n+        return typedReasonCodesToOrdinal();\n+    }\n+\n+    private List<Integer> typedReasonCodesToOrdinal() {\n+        List<Integer> intCodes = new ArrayList<Integer>(reasonCodes.size());\n+        for (MqttReasonCodes.SubAck code: reasonCodes) {\n+            intCodes.add(code.byteValue() & 0xFF);\n+        }\n+        return intCodes;\n+    }\n+\n+    public List<MqttReasonCodes.SubAck> typedReasonCodes() {\n         return reasonCodes;\n     }\n "
        },
        {
          "file": "codec-mqtt/src/main/java/io/netty/handler/codec/mqtt/MqttUnsubAckPayload.java",
          "patch": "@@ -27,7 +27,7 @@\n  */\n public final class MqttUnsubAckPayload {\n \n-    private final List<Short> unsubscribeReasonCodes;\n+    private final List<MqttReasonCodes.UnsubAck> unsubscribeReasonCodes;\n \n     private static final MqttUnsubAckPayload EMPTY = new MqttUnsubAckPayload();\n \n@@ -42,25 +42,37 @@ public static MqttUnsubAckPayload withEmptyDefaults(MqttUnsubAckPayload payload)\n     public MqttUnsubAckPayload(short... unsubscribeReasonCodes) {\n         ObjectUtil.checkNotNull(unsubscribeReasonCodes, \"unsubscribeReasonCodes\");\n \n-        List<Short> list = new ArrayList<Short>(unsubscribeReasonCodes.length);\n+        List<MqttReasonCodes.UnsubAck> list = new ArrayList<MqttReasonCodes.UnsubAck>(unsubscribeReasonCodes.length);\n         for (Short v: unsubscribeReasonCodes) {\n-            list.add(v);\n+            list.add(MqttReasonCodes.UnsubAck.valueOf((byte) (v & 0xFF)));\n         }\n         this.unsubscribeReasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public MqttUnsubAckPayload(Iterable<Short> unsubscribeReasonCodes) {\n         ObjectUtil.checkNotNull(unsubscribeReasonCodes, \"unsubscribeReasonCodes\");\n \n-        List<Short> list = new ArrayList<Short>();\n+        List<MqttReasonCodes.UnsubAck> list = new ArrayList<MqttReasonCodes.UnsubAck>();\n         for (Short v: unsubscribeReasonCodes) {\n             ObjectUtil.checkNotNull(v, \"unsubscribeReasonCode\");\n-            list.add(v);\n+            list.add(MqttReasonCodes.UnsubAck.valueOf(v.byteValue()));\n         }\n         this.unsubscribeReasonCodes = Collections.unmodifiableList(list);\n     }\n \n     public List<Short> unsubscribeReasonCodes() {\n+        return typedReasonCodesToOrdinal();\n+    }\n+\n+    private List<Short> typedReasonCodesToOrdinal() {\n+        List<Short> codes = new ArrayList<Short>(unsubscribeReasonCodes.size());\n+        for (MqttReasonCodes.UnsubAck code: unsubscribeReasonCodes) {\n+            codes.add((short) (code.byteValue() & 0xFF));\n+        }\n+        return codes;\n+    }\n+\n+    public List<MqttReasonCodes.UnsubAck> typedReasonCodes() {\n         return unsubscribeReasonCodes;\n     }\n "
        }
      ]
    },
    {
      "type": "comment",
      "id": 1691101379,
      "created_at": "2023-08-24T06:49:12Z",
      "user": "andsel",
      "body": "@chrisvest regarding \r\n> Where convenient, could we add some overloads that use the new enums?\r\n\r\nIn commit 75f337f98d8037310ebb1b840eab36e6dbfe6ee4 I've updated SubAck and UnsubAck payloads to use the newly introduced reason codes. Did you mean something like that?"
    },
    {
      "type": "commit",
      "date": "2023-08-25T07:48:56Z",
      "sha": "3a9ff0ddc2fdf77a688e231eef9f5f1c8210e65e",
      "message": "Removed useless comment line",
      "author": "andsel",
      "author_email": "selva.andre@gmail.com",
      "author_raw_date": "2023-08-25T07:48:49Z",
      "author_date": "2023-08-25T07:48:49Z",
      "committer": "andsel",
      "committer_email": "selva.andre@gmail.com",
      "raw_date": "2023-08-25T07:48:56Z",
      "diff_text": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java\n@@ -31,7 +31,6 @@\n import static io.netty.handler.codec.mqtt.MqttReasonCodes.UnsubAck;\n import static org.junit.jupiter.api.Assertions.assertEquals;\n \n-// ./mvnw -B -ntp --file pom.xml verify -pl codec-mqtt\n \n class MqttReasonCodesTest {\n ",
      "diff": [
        {
          "file": "codec-mqtt/src/test/java/io/netty/handler/codec/mqtt/MqttReasonCodesTest.java",
          "patch": "@@ -31,7 +31,6 @@\n import static io.netty.handler.codec.mqtt.MqttReasonCodes.UnsubAck;\n import static org.junit.jupiter.api.Assertions.assertEquals;\n \n-// ./mvnw -B -ntp --file pom.xml verify -pl codec-mqtt\n \n class MqttReasonCodesTest {\n "
        }
      ]
    },
    {
      "type": "comment",
      "id": 1696023173,
      "created_at": "2023-08-28T16:51:05Z",
      "user": "chrisvest",
      "body": "@andsel The Netty 5 mqtt module is [here](https://github.com/netty-contrib/codec-mqtt), if you're interested in porting the changes. Looks like it needs some love to make the build pass again."
    },
    {
      "type": "comment",
      "id": 1713264107,
      "created_at": "2023-09-11T06:44:45Z",
      "user": "andsel",
      "body": "@chrisvest I try to move it to a green state. If and when I've something shareable, I'll put a PR :-) "
    }
  ]
}