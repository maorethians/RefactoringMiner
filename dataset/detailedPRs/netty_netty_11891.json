{
  "repo": "netty/netty",
  "number": 11891,
  "pr_number": 11891,
  "title": "JdkZlibEncoder can use pooled heap buffers for deflater input",
  "user": "dferstay",
  "created_at": "2021-12-04T20:02:07Z",
  "merged_at": "2021-12-08T10:41:07Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "1e189654f913b13be3616c552a04459465b752b3",
  "merge_commit_sha": "d80a34ecf84bbe153b60123b059deabcc97a8935",
  "commits": 4,
  "issue_events": [
    "mentioned",
    "subscribed",
    "renamed",
    "referenced",
    "merged",
    "closed",
    "mentioned",
    "subscribed",
    "milestoned",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "referenced",
    "referenced",
    "referenced",
    "referenced"
  ],
  "url": "https://github.com/netty/netty/pull/11891",
  "timeline": [
    {
      "type": "commit",
      "date": "2021-12-04T19:59:24Z",
      "sha": "e0a25013c10e477eefb26926f540039fc0d66b32",
      "message": "JdkZlibEncoder uses pooled heap buffers for deflater input\n\nMotivation:\n\nPreviously, when the input ByteBuf was not heap allocated we copied the\ninput into a new byte array allocated on the heap for every encode() call.\nThis puts high-throughput clients that use the JdkZlibEncoder under memory\npressure.\n\nModifications:\n\nNow, when the input ByteBuf is not heap allocated we copy the input into a\nheap ByteBuf allocated from the PooledByteBufAllocator, releasing it after\nthe encode() has completed.\n\nResult:\n\nThe result is less heap allocation and GC activity.\n\nFixes #11890\n\nSigned-off-by: Daniel Ferstay <dferstay@splunk.com>",
      "author": "Daniel Ferstay",
      "author_email": "dferstay@splunk.com",
      "author_raw_date": "2021-12-02T21:03:12Z",
      "author_date": "2021-12-02T21:03:12Z",
      "committer": "Daniel Ferstay",
      "committer_email": "dferstay@splunk.com",
      "raw_date": "2021-12-04T19:59:24Z",
      "diff_text": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java\n@@ -16,6 +16,7 @@\n package io.netty.handler.codec.compression;\n \n import io.netty.buffer.ByteBuf;\n+import io.netty.buffer.PooledByteBufAllocator;\n import io.netty.channel.ChannelFuture;\n import io.netty.channel.ChannelFutureListener;\n import io.netty.channel.ChannelHandlerContext;\n@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }\n \n-        deflater.setInput(inAry, offset, len);\n-        for (;;) {\n-            deflate(out);\n-            if (deflater.needsInput()) {\n-                // Consumed everything\n-                break;\n-            } else {\n-                if (!out.isWritable()) {\n-                    // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                    // make more room.\n-                    out.ensureWritable(out.writerIndex());\n+            deflater.setInput(inAry, offset, len);\n+            for (; ; ) {\n+                deflate(out);\n+                if (deflater.needsInput()) {\n+                    // Consumed everything\n+                    break;\n+                } else {\n+                    if (!out.isWritable()) {\n+                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n+                        // make more room.\n+                        out.ensureWritable(out.writerIndex());\n+                    }\n                 }\n             }\n+        } finally {\n+            if (heapBuf != null) {\n+                heapBuf.release();\n+            }\n         }\n     }\n ",
      "diff": [
        {
          "file": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
          "patch": "@@ -16,6 +16,7 @@\n package io.netty.handler.codec.compression;\n \n import io.netty.buffer.ByteBuf;\n+import io.netty.buffer.PooledByteBufAllocator;\n import io.netty.channel.ChannelFuture;\n import io.netty.channel.ChannelFutureListener;\n import io.netty.channel.ChannelHandlerContext;\n@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }\n \n-        deflater.setInput(inAry, offset, len);\n-        for (;;) {\n-            deflate(out);\n-            if (deflater.needsInput()) {\n-                // Consumed everything\n-                break;\n-            } else {\n-                if (!out.isWritable()) {\n-                    // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                    // make more room.\n-                    out.ensureWritable(out.writerIndex());\n+            deflater.setInput(inAry, offset, len);\n+            for (; ; ) {\n+                deflate(out);\n+                if (deflater.needsInput()) {\n+                    // Consumed everything\n+                    break;\n+                } else {\n+                    if (!out.isWritable()) {\n+                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n+                        // make more room.\n+                        out.ensureWritable(out.writerIndex());\n+                    }\n                 }\n             }\n+        } finally {\n+            if (heapBuf != null) {\n+                heapBuf.release();\n+            }\n         }\n     }\n "
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2021-12-04T20:02:07Z",
      "user": "dferstay",
      "body": "Motivation:\r\n\r\nPreviously, when the input ByteBuf was not heap allocated we copied the\r\ninput into a new byte array allocated on the heap for every encode() call.\r\nThis puts high-throughput clients that use the JdkZlibEncoder under memory\r\npressure.\r\n\r\nModifications:\r\n\r\nNow, when the input ByteBuf is not heap allocated we copy the input into a\r\nheap ByteBuf allocated from the configured ByteBufAllocator (which might be pooled), releasing it after\r\nthe encode() has completed.\r\n\r\nResult:\r\n\r\nThe result is less heap allocation and GC activity when the PooledByteBufAllocator is used (which is the default)\r\n\r\nFixes #11890\r\n\r\nSigned-off-by: Daniel Ferstay <dferstay@splunk.com>"
    },
    {
      "type": "comment",
      "id": 986086685,
      "created_at": "2021-12-04T20:14:40Z",
      "user": "dferstay",
      "body": "Below is a memory profile before the change.\r\n\r\n![jdkzlibencoder_heap_alloc](https://user-images.githubusercontent.com/13126037/144723305-0e853b51-9b80-4a0f-890c-f51a5c4f921d.png)\r\n"
    },
    {
      "type": "comment",
      "id": 986086878,
      "created_at": "2021-12-04T20:16:15Z",
      "user": "dferstay",
      "body": "After the change `JdkZlibEncoder.encode` is not burning so bright.\r\n\r\n![jdkzlibencoder_pooled_heap_alloc](https://user-images.githubusercontent.com/13126037/144723348-1f4c989a-b966-483d-95f9-73e180872ff6.png)\r\n"
    },
    {
      "type": "review_comment",
      "id": 762523887,
      "created_at": "2021-12-05T07:48:23Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n                heapBuf = ctx.alloc().heapBuffer(len, len);\r\n```",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 213,
      "side": "RIGHT",
      "original_position": 32,
      "position": 1,
      "subject_type": "line",
      "reply": [
        {
          "id": 763239263,
          "user": "dferstay",
          "body": "Thank you; done.",
          "created_at": "2021-12-06T17:52:58Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 762523961,
      "created_at": "2021-12-05T07:49:03Z",
      "user": "normanmaurer",
      "body": "Please revert the formatting changes. ",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }\n \n-        deflater.setInput(inAry, offset, len);\n-        for (;;) {\n-            deflate(out);\n-            if (deflater.needsInput()) {\n-                // Consumed everything\n-                break;\n-            } else {\n-                if (!out.isWritable()) {\n-                    // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                    // make more room.\n-                    out.ensureWritable(out.writerIndex());\n+            deflater.setInput(inAry, offset, len);\n+            for (; ; ) {\n+                deflate(out);\n+                if (deflater.needsInput()) {\n+                    // Consumed everything\n+                    break;\n+                } else {\n+                    if (!out.isWritable()) {\n+                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n+                        // make more room.\n+                        out.ensureWritable(out.writerIndex());\n+                    }",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 240,
      "original_line": 241,
      "side": "RIGHT",
      "original_position": 79,
      "position": 71,
      "subject_type": "line",
      "reply": [
        {
          "id": 764334467,
          "user": "dferstay",
          "body": "@normanmaurer ,\r\nAre you referring to the additional indentation?  If so, it is due to the addition of the `try / finally` block to ensure that the allocated heapBuf is released on method return.  Do do you want me to remove the `try / finally`?  ",
          "created_at": "2021-12-07T20:22:09Z"
        },
        {
          "id": 764345474,
          "user": "normanmaurer",
          "body": "Check the checkstyle errors ",
          "created_at": "2021-12-07T20:40:16Z"
        },
        {
          "id": 764389771,
          "user": "dferstay",
          "body": "Got it; the checkstyle errors should now be addressed.",
          "created_at": "2021-12-07T21:52:21Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 762523995,
      "created_at": "2021-12-05T07:49:18Z",
      "user": "normanmaurer",
      "body": "please revert formatting changes",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 222,
      "original_line": 223,
      "side": "RIGHT",
      "original_position": 46,
      "position": 38,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 762524007,
      "created_at": "2021-12-05T07:49:31Z",
      "user": "normanmaurer",
      "body": "please revert formatting changes",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 227,
      "original_line": 228,
      "side": "RIGHT",
      "original_position": 55,
      "position": 47,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 762524053,
      "created_at": "2021-12-05T07:50:04Z",
      "user": "normanmaurer",
      "body": "make this final and assign in the `if / else ` blocks",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +201,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 203,
      "original_line": 204,
      "side": "RIGHT",
      "original_position": 23,
      "position": 15,
      "subject_type": "line",
      "reply": [
        {
          "id": 763245752,
          "user": "dferstay",
          "body": "@normanmaurer ,\r\n> make this final and assign in the if / else blocks\r\n\r\nI introduced a `try / finally` so that we would not leak the reference to the allocated heap buffer; this accounts for the formatting changes above and it also confuses the Java compiler when making `heapBuf` final; see attached screenshot.\r\n![Screenshot from 2021-12-06 10-00-16](https://user-images.githubusercontent.com/13126037/144897921-5aff2f72-9d7c-40a9-9012-072bf18b20e4.png)\r\n\r\n\r\n",
          "created_at": "2021-12-06T18:01:17Z"
        },
        {
          "id": 763881435,
          "user": "normanmaurer",
          "body": "got it... ",
          "created_at": "2021-12-07T11:03:59Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 823359024,
      "created_at": "2021-12-05T07:50:09Z",
      "user": "normanmaurer",
      "state": "CHANGES_REQUESTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 986182944,
      "created_at": "2021-12-05T07:50:24Z",
      "user": "normanmaurer",
      "body": "Also please sign or ICLA: https://netty.io/s/icla "
    },
    {
      "type": "commit",
      "date": "2021-12-06T17:52:01Z",
      "sha": "a16d32c09659b12ad78a821608f9352d9003b177",
      "message": "use ByteBufAllocator associated with the channel handler context",
      "author": "Daniel Ferstay",
      "author_email": "dferstay@splunk.com",
      "author_raw_date": "2021-12-06T17:52:01Z",
      "author_date": "2021-12-06T17:52:01Z",
      "committer": "Daniel Ferstay",
      "committer_email": "dferstay@splunk.com",
      "raw_date": "2021-12-06T17:52:01Z",
      "diff_text": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java\n@@ -16,7 +16,6 @@\n package io.netty.handler.codec.compression;\n \n import io.netty.buffer.ByteBuf;\n-import io.netty.buffer.PooledByteBufAllocator;\n import io.netty.channel.ChannelFuture;\n import io.netty.channel.ChannelFutureListener;\n import io.netty.channel.ChannelHandlerContext;\n@@ -210,7 +209,7 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n                 // skip all bytes as we will consume all of them\n                 uncompressed.skipBytes(len);\n             } else {\n-                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                heapBuf = ctx.alloc().heapBuffer(len, len);\n                 uncompressed.readBytes(heapBuf, len);\n                 inAry = heapBuf.array();\n                 offset = heapBuf.arrayOffset() + heapBuf.readerIndex();",
      "diff": [
        {
          "file": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
          "patch": "@@ -16,7 +16,6 @@\n package io.netty.handler.codec.compression;\n \n import io.netty.buffer.ByteBuf;\n-import io.netty.buffer.PooledByteBufAllocator;\n import io.netty.channel.ChannelFuture;\n import io.netty.channel.ChannelFutureListener;\n import io.netty.channel.ChannelHandlerContext;\n@@ -210,7 +209,7 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n                 // skip all bytes as we will consume all of them\n                 uncompressed.skipBytes(len);\n             } else {\n-                heapBuf = PooledByteBufAllocator.DEFAULT.heapBuffer(len, len);\n+                heapBuf = ctx.alloc().heapBuffer(len, len);\n                 uncompressed.readBytes(heapBuf, len);\n                 inAry = heapBuf.array();\n                 offset = heapBuf.arrayOffset() + heapBuf.readerIndex();"
        }
      ]
    },
    {
      "type": "review",
      "id": 824275710,
      "created_at": "2021-12-06T17:52:59Z",
      "user": "dferstay",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 824284367,
      "created_at": "2021-12-06T18:01:18Z",
      "user": "dferstay",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 987019776,
      "created_at": "2021-12-06T18:01:56Z",
      "user": "dferstay",
      "body": "> Also please sign or ICLA: https://netty.io/s/icla\r\n\r\nSigned :)"
    },
    {
      "type": "review",
      "id": 825119314,
      "created_at": "2021-12-07T11:03:59Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 987818465,
      "created_at": "2021-12-07T11:04:16Z",
      "user": "normanmaurer",
      "body": "@dferstay can you please revert the formatting changes ?"
    },
    {
      "type": "review",
      "id": 825739480,
      "created_at": "2021-12-07T20:22:09Z",
      "user": "dferstay",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 825753979,
      "created_at": "2021-12-07T20:40:16Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2021-12-07T21:50:03Z",
      "sha": "ffafae8634254ec7c18230f55dd68ad8d067d418",
      "message": "fix checkstyle errors",
      "author": "Daniel Ferstay",
      "author_email": "dferstay@splunk.com",
      "author_raw_date": "2021-12-07T21:50:03Z",
      "author_date": "2021-12-07T21:50:03Z",
      "committer": "Daniel Ferstay",
      "committer_email": "dferstay@splunk.com",
      "raw_date": "2021-12-07T21:50:03Z",
      "diff_text": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java\n@@ -227,15 +227,15 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n             }\n \n             deflater.setInput(inAry, offset, len);\n-            for (; ; ) {\n+            for (; ;) {\n                 deflate(out);\n                 if (deflater.needsInput()) {\n                     // Consumed everything\n                     break;\n                 } else {\n                     if (!out.isWritable()) {\n-                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                        // make more room.\n+                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity\n+                        // to make more room.\n                         out.ensureWritable(out.writerIndex());\n                     }\n                 }",
      "diff": [
        {
          "file": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
          "patch": "@@ -227,15 +227,15 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n             }\n \n             deflater.setInput(inAry, offset, len);\n-            for (; ; ) {\n+            for (; ;) {\n                 deflate(out);\n                 if (deflater.needsInput()) {\n                     // Consumed everything\n                     break;\n                 } else {\n                     if (!out.isWritable()) {\n-                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                        // make more room.\n+                        // We did not consume everything but the buffer is not writable anymore. Increase the capacity\n+                        // to make more room.\n                         out.ensureWritable(out.writerIndex());\n                     }\n                 }"
        }
      ]
    },
    {
      "type": "review",
      "id": 825809719,
      "created_at": "2021-12-07T21:52:22Z",
      "user": "dferstay",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 826079726,
      "created_at": "2021-12-08T07:18:09Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 764602512,
      "created_at": "2021-12-08T07:18:09Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n            for (;;) {\r\n```",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +200,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = ctx.alloc().heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }\n \n-        deflater.setInput(inAry, offset, len);\n-        for (;;) {\n-            deflate(out);\n-            if (deflater.needsInput()) {\n-                // Consumed everything\n-                break;\n-            } else {\n-                if (!out.isWritable()) {\n-                    // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                    // make more room.\n-                    out.ensureWritable(out.writerIndex());\n+            deflater.setInput(inAry, offset, len);\n+            for (; ; ) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 230,
      "side": "RIGHT",
      "original_position": 61,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 764602751,
      "created_at": "2021-12-08T07:18:40Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n            for (;;) {\r\n```",
      "path": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "diff_hunk": "@@ -200,42 +200,50 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n \n         int offset;\n         byte[] inAry;\n-        if (uncompressed.hasArray()) {\n-            // if it is backed by an array we not need to to do a copy at all\n-            inAry = uncompressed.array();\n-            offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n-            // skip all bytes as we will consume all of them\n-            uncompressed.skipBytes(len);\n-        } else {\n-            inAry = new byte[len];\n-            uncompressed.readBytes(inAry);\n-            offset = 0;\n-        }\n+        ByteBuf heapBuf = null;\n+        try {\n+            if (uncompressed.hasArray()) {\n+                // if it is backed by an array we not need to do a copy at all\n+                inAry = uncompressed.array();\n+                offset = uncompressed.arrayOffset() + uncompressed.readerIndex();\n+                // skip all bytes as we will consume all of them\n+                uncompressed.skipBytes(len);\n+            } else {\n+                heapBuf = ctx.alloc().heapBuffer(len, len);\n+                uncompressed.readBytes(heapBuf, len);\n+                inAry = heapBuf.array();\n+                offset = heapBuf.arrayOffset() + heapBuf.readerIndex();\n+            }\n \n-        if (writeHeader) {\n-            writeHeader = false;\n-            if (wrapper == ZlibWrapper.GZIP) {\n-                out.writeBytes(gzipHeader);\n+            if (writeHeader) {\n+                writeHeader = false;\n+                if (wrapper == ZlibWrapper.GZIP) {\n+                    out.writeBytes(gzipHeader);\n+                }\n             }\n-        }\n \n-        if (wrapper == ZlibWrapper.GZIP) {\n-            crc.update(inAry, offset, len);\n-        }\n+            if (wrapper == ZlibWrapper.GZIP) {\n+                crc.update(inAry, offset, len);\n+            }\n \n-        deflater.setInput(inAry, offset, len);\n-        for (;;) {\n-            deflate(out);\n-            if (deflater.needsInput()) {\n-                // Consumed everything\n-                break;\n-            } else {\n-                if (!out.isWritable()) {\n-                    // We did not consume everything but the buffer is not writable anymore. Increase the capacity to\n-                    // make more room.\n-                    out.ensureWritable(out.writerIndex());\n+            deflater.setInput(inAry, offset, len);\n+            for (; ;) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 230,
      "side": "RIGHT",
      "original_position": 61,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 826080064,
      "created_at": "2021-12-08T07:18:41Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2021-12-08T07:18:45Z",
      "sha": "4a08b615a361f9352310dea7530713718d3a22d1",
      "message": "Update codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2021-12-08T07:18:45Z",
      "author_date": "2021-12-08T07:18:45Z",
      "committer": "GitHub",
      "committer_email": "noreply@github.com",
      "raw_date": "2021-12-08T07:18:45Z",
      "diff_text": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java\n@@ -227,7 +227,7 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n             }\n \n             deflater.setInput(inAry, offset, len);\n-            for (; ;) {\n+            for (;;) {\n                 deflate(out);\n                 if (deflater.needsInput()) {\n                     // Consumed everything",
      "diff": [
        {
          "file": "codec/src/main/java/io/netty/handler/codec/compression/JdkZlibEncoder.java",
          "patch": "@@ -227,7 +227,7 @@ protected void encode(ChannelHandlerContext ctx, ByteBuf uncompressed, ByteBuf o\n             }\n \n             deflater.setInput(inAry, offset, len);\n-            for (; ;) {\n+            for (;;) {\n                 deflate(out);\n                 if (deflater.needsInput()) {\n                     // Consumed everything"
        }
      ]
    },
    {
      "type": "comment",
      "id": 988698604,
      "created_at": "2021-12-08T10:43:06Z",
      "user": "normanmaurer",
      "body": "@dferstay thanks a lot ! Would be cool if you can add details to the adopters page: https://netty.io/wiki/adopters.html"
    },
    {
      "type": "comment",
      "id": 988984336,
      "created_at": "2021-12-08T16:45:55Z",
      "user": "dferstay",
      "body": "> @dferstay thanks a lot ! Would be cool if you can add details to the adopters page: https://netty.io/wiki/adopters.html\r\n\r\n@normanmaurer,\r\nCheers; will do! :+1: "
    }
  ]
}