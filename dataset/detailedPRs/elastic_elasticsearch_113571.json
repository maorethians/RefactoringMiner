{
  "repo": "elastic/elasticsearch",
  "number": 113571,
  "pr_number": 113571,
  "title": "Memoize pattern match in GetAliases",
  "user": "bcully",
  "created_at": "2024-09-25T22:49:30Z",
  "merged_at": "2024-09-27T17:18:53Z",
  "merged": true,
  "state": "closed",
  "labels": [
    ">non-issue",
    ":Distributed/Distributed",
    "Team:Distributed",
    "auto-merge-without-approval",
    "v9.0.0"
  ],
  "base_commit": "74c708d81526e2bf90e52d588c0eac5637e08e81",
  "merge_commit_sha": "31bea5675d9b5cdf1bf8415b5d533a3c5e335bdd",
  "commits": 1,
  "issue_events": [
    "labeled",
    "labeled",
    "labeled",
    "labeled",
    "review_requested",
    "labeled",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "review_requested",
    "head_ref_force_pushed",
    "renamed",
    "merged",
    "closed",
    "referenced"
  ],
  "url": "https://github.com/elastic/elasticsearch/pull/113571",
  "timeline": [
    {
      "type": "description",
      "created_at": "2024-09-25T22:49:30Z",
      "user": "bcully",
      "body": "Previously, when GetAliases was called with multiple indices that share an alias, findAliases would recheck the alias against the supplied alias patterns for each index that used it. This change memoizes the pattern check to avoid that redundant work.\r\n\r\nCloses #102676 \r\n"
    },
    {
      "type": "comment",
      "id": 2375407344,
      "created_at": "2024-09-25T22:49:55Z",
      "user": "elasticsearchmachine",
      "body": "Pinging @elastic/es-distributed (Team:Distributed)"
    },
    {
      "type": "commit",
      "date": "2024-09-27T15:56:46Z",
      "sha": "b469e717113b2dfa59742f8a4a8145771b162239",
      "message": "Memoize pattern match in GetAliases\n\nPreviously, when GetAliases was called with multiple indices that share\nan alias, findAliases would recheck the alias against the supplied alias\npatterns for each index that used it. This change memoizes the pattern\ncheck to avoid that redundant work.\n\nCloses #102676",
      "author": "Brendan Cully",
      "author_email": "brendan.cully@elastic.co",
      "author_raw_date": "2024-09-25T22:36:08Z",
      "author_date": "2024-09-25T22:36:08Z",
      "committer": "Brendan Cully",
      "committer_email": "brendan.cully@elastic.co",
      "raw_date": "2024-09-27T15:56:46Z",
      "diff_text": "server/src/main/java/org/elasticsearch/cluster/metadata/Metadata.java\n@@ -855,7 +855,7 @@ public Map<String, List<DataStreamAlias>> findDataStreamAliases(final String[] a\n      *\n      * @param aliases The aliases to look for. Might contain include or exclude wildcards.\n      * @param possibleMatches The data streams or indices that the aliases must point to in order to be returned\n-     * @param getter A function that is used to get the alises for a given data stream or index\n+     * @param getter A function that is used to get the aliases for a given data stream or index\n      * @param setter A function that is used to keep track of the found aliases\n      */\n     private void findAliasInfo(final String[] aliases, final String[] possibleMatches, AliasInfoGetter getter, AliasInfoSetter setter) {\n@@ -881,24 +881,30 @@ private void findAliasInfo(final String[] aliases, final String[] possibleMatche\n \n         boolean matchAllAliases = patterns.length == 0;\n \n+        // memoize pattern match against aliases to avoid repeatedly matching when multiple indices share an alias\n+        HashMap<String, Boolean> seenAliases = new HashMap<>();\n+        Predicate<String> matcher = alias -> seenAliases.computeIfAbsent(alias, key -> {\n+            boolean matched = matchAllAliases;\n+            for (int i = 0; i < patterns.length; i++) {\n+                if (include[i]) {\n+                    if (matched == false) {\n+                        String pattern = patterns[i];\n+                        matched = ALL.equals(pattern) || Regex.simpleMatch(pattern, key);\n+                    }\n+                } else if (matched) {\n+                    matched = Regex.simpleMatch(patterns[i], key) == false;\n+                }\n+            }\n+\n+            return matched;\n+        });\n+\n         for (String index : possibleMatches) {\n             List<AliasInfo> filteredValues = new ArrayList<>();\n \n             List<? extends AliasInfo> entities = getter.get(index);\n             for (AliasInfo aliasInfo : entities) {\n-                boolean matched = matchAllAliases;\n-                String alias = aliasInfo.getAlias();\n-                for (int i = 0; i < patterns.length; i++) {\n-                    if (include[i]) {\n-                        if (matched == false) {\n-                            String pattern = patterns[i];\n-                            matched = ALL.equals(pattern) || Regex.simpleMatch(pattern, alias);\n-                        }\n-                    } else if (matched) {\n-                        matched = Regex.simpleMatch(patterns[i], alias) == false;\n-                    }\n-                }\n-                if (matched) {\n+                if (matcher.test(aliasInfo.getAlias())) {\n                     filteredValues.add(aliasInfo);\n                 }\n             }\nserver/src/test/java/org/elasticsearch/cluster/metadata/MetadataTests.java\n@@ -112,6 +112,14 @@ public void testFindAliases() {\n                     .putAlias(AliasMetadata.builder(\"alias1\").build())\n                     .putAlias(AliasMetadata.builder(\"alias2\").build())\n             )\n+            .put(\n+                IndexMetadata.builder(\"index2\")\n+                    .settings(Settings.builder().put(IndexMetadata.SETTING_VERSION_CREATED, IndexVersion.current()))\n+                    .numberOfShards(1)\n+                    .numberOfReplicas(0)\n+                    .putAlias(AliasMetadata.builder(\"alias2\").build())\n+                    .putAlias(AliasMetadata.builder(\"alias3\").build())\n+            )\n             .build();\n \n         {\n@@ -135,10 +143,12 @@ public void testFindAliases() {\n         }\n         {\n             GetAliasesRequest request = new GetAliasesRequest(\"alias*\");\n-            Map<String, List<AliasMetadata>> aliases = metadata.findAliases(request.aliases(), new String[] { \"index\" });\n-            assertThat(aliases, aMapWithSize(1));\n-            List<AliasMetadata> aliasMetadataList = aliases.get(\"index\");\n-            assertThat(aliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias1\", \"alias2\")));\n+            Map<String, List<AliasMetadata>> aliases = metadata.findAliases(request.aliases(), new String[] { \"index\", \"index2\" });\n+            assertThat(aliases, aMapWithSize(2));\n+            List<AliasMetadata> indexAliasMetadataList = aliases.get(\"index\");\n+            assertThat(indexAliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias1\", \"alias2\")));\n+            List<AliasMetadata> index2AliasMetadataList = aliases.get(\"index2\");\n+            assertThat(index2AliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias2\", \"alias3\")));\n         }\n         {\n             GetAliasesRequest request = new GetAliasesRequest(\"alias1\");",
      "diff": [
        {
          "file": "server/src/main/java/org/elasticsearch/cluster/metadata/Metadata.java",
          "patch": "@@ -855,7 +855,7 @@ public Map<String, List<DataStreamAlias>> findDataStreamAliases(final String[] a\n      *\n      * @param aliases The aliases to look for. Might contain include or exclude wildcards.\n      * @param possibleMatches The data streams or indices that the aliases must point to in order to be returned\n-     * @param getter A function that is used to get the alises for a given data stream or index\n+     * @param getter A function that is used to get the aliases for a given data stream or index\n      * @param setter A function that is used to keep track of the found aliases\n      */\n     private void findAliasInfo(final String[] aliases, final String[] possibleMatches, AliasInfoGetter getter, AliasInfoSetter setter) {\n@@ -881,24 +881,30 @@ private void findAliasInfo(final String[] aliases, final String[] possibleMatche\n \n         boolean matchAllAliases = patterns.length == 0;\n \n+        // memoize pattern match against aliases to avoid repeatedly matching when multiple indices share an alias\n+        HashMap<String, Boolean> seenAliases = new HashMap<>();\n+        Predicate<String> matcher = alias -> seenAliases.computeIfAbsent(alias, key -> {\n+            boolean matched = matchAllAliases;\n+            for (int i = 0; i < patterns.length; i++) {\n+                if (include[i]) {\n+                    if (matched == false) {\n+                        String pattern = patterns[i];\n+                        matched = ALL.equals(pattern) || Regex.simpleMatch(pattern, key);\n+                    }\n+                } else if (matched) {\n+                    matched = Regex.simpleMatch(patterns[i], key) == false;\n+                }\n+            }\n+\n+            return matched;\n+        });\n+\n         for (String index : possibleMatches) {\n             List<AliasInfo> filteredValues = new ArrayList<>();\n \n             List<? extends AliasInfo> entities = getter.get(index);\n             for (AliasInfo aliasInfo : entities) {\n-                boolean matched = matchAllAliases;\n-                String alias = aliasInfo.getAlias();\n-                for (int i = 0; i < patterns.length; i++) {\n-                    if (include[i]) {\n-                        if (matched == false) {\n-                            String pattern = patterns[i];\n-                            matched = ALL.equals(pattern) || Regex.simpleMatch(pattern, alias);\n-                        }\n-                    } else if (matched) {\n-                        matched = Regex.simpleMatch(patterns[i], alias) == false;\n-                    }\n-                }\n-                if (matched) {\n+                if (matcher.test(aliasInfo.getAlias())) {\n                     filteredValues.add(aliasInfo);\n                 }\n             }"
        },
        {
          "file": "server/src/test/java/org/elasticsearch/cluster/metadata/MetadataTests.java",
          "patch": "@@ -112,6 +112,14 @@ public void testFindAliases() {\n                     .putAlias(AliasMetadata.builder(\"alias1\").build())\n                     .putAlias(AliasMetadata.builder(\"alias2\").build())\n             )\n+            .put(\n+                IndexMetadata.builder(\"index2\")\n+                    .settings(Settings.builder().put(IndexMetadata.SETTING_VERSION_CREATED, IndexVersion.current()))\n+                    .numberOfShards(1)\n+                    .numberOfReplicas(0)\n+                    .putAlias(AliasMetadata.builder(\"alias2\").build())\n+                    .putAlias(AliasMetadata.builder(\"alias3\").build())\n+            )\n             .build();\n \n         {\n@@ -135,10 +143,12 @@ public void testFindAliases() {\n         }\n         {\n             GetAliasesRequest request = new GetAliasesRequest(\"alias*\");\n-            Map<String, List<AliasMetadata>> aliases = metadata.findAliases(request.aliases(), new String[] { \"index\" });\n-            assertThat(aliases, aMapWithSize(1));\n-            List<AliasMetadata> aliasMetadataList = aliases.get(\"index\");\n-            assertThat(aliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias1\", \"alias2\")));\n+            Map<String, List<AliasMetadata>> aliases = metadata.findAliases(request.aliases(), new String[] { \"index\", \"index2\" });\n+            assertThat(aliases, aMapWithSize(2));\n+            List<AliasMetadata> indexAliasMetadataList = aliases.get(\"index\");\n+            assertThat(indexAliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias1\", \"alias2\")));\n+            List<AliasMetadata> index2AliasMetadataList = aliases.get(\"index2\");\n+            assertThat(index2AliasMetadataList, transformedItemsMatch(AliasMetadata::alias, contains(\"alias2\", \"alias3\")));\n         }\n         {\n             GetAliasesRequest request = new GetAliasesRequest(\"alias1\");"
        }
      ]
    },
    {
      "type": "comment",
      "id": 2379606386,
      "created_at": "2024-09-27T15:59:04Z",
      "user": "bcully",
      "body": "Added a basic test of multiple indices sharing an alias."
    },
    {
      "type": "review",
      "id": 2334144762,
      "created_at": "2024-09-27T16:12:53Z",
      "user": "henningandersen",
      "state": "APPROVED",
      "body": "LGTM."
    },
    {
      "type": "review",
      "id": 2334508840,
      "created_at": "2024-09-27T19:39:22Z",
      "user": "ankikuma",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review_comment",
      "id": 1779075381,
      "created_at": "2024-09-27T19:39:22Z",
      "user": "ankikuma",
      "body": "Are we at all worried that this HashMap could get very large ?",
      "path": "server/src/main/java/org/elasticsearch/cluster/metadata/Metadata.java",
      "diff_hunk": "@@ -881,24 +881,30 @@ private void findAliasInfo(final String[] aliases, final String[] possibleMatche\n \n         boolean matchAllAliases = patterns.length == 0;\n \n+        // memoize pattern match against aliases to avoid repeatedly matching when multiple indices share an alias\n+        HashMap<String, Boolean> seenAliases = new HashMap<>();",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 885,
      "original_line": 885,
      "side": "RIGHT",
      "original_position": 14,
      "position": 14,
      "subject_type": "line",
      "reply": [
        {
          "id": 1779210188,
          "user": "bcully",
          "body": "It's not impossible, but the caller of this function is already building and [returning a map](https://github.com/elastic/elasticsearch/blob/57846444cc185f3a9a34c068ed566eb462eb565f/server/src/main/java/org/elasticsearch/cluster/metadata/Metadata.java#L811) that is roughly the same size (number of keys up to the sum of aliases across all concrete indices, with keys being pointers to existing strings and values being new boxes). So ~2x transient usage? The entries themselves are pretty small (pointer, boxed bool) so you'd need a lot to matter in absolute terms I think.\r\n\r\nHaving said that I don't know whether we have any limits or typical upper bounds on the number of aliases in a cluster. I also don't know whether we have any limits on the number of concurrent GetAliases calls that we admit. Either of those could I suppose make the memory usage of this map matter, though my guess is that we'd be in a somewhat pathological environment at this point, where we were having other problems not related to the size of this allocation.\r\n\r\nI suppose if we needed to we could bound this thing as some sort of LRU but I would guess that we'd have to then know how to size it, which depends probably on answers to the questions in the paragraph above about total number of aliases and max concurrency.",
          "created_at": "2024-09-27T21:51:54Z"
        }
      ]
    },
    {
      "type": "comment",
      "id": 2379962690,
      "created_at": "2024-09-27T19:57:45Z",
      "user": "ankikuma",
      "body": "Perhaps we can add a test to testFindAliasWithExclusion() as well just to see if it works for the exclusion case as well. "
    },
    {
      "type": "review",
      "id": 2334704053,
      "created_at": "2024-09-27T21:51:54Z",
      "user": "bcully",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 2389462152,
      "created_at": "2024-10-02T18:53:03Z",
      "user": "bcully",
      "body": "> Perhaps we can add a test to testFindAliasWithExclusion() as well just to see if it works for the exclusion case as well.\r\n\r\nAdded in https://github.com/elastic/elasticsearch/pull/113901"
    }
  ]
}