{
  "repo": "bazelbuild/bazel",
  "number": 21213,
  "pr_number": 21213,
  "title": "Account for cache type for uploads in AsyncTaskCache",
  "user": "DolceTriade",
  "created_at": "2024-02-06T06:34:11Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [
    "team-Remote-Exec",
    "awaiting-user-response"
  ],
  "base_commit": "f9243382bb21485d5dce6a0c80f3bdcb225cb70a",
  "merge_commit_sha": "b469ae41c1523a19ff29f463b0130546a0350183",
  "commits": 7,
  "issue_events": [
    "review_requested",
    "labeled",
    "labeled",
    "head_ref_force_pushed",
    "head_ref_force_pushed",
    "head_ref_force_pushed",
    "mentioned",
    "subscribed",
    "unlabeled",
    "labeled",
    "mentioned",
    "subscribed",
    "head_ref_force_pushed",
    "closed",
    "reopened",
    "labeled",
    "review_requested",
    "closed",
    "unlabeled",
    "referenced",
    "head_ref_deleted",
    "referenced",
    "referenced",
    "referenced",
    "referenced",
    "referenced",
    "referenced"
  ],
  "url": "https://github.com/bazelbuild/bazel/pull/21213",
  "timeline": [
    {
      "type": "description",
      "created_at": "2024-02-06T06:34:11Z",
      "user": "DolceTriade",
      "body": "Currently, the AsyncTaskCache for remote execution uses the digest as a key to determine whether or not a task has already been run. Normally this works if there is only a single cache in use. However, when using a disk cache and a remote cache, some calls try to upload files using only a single cache through one code path and another cache through another code path.\r\n\r\nThis is problematic if only disk caching is used through one code path, when remote caching is enabled though another code path (ie, uploading BES referenced artifacts), the files are not uploaded to the remote cache since the digest is the same. This change uses a String of the digest and the cache type as the key rather than just the digest to allow for uploading to both caches.\r\n\r\nThe download path is not affected by this bug, so we don't need to make any changes there.\r\n\r\nAlso, update the combined cache test to reflect what should happen:\r\nwhen remote_build_event_upload=all, *all* files referenced in the BES\r\nstream are uploaded, including build artifacts (this test now functions as intended based on documentation due to this change).\r\n\r\n\r\nFixes #20962, #20296"
    },
    {
      "type": "review",
      "id": 1866311021,
      "created_at": "2024-02-06T21:00:31Z",
      "user": "tjgq",
      "state": "CHANGES_REQUESTED",
      "body": "I'm not sure this is the right fix. The properties we are looking for are:\r\n\r\n1. If we do a combined (disk+remote) upload, we want a future disk-only or remote-only upload for the same digest to be a no-op.\r\n2. If we do a disk-only or remote-only upload, we want a future combined upload to only do the missing half.\r\n\r\nIf I understand this PR correctly:\r\n\r\n1. A combined upload would insert `(ANY, X)`. A subsequent disk-only upload would insert `(DISK, X)` (which would not be a no-op, as it's a distinct key) and cause a duplicate upload to the disk cache for that digest.\r\n2. A disk-only upload would insert `(DISK, X)`. A future combined upload would insert `(ANY, X)`, which would correctly cause an upload to the remote cache to occur, but also cause a duplicate upload to the disk cache.\r\n\r\n(And similarly for their remote-only counterparts.)\r\n\r\nSo I think what we actually need to do is to use only `(DISK, X)` and `(REMOTE, X)` keys, but have the combined path insert both keys, allowing for each one to be independently deduped against a previous insertion. Or - maybe even better - have two separate `AsyncTaskCaches`, keyed by `Digest` and managed by the respective `RemoteCacheClient` implementation, and have the combined client delegate to both."
    },
    {
      "type": "comment",
      "id": 1930830502,
      "created_at": "2024-02-06T22:05:24Z",
      "user": "DolceTriade",
      "body": "Hm... I think to do this without any hacks, we would need to move AsyncTaskCache into each respective Cache implementation.\r\n\r\nSince the RemoteCache has no knowledge about whether the cacheProtocol is a combined Disk and Remote cache or just a single remote cache, we would have to unconditionally populate the AsyncTaskCache with a null value...\r\n\r\nSomething like (naming tbd):\r\n```\r\nListenableFuture<Void> cacheUpload(context, digest, func, force) {\r\n  switch context.getWriteCachePolicy() {\r\n    case REMOTE_CACHE_ONLY:\r\n      casUploadCache.execute(digest + \"_REMOTE\",\r\n            RxFutures.toCompletable(\r\n                () -> func(), directExecutor()),\r\n            force);\r\n     case DISK_CACHE_ONLY:\r\n      return casUploadCache.execute(digest + \"_DISK\",\r\n            RxFutures.toCompletable(\r\n                () -> func(), directExecutor()),\r\n            force);\r\n\r\n     case ANY_CACHE:\r\n      casUploadCache.execute(digest + \"_DISK\",\r\n            RxFutures.toCompletable(\r\n                () -> {}),\r\n            force);\r\n      return casUploadCache.execute(digest + \"_REMOTE\",\r\n            RxFutures.toCompletable(\r\n                () -> func(), directExecutor()),\r\n            force);\r\n  }\r\n}\r\n```"
    },
    {
      "type": "comment",
      "id": 1930927083,
      "created_at": "2024-02-06T23:13:53Z",
      "user": "tjgq",
      "body": "> Hm... I think to do this without any hacks, we would need to move AsyncTaskCache into each respective Cache implementation.\r\n\r\nYes, I think that's probably the best architecture: push the `AsyncTaskCache` down into the `RemoteCacheClient` implementations. `DiskCacheClient` and `{Http,Grpc}CacheClient` each maintain an `AsyncTaskCache` of their own. `DiskAndRemoteCacheClient` doesn't maintain an `AsyncTaskCache`, instead it delegates to the other two clients (it can unconditionally call both, since each client is able to deduplicate for itself)."
    },
    {
      "type": "comment",
      "id": 1931838698,
      "created_at": "2024-02-07T11:24:15Z",
      "user": "DolceTriade",
      "body": "@tjgq What do you think of this approach? Tests are broken, but I'll fix them if you think this is the right direction.\r\n\r\nWith this change, I cannot reproduce missing logs."
    },
    {
      "type": "review_comment",
      "id": 1482721587,
      "created_at": "2024-02-08T10:11:54Z",
      "user": "tjgq",
      "body": "Suggestion: instead of requiring every client to provide two overloads of each method, add default methods for the \"no force argument\" overload here, so that clients only need to implement the other overload.\r\n\r\n(It's also not super clear to me why we need `force = true` at all; the only usage I could find outside of tests is in RemoteRepositoryRemoteExecutor.java, and code history doesn't offer an explanation. But let's not deal with it in this PR - I'll investigate it separately.)",
      "path": "src/main/java/com/google/devtools/build/lib/remote/common/RemoteCacheClient.java",
      "diff_hunk": "@@ -143,6 +143,18 @@ ListenableFuture<Void> downloadBlob(\n    */\n   ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file);\n ",
      "start_line": 147,
      "original_start_line": 144,
      "start_side": "RIGHT",
      "line": 148,
      "original_line": 148,
      "side": "RIGHT",
      "original_position": 3,
      "position": 3,
      "subject_type": "line",
      "reply": [
        {
          "id": 1523831721,
          "user": "DolceTriade",
          "body": "I didn't want to deal with the change of making RemoteCacheClient an abstract class. I feel that can be a follow up change.",
          "created_at": "2024-03-13T19:46:55Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 1869745891,
      "created_at": "2024-02-08T10:19:05Z",
      "user": "tjgq",
      "state": "CHANGES_REQUESTED",
      "body": "Yep, this is what I had in mind. I left one suggestion to avoid code duplication; I'll do a more thorough review once the tests are fixed, but architecturally it looks good to me."
    },
    {
      "type": "comment",
      "id": 1991846185,
      "created_at": "2024-03-12T14:55:41Z",
      "user": "tjgq",
      "body": "@DolceTriade Are you still working on this? We're still interested in getting it merged in.\r\n\r\nFWIW, I think the CI failures are flaky and should have been fixed in 3cccb61183c4bb946a814f9d55c1dcd026bed2fa. Let me know if there's anything else you're unsure about."
    },
    {
      "type": "comment",
      "id": 1992121787,
      "created_at": "2024-03-12T16:52:50Z",
      "user": "DolceTriade",
      "body": "Sorry, I'm still trying to figure out how to write proper tests for this and got side tracked"
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:01:14Z",
      "sha": "a6478110e280774ffcedc1ef863fc2a5038f135d",
      "message": "Account for cache type for uploads in AsyncTaskCache\n\nCurrently, the AsyncTaskCache for remote execution uses the digest as a\nkey to determine whether or not a task has already been run. Normally\nthis works if there is only a single cache in use. However, when using a\ndisk cache and a remote cache, some calls try to upload files using only\na single cache through one code path and another cache through another\ncode path.\n\nThis is problematic if only disk caching is used through one code path,\nwhen remote caching is enabled though another code path (ie, uploading\nBES referenced artifacts), the files are not uploaded to the remote\ncache since the digest is the same. This change uses a String of the\ndigest and the cache type as the key rather than just the digest to\nallow for uploading to both caches.\n\nUpdate the combined cache test to reflect what should happen:\nwhen remote_build_event_upload=all, *all* files referenced in the BES\nstream are uploaded, including build artifacts.\n\nFixes #20962, #20296\n\nSigned-off-by: Harsh Modi <hmodi@aviatrix.com>",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-06T06:29:10Z",
      "author_date": "2024-02-06T06:29:10Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:01:14Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/RemoteCache.java\n@@ -83,7 +83,7 @@ public class RemoteCache extends AbstractReferenceCounted {\n   private static final ListenableFuture<byte[]> EMPTY_BYTES = immediateFuture(new byte[0]);\n \n   private final CountDownLatch closeCountDownLatch = new CountDownLatch(1);\n-  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n \n   protected final RemoteCacheClient cacheProtocol;\n   protected final RemoteOptions options;\n@@ -162,7 +162,7 @@ protected ListenableFuture<Void> uploadFile(\n \n     Completable upload =\n         casUploadCache.execute(\n-            digest,\n+            digest + context.getWriteCachePolicy().toString(),\n             RxFutures.toCompletable(\n                 () -> cacheProtocol.uploadFile(context, digest, file), directExecutor()),\n             force);\n@@ -193,7 +193,7 @@ protected ListenableFuture<Void> uploadBlob(\n \n     Completable upload =\n         casUploadCache.execute(\n-            digest,\n+            digest + context.getWriteCachePolicy().toString(),\n             RxFutures.toCompletable(\n                 () -> cacheProtocol.uploadBlob(context, digest, data), directExecutor()),\n             force);\nsrc/main/java/com/google/devtools/build/lib/remote/RemoteExecutionCache.java\n@@ -196,7 +196,7 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n           uploadTask.completion = Completable.fromObservable(completion);\n           Completable upload =\n               casUploadCache.execute(\n-                  digest,\n+                  digest.getHash() + context.getWriteCachePolicy().toString(),\n                   Single.<Boolean>create(\n                           continuation -> {\n                             uploadTask.continuation = continuation;\nsrc/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java\n@@ -626,7 +626,8 @@ public void shutdownNow_cancelInProgressUploads() throws Exception {\n \n     ListenableFuture<Void> upload =\n         remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(digest);\n+    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(\n+        digest + remoteActionExecutionContext.getWriteCachePolicy().toString());\n     remoteCache.shutdownNow();\n \n     assertThat(upload.isCancelled()).isTrue();\nsrc/test/shell/bazel/remote/remote_build_event_uploader_test.sh\n@@ -270,10 +270,10 @@ EOF\n       --build_event_json_file=$BEP_JSON \\\n       //a:foo >& $TEST_log || fail \"Failed to build\"\n \n-  expect_bes_file_not_uploaded foo.txt\n+  expect_bes_file_uploaded foo.txt\n   expect_bes_file_uploaded command.profile.gz\n   remote_cas_files=\"$(count_remote_cas_files)\"\n-  [[ \"$remote_cas_files\" == 1 ]] || fail \"Expected 1 remote cas entries, not $remote_cas_files\"\n+  [[ \"$remote_cas_files\" == 2 ]] || fail \"Expected 2 remote cas entries, not $remote_cas_files\"\n   disk_cas_files=\"$(count_disk_cas_files $cache_dir)\"\n   # foo.txt, stdout and stderr for action 'foo'\n   [[ \"$disk_cas_files\" == 3 ]] || fail \"Expected 3 disk cas entries, not $disk_cas_files\"",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/RemoteCache.java",
          "patch": "@@ -83,7 +83,7 @@ public class RemoteCache extends AbstractReferenceCounted {\n   private static final ListenableFuture<byte[]> EMPTY_BYTES = immediateFuture(new byte[0]);\n \n   private final CountDownLatch closeCountDownLatch = new CountDownLatch(1);\n-  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n \n   protected final RemoteCacheClient cacheProtocol;\n   protected final RemoteOptions options;\n@@ -162,7 +162,7 @@ protected ListenableFuture<Void> uploadFile(\n \n     Completable upload =\n         casUploadCache.execute(\n-            digest,\n+            digest + context.getWriteCachePolicy().toString(),\n             RxFutures.toCompletable(\n                 () -> cacheProtocol.uploadFile(context, digest, file), directExecutor()),\n             force);\n@@ -193,7 +193,7 @@ protected ListenableFuture<Void> uploadBlob(\n \n     Completable upload =\n         casUploadCache.execute(\n-            digest,\n+            digest + context.getWriteCachePolicy().toString(),\n             RxFutures.toCompletable(\n                 () -> cacheProtocol.uploadBlob(context, digest, data), directExecutor()),\n             force);"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/RemoteExecutionCache.java",
          "patch": "@@ -196,7 +196,7 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n           uploadTask.completion = Completable.fromObservable(completion);\n           Completable upload =\n               casUploadCache.execute(\n-                  digest,\n+                  digest.getHash() + context.getWriteCachePolicy().toString(),\n                   Single.<Boolean>create(\n                           continuation -> {\n                             uploadTask.continuation = continuation;"
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java",
          "patch": "@@ -626,7 +626,8 @@ public void shutdownNow_cancelInProgressUploads() throws Exception {\n \n     ListenableFuture<Void> upload =\n         remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(digest);\n+    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(\n+        digest + remoteActionExecutionContext.getWriteCachePolicy().toString());\n     remoteCache.shutdownNow();\n \n     assertThat(upload.isCancelled()).isTrue();"
        },
        {
          "file": "src/test/shell/bazel/remote/remote_build_event_uploader_test.sh",
          "patch": "@@ -270,10 +270,10 @@ EOF\n       --build_event_json_file=$BEP_JSON \\\n       //a:foo >& $TEST_log || fail \"Failed to build\"\n \n-  expect_bes_file_not_uploaded foo.txt\n+  expect_bes_file_uploaded foo.txt\n   expect_bes_file_uploaded command.profile.gz\n   remote_cas_files=\"$(count_remote_cas_files)\"\n-  [[ \"$remote_cas_files\" == 1 ]] || fail \"Expected 1 remote cas entries, not $remote_cas_files\"\n+  [[ \"$remote_cas_files\" == 2 ]] || fail \"Expected 2 remote cas entries, not $remote_cas_files\"\n   disk_cas_files=\"$(count_disk_cas_files $cache_dir)\"\n   # foo.txt, stdout and stderr for action 'foo'\n   [[ \"$disk_cas_files\" == 3 ]] || fail \"Expected 3 disk cas entries, not $disk_cas_files\""
        }
      ]
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:02:36Z",
      "sha": "d5d604f199a6c53f9cd5e04c43b6125dc65b35e5",
      "message": "migrate disk cache client and combined client",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-07T09:09:02Z",
      "author_date": "2024-02-07T09:09:02Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:02:36Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/common/RemoteCacheClient.java\n@@ -146,6 +146,18 @@ ListenableFuture<Void> downloadBlob(\n    */\n   ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file);\n \n+  /**\n+   * Uploads a {@code file} to the CAS.\n+   *\n+   * @param context the context for the action.\n+   * @param digest The digest of the file.\n+   * @param file The file to upload.\n+   * @param force Whether to force the upload, even if an upload was already attempted.\n+   * @return A future representing pending completion of the upload.\n+   */\n+  ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n+\n+\n   /**\n    * Uploads a BLOB to the CAS.\n    *\n@@ -157,6 +169,18 @@ ListenableFuture<Void> downloadBlob(\n   ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data);\n \n+  /**\n+   * Uploads a BLOB to the CAS.\n+   *\n+   * @param context the context for the action.\n+   * @param digest The digest of the blob.\n+   * @param data The BLOB to upload.\n+   * @param force Whether to force the upload, even if an upload was already attempted.\n+   * @return A future representing pending completion of the upload.\n+   */\n+  ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);      \n+\n   /** Close resources associated with the remote cache. */\n   void close();\n }\nsrc/main/java/com/google/devtools/build/lib/remote/disk/BUILD\n@@ -23,6 +23,7 @@ java_library(\n         \"//src/main/java/com/google/devtools/build/lib/vfs\",\n         \"//third_party:guava\",\n         \"//third_party:jsr305\",\n+        \"//third_party:rxjava3\",\n         \"//third_party/protobuf:protobuf_java\",\n         \"@remoteapis//:build_bazel_remote_execution_v2_remote_execution_java_proto\",\n     ],\nsrc/main/java/com/google/devtools/build/lib/remote/disk/DiskAndRemoteCacheClient.java\n@@ -74,36 +74,48 @@ public void close() {\n     remoteCache.close();\n   }\n \n+  @Override\n+    public ListenableFuture<Void> uploadFile(\n+        RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /*force=*/false);\n+  }  \n+\n   @Override\n   public ListenableFuture<Void> uploadFile(\n-      RemoteActionExecutionContext context, Digest digest, Path file) {\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     ListenableFuture<Void> future = Futures.immediateVoidFuture();\n \n     if (context.getWriteCachePolicy().allowDiskCache()) {\n-      future = diskCache.uploadFile(context, digest, file);\n+      future = diskCache.uploadFile(context, digest, file, force);\n     }\n \n     if (context.getWriteCachePolicy().allowRemoteCache()) {\n       future =\n           Futures.transformAsync(\n-              future, v -> remoteCache.uploadFile(context, digest, file), directExecutor());\n+              future, v -> remoteCache.uploadFile(context, digest, file, force), directExecutor());\n     }\n     return future;\n   }\n \n+  \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /*force=*/false);\n+  }\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     ListenableFuture<Void> future = Futures.immediateVoidFuture();\n \n     if (context.getWriteCachePolicy().allowDiskCache()) {\n-      future = diskCache.uploadBlob(context, digest, data);\n+      future = diskCache.uploadBlob(context, digest, data, force);\n     }\n \n     if (context.getWriteCachePolicy().allowRemoteCache()) {\n       future =\n           Futures.transformAsync(\n-              future, v -> remoteCache.uploadBlob(context, digest, data), directExecutor());\n+              future, v -> remoteCache.uploadBlob(context, digest, data, force), directExecutor());\n     }\n     return future;\n   }\nsrc/main/java/com/google/devtools/build/lib/remote/disk/DiskCacheClient.java\n@@ -36,12 +36,15 @@\n import com.google.devtools.build.lib.remote.common.CacheNotFoundException;\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.vfs.Path;\n import com.google.protobuf.ByteString;\n import com.google.protobuf.ExtensionRegistryLite;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.FileNotFoundException;\n import java.io.FileOutputStream;\n import java.io.IOException;\n@@ -72,6 +75,8 @@ public class DiskCacheClient implements RemoteCacheClient {\n   private final boolean verifyDownloads;\n   private final DigestUtil digestUtil;\n \n+  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   /**\n    * @param verifyDownloads whether verify the digest of downloaded content are the same as the\n    *     digest used to index that file.\n@@ -276,28 +281,71 @@ public ListenableFuture<Void> uploadActionResult(\n   @Override\n   public void close() {}\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n+  private ListenableFuture<Void> uploadInternal(\n+      RemoteActionExecutionContext context,\n+      Digest digest,\n+      Store store,\n+      InputStream in,\n+      boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            digest,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  try {\n+                    saveFile(digest, store, in);\n+                    return immediateFuture(null);\n+                  } catch (IOException e) {\n+                    return Futures.immediateFailedFuture(e);\n+                  }\n+                },\n+                executorService),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n+  }\n+\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n-    return executorService.submit(\n-        () -> {\n-          try (InputStream in = file.getInputStream()) {\n-            saveFile(digest, Store.CAS, in);\n-          }\n-          return null;\n-        });\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n+    try (InputStream in = file.getInputStream()) {\n+      return uploadInternal(context, digest, Store.CAS, in, force);\n+    } catch (IOException e) {\n+      return Futures.immediateFailedFuture(e);\n+    }\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return executorService.submit(\n-        () -> {\n-          try (InputStream in = data.newInput()) {\n-            saveFile(digest, Store.CAS, in);\n-          }\n-          return null;\n-        });\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n+    try (InputStream in = data.newInput()) {\n+      return uploadInternal(context, digest, Store.CAS, in, force);\n+    } catch (IOException e) {\n+      return Futures.immediateFailedFuture(e);\n+    }\n   }\n \n   @Override",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/common/RemoteCacheClient.java",
          "patch": "@@ -146,6 +146,18 @@ ListenableFuture<Void> downloadBlob(\n    */\n   ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file);\n \n+  /**\n+   * Uploads a {@code file} to the CAS.\n+   *\n+   * @param context the context for the action.\n+   * @param digest The digest of the file.\n+   * @param file The file to upload.\n+   * @param force Whether to force the upload, even if an upload was already attempted.\n+   * @return A future representing pending completion of the upload.\n+   */\n+  ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n+\n+\n   /**\n    * Uploads a BLOB to the CAS.\n    *\n@@ -157,6 +169,18 @@ ListenableFuture<Void> downloadBlob(\n   ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data);\n \n+  /**\n+   * Uploads a BLOB to the CAS.\n+   *\n+   * @param context the context for the action.\n+   * @param digest The digest of the blob.\n+   * @param data The BLOB to upload.\n+   * @param force Whether to force the upload, even if an upload was already attempted.\n+   * @return A future representing pending completion of the upload.\n+   */\n+  ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);      \n+\n   /** Close resources associated with the remote cache. */\n   void close();\n }"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/disk/BUILD",
          "patch": "@@ -23,6 +23,7 @@ java_library(\n         \"//src/main/java/com/google/devtools/build/lib/vfs\",\n         \"//third_party:guava\",\n         \"//third_party:jsr305\",\n+        \"//third_party:rxjava3\",\n         \"//third_party/protobuf:protobuf_java\",\n         \"@remoteapis//:build_bazel_remote_execution_v2_remote_execution_java_proto\",\n     ],"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/disk/DiskAndRemoteCacheClient.java",
          "patch": "@@ -74,36 +74,48 @@ public void close() {\n     remoteCache.close();\n   }\n \n+  @Override\n+    public ListenableFuture<Void> uploadFile(\n+        RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /*force=*/false);\n+  }  \n+\n   @Override\n   public ListenableFuture<Void> uploadFile(\n-      RemoteActionExecutionContext context, Digest digest, Path file) {\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     ListenableFuture<Void> future = Futures.immediateVoidFuture();\n \n     if (context.getWriteCachePolicy().allowDiskCache()) {\n-      future = diskCache.uploadFile(context, digest, file);\n+      future = diskCache.uploadFile(context, digest, file, force);\n     }\n \n     if (context.getWriteCachePolicy().allowRemoteCache()) {\n       future =\n           Futures.transformAsync(\n-              future, v -> remoteCache.uploadFile(context, digest, file), directExecutor());\n+              future, v -> remoteCache.uploadFile(context, digest, file, force), directExecutor());\n     }\n     return future;\n   }\n \n+  \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /*force=*/false);\n+  }\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     ListenableFuture<Void> future = Futures.immediateVoidFuture();\n \n     if (context.getWriteCachePolicy().allowDiskCache()) {\n-      future = diskCache.uploadBlob(context, digest, data);\n+      future = diskCache.uploadBlob(context, digest, data, force);\n     }\n \n     if (context.getWriteCachePolicy().allowRemoteCache()) {\n       future =\n           Futures.transformAsync(\n-              future, v -> remoteCache.uploadBlob(context, digest, data), directExecutor());\n+              future, v -> remoteCache.uploadBlob(context, digest, data, force), directExecutor());\n     }\n     return future;\n   }"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/disk/DiskCacheClient.java",
          "patch": "@@ -36,12 +36,15 @@\n import com.google.devtools.build.lib.remote.common.CacheNotFoundException;\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.vfs.Path;\n import com.google.protobuf.ByteString;\n import com.google.protobuf.ExtensionRegistryLite;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.FileNotFoundException;\n import java.io.FileOutputStream;\n import java.io.IOException;\n@@ -72,6 +75,8 @@ public class DiskCacheClient implements RemoteCacheClient {\n   private final boolean verifyDownloads;\n   private final DigestUtil digestUtil;\n \n+  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   /**\n    * @param verifyDownloads whether verify the digest of downloaded content are the same as the\n    *     digest used to index that file.\n@@ -276,28 +281,71 @@ public ListenableFuture<Void> uploadActionResult(\n   @Override\n   public void close() {}\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n+  private ListenableFuture<Void> uploadInternal(\n+      RemoteActionExecutionContext context,\n+      Digest digest,\n+      Store store,\n+      InputStream in,\n+      boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            digest,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  try {\n+                    saveFile(digest, store, in);\n+                    return immediateFuture(null);\n+                  } catch (IOException e) {\n+                    return Futures.immediateFailedFuture(e);\n+                  }\n+                },\n+                executorService),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n+  }\n+\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n-    return executorService.submit(\n-        () -> {\n-          try (InputStream in = file.getInputStream()) {\n-            saveFile(digest, Store.CAS, in);\n-          }\n-          return null;\n-        });\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n+    try (InputStream in = file.getInputStream()) {\n+      return uploadInternal(context, digest, Store.CAS, in, force);\n+    } catch (IOException e) {\n+      return Futures.immediateFailedFuture(e);\n+    }\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return executorService.submit(\n-        () -> {\n-          try (InputStream in = data.newInput()) {\n-            saveFile(digest, Store.CAS, in);\n-          }\n-          return null;\n-        });\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n+    try (InputStream in = data.newInput()) {\n+      return uploadInternal(context, digest, Store.CAS, in, force);\n+    } catch (IOException e) {\n+      return Futures.immediateFailedFuture(e);\n+    }\n   }\n \n   @Override"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:03:20Z",
      "sha": "c49f89e7e80431e34471dbc8026876ff2aa9ee0a",
      "message": "add http cache client",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-07T09:38:15Z",
      "author_date": "2024-02-07T09:38:15Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:03:20Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/http/BUILD\n@@ -33,6 +33,7 @@ java_library(\n         \"//third_party:jsr305\",\n         \"//third_party:netty\",\n         \"//third_party/protobuf:protobuf_java\",\n+        \"@maven//:io_reactivex_rxjava3_rxjava\",\n         \"@remoteapis//:build_bazel_remote_execution_v2_remote_execution_java_proto\",\n     ],\n )\nsrc/main/java/com/google/devtools/build/lib/remote/http/HttpCacheClient.java\n@@ -13,6 +13,8 @@\n // limitations under the License.\n package com.google.devtools.build.lib.remote.http;\n \n+import static com.google.common.util.concurrent.MoreExecutors.directExecutor;\n+\n import build.bazel.remote.execution.v2.ActionCacheUpdateCapabilities;\n import build.bazel.remote.execution.v2.ActionResult;\n import build.bazel.remote.execution.v2.CacheCapabilities;\n@@ -34,8 +36,10 @@\n import com.google.devtools.build.lib.remote.common.LazyFileInputStream;\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.vfs.Path;\n import com.google.protobuf.ByteString;\n@@ -75,6 +79,7 @@\n import io.netty.handler.timeout.WriteTimeoutException;\n import io.netty.util.concurrent.Future;\n import io.netty.util.concurrent.Promise;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.File;\n import java.io.FileInputStream;\n import java.io.FilterInputStream;\n@@ -155,6 +160,8 @@ public final class HttpCacheClient implements RemoteCacheClient {\n   @GuardedBy(\"credentialsLock\")\n   private long lastRefreshTime;\n \n+  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   public static HttpCacheClient create(\n       URI uri,\n       int timeoutSeconds,\n@@ -636,6 +643,21 @@ public ListenableFuture<CachedActionResult> downloadActionResult(\n         MoreExecutors.directExecutor());\n   }\n \n+  @SuppressWarnings(\"FutureReturnValueIgnored\")\n+  private ListenableFuture<Void> uploadAsyncCached(\n+      String key, long length, InputStream in, boolean casUpload, boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            key,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  return uploadAsync(key, length, in, casUpload);\n+                },\n+                directExecutor()),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n+  }\n+\n   @SuppressWarnings(\"FutureReturnValueIgnored\")\n   private ListenableFuture<Void> uploadAsync(\n       String key, long length, InputStream in, boolean casUpload) {\n@@ -725,22 +747,39 @@ private void uploadAfterCredentialRefresh(UploadCommand upload, SettableFuture<V\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     return retrier.executeAsync(\n         () ->\n-            uploadAsync(\n+            uploadAsyncCached(\n                 digest.getHash(),\n                 digest.getSizeBytes(),\n                 new LazyFileInputStream(file),\n-                /* casUpload= */ true));\n+                /* casUpload= */ true,\n+                /* force */ force));\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     return retrier.executeAsync(\n         () ->\n-            uploadAsync(\n-                digest.getHash(), digest.getSizeBytes(), data.newInput(), /* casUpload= */ true));\n+            uploadAsyncCached(\n+                digest.getHash(),\n+                digest.getSizeBytes(),\n+                data.newInput(),\n+                /* casUpload= */ true,\n+                /* force= */ force));\n   }\n \n   @Override\n@@ -807,6 +846,18 @@ public void close() {\n     }\n   }\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n   private boolean cacheMiss(HttpResponseStatus status) {\n     // Supporting NO_CONTENT for nginx webdav compatibility.\n     return status.equals(HttpResponseStatus.NOT_FOUND)",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/http/BUILD",
          "patch": "@@ -33,6 +33,7 @@ java_library(\n         \"//third_party:jsr305\",\n         \"//third_party:netty\",\n         \"//third_party/protobuf:protobuf_java\",\n+        \"@maven//:io_reactivex_rxjava3_rxjava\",\n         \"@remoteapis//:build_bazel_remote_execution_v2_remote_execution_java_proto\",\n     ],\n )"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/http/HttpCacheClient.java",
          "patch": "@@ -13,6 +13,8 @@\n // limitations under the License.\n package com.google.devtools.build.lib.remote.http;\n \n+import static com.google.common.util.concurrent.MoreExecutors.directExecutor;\n+\n import build.bazel.remote.execution.v2.ActionCacheUpdateCapabilities;\n import build.bazel.remote.execution.v2.ActionResult;\n import build.bazel.remote.execution.v2.CacheCapabilities;\n@@ -34,8 +36,10 @@\n import com.google.devtools.build.lib.remote.common.LazyFileInputStream;\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.vfs.Path;\n import com.google.protobuf.ByteString;\n@@ -75,6 +79,7 @@\n import io.netty.handler.timeout.WriteTimeoutException;\n import io.netty.util.concurrent.Future;\n import io.netty.util.concurrent.Promise;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.File;\n import java.io.FileInputStream;\n import java.io.FilterInputStream;\n@@ -155,6 +160,8 @@ public final class HttpCacheClient implements RemoteCacheClient {\n   @GuardedBy(\"credentialsLock\")\n   private long lastRefreshTime;\n \n+  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   public static HttpCacheClient create(\n       URI uri,\n       int timeoutSeconds,\n@@ -636,6 +643,21 @@ public ListenableFuture<CachedActionResult> downloadActionResult(\n         MoreExecutors.directExecutor());\n   }\n \n+  @SuppressWarnings(\"FutureReturnValueIgnored\")\n+  private ListenableFuture<Void> uploadAsyncCached(\n+      String key, long length, InputStream in, boolean casUpload, boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            key,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  return uploadAsync(key, length, in, casUpload);\n+                },\n+                directExecutor()),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n+  }\n+\n   @SuppressWarnings(\"FutureReturnValueIgnored\")\n   private ListenableFuture<Void> uploadAsync(\n       String key, long length, InputStream in, boolean casUpload) {\n@@ -725,22 +747,39 @@ private void uploadAfterCredentialRefresh(UploadCommand upload, SettableFuture<V\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     return retrier.executeAsync(\n         () ->\n-            uploadAsync(\n+            uploadAsyncCached(\n                 digest.getHash(),\n                 digest.getSizeBytes(),\n                 new LazyFileInputStream(file),\n-                /* casUpload= */ true));\n+                /* casUpload= */ true,\n+                /* force */ force));\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     return retrier.executeAsync(\n         () ->\n-            uploadAsync(\n-                digest.getHash(), digest.getSizeBytes(), data.newInput(), /* casUpload= */ true));\n+            uploadAsyncCached(\n+                digest.getHash(),\n+                digest.getSizeBytes(),\n+                data.newInput(),\n+                /* casUpload= */ true,\n+                /* force= */ force));\n   }\n \n   @Override\n@@ -807,6 +846,18 @@ public void close() {\n     }\n   }\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n   private boolean cacheMiss(HttpResponseStatus status) {\n     // Supporting NO_CONTENT for nginx webdav compatibility.\n     return status.equals(HttpResponseStatus.NOT_FOUND)"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:03:39Z",
      "sha": "85cf6a3a4ceb6e022c7bbac45ca01bd356538947",
      "message": "add grpc",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-07T09:45:42Z",
      "author_date": "2024-02-07T09:45:42Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:03:39Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/GrpcCacheClient.java\n@@ -55,8 +55,10 @@\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.TracingMetadataUtils;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.remote.zstd.ZstdDecompressingOutputStream;\n@@ -68,6 +70,7 @@\n import io.grpc.StatusRuntimeException;\n import io.grpc.stub.ClientCallStreamObserver;\n import io.grpc.stub.ClientResponseObserver;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.IOException;\n import java.io.OutputStream;\n import java.util.ArrayList;\n@@ -95,6 +98,8 @@ public class GrpcCacheClient implements RemoteCacheClient, MissingDigestsFinder\n \n   private AtomicBoolean closed = new AtomicBoolean();\n \n+  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   @VisibleForTesting\n   public GrpcCacheClient(\n       ReferenceCountedChannel channel,\n@@ -492,25 +497,53 @@ private void releaseOut() {\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path path) {\n-    return uploadChunker(\n+    return uploadFile(context, digest, path, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path path, boolean force) {\n+    return uploadChunkerCached(\n         context,\n         digest,\n         Chunker.builder()\n             .setInput(digest.getSizeBytes(), path)\n             .setCompressed(shouldCompress(digest))\n-            .build());\n+            .build(),\n+        force);\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return uploadChunker(\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n+    return uploadChunkerCached(\n         context,\n         digest,\n         Chunker.builder()\n             .setInput(data.toByteArray())\n             .setCompressed(shouldCompress(digest))\n-            .build());\n+            .build(),\n+        force);\n+  }\n+\n+  private ListenableFuture<Void> uploadChunkerCached(\n+      RemoteActionExecutionContext context, Digest digest, Chunker chunker, boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            digest,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  return uploadChunker(context, digest, chunker);\n+                },\n+                MoreExecutors.directExecutor()),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n   }\n \n   ListenableFuture<Void> uploadChunker(",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/GrpcCacheClient.java",
          "patch": "@@ -55,8 +55,10 @@\n import com.google.devtools.build.lib.remote.common.RemoteActionExecutionContext;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestOutputStream;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.TracingMetadataUtils;\n import com.google.devtools.build.lib.remote.util.Utils;\n import com.google.devtools.build.lib.remote.zstd.ZstdDecompressingOutputStream;\n@@ -68,6 +70,7 @@\n import io.grpc.StatusRuntimeException;\n import io.grpc.stub.ClientCallStreamObserver;\n import io.grpc.stub.ClientResponseObserver;\n+import io.reactivex.rxjava3.core.Completable;\n import java.io.IOException;\n import java.io.OutputStream;\n import java.util.ArrayList;\n@@ -95,6 +98,8 @@ public class GrpcCacheClient implements RemoteCacheClient, MissingDigestsFinder\n \n   private AtomicBoolean closed = new AtomicBoolean();\n \n+  protected final AsyncTaskCache.NoResult<Digest> casUploadCache = AsyncTaskCache.NoResult.create();\n+\n   @VisibleForTesting\n   public GrpcCacheClient(\n       ReferenceCountedChannel channel,\n@@ -492,25 +497,53 @@ private void releaseOut() {\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path path) {\n-    return uploadChunker(\n+    return uploadFile(context, digest, path, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path path, boolean force) {\n+    return uploadChunkerCached(\n         context,\n         digest,\n         Chunker.builder()\n             .setInput(digest.getSizeBytes(), path)\n             .setCompressed(shouldCompress(digest))\n-            .build());\n+            .build(),\n+        force);\n   }\n \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return uploadChunker(\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n+    return uploadChunkerCached(\n         context,\n         digest,\n         Chunker.builder()\n             .setInput(data.toByteArray())\n             .setCompressed(shouldCompress(digest))\n-            .build());\n+            .build(),\n+        force);\n+  }\n+\n+  private ListenableFuture<Void> uploadChunkerCached(\n+      RemoteActionExecutionContext context, Digest digest, Chunker chunker, boolean force) {\n+    Completable upload =\n+        casUploadCache.execute(\n+            digest,\n+            RxFutures.toCompletable(\n+                () -> {\n+                  return uploadChunker(context, digest, chunker);\n+                },\n+                MoreExecutors.directExecutor()),\n+            force);\n+    return RxFutures.toListenableFuture(upload);\n   }\n \n   ListenableFuture<Void> uploadChunker("
        }
      ]
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:03:39Z",
      "sha": "c3dfcfc0d00457deae249d518a9f1b83cb2d28b5",
      "message": "plumb through shutdownnow and awaittermination",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-07T11:19:58Z",
      "author_date": "2024-02-07T11:19:58Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:03:39Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/GrpcCacheClient.java\n@@ -181,6 +181,18 @@ public void close() {\n     channel.release();\n   }\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n   /** Returns true if 'options.remoteCache' uses 'grpc' or an empty scheme */\n   public static boolean isRemoteCacheOptions(RemoteOptions options) {\n     if (isNullOrEmpty(options.remoteCache)) {\nsrc/main/java/com/google/devtools/build/lib/remote/RemoteCache.java\n@@ -42,7 +42,6 @@\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient.ActionKey;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient.CachedActionResult;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n-import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.server.FailureDetails.FailureDetail;\n@@ -83,7 +82,6 @@ public class RemoteCache extends AbstractReferenceCounted {\n   private static final ListenableFuture<byte[]> EMPTY_BYTES = immediateFuture(new byte[0]);\n \n   private final CountDownLatch closeCountDownLatch = new CountDownLatch(1);\n-  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n \n   protected final RemoteCacheClient cacheProtocol;\n   protected final RemoteOptions options;\n@@ -160,14 +158,8 @@ protected ListenableFuture<Void> uploadFile(\n       return COMPLETED_SUCCESS;\n     }\n \n-    Completable upload =\n-        casUploadCache.execute(\n-            digest + context.getWriteCachePolicy().toString(),\n-            RxFutures.toCompletable(\n-                () -> cacheProtocol.uploadFile(context, digest, file), directExecutor()),\n-            force);\n-\n-    return RxFutures.toListenableFuture(upload);\n+    return cacheProtocol.uploadFile(context, digest, file, force);\n+ \n   }\n \n   /**\n@@ -191,14 +183,7 @@ protected ListenableFuture<Void> uploadBlob(\n       return COMPLETED_SUCCESS;\n     }\n \n-    Completable upload =\n-        casUploadCache.execute(\n-            digest + context.getWriteCachePolicy().toString(),\n-            RxFutures.toCompletable(\n-                () -> cacheProtocol.uploadBlob(context, digest, data), directExecutor()),\n-            force);\n-\n-    return RxFutures.toListenableFuture(upload);\n+    return cacheProtocol.uploadBlob(context, digest, data, force);\n   }\n \n   public ListenableFuture<byte[]> downloadBlob(\n@@ -489,7 +474,6 @@ public final List<ListenableFuture<Void>> downloadOutErr(\n \n   @Override\n   protected void deallocate() {\n-    casUploadCache.shutdown();\n     cacheProtocol.close();\n \n     closeCountDownLatch.countDown();\n@@ -509,13 +493,13 @@ public RemoteCache retain() {\n \n   /** Waits for active network I/Os to finish. */\n   public void awaitTermination() throws InterruptedException {\n-    casUploadCache.awaitTermination();\n+    cacheProtocol.awaitTermination();\n     closeCountDownLatch.await();\n   }\n \n   /** Shuts the cache down and cancels active network I/Os. */\n   public void shutdownNow() {\n-    casUploadCache.shutdownNow();\n+    cacheProtocol.shutdownNow();\n   }\n \n   public static FailureDetail createFailureDetail(String message, Code detailedCode) {\nsrc/main/java/com/google/devtools/build/lib/remote/RemoteExecutionCache.java\n@@ -131,23 +131,23 @@ private ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context,\n       Digest digest,\n       MerkleTree merkleTree,\n-      Map<Digest, Message> additionalInputs) {\n+      Map<Digest, Message> additionalInputs, boolean force) {\n     Directory node = merkleTree.getDirectoryByDigest(digest);\n     if (node != null) {\n-      return cacheProtocol.uploadBlob(context, digest, node.toByteString());\n+      return cacheProtocol.uploadBlob(context, digest, node.toByteString(), force);\n     }\n \n     PathOrBytes file = merkleTree.getFileByDigest(digest);\n     if (file != null) {\n       if (file.getBytes() != null) {\n-        return cacheProtocol.uploadBlob(context, digest, file.getBytes());\n+        return cacheProtocol.uploadBlob(context, digest, file.getBytes(), force);\n       }\n-      return cacheProtocol.uploadFile(context, digest, file.getPath());\n+      return cacheProtocol.uploadFile(context, digest, file.getPath(), force);\n     }\n \n     Message message = additionalInputs.get(digest);\n     if (message != null) {\n-      return cacheProtocol.uploadBlob(context, digest, message.toByteString());\n+      return cacheProtocol.uploadBlob(context, digest, message.toByteString(), force);\n     }\n \n     return immediateFailedFuture(\n@@ -195,8 +195,6 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n           uploadTask.disposable = new AtomicReference<>();\n           uploadTask.completion = Completable.fromObservable(completion);\n           Completable upload =\n-              casUploadCache.execute(\n-                  digest.getHash() + context.getWriteCachePolicy().toString(),\n                   Single.<Boolean>create(\n                           continuation -> {\n                             uploadTask.continuation = continuation;\n@@ -211,12 +209,9 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n                             return toCompletable(\n                                 () ->\n                                     uploadBlob(\n-                                        context, uploadTask.digest, merkleTree, additionalInputs),\n+                                        context, uploadTask.digest, merkleTree, additionalInputs, force),\n                                 directExecutor());\n-                          }),\n-                  /* onAlreadyRunning= */ () -> emitter.onSuccess(uploadTask),\n-                  /* onAlreadyFinished= */ emitter::onComplete,\n-                  force);\n+                          });\n           upload.subscribe(\n               new CompletableObserver() {\n                 @Override\nsrc/main/java/com/google/devtools/build/lib/remote/common/RemoteCacheClient.java\n@@ -155,8 +155,8 @@ ListenableFuture<Void> downloadBlob(\n    * @param force Whether to force the upload, even if an upload was already attempted.\n    * @return A future representing pending completion of the upload.\n    */\n-  ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n-\n+  ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n \n   /**\n    * Uploads a BLOB to the CAS.\n@@ -179,8 +179,14 @@ ListenableFuture<Void> uploadBlob(\n    * @return A future representing pending completion of the upload.\n    */\n   ListenableFuture<Void> uploadBlob(\n-      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);      \n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);\n \n   /** Close resources associated with the remote cache. */\n   void close();\n+\n+  /** Waits for active network I/Os to finish. */\n+  void awaitTermination() throws InterruptedException;\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  void shutdownNow();\n }\nsrc/main/java/com/google/devtools/build/lib/remote/disk/DiskAndRemoteCacheClient.java\n@@ -74,11 +74,25 @@ public void close() {\n     remoteCache.close();\n   }\n \n+  /** Waits for active network I/Os to finish. */\n   @Override\n-    public ListenableFuture<Void> uploadFile(\n-        RemoteActionExecutionContext context, Digest digest, Path file) {\n-    return uploadFile(context, digest, file, /*force=*/false);\n-  }  \n+  public void awaitTermination() throws InterruptedException {\n+    diskCache.awaitTermination();\n+    remoteCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    diskCache.shutdownNow();\n+    remoteCache.shutdownNow();\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n \n   @Override\n   public ListenableFuture<Void> uploadFile(\n@@ -97,12 +111,12 @@ public ListenableFuture<Void> uploadFile(\n     return future;\n   }\n \n-  \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return uploadBlob(context, digest, data, /*force=*/false);\n+    return uploadBlob(context, digest, data, /* force= */ false);\n   }\n+\n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/GrpcCacheClient.java",
          "patch": "@@ -181,6 +181,18 @@ public void close() {\n     channel.release();\n   }\n \n+  /** Waits for active network I/Os to finish. */\n+  @Override\n+  public void awaitTermination() throws InterruptedException {\n+    casUploadCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    casUploadCache.shutdownNow();\n+  }\n+\n   /** Returns true if 'options.remoteCache' uses 'grpc' or an empty scheme */\n   public static boolean isRemoteCacheOptions(RemoteOptions options) {\n     if (isNullOrEmpty(options.remoteCache)) {"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/RemoteCache.java",
          "patch": "@@ -42,7 +42,6 @@\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient.ActionKey;\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient.CachedActionResult;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n-import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.server.FailureDetails.FailureDetail;\n@@ -83,7 +82,6 @@ public class RemoteCache extends AbstractReferenceCounted {\n   private static final ListenableFuture<byte[]> EMPTY_BYTES = immediateFuture(new byte[0]);\n \n   private final CountDownLatch closeCountDownLatch = new CountDownLatch(1);\n-  protected final AsyncTaskCache.NoResult<String> casUploadCache = AsyncTaskCache.NoResult.create();\n \n   protected final RemoteCacheClient cacheProtocol;\n   protected final RemoteOptions options;\n@@ -160,14 +158,8 @@ protected ListenableFuture<Void> uploadFile(\n       return COMPLETED_SUCCESS;\n     }\n \n-    Completable upload =\n-        casUploadCache.execute(\n-            digest + context.getWriteCachePolicy().toString(),\n-            RxFutures.toCompletable(\n-                () -> cacheProtocol.uploadFile(context, digest, file), directExecutor()),\n-            force);\n-\n-    return RxFutures.toListenableFuture(upload);\n+    return cacheProtocol.uploadFile(context, digest, file, force);\n+ \n   }\n \n   /**\n@@ -191,14 +183,7 @@ protected ListenableFuture<Void> uploadBlob(\n       return COMPLETED_SUCCESS;\n     }\n \n-    Completable upload =\n-        casUploadCache.execute(\n-            digest + context.getWriteCachePolicy().toString(),\n-            RxFutures.toCompletable(\n-                () -> cacheProtocol.uploadBlob(context, digest, data), directExecutor()),\n-            force);\n-\n-    return RxFutures.toListenableFuture(upload);\n+    return cacheProtocol.uploadBlob(context, digest, data, force);\n   }\n \n   public ListenableFuture<byte[]> downloadBlob(\n@@ -489,7 +474,6 @@ public final List<ListenableFuture<Void>> downloadOutErr(\n \n   @Override\n   protected void deallocate() {\n-    casUploadCache.shutdown();\n     cacheProtocol.close();\n \n     closeCountDownLatch.countDown();\n@@ -509,13 +493,13 @@ public RemoteCache retain() {\n \n   /** Waits for active network I/Os to finish. */\n   public void awaitTermination() throws InterruptedException {\n-    casUploadCache.awaitTermination();\n+    cacheProtocol.awaitTermination();\n     closeCountDownLatch.await();\n   }\n \n   /** Shuts the cache down and cancels active network I/Os. */\n   public void shutdownNow() {\n-    casUploadCache.shutdownNow();\n+    cacheProtocol.shutdownNow();\n   }\n \n   public static FailureDetail createFailureDetail(String message, Code detailedCode) {"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/RemoteExecutionCache.java",
          "patch": "@@ -131,23 +131,23 @@ private ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context,\n       Digest digest,\n       MerkleTree merkleTree,\n-      Map<Digest, Message> additionalInputs) {\n+      Map<Digest, Message> additionalInputs, boolean force) {\n     Directory node = merkleTree.getDirectoryByDigest(digest);\n     if (node != null) {\n-      return cacheProtocol.uploadBlob(context, digest, node.toByteString());\n+      return cacheProtocol.uploadBlob(context, digest, node.toByteString(), force);\n     }\n \n     PathOrBytes file = merkleTree.getFileByDigest(digest);\n     if (file != null) {\n       if (file.getBytes() != null) {\n-        return cacheProtocol.uploadBlob(context, digest, file.getBytes());\n+        return cacheProtocol.uploadBlob(context, digest, file.getBytes(), force);\n       }\n-      return cacheProtocol.uploadFile(context, digest, file.getPath());\n+      return cacheProtocol.uploadFile(context, digest, file.getPath(), force);\n     }\n \n     Message message = additionalInputs.get(digest);\n     if (message != null) {\n-      return cacheProtocol.uploadBlob(context, digest, message.toByteString());\n+      return cacheProtocol.uploadBlob(context, digest, message.toByteString(), force);\n     }\n \n     return immediateFailedFuture(\n@@ -195,8 +195,6 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n           uploadTask.disposable = new AtomicReference<>();\n           uploadTask.completion = Completable.fromObservable(completion);\n           Completable upload =\n-              casUploadCache.execute(\n-                  digest.getHash() + context.getWriteCachePolicy().toString(),\n                   Single.<Boolean>create(\n                           continuation -> {\n                             uploadTask.continuation = continuation;\n@@ -211,12 +209,9 @@ private Maybe<UploadTask> maybeCreateUploadTask(\n                             return toCompletable(\n                                 () ->\n                                     uploadBlob(\n-                                        context, uploadTask.digest, merkleTree, additionalInputs),\n+                                        context, uploadTask.digest, merkleTree, additionalInputs, force),\n                                 directExecutor());\n-                          }),\n-                  /* onAlreadyRunning= */ () -> emitter.onSuccess(uploadTask),\n-                  /* onAlreadyFinished= */ emitter::onComplete,\n-                  force);\n+                          });\n           upload.subscribe(\n               new CompletableObserver() {\n                 @Override"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/common/RemoteCacheClient.java",
          "patch": "@@ -155,8 +155,8 @@ ListenableFuture<Void> downloadBlob(\n    * @param force Whether to force the upload, even if an upload was already attempted.\n    * @return A future representing pending completion of the upload.\n    */\n-  ListenableFuture<Void> uploadFile(RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n-\n+  ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force);\n \n   /**\n    * Uploads a BLOB to the CAS.\n@@ -179,8 +179,14 @@ ListenableFuture<Void> uploadBlob(\n    * @return A future representing pending completion of the upload.\n    */\n   ListenableFuture<Void> uploadBlob(\n-      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);      \n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force);\n \n   /** Close resources associated with the remote cache. */\n   void close();\n+\n+  /** Waits for active network I/Os to finish. */\n+  void awaitTermination() throws InterruptedException;\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  void shutdownNow();\n }"
        },
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/disk/DiskAndRemoteCacheClient.java",
          "patch": "@@ -74,11 +74,25 @@ public void close() {\n     remoteCache.close();\n   }\n \n+  /** Waits for active network I/Os to finish. */\n   @Override\n-    public ListenableFuture<Void> uploadFile(\n-        RemoteActionExecutionContext context, Digest digest, Path file) {\n-    return uploadFile(context, digest, file, /*force=*/false);\n-  }  \n+  public void awaitTermination() throws InterruptedException {\n+    diskCache.awaitTermination();\n+    remoteCache.awaitTermination();\n+  }\n+\n+  /** Shuts the cache down and cancels active network I/Os. */\n+  @Override\n+  public void shutdownNow() {\n+    diskCache.shutdownNow();\n+    remoteCache.shutdownNow();\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }\n \n   @Override\n   public ListenableFuture<Void> uploadFile(\n@@ -97,12 +111,12 @@ public ListenableFuture<Void> uploadFile(\n     return future;\n   }\n \n-  \n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n-    return uploadBlob(context, digest, data, /*force=*/false);\n+    return uploadBlob(context, digest, data, /* force= */ false);\n   }\n+\n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {"
        }
      ]
    },
    {
      "type": "commit",
      "date": "2024-03-13T06:03:39Z",
      "sha": "48766e84e7aa759667c15c9fc17a61a4be2013b2",
      "message": "fix tests",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-02-24T09:00:56Z",
      "author_date": "2024-02-24T09:00:56Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-13T06:03:39Z",
      "diff_text": "src/test/java/com/google/devtools/build/lib/remote/GrpcCacheClientTest.java\n@@ -877,6 +877,14 @@ public void updateActionResult(\n     assertThat(result).isEqualTo(expectedResult.build());\n   }\n \n+  @Test\n+  public void testUpload_deduplicationWorks() throws Exception {\n+    RemoteOptions remoteOptions = Options.getDefaults(RemoteOptions.class);\n+    GrpcCacheClient client = newClient(remoteOptions);\n+    RemoteCache remoteCache = new RemoteCache(client, remoteOptions, DIGEST_UTIL);\n+\n+  }\n+\n   @Test\n   public void testUploadSplitMissingDigestsCall() throws Exception {\n     RemoteOptions remoteOptions = Options.getDefaults(RemoteOptions.class);\n@@ -1409,3 +1417,4 @@ public void isRemoteCacheOptionsWhenRemoteCacheDisabled() {\n     assertThat(GrpcCacheClient.isRemoteCacheOptions(options)).isFalse();\n   }\n }\n+\nsrc/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java\n@@ -17,6 +17,7 @@\n import static com.google.devtools.build.lib.remote.util.Utils.getFromFuture;\n import static com.google.devtools.build.lib.remote.util.Utils.waitForBulkTransfer;\n import static org.mockito.ArgumentMatchers.any;\n+import static org.mockito.ArgumentMatchers.anyBoolean;\n import static org.mockito.Mockito.doAnswer;\n import static org.mockito.Mockito.mock;\n import static org.mockito.Mockito.spy;\n@@ -250,77 +251,6 @@ public void upload_emptyBlobAndFile_doNotPerformUpload() throws Exception {\n         .containsExactly(emptyDigest);\n   }\n \n-  @Test\n-  @SuppressWarnings(\"FutureReturnValueIgnored\")\n-  public void upload_deduplicationWorks() throws IOException {\n-    RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n-    AtomicInteger times = new AtomicInteger(0);\n-    doAnswer(\n-            invocationOnMock -> {\n-              times.incrementAndGet();\n-              return SettableFuture.create();\n-            })\n-        .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n-    RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n-    Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n-    Path file = execRoot.getRelative(\"file\");\n-\n-    remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-\n-    assertThat(times.get()).isEqualTo(1);\n-  }\n-\n-  @Test\n-  public void upload_failedUploads_doNotDeduplicate() throws Exception {\n-    AtomicBoolean failRequest = new AtomicBoolean(true);\n-    InMemoryCacheClient inMemoryCacheClient = new InMemoryCacheClient();\n-    RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n-    doAnswer(\n-            invocationOnMock -> {\n-              if (failRequest.getAndSet(false)) {\n-                return Futures.immediateFailedFuture(new IOException(\"Failed\"));\n-              }\n-              return inMemoryCacheClient.uploadFile(\n-                  invocationOnMock.getArgument(0),\n-                  invocationOnMock.getArgument(1),\n-                  invocationOnMock.getArgument(2));\n-            })\n-        .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n-    doAnswer(\n-            invocationOnMock ->\n-                inMemoryCacheClient.findMissingDigests(\n-                    invocationOnMock.getArgument(0), invocationOnMock.getArgument(1)))\n-        .when(remoteCacheClient)\n-        .findMissingDigests(any(), any());\n-    RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n-    Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n-    Path file = execRoot.getRelative(\"file\");\n-    assertThat(\n-            getFromFuture(\n-                remoteCache.findMissingDigests(\n-                    remoteActionExecutionContext, ImmutableList.of(digest))))\n-        .containsExactly(digest);\n-\n-    Exception thrown = null;\n-    try {\n-      getFromFuture(remoteCache.uploadFile(remoteActionExecutionContext, digest, file));\n-    } catch (IOException e) {\n-      thrown = e;\n-    }\n-    assertThat(thrown).isNotNull();\n-    assertThat(thrown).isInstanceOf(IOException.class);\n-    getFromFuture(remoteCache.uploadFile(remoteActionExecutionContext, digest, file));\n-\n-    assertThat(\n-            getFromFuture(\n-                remoteCache.findMissingDigests(\n-                    remoteActionExecutionContext, ImmutableList.of(digest))))\n-        .isEmpty();\n-  }\n-\n   @Test\n   public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUploadTasks()\n       throws Exception {\n@@ -338,7 +268,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n@@ -347,7 +277,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -373,14 +303,11 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     thread.start();\n     uploadBlobCalls.await();\n     assertThat(futures).hasSize(2);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isNotEmpty();\n \n     thread.interrupt();\n     ensureInputsPresentReturned.await();\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isTrue();\n     }\n@@ -413,7 +340,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n@@ -422,7 +349,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -460,8 +387,6 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     assertThat(futures).hasSize(2);\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(2);\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isFalse();\n     }\n@@ -470,8 +395,6 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n       future.set(null);\n     }\n     ensureInputsPresentReturned.await();\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).hasSize(2);\n   }\n \n   @Test\n@@ -494,7 +417,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               Path file = invocationOnMock.getArgument(2, Path.class);\n@@ -504,7 +427,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path foo = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(foo, \"foo\");\n@@ -559,14 +482,11 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     uploadFileCalls.await();\n     assertThat(uploadBlobFutures).hasSize(2);\n     assertThat(uploadFileFutures).hasSize(3);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(5);\n \n     thread1.interrupt();\n     ensureInterrupted.await();\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(3);\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (Map.Entry<Path, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n       Path file = entry.getKey();\n       SettableFuture<Void> future = entry.getValue();\n@@ -584,19 +504,17 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n       future.set(null);\n     }\n     ensureInputsPresentReturned.await();\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).hasSize(3);\n   }\n \n   @Test\n   public void ensureInputsPresent_uploadFailed_propagateErrors() throws Exception {\n     RemoteCacheClient cacheProtocol = spy(new InMemoryCacheClient());\n     doAnswer(invocationOnMock -> Futures.immediateFailedFuture(new IOException(\"upload failed\")))\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(invocationOnMock -> Futures.immediateFailedFuture(new IOException(\"upload failed\")))\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n     RemoteExecutionCache remoteCache = spy(newRemoteExecutionCache(cacheProtocol));\n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -617,17 +535,16 @@ public void ensureInputsPresent_uploadFailed_propagateErrors() throws Exception\n   public void shutdownNow_cancelInProgressUploads() throws Exception {\n     RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n     // Return a future that never completes\n-    doAnswer(invocationOnMock -> SettableFuture.create())\n+    doAnswer(invocationOnMock -> Futures.immediateCancelledFuture())\n         .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n     RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n     Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n     Path file = execRoot.getRelative(\"file\");\n+    \n \n     ListenableFuture<Void> upload =\n         remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(\n-        digest + remoteActionExecutionContext.getWriteCachePolicy().toString());\n     remoteCache.shutdownNow();\n \n     assertThat(upload.isCancelled()).isTrue();\nsrc/test/java/com/google/devtools/build/lib/remote/RemoteExecutionServiceTest.java\n@@ -25,6 +25,7 @@\n import static java.nio.charset.StandardCharsets.UTF_8;\n import static org.junit.Assert.assertThrows;\n import static org.mockito.ArgumentMatchers.any;\n+import static org.mockito.ArgumentMatchers.anyBoolean;\n import static org.mockito.Mockito.doAnswer;\n import static org.mockito.Mockito.doReturn;\n import static org.mockito.Mockito.mock;\n@@ -1939,47 +1940,6 @@ public void uploadOutputs_missingMandatoryOutputs_dontUpload() throws Exception\n     assertThat(cache.getNumFindMissingDigests()).isEmpty();\n   }\n \n-  @Test\n-  public void uploadInputsIfNotPresent_deduplicateFindMissingBlobCalls() throws Exception {\n-    int taskCount = 100;\n-    ExecutorService executorService = Executors.newFixedThreadPool(taskCount);\n-    AtomicReference<Throwable> error = new AtomicReference<>(null);\n-    Semaphore semaphore = new Semaphore(0);\n-    ActionInput input = ActionInputHelper.fromPath(\"inputs/foo\");\n-    Digest inputDigest = fakeFileCache.createScratchInput(input, \"input-foo\");\n-    RemoteExecutionService service = newRemoteExecutionService();\n-    Spawn spawn =\n-        newSpawn(\n-            ImmutableMap.of(),\n-            ImmutableSet.of(),\n-            NestedSetBuilder.create(Order.STABLE_ORDER, input));\n-    FakeSpawnExecutionContext context = newSpawnExecutionContext(spawn);\n-    RemoteAction action = service.buildRemoteAction(spawn, context);\n-\n-    for (int i = 0; i < taskCount; ++i) {\n-      executorService.execute(\n-          () -> {\n-            try {\n-              service.uploadInputsIfNotPresent(action, /* force= */ false);\n-            } catch (Throwable e) {\n-              if (e instanceof InterruptedException) {\n-                Thread.currentThread().interrupt();\n-              }\n-              error.set(e);\n-            } finally {\n-              semaphore.release();\n-            }\n-          });\n-    }\n-    semaphore.acquire(taskCount);\n-\n-    assertThat(error.get()).isNull();\n-    assertThat(cache.getNumFindMissingDigests()).containsEntry(inputDigest, 1);\n-    for (Integer num : cache.getNumFindMissingDigests().values()) {\n-      assertThat(num).isEqualTo(1);\n-    }\n-  }\n-\n   @Test\n   public void uploadInputsIfNotPresent_sameInputs_interruptOne_keepOthers() throws Exception {\n     int taskCount = 100;\n@@ -2034,7 +1994,7 @@ public void uploadInputsIfNotPresent_interrupted_requestCancelled() throws Excep\n               return future;\n             })\n         .when(cache.cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     ActionInput input = ActionInputHelper.fromPath(\"inputs/foo\");\n     fakeFileCache.createScratchInput(input, \"input-foo\");\n     RemoteExecutionService service = newRemoteExecutionService();\nsrc/test/java/com/google/devtools/build/lib/remote/util/InMemoryCacheClient.java\n@@ -136,6 +136,12 @@ public ListenableFuture<Void> uploadActionResult(\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }  \n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     try (InputStream in = file.getInputStream()) {\n       cas.put(digest, ByteStreams.toByteArray(in));\n     } catch (IOException e) {\n@@ -147,6 +153,12 @@ public ListenableFuture<Void> uploadFile(\n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     try (InputStream in = data.newInput()) {\n       cas.put(digest, data.toByteArray());\n     } catch (IOException e) {\n@@ -178,4 +190,13 @@ public void close() {\n     cas.clear();\n     ac.clear();\n   }\n+\n+  @Override\n+  public void shutdownNow() {\n+    \n+  }\n+\n+  @Override\n+  public void awaitTermination() throws InterruptedException {}\n+\n }",
      "diff": [
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/GrpcCacheClientTest.java",
          "patch": "@@ -877,6 +877,14 @@ public void updateActionResult(\n     assertThat(result).isEqualTo(expectedResult.build());\n   }\n \n+  @Test\n+  public void testUpload_deduplicationWorks() throws Exception {\n+    RemoteOptions remoteOptions = Options.getDefaults(RemoteOptions.class);\n+    GrpcCacheClient client = newClient(remoteOptions);\n+    RemoteCache remoteCache = new RemoteCache(client, remoteOptions, DIGEST_UTIL);\n+\n+  }\n+\n   @Test\n   public void testUploadSplitMissingDigestsCall() throws Exception {\n     RemoteOptions remoteOptions = Options.getDefaults(RemoteOptions.class);\n@@ -1409,3 +1417,4 @@ public void isRemoteCacheOptionsWhenRemoteCacheDisabled() {\n     assertThat(GrpcCacheClient.isRemoteCacheOptions(options)).isFalse();\n   }\n }\n+"
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java",
          "patch": "@@ -17,6 +17,7 @@\n import static com.google.devtools.build.lib.remote.util.Utils.getFromFuture;\n import static com.google.devtools.build.lib.remote.util.Utils.waitForBulkTransfer;\n import static org.mockito.ArgumentMatchers.any;\n+import static org.mockito.ArgumentMatchers.anyBoolean;\n import static org.mockito.Mockito.doAnswer;\n import static org.mockito.Mockito.mock;\n import static org.mockito.Mockito.spy;\n@@ -250,77 +251,6 @@ public void upload_emptyBlobAndFile_doNotPerformUpload() throws Exception {\n         .containsExactly(emptyDigest);\n   }\n \n-  @Test\n-  @SuppressWarnings(\"FutureReturnValueIgnored\")\n-  public void upload_deduplicationWorks() throws IOException {\n-    RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n-    AtomicInteger times = new AtomicInteger(0);\n-    doAnswer(\n-            invocationOnMock -> {\n-              times.incrementAndGet();\n-              return SettableFuture.create();\n-            })\n-        .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n-    RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n-    Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n-    Path file = execRoot.getRelative(\"file\");\n-\n-    remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-\n-    assertThat(times.get()).isEqualTo(1);\n-  }\n-\n-  @Test\n-  public void upload_failedUploads_doNotDeduplicate() throws Exception {\n-    AtomicBoolean failRequest = new AtomicBoolean(true);\n-    InMemoryCacheClient inMemoryCacheClient = new InMemoryCacheClient();\n-    RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n-    doAnswer(\n-            invocationOnMock -> {\n-              if (failRequest.getAndSet(false)) {\n-                return Futures.immediateFailedFuture(new IOException(\"Failed\"));\n-              }\n-              return inMemoryCacheClient.uploadFile(\n-                  invocationOnMock.getArgument(0),\n-                  invocationOnMock.getArgument(1),\n-                  invocationOnMock.getArgument(2));\n-            })\n-        .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n-    doAnswer(\n-            invocationOnMock ->\n-                inMemoryCacheClient.findMissingDigests(\n-                    invocationOnMock.getArgument(0), invocationOnMock.getArgument(1)))\n-        .when(remoteCacheClient)\n-        .findMissingDigests(any(), any());\n-    RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n-    Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n-    Path file = execRoot.getRelative(\"file\");\n-    assertThat(\n-            getFromFuture(\n-                remoteCache.findMissingDigests(\n-                    remoteActionExecutionContext, ImmutableList.of(digest))))\n-        .containsExactly(digest);\n-\n-    Exception thrown = null;\n-    try {\n-      getFromFuture(remoteCache.uploadFile(remoteActionExecutionContext, digest, file));\n-    } catch (IOException e) {\n-      thrown = e;\n-    }\n-    assertThat(thrown).isNotNull();\n-    assertThat(thrown).isInstanceOf(IOException.class);\n-    getFromFuture(remoteCache.uploadFile(remoteActionExecutionContext, digest, file));\n-\n-    assertThat(\n-            getFromFuture(\n-                remoteCache.findMissingDigests(\n-                    remoteActionExecutionContext, ImmutableList.of(digest))))\n-        .isEmpty();\n-  }\n-\n   @Test\n   public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUploadTasks()\n       throws Exception {\n@@ -338,7 +268,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n@@ -347,7 +277,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -373,14 +303,11 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     thread.start();\n     uploadBlobCalls.await();\n     assertThat(futures).hasSize(2);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isNotEmpty();\n \n     thread.interrupt();\n     ensureInputsPresentReturned.await();\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isTrue();\n     }\n@@ -413,7 +340,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n@@ -422,7 +349,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -460,8 +387,6 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     assertThat(futures).hasSize(2);\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(2);\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isFalse();\n     }\n@@ -470,8 +395,6 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n       future.set(null);\n     }\n     ensureInputsPresentReturned.await();\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).hasSize(2);\n   }\n \n   @Test\n@@ -494,7 +417,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(\n             invocationOnMock -> {\n               Path file = invocationOnMock.getArgument(2, Path.class);\n@@ -504,7 +427,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n               return future;\n             })\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n \n     Path foo = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(foo, \"foo\");\n@@ -559,14 +482,11 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     uploadFileCalls.await();\n     assertThat(uploadBlobFutures).hasSize(2);\n     assertThat(uploadFileFutures).hasSize(3);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(5);\n \n     thread1.interrupt();\n     ensureInterrupted.await();\n \n     // assert\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).hasSize(3);\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).isEmpty();\n     for (Map.Entry<Path, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n       Path file = entry.getKey();\n       SettableFuture<Void> future = entry.getValue();\n@@ -584,19 +504,17 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n       future.set(null);\n     }\n     ensureInputsPresentReturned.await();\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).isEmpty();\n-    assertThat(remoteCache.casUploadCache.getFinishedTasks()).hasSize(3);\n   }\n \n   @Test\n   public void ensureInputsPresent_uploadFailed_propagateErrors() throws Exception {\n     RemoteCacheClient cacheProtocol = spy(new InMemoryCacheClient());\n     doAnswer(invocationOnMock -> Futures.immediateFailedFuture(new IOException(\"upload failed\")))\n         .when(cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     doAnswer(invocationOnMock -> Futures.immediateFailedFuture(new IOException(\"upload failed\")))\n         .when(cacheProtocol)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n     RemoteExecutionCache remoteCache = spy(newRemoteExecutionCache(cacheProtocol));\n     Path path = fs.getPath(\"/execroot/foo\");\n     FileSystemUtils.writeContentAsLatin1(path, \"bar\");\n@@ -617,17 +535,16 @@ public void ensureInputsPresent_uploadFailed_propagateErrors() throws Exception\n   public void shutdownNow_cancelInProgressUploads() throws Exception {\n     RemoteCacheClient remoteCacheClient = mock(RemoteCacheClient.class);\n     // Return a future that never completes\n-    doAnswer(invocationOnMock -> SettableFuture.create())\n+    doAnswer(invocationOnMock -> Futures.immediateCancelledFuture())\n         .when(remoteCacheClient)\n-        .uploadFile(any(), any(), any());\n+        .uploadFile(any(), any(), any(), anyBoolean());\n     RemoteCache remoteCache = newRemoteCache(remoteCacheClient);\n     Digest digest = fakeFileCache.createScratchInput(ActionInputHelper.fromPath(\"file\"), \"content\");\n     Path file = execRoot.getRelative(\"file\");\n+    \n \n     ListenableFuture<Void> upload =\n         remoteCache.uploadFile(remoteActionExecutionContext, digest, file);\n-    assertThat(remoteCache.casUploadCache.getInProgressTasks()).contains(\n-        digest + remoteActionExecutionContext.getWriteCachePolicy().toString());\n     remoteCache.shutdownNow();\n \n     assertThat(upload.isCancelled()).isTrue();"
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/RemoteExecutionServiceTest.java",
          "patch": "@@ -25,6 +25,7 @@\n import static java.nio.charset.StandardCharsets.UTF_8;\n import static org.junit.Assert.assertThrows;\n import static org.mockito.ArgumentMatchers.any;\n+import static org.mockito.ArgumentMatchers.anyBoolean;\n import static org.mockito.Mockito.doAnswer;\n import static org.mockito.Mockito.doReturn;\n import static org.mockito.Mockito.mock;\n@@ -1939,47 +1940,6 @@ public void uploadOutputs_missingMandatoryOutputs_dontUpload() throws Exception\n     assertThat(cache.getNumFindMissingDigests()).isEmpty();\n   }\n \n-  @Test\n-  public void uploadInputsIfNotPresent_deduplicateFindMissingBlobCalls() throws Exception {\n-    int taskCount = 100;\n-    ExecutorService executorService = Executors.newFixedThreadPool(taskCount);\n-    AtomicReference<Throwable> error = new AtomicReference<>(null);\n-    Semaphore semaphore = new Semaphore(0);\n-    ActionInput input = ActionInputHelper.fromPath(\"inputs/foo\");\n-    Digest inputDigest = fakeFileCache.createScratchInput(input, \"input-foo\");\n-    RemoteExecutionService service = newRemoteExecutionService();\n-    Spawn spawn =\n-        newSpawn(\n-            ImmutableMap.of(),\n-            ImmutableSet.of(),\n-            NestedSetBuilder.create(Order.STABLE_ORDER, input));\n-    FakeSpawnExecutionContext context = newSpawnExecutionContext(spawn);\n-    RemoteAction action = service.buildRemoteAction(spawn, context);\n-\n-    for (int i = 0; i < taskCount; ++i) {\n-      executorService.execute(\n-          () -> {\n-            try {\n-              service.uploadInputsIfNotPresent(action, /* force= */ false);\n-            } catch (Throwable e) {\n-              if (e instanceof InterruptedException) {\n-                Thread.currentThread().interrupt();\n-              }\n-              error.set(e);\n-            } finally {\n-              semaphore.release();\n-            }\n-          });\n-    }\n-    semaphore.acquire(taskCount);\n-\n-    assertThat(error.get()).isNull();\n-    assertThat(cache.getNumFindMissingDigests()).containsEntry(inputDigest, 1);\n-    for (Integer num : cache.getNumFindMissingDigests().values()) {\n-      assertThat(num).isEqualTo(1);\n-    }\n-  }\n-\n   @Test\n   public void uploadInputsIfNotPresent_sameInputs_interruptOne_keepOthers() throws Exception {\n     int taskCount = 100;\n@@ -2034,7 +1994,7 @@ public void uploadInputsIfNotPresent_interrupted_requestCancelled() throws Excep\n               return future;\n             })\n         .when(cache.cacheProtocol)\n-        .uploadBlob(any(), any(), any());\n+        .uploadBlob(any(), any(), any(), anyBoolean());\n     ActionInput input = ActionInputHelper.fromPath(\"inputs/foo\");\n     fakeFileCache.createScratchInput(input, \"input-foo\");\n     RemoteExecutionService service = newRemoteExecutionService();"
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/util/InMemoryCacheClient.java",
          "patch": "@@ -136,6 +136,12 @@ public ListenableFuture<Void> uploadActionResult(\n   @Override\n   public ListenableFuture<Void> uploadFile(\n       RemoteActionExecutionContext context, Digest digest, Path file) {\n+    return uploadFile(context, digest, file, /* force= */ false);\n+  }  \n+\n+  @Override\n+  public ListenableFuture<Void> uploadFile(\n+      RemoteActionExecutionContext context, Digest digest, Path file, boolean force) {\n     try (InputStream in = file.getInputStream()) {\n       cas.put(digest, ByteStreams.toByteArray(in));\n     } catch (IOException e) {\n@@ -147,6 +153,12 @@ public ListenableFuture<Void> uploadFile(\n   @Override\n   public ListenableFuture<Void> uploadBlob(\n       RemoteActionExecutionContext context, Digest digest, ByteString data) {\n+    return uploadBlob(context, digest, data, /* force= */ false);\n+  }\n+\n+  @Override\n+  public ListenableFuture<Void> uploadBlob(\n+      RemoteActionExecutionContext context, Digest digest, ByteString data, boolean force) {\n     try (InputStream in = data.newInput()) {\n       cas.put(digest, data.toByteArray());\n     } catch (IOException e) {\n@@ -178,4 +190,13 @@ public void close() {\n     cas.clear();\n     ac.clear();\n   }\n+\n+  @Override\n+  public void shutdownNow() {\n+    \n+  }\n+\n+  @Override\n+  public void awaitTermination() throws InterruptedException {}\n+\n }"
        }
      ]
    },
    {
      "type": "comment",
      "id": 1993727684,
      "created_at": "2024-03-13T07:31:29Z",
      "user": "DolceTriade",
      "body": "So, the errors here surround cancellation and I'm not sure what to do. I think that before the asynctaskexecutor would handle cancelation, but now since this has been moved into each cache protocol, these tests are no longer applicable. These tests don't call any cache protocol. Should I just remove them?\r\n\r\nAll of these tests are now flaky because I'm uncertain what exactly is responsible for cancellation now."
    },
    {
      "type": "comment",
      "id": 1994028952,
      "created_at": "2024-03-13T10:14:10Z",
      "user": "tjgq",
      "body": "> So, the errors here surround cancellation and I'm not sure what to do. I think that before the asynctaskexecutor would handle cancelation, but now since this has been moved into each cache protocol, these tests are no longer applicable. These tests don't call any cache protocol. Should I just remove them?\r\n> \r\n> All of these tests are now flaky because I'm uncertain what exactly is responsible for cancellation now.\r\n\r\nI know this is confusing, but most of the test output for `RemoteTests` almost certainly doesn't have anything to do with the actual test failure - they're just spam. The actual failure is at the end:\r\n\r\n```\r\n1) ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(com.google.devtools.build.lib.remote.RemoteCacheTest)\r\nexpected to be false\r\n\tat com.google.devtools.build.lib.remote.RemoteCacheTest.ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(RemoteCacheTest.java:496)\r\n```\r\n\r\nAs I said, this particular failure is a flake that should have been fixed in 3cccb61183c4bb946a814f9d55c1dcd026bed2fa. The one in `bazel_external_repository_test` seems to be a flake too. Can you rebase on top of the current master so that the tests run again?"
    },
    {
      "type": "comment",
      "id": 1994036934,
      "created_at": "2024-03-13T10:18:27Z",
      "user": "tjgq",
      "body": "> > So, the errors here surround cancellation and I'm not sure what to do. I think that before the asynctaskexecutor would handle cancelation, but now since this has been moved into each cache protocol, these tests are no longer applicable. These tests don't call any cache protocol. Should I just remove them?\r\n> > All of these tests are now flaky because I'm uncertain what exactly is responsible for cancellation now.\r\n> \r\n> I know this is confusing, but most of the test output for `RemoteTests` almost certainly doesn't have anything to do with the actual test failure - they're just spam. The actual failure is at the end:\r\n> \r\n> ```\r\n> 1) ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(com.google.devtools.build.lib.remote.RemoteCacheTest)\r\n> expected to be false\r\n> \tat com.google.devtools.build.lib.remote.RemoteCacheTest.ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(RemoteCacheTest.java:496)\r\n> ```\r\n> \r\n> As I said, this particular failure is a flake that should have been fixed in [3cccb61](https://github.com/bazelbuild/bazel/commit/3cccb61183c4bb946a814f9d55c1dcd026bed2fa). The one in `bazel_external_repository_test` seems to be a flake too. Can you rebase on top of the current master so that the tests run again?\r\n\r\nActually, nevermind - this does seem to be distinct from the issue fixed in 3cccb61183c4bb946a814f9d55c1dcd026bed2fa. Let me take a look and get back to you."
    },
    {
      "type": "comment",
      "id": 1994492439,
      "created_at": "2024-03-13T14:08:07Z",
      "user": "tjgq",
      "body": "> > > So, the errors here surround cancellation and I'm not sure what to do. I think that before the asynctaskexecutor would handle cancelation, but now since this has been moved into each cache protocol, these tests are no longer applicable. These tests don't call any cache protocol. Should I just remove them?\r\n> > > All of these tests are now flaky because I'm uncertain what exactly is responsible for cancellation now.\r\n> > \r\n> > \r\n> > I know this is confusing, but most of the test output for `RemoteTests` almost certainly doesn't have anything to do with the actual test failure - they're just spam. The actual failure is at the end:\r\n> > ```\r\n> > 1) ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(com.google.devtools.build.lib.remote.RemoteCacheTest)\r\n> > expected to be false\r\n> > \tat com.google.devtools.build.lib.remote.RemoteCacheTest.ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks(RemoteCacheTest.java:496)\r\n> > ```\r\n> > \r\n> > \r\n> >     \r\n> >       \r\n> >     \r\n> > \r\n> >       \r\n> >     \r\n> > \r\n> >     \r\n> >   \r\n> > As I said, this particular failure is a flake that should have been fixed in [3cccb61](https://github.com/bazelbuild/bazel/commit/3cccb61183c4bb946a814f9d55c1dcd026bed2fa). The one in `bazel_external_repository_test` seems to be a flake too. Can you rebase on top of the current master so that the tests run again?\r\n> \r\n> Actually, nevermind - this does seem to be distinct from the issue fixed in [3cccb61](https://github.com/bazelbuild/bazel/commit/3cccb61183c4bb946a814f9d55c1dcd026bed2fa). Let me take a look and get back to you.\r\n\r\nSo, the `ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks` can fail in one of two places (lines 493-497 in RemoteCacheTest.java):\r\n\r\n```\r\n      if (file.equals(foo)) {\r\n        assertThat(future.isCancelled()).isTrue();  // (1)\r\n      } else {\r\n        assertThat(future.isCancelled()).isFalse(); // (2)\r\n      }\r\n```\r\n\r\nI believe failures in (1) are always flaky (cancellation hasn't yet propagated to the future by the time the assertion is made). While testing, I suggest adding a `Thread.sleep(1000)` before this line, which in my experience makes the flake go away. (We need to figure out a proper fix, but this PR isn't the right place for it.)\r\n\r\nFailures in (2), on the other hand, are deterministic. I think you're theory is correct and an AsyncTaskCache is required to implement cancellation correctly. But I really don't want to delete these tests, because cancellation is important to get right.\r\n\r\nWDYT about also adding an AsyncTaskCache to InMemoryCacheClient (assuming that does indeed fix the test failures)? This way, we still verify that the integration between RemoteExecutionCache and (a fake implementation of a) RemoteCacheClient works properly. We're going to have to rethink how to extend the tests to cover the real implementations, but again, I think it's fair to leave it for later."
    },
    {
      "type": "comment",
      "id": 1995296702,
      "created_at": "2024-03-13T18:28:48Z",
      "user": "DolceTriade",
      "body": "> WDYT about also adding an AsyncTaskCache to InMemoryCacheClient\r\n\r\nI tried this. It seems that calling `spy(new InMemoryCache())` makes it so that the real method isn't called. Even when adding `invocationOnMock.callRealMethod()` there are other failure modes. \r\n\r\nI'll try some other approaches."
    },
    {
      "type": "review",
      "id": 1935076098,
      "created_at": "2024-03-13T19:46:55Z",
      "user": "DolceTriade",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 1995698154,
      "created_at": "2024-03-13T20:30:42Z",
      "user": "tjgq",
      "body": "> > WDYT about also adding an AsyncTaskCache to InMemoryCacheClient\r\n> \r\n> I tried this. It seems that calling `spy(new InMemoryCache())` makes it so that the real method isn't called. Even when adding `invocationOnMock.callRealMethod()` there are other failure modes.\r\n> \r\n> I'll try some other approaches.\r\n\r\nWe need a mock because this test needs to simulate the case where a thread is interrupted before the future returned by `uploadFile` completes. Calling the real implementation won't work because it would return a future that eventually completes, so it's impossible to write the test in a non-racy way. (I could have realized this earlier and save you the trouble, sorry.)\r\n\r\nI think the options are to either use a completely fake implementation (in which case we aren't testing anything except the fake, and might as well delete the test), or to use one of the real implementations, but add a way to prevent the future from completing for test purposes, while still keeping the AsyncTaskCache logic in place. Or, even better: introduce an abstract class containing only the AsyncTaskCache logic, which GrpcRemoteCacheClient and DiskCacheClient inherit from; then the test can provide its own subclass that returns futures in the right state.\r\n\r\nI cannot accept a change that just deletes the affected tests because, quite frankly, I won't otherwise be able to convince myself that we haven't introduced a regression. But I would completely understand if you don't have the time or motivation to keep working on this; it won't be forgotten and we will eventually get round to it, but it might take a while for us to find the time to do so.\r\n\r\n(Sorry about the unintentional closing and reopening - I clicked the wrong button.)"
    },
    {
      "type": "comment",
      "id": 1996305172,
      "created_at": "2024-03-14T03:03:21Z",
      "user": "DolceTriade",
      "body": "> I could have realized this earlier and save you the trouble, sorry\r\n\r\nNo worries, I tried this weeks ago before I got side tracked.\r\n\r\n> I cannot accept a change that just deletes the affected tests\r\n\r\nAgreed. My hope was to write new tests for each implementation to verify cancelation. It's clear that cancelation must be handled at a lower layer than here.\r\n\r\n> introduce an abstract class containing only the AsyncTaskCache logic, which GrpcRemoteCacheClient and DiskCacheClient inherit from\r\n\r\nMaybe I'm thinking about this wrong, but it seems the only way to do this would be to have a base class that by default runs an abstract onFoo method that the disk and grpc classes would override. I'm not sure I see more value in that than just providing a fake.\r\n\r\nThe real issue with the test is that previously, the test methods would not even be called because they were cached at a higher level and this fake implementation is wrong now. What these series of tests are verifying are that:\r\n - If thread A and thread B are trying to upload the files, and thread B is canceled, only the futures unique to thread B should be canceled.\r\n\r\nI think this is the source of the flakiness, right now, depending on which call happens last, that future gets put into the map and that future is checked. If that future is thread A, then the test passes because it is canceled, but if it is thread B, then it is not canceled.\r\n\r\nThis feels like the right behavior and if putting an async task cache in the fake is sufficient to fix it, I will add back the tests I deleted and update them.\r\n\r\n>  would completely understand if you don't have the time or motivation to keep working on this\r\n\r\nSadly is quite an annoying issue, so I'll keep trying to fix it. I really appreciate your reviews and time explaining this :)\r\n\r\n"
    },
    {
      "type": "comment",
      "id": 1996603182,
      "created_at": "2024-03-14T06:17:49Z",
      "user": "DolceTriade",
      "body": "So, there are two tests I deleted, both testing deduplication. Do you think those are valid to delete since deduplication is not being handled at this layer? I feel I should add analogous tests at the grpc and disk (and http) layers?\r\n\r\nAs for the two flaky tests: the first one is definitely a flake. Adding that sleep does fix it.\r\n\r\nThe 2nd one, is a broken test now that de-duplication isn't being done at that layer. We just need to make the map thread aware. Cancelation does indeed work properly. I'll see about adding some tests to the cache protocol implementations."
    },
    {
      "type": "commit",
      "date": "2024-03-14T06:51:19Z",
      "sha": "7b4050745069a97e3ebdb2c386d37a72ed01f9cb",
      "message": "fix flakiness in remotecachetest",
      "author": "Harsh Modi",
      "author_email": "hmodi@aviatrix.com",
      "author_raw_date": "2024-03-14T06:51:19Z",
      "author_date": "2024-03-14T06:51:19Z",
      "committer": "Harsh Modi",
      "committer_email": "hmodi@aviatrix.com",
      "raw_date": "2024-03-14T06:51:19Z",
      "diff_text": "src/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java\n@@ -50,8 +50,10 @@\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n import com.google.devtools.build.lib.remote.merkletree.MerkleTree;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n import com.google.devtools.build.lib.remote.util.InMemoryCacheClient;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.RxNoGlobalErrorsRule;\n import com.google.devtools.build.lib.remote.util.TracingMetadataUtils;\n import com.google.devtools.build.lib.testutil.TestUtils;\n@@ -70,6 +72,7 @@\n import java.nio.charset.StandardCharsets;\n import java.util.Deque;\n import java.util.Map;\n+import java.util.HashMap;\n import java.util.SortedMap;\n import java.util.TreeMap;\n import java.util.concurrent.ConcurrentHashMap;\n@@ -263,8 +266,9 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n-              futures.add(future);\n               uploadBlobCalls.countDown();\n+              futures.add(future);\n+              \n               return future;\n             })\n         .when(cacheProtocol)\n@@ -307,6 +311,10 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     thread.interrupt();\n     ensureInputsPresentReturned.await();\n \n+    // 🤮🤮🤮 But this fixes flakiness in propagating cancelation to the future.\n+    // This flakes about 1 in 100 without the sleep.\n+    Thread.sleep(100);\n+\n     // assert\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isTrue();\n@@ -401,16 +409,31 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n   public void\n       ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks()\n           throws Exception {\n+    class PerThreadPath {\n+      Path file;\n+      long id;\n+\n+      public PerThreadPath(Path file, Thread thread) {\n+        this.file = file;\n+        this.id = thread.threadId();\n+      }\n+\n+      public PerThreadPath(Path file) {\n+        this(file, Thread.currentThread());\n+      }\n+    }\n     // arrange\n     RemoteCacheClient cacheProtocol = spy(new InMemoryCacheClient());\n     RemoteExecutionCache remoteCache = spy(newRemoteExecutionCache(cacheProtocol));\n \n     ConcurrentLinkedDeque<SettableFuture<Void>> uploadBlobFutures = new ConcurrentLinkedDeque<>();\n-    Map<Path, SettableFuture<Void>> uploadFileFutures = Maps.newConcurrentMap();\n+    Map<PerThreadPath, SettableFuture<Void>> uploadFileFutures = Maps.newConcurrentMap();\n     CountDownLatch uploadBlobCalls = new CountDownLatch(2);\n-    CountDownLatch uploadFileCalls = new CountDownLatch(3);\n+    CountDownLatch uploadFileCalls = new CountDownLatch(4);\n+\n     doAnswer(\n             invocationOnMock -> {\n+              Digest digest = invocationOnMock.getArgument(1, Digest.class);\n               SettableFuture<Void> future = SettableFuture.create();\n               uploadBlobFutures.add(future);\n               uploadBlobCalls.countDown();\n@@ -422,7 +445,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n             invocationOnMock -> {\n               Path file = invocationOnMock.getArgument(2, Path.class);\n               SettableFuture<Void> future = SettableFuture.create();\n-              uploadFileFutures.put(file, future);\n+              uploadFileFutures.put(new PerThreadPath(file), future);\n               uploadFileCalls.countDown();\n               return future;\n             })\n@@ -478,23 +501,28 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     // act\n     thread1.start();\n     thread2.start();\n+    System.out.println(\"THREAD 1 = \" + thread1.threadId() + \" THREAD 2 = \" + thread2.threadId());\n     uploadBlobCalls.await();\n     uploadFileCalls.await();\n     assertThat(uploadBlobFutures).hasSize(2);\n-    assertThat(uploadFileFutures).hasSize(3);\n+    assertThat(uploadFileFutures).hasSize(4);\n \n     thread1.interrupt();\n     ensureInterrupted.await();\n+    // 🤮🤮🤮 But this fixes flakiness in propagating cancelation to the future.\n+    // This flakes about 1 in 100 without the sleep.\n+    Thread.sleep(100);\n \n     // assert\n-    for (Map.Entry<Path, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n-      Path file = entry.getKey();\n+    Map<Long, Boolean> futureResultByThread = new HashMap<>();\n+    for (Map.Entry<PerThreadPath, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n+      PerThreadPath file = entry.getKey();\n       SettableFuture<Void> future = entry.getValue();\n-      if (file.equals(foo)) {\n-        assertThat(future.isCancelled()).isTrue();\n-      } else {\n-        assertThat(future.isCancelled()).isFalse();\n+      System.out.println(\"CHECKING \" + file.file.toString() + \" \" + file.id + \" \" + future.isCancelled());\n+      if (futureResultByThread.containsKey(file.id)) {\n+        assertThat(futureResultByThread.get(file.id)).isEqualTo(future.isCancelled());\n       }\n+      futureResultByThread.put(file.id, future.isCancelled());\n     }\n \n     for (SettableFuture<Void> future : uploadBlobFutures) {",
      "diff": [
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/RemoteCacheTest.java",
          "patch": "@@ -50,8 +50,10 @@\n import com.google.devtools.build.lib.remote.common.RemoteCacheClient;\n import com.google.devtools.build.lib.remote.merkletree.MerkleTree;\n import com.google.devtools.build.lib.remote.options.RemoteOptions;\n+import com.google.devtools.build.lib.remote.util.AsyncTaskCache;\n import com.google.devtools.build.lib.remote.util.DigestUtil;\n import com.google.devtools.build.lib.remote.util.InMemoryCacheClient;\n+import com.google.devtools.build.lib.remote.util.RxFutures;\n import com.google.devtools.build.lib.remote.util.RxNoGlobalErrorsRule;\n import com.google.devtools.build.lib.remote.util.TracingMetadataUtils;\n import com.google.devtools.build.lib.testutil.TestUtils;\n@@ -70,6 +72,7 @@\n import java.nio.charset.StandardCharsets;\n import java.util.Deque;\n import java.util.Map;\n+import java.util.HashMap;\n import java.util.SortedMap;\n import java.util.TreeMap;\n import java.util.concurrent.ConcurrentHashMap;\n@@ -263,8 +266,9 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     doAnswer(\n             invocationOnMock -> {\n               SettableFuture<Void> future = SettableFuture.create();\n-              futures.add(future);\n               uploadBlobCalls.countDown();\n+              futures.add(future);\n+              \n               return future;\n             })\n         .when(cacheProtocol)\n@@ -307,6 +311,10 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     thread.interrupt();\n     ensureInputsPresentReturned.await();\n \n+    // 🤮🤮🤮 But this fixes flakiness in propagating cancelation to the future.\n+    // This flakes about 1 in 100 without the sleep.\n+    Thread.sleep(100);\n+\n     // assert\n     for (SettableFuture<Void> future : futures) {\n       assertThat(future.isCancelled()).isTrue();\n@@ -401,16 +409,31 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n   public void\n       ensureInputsPresent_multipleConsumers_interruptedOneDuringUploadBlobs_keepInProgressUploadTasks()\n           throws Exception {\n+    class PerThreadPath {\n+      Path file;\n+      long id;\n+\n+      public PerThreadPath(Path file, Thread thread) {\n+        this.file = file;\n+        this.id = thread.threadId();\n+      }\n+\n+      public PerThreadPath(Path file) {\n+        this(file, Thread.currentThread());\n+      }\n+    }\n     // arrange\n     RemoteCacheClient cacheProtocol = spy(new InMemoryCacheClient());\n     RemoteExecutionCache remoteCache = spy(newRemoteExecutionCache(cacheProtocol));\n \n     ConcurrentLinkedDeque<SettableFuture<Void>> uploadBlobFutures = new ConcurrentLinkedDeque<>();\n-    Map<Path, SettableFuture<Void>> uploadFileFutures = Maps.newConcurrentMap();\n+    Map<PerThreadPath, SettableFuture<Void>> uploadFileFutures = Maps.newConcurrentMap();\n     CountDownLatch uploadBlobCalls = new CountDownLatch(2);\n-    CountDownLatch uploadFileCalls = new CountDownLatch(3);\n+    CountDownLatch uploadFileCalls = new CountDownLatch(4);\n+\n     doAnswer(\n             invocationOnMock -> {\n+              Digest digest = invocationOnMock.getArgument(1, Digest.class);\n               SettableFuture<Void> future = SettableFuture.create();\n               uploadBlobFutures.add(future);\n               uploadBlobCalls.countDown();\n@@ -422,7 +445,7 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n             invocationOnMock -> {\n               Path file = invocationOnMock.getArgument(2, Path.class);\n               SettableFuture<Void> future = SettableFuture.create();\n-              uploadFileFutures.put(file, future);\n+              uploadFileFutures.put(new PerThreadPath(file), future);\n               uploadFileCalls.countDown();\n               return future;\n             })\n@@ -478,23 +501,28 @@ public void ensureInputsPresent_interruptedDuringUploadBlobs_cancelInProgressUpl\n     // act\n     thread1.start();\n     thread2.start();\n+    System.out.println(\"THREAD 1 = \" + thread1.threadId() + \" THREAD 2 = \" + thread2.threadId());\n     uploadBlobCalls.await();\n     uploadFileCalls.await();\n     assertThat(uploadBlobFutures).hasSize(2);\n-    assertThat(uploadFileFutures).hasSize(3);\n+    assertThat(uploadFileFutures).hasSize(4);\n \n     thread1.interrupt();\n     ensureInterrupted.await();\n+    // 🤮🤮🤮 But this fixes flakiness in propagating cancelation to the future.\n+    // This flakes about 1 in 100 without the sleep.\n+    Thread.sleep(100);\n \n     // assert\n-    for (Map.Entry<Path, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n-      Path file = entry.getKey();\n+    Map<Long, Boolean> futureResultByThread = new HashMap<>();\n+    for (Map.Entry<PerThreadPath, SettableFuture<Void>> entry : uploadFileFutures.entrySet()) {\n+      PerThreadPath file = entry.getKey();\n       SettableFuture<Void> future = entry.getValue();\n-      if (file.equals(foo)) {\n-        assertThat(future.isCancelled()).isTrue();\n-      } else {\n-        assertThat(future.isCancelled()).isFalse();\n+      System.out.println(\"CHECKING \" + file.file.toString() + \" \" + file.id + \" \" + future.isCancelled());\n+      if (futureResultByThread.containsKey(file.id)) {\n+        assertThat(futureResultByThread.get(file.id)).isEqualTo(future.isCancelled());\n       }\n+      futureResultByThread.put(file.id, future.isCancelled());\n     }\n \n     for (SettableFuture<Void> future : uploadBlobFutures) {"
        }
      ]
    },
    {
      "type": "comment",
      "id": 1996702059,
      "created_at": "2024-03-14T07:07:07Z",
      "user": "DolceTriade",
      "body": "I'm having difficulty finding tests that actually test the gRPC cache client. As far as I can tell, there aren't any tests which call the upload functions (the tests for uploading don't seem to do so...).\r\n\r\nWhat would be your advice on proceeding here? "
    }
  ]
}