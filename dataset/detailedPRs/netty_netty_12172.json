{
  "repo": "netty/netty",
  "number": 12172,
  "pr_number": 12172,
  "title": "Update LengthFieldBasedFrameDecoder.java",
  "user": "AbcCreatorDeep",
  "created_at": "2022-03-15T09:45:21Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [],
  "base_commit": "30855c71c843db6d3d0c769d2547b948f61500a2",
  "merge_commit_sha": "95851272e1e4bb0d6b9aacbd0c1afd3302f934f6",
  "commits": 1,
  "issue_events": [
    "closed"
  ],
  "url": "https://github.com/netty/netty/pull/12172",
  "timeline": [
    {
      "type": "commit",
      "date": "2022-03-15T09:40:36Z",
      "sha": "60a48790617f5798255731ac88a7e43ec03cf363",
      "message": "Update LengthFieldBasedFrameDecoder.java",
      "author": "黄俊",
      "author_email": "993831927@qq.com",
      "author_raw_date": "2022-03-15T09:40:36Z",
      "author_date": "2022-03-15T09:40:36Z",
      "committer": "黄俊",
      "committer_email": "993831927@qq.com",
      "raw_date": "2022-03-15T09:40:36Z",
      "diff_text": "codec/src/main/java/io/netty/handler/codec/LengthFieldBasedFrameDecoder.java\n@@ -15,15 +15,13 @@\n  */\n package io.netty.handler.codec;\n \n-import static io.netty.util.internal.ObjectUtil.checkNotNull;\n-import static io.netty.util.internal.ObjectUtil.checkPositive;\n-import static io.netty.util.internal.ObjectUtil.checkPositiveOrZero;\n+import io.netty.buffer.ByteBuf;\n+import io.netty.channel.ChannelHandlerContext;\n \n import java.nio.ByteOrder;\n import java.util.List;\n \n-import io.netty.buffer.ByteBuf;\n-import io.netty.channel.ChannelHandlerContext;\n+import static io.netty.util.internal.ObjectUtil.*;\n \n /**\n  * A decoder that splits the received {@link ByteBuf}s dynamically by the\n@@ -198,6 +196,8 @@ public class LengthFieldBasedFrameDecoder extends ByteToMessageDecoder {\n     private long tooLongFrameLength;\n     private long bytesToDiscard;\n \n+    private int frameLengthInt = -1;\n+\n     /**\n      * Creates a new instance.\n      *\n@@ -394,38 +394,40 @@ private static void failOnFrameLengthLessThanInitialBytesToStrip(ByteBuf in,\n      *                          be created.\n      */\n     protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {\n-        if (discardingTooLongFrame) {\n-            discardingTooLongFrame(in);\n-        }\n+        long frameLength = 0;\n+        if(frameLengthInt == -1) { // new frame\n \n-        if (in.readableBytes() < lengthFieldEndOffset) {\n-            return null;\n-        }\n+            if (discardingTooLongFrame) {\n+                discardingTooLongFrame(in);\n+            }\n \n-        int actualLengthFieldOffset = in.readerIndex() + lengthFieldOffset;\n-        long frameLength = getUnadjustedFrameLength(in, actualLengthFieldOffset, lengthFieldLength, byteOrder);\n+            if (in.readableBytes() < lengthFieldEndOffset) {\n+                return null;\n+            }\n \n-        if (frameLength < 0) {\n-            failOnNegativeLengthField(in, frameLength, lengthFieldEndOffset);\n-        }\n+            int actualLengthFieldOffset = in.readerIndex() + lengthFieldOffset;\n+            frameLength = getUnadjustedFrameLength(in, actualLengthFieldOffset, lengthFieldLength, byteOrder);\n \n-        frameLength += lengthAdjustment + lengthFieldEndOffset;\n+            if (frameLength < 0) {\n+                failOnNegativeLengthField(in, frameLength, lengthFieldEndOffset);\n+            }\n \n-        if (frameLength < lengthFieldEndOffset) {\n-            failOnFrameLengthLessThanLengthFieldEndOffset(in, frameLength, lengthFieldEndOffset);\n-        }\n+            frameLength += lengthAdjustment + lengthFieldEndOffset;\n \n-        if (frameLength > maxFrameLength) {\n-            exceededFrameLength(in, frameLength);\n-            return null;\n-        }\n+            if (frameLength < lengthFieldEndOffset) {\n+                failOnFrameLengthLessThanLengthFieldEndOffset(in, frameLength, lengthFieldEndOffset);\n+            }\n \n-        // never overflows because it's less than maxFrameLength\n-        int frameLengthInt = (int) frameLength;\n-        if (in.readableBytes() < frameLengthInt) {\n+            if (frameLength > maxFrameLength) {\n+                exceededFrameLength(in, frameLength);\n+                return null;\n+            }\n+            // never overflows because it's less than maxFrameLength\n+            frameLengthInt = (int) frameLength;\n+        }\n+        if (in.readableBytes() < frameLengthInt) { // frameLengthInt exist , just check buf\n             return null;\n         }\n-\n         if (initialBytesToStrip > frameLengthInt) {\n             failOnFrameLengthLessThanInitialBytesToStrip(in, frameLength, initialBytesToStrip);\n         }\n@@ -436,6 +438,7 @@ protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception\n         int actualFrameLength = frameLengthInt - initialBytesToStrip;\n         ByteBuf frame = extractFrame(ctx, in, readerIndex, actualFrameLength);\n         in.readerIndex(readerIndex + actualFrameLength);\n+        frameLengthInt = -1; // start processing the next frame\n         return frame;\n     }\n ",
      "diff": [
        {
          "file": "codec/src/main/java/io/netty/handler/codec/LengthFieldBasedFrameDecoder.java",
          "patch": "@@ -15,15 +15,13 @@\n  */\n package io.netty.handler.codec;\n \n-import static io.netty.util.internal.ObjectUtil.checkNotNull;\n-import static io.netty.util.internal.ObjectUtil.checkPositive;\n-import static io.netty.util.internal.ObjectUtil.checkPositiveOrZero;\n+import io.netty.buffer.ByteBuf;\n+import io.netty.channel.ChannelHandlerContext;\n \n import java.nio.ByteOrder;\n import java.util.List;\n \n-import io.netty.buffer.ByteBuf;\n-import io.netty.channel.ChannelHandlerContext;\n+import static io.netty.util.internal.ObjectUtil.*;\n \n /**\n  * A decoder that splits the received {@link ByteBuf}s dynamically by the\n@@ -198,6 +196,8 @@ public class LengthFieldBasedFrameDecoder extends ByteToMessageDecoder {\n     private long tooLongFrameLength;\n     private long bytesToDiscard;\n \n+    private int frameLengthInt = -1;\n+\n     /**\n      * Creates a new instance.\n      *\n@@ -394,38 +394,40 @@ private static void failOnFrameLengthLessThanInitialBytesToStrip(ByteBuf in,\n      *                          be created.\n      */\n     protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {\n-        if (discardingTooLongFrame) {\n-            discardingTooLongFrame(in);\n-        }\n+        long frameLength = 0;\n+        if(frameLengthInt == -1) { // new frame\n \n-        if (in.readableBytes() < lengthFieldEndOffset) {\n-            return null;\n-        }\n+            if (discardingTooLongFrame) {\n+                discardingTooLongFrame(in);\n+            }\n \n-        int actualLengthFieldOffset = in.readerIndex() + lengthFieldOffset;\n-        long frameLength = getUnadjustedFrameLength(in, actualLengthFieldOffset, lengthFieldLength, byteOrder);\n+            if (in.readableBytes() < lengthFieldEndOffset) {\n+                return null;\n+            }\n \n-        if (frameLength < 0) {\n-            failOnNegativeLengthField(in, frameLength, lengthFieldEndOffset);\n-        }\n+            int actualLengthFieldOffset = in.readerIndex() + lengthFieldOffset;\n+            frameLength = getUnadjustedFrameLength(in, actualLengthFieldOffset, lengthFieldLength, byteOrder);\n \n-        frameLength += lengthAdjustment + lengthFieldEndOffset;\n+            if (frameLength < 0) {\n+                failOnNegativeLengthField(in, frameLength, lengthFieldEndOffset);\n+            }\n \n-        if (frameLength < lengthFieldEndOffset) {\n-            failOnFrameLengthLessThanLengthFieldEndOffset(in, frameLength, lengthFieldEndOffset);\n-        }\n+            frameLength += lengthAdjustment + lengthFieldEndOffset;\n \n-        if (frameLength > maxFrameLength) {\n-            exceededFrameLength(in, frameLength);\n-            return null;\n-        }\n+            if (frameLength < lengthFieldEndOffset) {\n+                failOnFrameLengthLessThanLengthFieldEndOffset(in, frameLength, lengthFieldEndOffset);\n+            }\n \n-        // never overflows because it's less than maxFrameLength\n-        int frameLengthInt = (int) frameLength;\n-        if (in.readableBytes() < frameLengthInt) {\n+            if (frameLength > maxFrameLength) {\n+                exceededFrameLength(in, frameLength);\n+                return null;\n+            }\n+            // never overflows because it's less than maxFrameLength\n+            frameLengthInt = (int) frameLength;\n+        }\n+        if (in.readableBytes() < frameLengthInt) { // frameLengthInt exist , just check buf\n             return null;\n         }\n-\n         if (initialBytesToStrip > frameLengthInt) {\n             failOnFrameLengthLessThanInitialBytesToStrip(in, frameLength, initialBytesToStrip);\n         }\n@@ -436,6 +438,7 @@ protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception\n         int actualFrameLength = frameLengthInt - initialBytesToStrip;\n         ByteBuf frame = extractFrame(ctx, in, readerIndex, actualFrameLength);\n         in.readerIndex(readerIndex + actualFrameLength);\n+        frameLengthInt = -1; // start processing the next frame\n         return frame;\n     }\n "
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2022-03-15T09:45:21Z",
      "user": "AbcCreatorDeep",
      "body": "Motivation:\r\n\r\nLengthFieldBasedFrameDecoder . decode(ChannelHandlerContext ctx, ByteBuf in) method，Can be optimized - Can we keep frameLengthInt in instance variable? so , When there is not enough data in the buffer, the length of the data frame does not need to be recalculated at the next decoding。code:\r\n\r\n// LengthFieldBasedFrameDecoder class\r\nprotected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {\r\n... // check maxFrame\r\nint frameLengthInt = (int) frameLength;\r\nif (in.readableBytes() < frameLengthInt) { // When there is not enough data in the buffer, The above code will be executed again, and I just need to wait for the buffer to contain enough data . In fact, we can save the frameLengthInt variable without having to\r\nreturn null;\r\n}\r\n... // extract frame\r\n}\r\n\r\nModification:\r\n\r\nprivate int frameLengthInt = -1;\r\nprotected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {\r\n        long frameLength = 0;\r\n        if(frameLengthInt == -1) { // new frame\r\n\r\n            if (discardingTooLongFrame) {\r\n                discardingTooLongFrame(in);\r\n            }\r\n\r\n            if (in.readableBytes() < lengthFieldEndOffset) {\r\n                return null;\r\n            }\r\n\r\n            int actualLengthFieldOffset = in.readerIndex() + lengthFieldOffset;\r\n            frameLength = getUnadjustedFrameLength(in, actualLengthFieldOffset, lengthFieldLength, byteOrder);\r\n\r\n            if (frameLength < 0) {\r\n                failOnNegativeLengthField(in, frameLength, lengthFieldEndOffset);\r\n            }\r\n\r\n            frameLength += lengthAdjustment + lengthFieldEndOffset;\r\n\r\n            if (frameLength < lengthFieldEndOffset) {\r\n                failOnFrameLengthLessThanLengthFieldEndOffset(in, frameLength, lengthFieldEndOffset);\r\n            }\r\n\r\n            if (frameLength > maxFrameLength) {\r\n                exceededFrameLength(in, frameLength);\r\n                return null;\r\n            }\r\n            // never overflows because it's less than maxFrameLength\r\n            frameLengthInt = (int) frameLength;\r\n        }\r\n        if (in.readableBytes() < frameLengthInt) { // frameLengthInt exist , just check buf\r\n            return null;\r\n        }\r\n        if (initialBytesToStrip > frameLengthInt) {\r\n            failOnFrameLengthLessThanInitialBytesToStrip(in, frameLength, initialBytesToStrip);\r\n        }\r\n        in.skipBytes(initialBytesToStrip);\r\n\r\n        // extract frame\r\n        int readerIndex = in.readerIndex();\r\n        int actualFrameLength = frameLengthInt - initialBytesToStrip;\r\n        ByteBuf frame = extractFrame(ctx, in, readerIndex, actualFrameLength);\r\n        in.readerIndex(readerIndex + actualFrameLength);\r\n        frameLengthInt = -1; // start processing the next frame\r\n        return frame;\r\n    }\r\n\r\nadd frameLengthInt as instance varibale  to  avoid unnecessary calculation\r\n\r\nFixes #12171.\r\n\r\n"
    },
    {
      "type": "review_comment",
      "id": 826780462,
      "created_at": "2022-03-15T09:56:52Z",
      "user": "normanmaurer",
      "body": "please revert... we don't use wildcard imports ",
      "path": "codec/src/main/java/io/netty/handler/codec/LengthFieldBasedFrameDecoder.java",
      "diff_hunk": "@@ -15,15 +15,13 @@\n  */\n package io.netty.handler.codec;\n \n-import static io.netty.util.internal.ObjectUtil.checkNotNull;\n-import static io.netty.util.internal.ObjectUtil.checkPositive;\n-import static io.netty.util.internal.ObjectUtil.checkPositiveOrZero;\n+import io.netty.buffer.ByteBuf;\n+import io.netty.channel.ChannelHandlerContext;\n \n import java.nio.ByteOrder;\n import java.util.List;\n \n-import io.netty.buffer.ByteBuf;\n-import io.netty.channel.ChannelHandlerContext;\n+import static io.netty.util.internal.ObjectUtil.*;",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 24,
      "original_line": 24,
      "side": "RIGHT",
      "original_position": 15,
      "position": 15,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 826780998,
      "created_at": "2022-03-15T09:57:28Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n        if (frameLengthInt == -1) { // new frame\r\n```",
      "path": "codec/src/main/java/io/netty/handler/codec/LengthFieldBasedFrameDecoder.java",
      "diff_hunk": "@@ -394,38 +394,40 @@ private static void failOnFrameLengthLessThanInitialBytesToStrip(ByteBuf in,\n      *                          be created.\n      */\n     protected Object decode(ChannelHandlerContext ctx, ByteBuf in) throws Exception {\n-        if (discardingTooLongFrame) {\n-            discardingTooLongFrame(in);\n-        }\n+        long frameLength = 0;\n+        if(frameLengthInt == -1) { // new frame",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 398,
      "original_line": 398,
      "side": "RIGHT",
      "original_position": 36,
      "position": 36,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review_comment",
      "id": 826781287,
      "created_at": "2022-03-15T09:57:43Z",
      "user": "normanmaurer",
      "body": "```suggestion\r\n    private int frameLengthInt = -1;\r\n```",
      "path": "codec/src/main/java/io/netty/handler/codec/LengthFieldBasedFrameDecoder.java",
      "diff_hunk": "@@ -198,6 +196,8 @@\n     private long tooLongFrameLength;\n     private long bytesToDiscard;\n \n+    private int frameLengthInt = -1;",
      "start_line": 198,
      "original_start_line": 198,
      "start_side": "RIGHT",
      "line": 199,
      "original_line": 199,
      "side": "RIGHT",
      "original_position": 23,
      "position": 23,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 909926724,
      "created_at": "2022-03-15T09:58:01Z",
      "user": "normanmaurer",
      "state": "CHANGES_REQUESTED",
      "body": ""
    }
  ]
}