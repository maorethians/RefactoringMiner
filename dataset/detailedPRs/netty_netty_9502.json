{
  "repo": "netty/netty",
  "number": 9502,
  "pr_number": 9502,
  "title": "Reduce GC produced by AbstractByteBuf.indexOf(..) implementation",
  "user": "normanmaurer",
  "created_at": "2019-08-22T17:55:02Z",
  "merged_at": "2019-08-24T11:46:29Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "9fa974f6a5fa4d90fc13572c92d94e0681db4570",
  "merge_commit_sha": "da2aba574239367f2641cb7b0802840739e61520",
  "commits": 2,
  "issue_events": [
    "review_requested",
    "review_requested",
    "milestoned",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "mentioned",
    "subscribed",
    "referenced",
    "merged",
    "closed",
    "head_ref_deleted",
    "referenced",
    "demilestoned",
    "milestoned"
  ],
  "url": "https://github.com/netty/netty/pull/9502",
  "timeline": [
    {
      "type": "commit",
      "date": "2019-08-22T17:48:05Z",
      "sha": "e52f0052489a647f85a70a78b4a9b5e4f33ce9c8",
      "message": "Reduce GC produced by AbstractByteBuf.indexOf(..) implementation\n\nMotivation:\n\nAbstractByteBuf.indexOf(...) currently delegates to ByteBufUtils.indexOf(...) which will create a new ByteBufProcessor on each call. This is done to reduce overhead of bounds-checks. Unfortunally while this reduces bounds checks it produces a lot of GC. We can just implement our own version in AbstractByteBuf which makes use of _getByte(...) and so does no bound checks as well but also not need to create any garbage.\n\nModifications:\n\nWrite optimized implementation of indexOf(...) for AbstractByteBuf\n\nResult:\n\nFixes https://github.com/netty/netty/issues/9499.",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2019-08-22T17:48:05Z",
      "author_date": "2019-08-22T17:48:05Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2019-08-22T17:48:05Z",
      "diff_text": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java\n@@ -1263,7 +1263,41 @@ public String toString(int index, int length, Charset charset) {\n \n     @Override\n     public int indexOf(int fromIndex, int toIndex, byte value) {\n-        return ByteBufUtil.indexOf(this, fromIndex, toIndex, value);\n+        if (fromIndex <= toIndex) {\n+            return firstIndexOf(fromIndex, toIndex, value);\n+        } else {\n+            return lastIndexOf(fromIndex, toIndex, value);\n+        }\n+    }\n+\n+    private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.max(fromIndex, 0);\n+        if (fromIndex >= toIndex || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex; i < toIndex; i ++) {\n+            if (_getByte(i) == value) {\n+                return i;\n+            }\n+        }\n+\n+        return -1;\n+    }\n+\n+    private int lastIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.min(fromIndex, capacity());\n+        if (fromIndex < 0 || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex - 1; i >= toIndex; i --) {\n+            if (_getByte(i) == value) {\n+                return i;\n+            }\n+        }\n+\n+        return -1;\n     }\n \n     @Override",
      "diff": [
        {
          "file": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java",
          "patch": "@@ -1263,7 +1263,41 @@ public String toString(int index, int length, Charset charset) {\n \n     @Override\n     public int indexOf(int fromIndex, int toIndex, byte value) {\n-        return ByteBufUtil.indexOf(this, fromIndex, toIndex, value);\n+        if (fromIndex <= toIndex) {\n+            return firstIndexOf(fromIndex, toIndex, value);\n+        } else {\n+            return lastIndexOf(fromIndex, toIndex, value);\n+        }\n+    }\n+\n+    private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.max(fromIndex, 0);\n+        if (fromIndex >= toIndex || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex; i < toIndex; i ++) {\n+            if (_getByte(i) == value) {\n+                return i;\n+            }\n+        }\n+\n+        return -1;\n+    }\n+\n+    private int lastIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.min(fromIndex, capacity());\n+        if (fromIndex < 0 || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex - 1; i >= toIndex; i --) {\n+            if (_getByte(i) == value) {\n+                return i;\n+            }\n+        }\n+\n+        return -1;\n     }\n \n     @Override"
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2019-08-22T17:55:02Z",
      "user": "normanmaurer",
      "body": "Motivation:\r\n\r\nAbstractByteBuf.indexOf(...) currently delegates to ByteBufUtils.indexOf(...) which will create a new ByteBufProcessor on each call. This is done to reduce overhead of bounds-checks. Unfortunally while this reduces bounds checks it produces a lot of GC. We can just implement our own version in AbstractByteBuf which makes use of _getByte(...) and so does no bound checks as well but also not need to create any garbage.\r\n\r\nModifications:\r\n\r\nWrite optimized implementation of indexOf(...) for AbstractByteBuf\r\n\r\nResult:\r\n\r\nFixes https://github.com/netty/netty/issues/9499."
    },
    {
      "type": "review_comment",
      "id": 316843926,
      "created_at": "2019-08-22T19:16:21Z",
      "user": "franz1981",
      "body": "Probably uneeded but I remember and old issue with counted loop causing long time to safe point pauses: maybe would be nice to check if the range is too big and use a long based loop in that case",
      "path": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java",
      "diff_hunk": "@@ -1263,7 +1263,41 @@ public String toString(int index, int length, Charset charset) {\n \n     @Override\n     public int indexOf(int fromIndex, int toIndex, byte value) {\n-        return ByteBufUtil.indexOf(this, fromIndex, toIndex, value);\n+        if (fromIndex <= toIndex) {\n+            return firstIndexOf(fromIndex, toIndex, value);\n+        } else {\n+            return lastIndexOf(fromIndex, toIndex, value);\n+        }\n+    }\n+\n+    private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.max(fromIndex, 0);\n+        if (fromIndex >= toIndex || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex; i < toIndex; i ++) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 1280,
      "original_line": 1279,
      "side": "RIGHT",
      "original_position": 18,
      "position": 19,
      "subject_type": "line",
      "reply": [
        {
          "id": 316983221,
          "user": "normanmaurer",
          "body": "I think it is fine as it is.... this would be true for many other places as well",
          "created_at": "2019-08-23T05:56:18Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 278632658,
      "created_at": "2019-08-22T19:16:28Z",
      "user": "franz1981",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 278616388,
      "created_at": "2019-08-22T19:19:36Z",
      "user": "njhill",
      "state": "CHANGES_REQUESTED",
      "body": "The prior bounds-checking behaviour of this method is a bit strange.. it will allow _any_ value for `fromIndex` (truncating as necessary) but will throw exceptions for a `toIndex` outside of the buffer's range. This isn't clear from the javadoc in `ByteBuf`. That javadoc is also incorrect w.r.t. its use of \"inclusive\" and \"exclusive\" ... the corresponding statement only holds when `toIndex >= fromIndex`, otherwise they should be reversed.\r\n\r\nThe bounds/accessibility checks are missing from this change ... comments inline suggest what I think is the simplest way to fix that such that the precise prior behaviour is restored (despite its existing inconsistency)"
    },
    {
      "type": "review_comment",
      "id": 316845650,
      "created_at": "2019-08-22T19:21:05Z",
      "user": "njhill",
      "body": "replace the `if` with `checkIndex(toIndex - 1)`",
      "path": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java",
      "diff_hunk": "@@ -1263,7 +1263,41 @@ public String toString(int index, int length, Charset charset) {\n \n     @Override\n     public int indexOf(int fromIndex, int toIndex, byte value) {\n-        return ByteBufUtil.indexOf(this, fromIndex, toIndex, value);\n+        if (fromIndex <= toIndex) {\n+            return firstIndexOf(fromIndex, toIndex, value);\n+        } else {\n+            return lastIndexOf(fromIndex, toIndex, value);\n+        }\n+    }\n+\n+    private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.max(fromIndex, 0);\n+        if (fromIndex >= toIndex || capacity() == 0) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 1275,
      "original_line": 1275,
      "side": "RIGHT",
      "original_position": 14,
      "position": 14,
      "subject_type": "line",
      "reply": [
        {
          "id": 317007820,
          "user": "normanmaurer",
          "body": "hmm... This code is the same as in the existing method. So I don't think we should change behaviour:\r\n\r\nhttps://github.com/netty/netty/blob/4.1/buffer/src/main/java/io/netty/buffer/ByteBufUtil.java#L459",
          "created_at": "2019-08-23T07:35:58Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 316845894,
      "created_at": "2019-08-22T19:21:47Z",
      "user": "njhill",
      "body": "replace the `if` with `checkIndex(toIndex)`",
      "path": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java",
      "diff_hunk": "@@ -1263,7 +1263,41 @@ public String toString(int index, int length, Charset charset) {\n \n     @Override\n     public int indexOf(int fromIndex, int toIndex, byte value) {\n-        return ByteBufUtil.indexOf(this, fromIndex, toIndex, value);\n+        if (fromIndex <= toIndex) {\n+            return firstIndexOf(fromIndex, toIndex, value);\n+        } else {\n+            return lastIndexOf(fromIndex, toIndex, value);\n+        }\n+    }\n+\n+    private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.max(fromIndex, 0);\n+        if (fromIndex >= toIndex || capacity() == 0) {\n+            return -1;\n+        }\n+\n+        for (int i = fromIndex; i < toIndex; i ++) {\n+            if (_getByte(i) == value) {\n+                return i;\n+            }\n+        }\n+\n+        return -1;\n+    }\n+\n+    private int lastIndexOf(int fromIndex, int toIndex, byte value) {\n+        fromIndex = Math.min(fromIndex, capacity());\n+        if (fromIndex < 0 || capacity() == 0) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 1291,
      "original_line": 1290,
      "side": "RIGHT",
      "original_position": 29,
      "position": 30,
      "subject_type": "line",
      "reply": [
        {
          "id": 317008169,
          "user": "normanmaurer",
          "body": "same... This is the same code as it existed before:\r\n\r\nhttps://github.com/netty/netty/blob/4.1/buffer/src/main/java/io/netty/buffer/ByteBufUtil.java#L469",
          "created_at": "2019-08-23T07:37:01Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 278634892,
      "created_at": "2019-08-22T19:22:45Z",
      "user": "njhill",
      "state": "COMMENTED",
      "body": "Sorry, github is behaving strangely for me ... my inline comments from the above review were messed up, hopefully they are attached to this one..."
    },
    {
      "type": "review",
      "id": 278807278,
      "created_at": "2019-08-23T05:56:18Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 278839145,
      "created_at": "2019-08-23T07:35:58Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 278839539,
      "created_at": "2019-08-23T07:37:01Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2019-08-23T08:21:06Z",
      "sha": "56ffa8661d5af815ff1bce9303daa11cdcc60247",
      "message": "Add more tests to ensure we handle the bounds and ref cnt checks like with the old impl",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2019-08-23T08:12:07Z",
      "author_date": "2019-08-23T08:12:07Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2019-08-23T08:21:06Z",
      "diff_text": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java\n@@ -1275,6 +1275,7 @@ private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n         if (fromIndex >= toIndex || capacity() == 0) {\n             return -1;\n         }\n+        checkIndex(fromIndex, toIndex - fromIndex);\n \n         for (int i = fromIndex; i < toIndex; i ++) {\n             if (_getByte(i) == value) {\n@@ -1291,6 +1292,8 @@ private int lastIndexOf(int fromIndex, int toIndex, byte value) {\n             return -1;\n         }\n \n+        checkIndex(toIndex, fromIndex - toIndex);\n+\n         for (int i = fromIndex - 1; i >= toIndex; i --) {\n             if (_getByte(i) == value) {\n                 return i;\nbuffer/src/test/java/io/netty/buffer/AbstractByteBufTest.java\n@@ -2125,6 +2125,9 @@ public void run() {\n     @Test\n     public void testIndexOf() {\n         buffer.clear();\n+        // Ensure the buffer is completely zero'ed.\n+        buffer.setZero(0, buffer.capacity());\n+\n         buffer.writeByte((byte) 1);\n         buffer.writeByte((byte) 2);\n         buffer.writeByte((byte) 3);\n@@ -2135,6 +2138,38 @@ public void testIndexOf() {\n         assertEquals(-1, buffer.indexOf(4, 1, (byte) 1));\n         assertEquals(1, buffer.indexOf(1, 4, (byte) 2));\n         assertEquals(3, buffer.indexOf(4, 1, (byte) 2));\n+\n+        try {\n+            buffer.indexOf(0, buffer.capacity() + 1, (byte) 0);\n+            fail();\n+        } catch (IndexOutOfBoundsException expected) {\n+            // expected\n+        }\n+\n+        try {\n+            buffer.indexOf(buffer.capacity(), -1, (byte) 0);\n+            fail();\n+        } catch (IndexOutOfBoundsException expected) {\n+            // expected\n+        }\n+\n+        assertEquals(4, buffer.indexOf(buffer.capacity() + 1, 0, (byte) 1));\n+        assertEquals(0, buffer.indexOf(-1, buffer.capacity(), (byte) 1));\n+    }\n+\n+    @Test\n+    public void testIndexOfReleaseBuffer() {\n+        ByteBuf buffer = releasedBuffer();\n+        if (buffer.capacity() != 0) {\n+            try {\n+                buffer.indexOf(0, 1, (byte) 1);\n+                fail();\n+            } catch (IllegalReferenceCountException expected) {\n+                // expected\n+            }\n+        } else {\n+            assertEquals(-1, buffer.indexOf(0, 1, (byte) 1));\n+        }\n     }\n \n     @Test\n@@ -2570,7 +2605,6 @@ public void testLittleEndianWithExpand() {\n \n     private ByteBuf releasedBuffer() {\n         ByteBuf buffer = newBuffer(8);\n-\n         // Clear the buffer so we are sure the reader and writer indices are 0.\n         // This is important as we may return a slice from newBuffer(...).\n         buffer.clear();",
      "diff": [
        {
          "file": "buffer/src/main/java/io/netty/buffer/AbstractByteBuf.java",
          "patch": "@@ -1275,6 +1275,7 @@ private int firstIndexOf(int fromIndex, int toIndex, byte value) {\n         if (fromIndex >= toIndex || capacity() == 0) {\n             return -1;\n         }\n+        checkIndex(fromIndex, toIndex - fromIndex);\n \n         for (int i = fromIndex; i < toIndex; i ++) {\n             if (_getByte(i) == value) {\n@@ -1291,6 +1292,8 @@ private int lastIndexOf(int fromIndex, int toIndex, byte value) {\n             return -1;\n         }\n \n+        checkIndex(toIndex, fromIndex - toIndex);\n+\n         for (int i = fromIndex - 1; i >= toIndex; i --) {\n             if (_getByte(i) == value) {\n                 return i;"
        },
        {
          "file": "buffer/src/test/java/io/netty/buffer/AbstractByteBufTest.java",
          "patch": "@@ -2125,6 +2125,9 @@ public void run() {\n     @Test\n     public void testIndexOf() {\n         buffer.clear();\n+        // Ensure the buffer is completely zero'ed.\n+        buffer.setZero(0, buffer.capacity());\n+\n         buffer.writeByte((byte) 1);\n         buffer.writeByte((byte) 2);\n         buffer.writeByte((byte) 3);\n@@ -2135,6 +2138,38 @@ public void testIndexOf() {\n         assertEquals(-1, buffer.indexOf(4, 1, (byte) 1));\n         assertEquals(1, buffer.indexOf(1, 4, (byte) 2));\n         assertEquals(3, buffer.indexOf(4, 1, (byte) 2));\n+\n+        try {\n+            buffer.indexOf(0, buffer.capacity() + 1, (byte) 0);\n+            fail();\n+        } catch (IndexOutOfBoundsException expected) {\n+            // expected\n+        }\n+\n+        try {\n+            buffer.indexOf(buffer.capacity(), -1, (byte) 0);\n+            fail();\n+        } catch (IndexOutOfBoundsException expected) {\n+            // expected\n+        }\n+\n+        assertEquals(4, buffer.indexOf(buffer.capacity() + 1, 0, (byte) 1));\n+        assertEquals(0, buffer.indexOf(-1, buffer.capacity(), (byte) 1));\n+    }\n+\n+    @Test\n+    public void testIndexOfReleaseBuffer() {\n+        ByteBuf buffer = releasedBuffer();\n+        if (buffer.capacity() != 0) {\n+            try {\n+                buffer.indexOf(0, 1, (byte) 1);\n+                fail();\n+            } catch (IllegalReferenceCountException expected) {\n+                // expected\n+            }\n+        } else {\n+            assertEquals(-1, buffer.indexOf(0, 1, (byte) 1));\n+        }\n     }\n \n     @Test\n@@ -2570,7 +2605,6 @@ public void testLittleEndianWithExpand() {\n \n     private ByteBuf releasedBuffer() {\n         ByteBuf buffer = newBuffer(8);\n-\n         // Clear the buffer so we are sure the reader and writer indices are 0.\n         // This is important as we may return a slice from newBuffer(...).\n         buffer.clear();"
        }
      ]
    },
    {
      "type": "comment",
      "id": 524222627,
      "created_at": "2019-08-23T08:22:50Z",
      "user": "normanmaurer",
      "body": "@njhill @franz1981 I added some more tests to ensure the range checks and ref-count checks behave like with the old implementation "
    },
    {
      "type": "comment",
      "id": 524312044,
      "created_at": "2019-08-23T13:18:46Z",
      "user": "franz1981",
      "body": "Good for the tests, LGTM!"
    },
    {
      "type": "review",
      "id": 278992889,
      "created_at": "2019-08-23T13:18:56Z",
      "user": "franz1981",
      "state": "APPROVED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 524388843,
      "created_at": "2019-08-23T16:59:30Z",
      "user": "normanmaurer",
      "body": "@njhill once you approve I will merge :)"
    },
    {
      "type": "comment",
      "id": 524396051,
      "created_at": "2019-08-23T17:23:07Z",
      "user": "njhill",
      "body": "@normanmaurer sorry late start today ... +1 for the extra tests. What I was suggesting doesn't change the prior behaviour, just removes some redundant checks and is less code. The tests still pass fine."
    },
    {
      "type": "comment",
      "id": 524396346,
      "created_at": "2019-08-23T17:24:01Z",
      "user": "normanmaurer",
      "body": "@njhill maybe I missunderstand you then... can you show a diff ?"
    },
    {
      "type": "comment",
      "id": 524397412,
      "created_at": "2019-08-23T17:27:24Z",
      "user": "njhill",
      "body": "@normanmaurer \r\n![image](https://user-images.githubusercontent.com/16958488/63611360-9064d300-c590-11e9-9904-2cbdcc180544.png)\r\n"
    },
    {
      "type": "comment",
      "id": 524397862,
      "created_at": "2019-08-23T17:28:55Z",
      "user": "normanmaurer",
      "body": "@njhill would this be different for `capacity() == 0` ?"
    },
    {
      "type": "comment",
      "id": 524403344,
      "created_at": "2019-08-23T17:46:30Z",
      "user": "njhill",
      "body": "doh you're right, sorry!! But I think that's a special case for whatever reason (strange rules). So that could just be added in isolation up front, even in the `indexOf` method directly since it has nothing to do with the supplied parameters"
    },
    {
      "type": "review",
      "id": 279145571,
      "created_at": "2019-08-23T18:03:45Z",
      "user": "njhill",
      "state": "APPROVED",
      "body": "Maybe I am being OCD w.r.t. the redundant checks, don't let me hold it up if you just want to merge!"
    },
    {
      "type": "comment",
      "id": 524425674,
      "created_at": "2019-08-23T18:56:23Z",
      "user": "normanmaurer",
      "body": "@njhill yeah lets keep it then.. "
    }
  ]
}