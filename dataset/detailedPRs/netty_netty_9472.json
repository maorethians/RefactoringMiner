{
  "repo": "netty/netty",
  "number": 9472,
  "pr_number": 9472,
  "title": "Ensure we replace WebSocketServerProtocolHandshakeHandler before doin…",
  "user": "normanmaurer",
  "created_at": "2019-08-16T08:37:07Z",
  "merged_at": "2019-08-17T08:00:02Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "bcad76e2db425fc319d881c1ad1041226c536c92",
  "merge_commit_sha": "839aab855844e23f0f0d394155baa2d0d63507ff",
  "commits": 1,
  "issue_events": [
    "mentioned",
    "subscribed",
    "milestoned",
    "mentioned",
    "subscribed",
    "review_requested",
    "review_requested",
    "referenced",
    "merged",
    "closed",
    "head_ref_deleted",
    "referenced",
    "demilestoned",
    "milestoned"
  ],
  "url": "https://github.com/netty/netty/pull/9472",
  "timeline": [
    {
      "type": "commit",
      "date": "2019-08-16T08:35:08Z",
      "sha": "648d5813ff4ccac1ecba9439e6ca76026cc97832",
      "message": "Ensure we replace WebSocketServerProtocolHandshakeHandler before doing the handshake\n\nMotivation:\n\nWe need to ensure we replace WebSocketServerProtocolHandshakeHandler before doing the actual handshake as the handshake itself may complete directly and so forward pending bytes through the pipeline.\n\nModifications:\n\nReplace the handler before doing the actual handshake.\n\nResult:\n\nFixes https://github.com/netty/netty/issues/9471.",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2019-08-16T08:35:08Z",
      "author_date": "2019-08-16T08:35:08Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2019-08-16T08:35:08Z",
      "diff_text": "codec-http/src/main/java/io/netty/handler/codec/http/websocketx/WebSocketServerProtocolHandshakeHandler.java\n@@ -88,6 +88,15 @@ public void channelRead(final ChannelHandlerContext ctx, Object msg) throws Exce\n             if (handshaker == null) {\n                 WebSocketServerHandshakerFactory.sendUnsupportedVersionResponse(ctx.channel());\n             } else {\n+                // Ensure we set the handshaker and replace this handler before we\n+                // trigger the actual handshake. Otherwise we may receive websocket bytes in this handler\n+                // before we had a chance to replace it.\n+                //\n+                // See https://github.com/netty/netty/issues/9471.\n+                WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);\n+                ctx.pipeline().replace(this, \"WS403Responder\",\n+                        WebSocketServerProtocolHandler.forbiddenHttpRequestResponder());\n+\n                 final ChannelFuture handshakeFuture = handshaker.handshake(ctx.channel(), req);\n                 handshakeFuture.addListener(new ChannelFutureListener() {\n                     @Override\n@@ -107,9 +116,6 @@ public void operationComplete(ChannelFuture future) throws Exception {\n                     }\n                 });\n                 applyHandshakeTimeout();\n-                WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);\n-                ctx.pipeline().replace(this, \"WS403Responder\",\n-                        WebSocketServerProtocolHandler.forbiddenHttpRequestResponder());\n             }\n         } finally {\n             req.release();\ncodec-http/src/test/java/io/netty/handler/codec/http/websocketx/WebSocketServerProtocolHandlerTest.java\n@@ -63,6 +63,28 @@ public void testHttpUpgradeRequest() throws Exception {\n         assertFalse(ch.finish());\n     }\n \n+    @Test\n+    public void testWebSocketServerProtocolHandshakeHandlerReplacedBeforeHandshake() throws Exception {\n+        EmbeddedChannel ch = createChannel(new MockOutboundHandler());\n+        ChannelHandlerContext handshakerCtx = ch.pipeline().context(WebSocketServerProtocolHandshakeHandler.class);\n+        ch.pipeline().addLast(new ChannelInboundHandlerAdapter() {\n+            @Override\n+            public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n+                if (evt instanceof WebSocketServerProtocolHandler.HandshakeComplete) {\n+                    // We should have removed the handler already.\n+                    assertNull(ctx.pipeline().context(WebSocketServerProtocolHandshakeHandler.class));\n+                }\n+            }\n+        });\n+        writeUpgradeRequest(ch);\n+\n+        FullHttpResponse response = responses.remove();\n+        assertEquals(SWITCHING_PROTOCOLS, response.status());\n+        response.release();\n+        assertNotNull(WebSocketServerProtocolHandler.getHandshaker(handshakerCtx.channel()));\n+        assertFalse(ch.finish());\n+    }\n+\n     @Test\n     public void testSubsequentHttpRequestsAfterUpgradeShouldReturn403() throws Exception {\n         EmbeddedChannel ch = createChannel();",
      "diff": [
        {
          "file": "codec-http/src/main/java/io/netty/handler/codec/http/websocketx/WebSocketServerProtocolHandshakeHandler.java",
          "patch": "@@ -88,6 +88,15 @@ public void channelRead(final ChannelHandlerContext ctx, Object msg) throws Exce\n             if (handshaker == null) {\n                 WebSocketServerHandshakerFactory.sendUnsupportedVersionResponse(ctx.channel());\n             } else {\n+                // Ensure we set the handshaker and replace this handler before we\n+                // trigger the actual handshake. Otherwise we may receive websocket bytes in this handler\n+                // before we had a chance to replace it.\n+                //\n+                // See https://github.com/netty/netty/issues/9471.\n+                WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);\n+                ctx.pipeline().replace(this, \"WS403Responder\",\n+                        WebSocketServerProtocolHandler.forbiddenHttpRequestResponder());\n+\n                 final ChannelFuture handshakeFuture = handshaker.handshake(ctx.channel(), req);\n                 handshakeFuture.addListener(new ChannelFutureListener() {\n                     @Override\n@@ -107,9 +116,6 @@ public void operationComplete(ChannelFuture future) throws Exception {\n                     }\n                 });\n                 applyHandshakeTimeout();\n-                WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);\n-                ctx.pipeline().replace(this, \"WS403Responder\",\n-                        WebSocketServerProtocolHandler.forbiddenHttpRequestResponder());\n             }\n         } finally {\n             req.release();"
        },
        {
          "file": "codec-http/src/test/java/io/netty/handler/codec/http/websocketx/WebSocketServerProtocolHandlerTest.java",
          "patch": "@@ -63,6 +63,28 @@ public void testHttpUpgradeRequest() throws Exception {\n         assertFalse(ch.finish());\n     }\n \n+    @Test\n+    public void testWebSocketServerProtocolHandshakeHandlerReplacedBeforeHandshake() throws Exception {\n+        EmbeddedChannel ch = createChannel(new MockOutboundHandler());\n+        ChannelHandlerContext handshakerCtx = ch.pipeline().context(WebSocketServerProtocolHandshakeHandler.class);\n+        ch.pipeline().addLast(new ChannelInboundHandlerAdapter() {\n+            @Override\n+            public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception {\n+                if (evt instanceof WebSocketServerProtocolHandler.HandshakeComplete) {\n+                    // We should have removed the handler already.\n+                    assertNull(ctx.pipeline().context(WebSocketServerProtocolHandshakeHandler.class));\n+                }\n+            }\n+        });\n+        writeUpgradeRequest(ch);\n+\n+        FullHttpResponse response = responses.remove();\n+        assertEquals(SWITCHING_PROTOCOLS, response.status());\n+        response.release();\n+        assertNotNull(WebSocketServerProtocolHandler.getHandshaker(handshakerCtx.channel()));\n+        assertFalse(ch.finish());\n+    }\n+\n     @Test\n     public void testSubsequentHttpRequestsAfterUpgradeShouldReturn403() throws Exception {\n         EmbeddedChannel ch = createChannel();"
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2019-08-16T08:37:07Z",
      "user": "normanmaurer",
      "body": "…g the handshake\r\n\r\nMotivation:\r\n\r\nWe need to ensure we replace WebSocketServerProtocolHandshakeHandler before doing the actual handshake as the handshake itself may complete directly and so forward pending bytes through the pipeline.\r\n\r\nModifications:\r\n\r\nReplace the handler before doing the actual handshake.\r\n\r\nResult:\r\n\r\nFixes https://github.com/netty/netty/issues/9471."
    },
    {
      "type": "comment",
      "id": 521959052,
      "created_at": "2019-08-16T10:07:41Z",
      "user": "normanmaurer",
      "body": "@netty-bot test this please "
    },
    {
      "type": "review_comment",
      "id": 314848739,
      "created_at": "2019-08-16T18:53:24Z",
      "user": "njhill",
      "body": "Unrelated to the fix but could simplify this to:\r\n```java\r\nhandshaker.handshake(ctx.channel(), req).addListener(new ChannelFutureListener() {\r\n    ...\r\n```",
      "path": "codec-http/src/main/java/io/netty/handler/codec/http/websocketx/WebSocketServerProtocolHandshakeHandler.java",
      "diff_hunk": "@@ -88,6 +88,15 @@ public void channelRead(final ChannelHandlerContext ctx, Object msg) throws Exce\n             if (handshaker == null) {\n                 WebSocketServerHandshakerFactory.sendUnsupportedVersionResponse(ctx.channel());\n             } else {\n+                // Ensure we set the handshaker and replace this handler before we\n+                // trigger the actual handshake. Otherwise we may receive websocket bytes in this handler\n+                // before we had a chance to replace it.\n+                //\n+                // See https://github.com/netty/netty/issues/9471.\n+                WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);\n+                ctx.pipeline().replace(this, \"WS403Responder\",\n+                        WebSocketServerProtocolHandler.forbiddenHttpRequestResponder());\n+\n                 final ChannelFuture handshakeFuture = handshaker.handshake(ctx.channel(), req);\n                 handshakeFuture.addListener(new ChannelFutureListener() {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 101,
      "original_line": 101,
      "side": "RIGHT",
      "original_position": 14,
      "position": 14,
      "subject_type": "line",
      "reply": [
        {
          "id": 314939038,
          "user": "normanmaurer",
          "body": "will do as a followup",
          "created_at": "2019-08-17T07:59:54Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 276115276,
      "created_at": "2019-08-16T18:53:31Z",
      "user": "njhill",
      "state": "APPROVED",
      "body": "LGTM"
    },
    {
      "type": "review",
      "id": 276228486,
      "created_at": "2019-08-17T07:59:54Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    }
  ]
}