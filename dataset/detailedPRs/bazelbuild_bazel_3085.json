{
  "repo": "bazelbuild/bazel",
  "number": 3085,
  "pr_number": 3085,
  "title": "Remote caching: don't crash for actions with no inputs",
  "user": "ulfjack",
  "created_at": "2017-05-31T09:58:28Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [
    "cla: yes"
  ],
  "base_commit": "74cea8368d00ed222b5afee4022208022918eb8f",
  "merge_commit_sha": "578136c61bec088a769a5e56219c221a8ce2a0ed",
  "commits": 1,
  "issue_events": [
    "labeled",
    "review_requested",
    "head_ref_force_pushed",
    "head_ref_force_pushed",
    "closed",
    "head_ref_deleted"
  ],
  "url": "https://github.com/bazelbuild/bazel/pull/3085",
  "timeline": [
    {
      "type": "description",
      "created_at": "2017-05-31T09:58:28Z",
      "user": "ulfjack",
      "body": "Fixes #3004.\r\n\r\nChange-Id: I6dcef92e9f23df574e0874a81b2901754042cf9a"
    },
    {
      "type": "review_comment",
      "id": 119330657,
      "created_at": "2017-05-31T11:10:15Z",
      "user": "philwo",
      "body": "Maybe add:\r\n// Handle actions without inputs.",
      "path": "src/main/java/com/google/devtools/build/lib/remote/TreeNodeRepository.java",
      "diff_hunk": "@@ -264,6 +264,10 @@ private TreeNode buildParentNode(\n       int inputsStart,\n       int inputsEnd,\n       int segmentIndex) {\n+    if (segments.isEmpty()) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 273,
      "original_line": 273,
      "side": "RIGHT",
      "original_position": 4,
      "position": 31,
      "subject_type": "line",
      "reply": [
        {
          "id": 119332258,
          "user": "ulfjack",
          "body": "Done.",
          "created_at": "2017-05-31T11:20:46Z"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 119330756,
      "created_at": "2017-05-31T11:10:52Z",
      "user": "philwo",
      "body": "Can we instead store \"new TreeNode(ImmutableList.<TreeNode.ChildEntry>of())\" in a static final field and use that instead of calling interner.intern on a new instance everytime?",
      "path": "src/main/java/com/google/devtools/build/lib/remote/TreeNodeRepository.java",
      "diff_hunk": "@@ -264,6 +264,10 @@ private TreeNode buildParentNode(\n       int inputsStart,\n       int inputsEnd,\n       int segmentIndex) {\n+    if (segments.isEmpty()) {\n+      Preconditions.checkState(inputs.isEmpty());\n+      return interner.intern(new TreeNode(ImmutableList.<TreeNode.ChildEntry>of()));",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": null,
      "original_line": 269,
      "side": "RIGHT",
      "original_position": 6,
      "position": 1,
      "subject_type": "line",
      "reply": []
    },
    {
      "type": "review",
      "id": 41198854,
      "created_at": "2017-05-31T11:11:05Z",
      "user": "philwo",
      "state": "CHANGES_REQUESTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 41200671,
      "created_at": "2017-05-31T11:20:46Z",
      "user": "ulfjack",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "commit",
      "date": "2017-05-31T11:21:28Z",
      "sha": "eecbfa97edd8f29a4feca189c3823fa99ce88147",
      "message": "Remote caching: don't crash for actions with no inputs\n\nFixes #3004.\n\nChange-Id: I6dcef92e9f23df574e0874a81b2901754042cf9a",
      "author": "Ulf Adams",
      "author_email": "ulfjack@google.com",
      "author_raw_date": "2017-05-31T09:54:27Z",
      "author_date": "2017-05-31T09:54:27Z",
      "committer": "Ulf Adams",
      "committer_email": "ulfjack@google.com",
      "raw_date": "2017-05-31T11:21:28Z",
      "diff_text": "src/main/java/com/google/devtools/build/lib/remote/TreeNodeRepository.java\n@@ -63,11 +63,15 @@ public static final class TreeNode {\n \n     private final int hashCode;\n     private final ImmutableList<ChildEntry> childEntries; // no need to make it a map thus far.\n-    @Nullable private final ActionInput actionInput; // Null iff this is a directory.\n+    @Nullable\n+    private final ActionInput actionInput; // Null iff this is a directory.\n \n-    /** A pair of path segment, TreeNode. */\n+    /**\n+     * A pair of path segment, TreeNode.\n+     */\n     @Immutable\n     public static final class ChildEntry {\n+\n       private final String segment;\n       private final TreeNode child;\n \n@@ -176,6 +180,8 @@ public String toDebugString() {\n     }\n   }\n \n+  private static final TreeNode EMPTY_NODE = new TreeNode(ImmutableList.<TreeNode.ChildEntry>of());\n+\n   // Keep only one canonical instance of every TreeNode in the repository.\n   private final Interner<TreeNode> interner = BlazeInterners.newWeakInterner();\n   // Merkle hashes are computed and cached by the repository, therefore execRoot must\n@@ -264,6 +270,12 @@ private TreeNode buildParentNode(\n       int inputsStart,\n       int inputsEnd,\n       int segmentIndex) {\n+    if (segments.isEmpty()) {\n+      // We sometimes have actions with no inputs (e.g., echo \"xyz\" > $@), so we need to handle that\n+      // case here.\n+      Preconditions.checkState(inputs.isEmpty());\n+      return EMPTY_NODE;\n+    }\n     if (segmentIndex == segments.get(inputsStart).size()) {\n       // Leaf node reached. Must be unique.\n       Preconditions.checkArgument(\nsrc/test/java/com/google/devtools/build/lib/remote/TreeNodeRepositoryTest.java\n@@ -152,4 +152,14 @@ public void testNullArtifacts() throws Exception {\n         .containsExactly(\n             rootDigest, aDigest, barDigest, barContentsDigest, fooDigest, fooContentsDigest);\n   }\n+\n+  @Test\n+  public void testEmptyTree() throws Exception {\n+    SortedMap<PathFragment, ActionInput> inputs = new TreeMap<>();\n+    TreeNodeRepository repo = createTestTreeNodeRepository();\n+    TreeNode root = repo.buildFromActionInputs(inputs);\n+    repo.computeMerkleDigests(root);\n+\n+    assertThat(root.getChildEntries()).isEmpty();\n+  }\n }\nsrc/test/shell/bazel/remote_execution_test.sh\n@@ -224,6 +224,33 @@ EOF\n       || fail \"Failed to run //a:test with remote execution\"\n }\n \n+function test_noinput_action() {\n+  mkdir -p a\n+  cat > a/rule.bzl <<'EOF'\n+def _impl(ctx):\n+  output = ctx.outputs.out\n+  ctx.action(\n+      outputs=[output],\n+      command=\"echo 'Hello World' > %s\" % (output.path))\n+\n+empty = rule(\n+    implementation=_impl,\n+    outputs={\"out\": \"%{name}.txt\"},\n+)\n+EOF\n+  cat > a/BUILD <<'EOF'\n+load(\"//a:rule.bzl\", \"empty\")\n+package(default_visibility = [\"//visibility:public\"])\n+empty(name = 'test')\n+EOF\n+  bazel --host_jvm_args=-Dbazel.DigestFunction=SHA1 build \\\n+      --spawn_strategy=remote \\\n+      --remote_cache=localhost:${worker_port} \\\n+      --test_output=errors \\\n+      //a:test >& $TEST_log \\\n+      || fail \"Failed to run //a:test with remote execution\"\n+}\n+\n # TODO(alpha): Add a test that fails remote execution when remote worker\n # supports sandbox.\n ",
      "diff": [
        {
          "file": "src/main/java/com/google/devtools/build/lib/remote/TreeNodeRepository.java",
          "patch": "@@ -63,11 +63,15 @@ public static final class TreeNode {\n \n     private final int hashCode;\n     private final ImmutableList<ChildEntry> childEntries; // no need to make it a map thus far.\n-    @Nullable private final ActionInput actionInput; // Null iff this is a directory.\n+    @Nullable\n+    private final ActionInput actionInput; // Null iff this is a directory.\n \n-    /** A pair of path segment, TreeNode. */\n+    /**\n+     * A pair of path segment, TreeNode.\n+     */\n     @Immutable\n     public static final class ChildEntry {\n+\n       private final String segment;\n       private final TreeNode child;\n \n@@ -176,6 +180,8 @@ public String toDebugString() {\n     }\n   }\n \n+  private static final TreeNode EMPTY_NODE = new TreeNode(ImmutableList.<TreeNode.ChildEntry>of());\n+\n   // Keep only one canonical instance of every TreeNode in the repository.\n   private final Interner<TreeNode> interner = BlazeInterners.newWeakInterner();\n   // Merkle hashes are computed and cached by the repository, therefore execRoot must\n@@ -264,6 +270,12 @@ private TreeNode buildParentNode(\n       int inputsStart,\n       int inputsEnd,\n       int segmentIndex) {\n+    if (segments.isEmpty()) {\n+      // We sometimes have actions with no inputs (e.g., echo \"xyz\" > $@), so we need to handle that\n+      // case here.\n+      Preconditions.checkState(inputs.isEmpty());\n+      return EMPTY_NODE;\n+    }\n     if (segmentIndex == segments.get(inputsStart).size()) {\n       // Leaf node reached. Must be unique.\n       Preconditions.checkArgument("
        },
        {
          "file": "src/test/java/com/google/devtools/build/lib/remote/TreeNodeRepositoryTest.java",
          "patch": "@@ -152,4 +152,14 @@ public void testNullArtifacts() throws Exception {\n         .containsExactly(\n             rootDigest, aDigest, barDigest, barContentsDigest, fooDigest, fooContentsDigest);\n   }\n+\n+  @Test\n+  public void testEmptyTree() throws Exception {\n+    SortedMap<PathFragment, ActionInput> inputs = new TreeMap<>();\n+    TreeNodeRepository repo = createTestTreeNodeRepository();\n+    TreeNode root = repo.buildFromActionInputs(inputs);\n+    repo.computeMerkleDigests(root);\n+\n+    assertThat(root.getChildEntries()).isEmpty();\n+  }\n }"
        },
        {
          "file": "src/test/shell/bazel/remote_execution_test.sh",
          "patch": "@@ -224,6 +224,33 @@ EOF\n       || fail \"Failed to run //a:test with remote execution\"\n }\n \n+function test_noinput_action() {\n+  mkdir -p a\n+  cat > a/rule.bzl <<'EOF'\n+def _impl(ctx):\n+  output = ctx.outputs.out\n+  ctx.action(\n+      outputs=[output],\n+      command=\"echo 'Hello World' > %s\" % (output.path))\n+\n+empty = rule(\n+    implementation=_impl,\n+    outputs={\"out\": \"%{name}.txt\"},\n+)\n+EOF\n+  cat > a/BUILD <<'EOF'\n+load(\"//a:rule.bzl\", \"empty\")\n+package(default_visibility = [\"//visibility:public\"])\n+empty(name = 'test')\n+EOF\n+  bazel --host_jvm_args=-Dbazel.DigestFunction=SHA1 build \\\n+      --spawn_strategy=remote \\\n+      --remote_cache=localhost:${worker_port} \\\n+      --test_output=errors \\\n+      //a:test >& $TEST_log \\\n+      || fail \"Failed to run //a:test with remote execution\"\n+}\n+\n # TODO(alpha): Add a test that fails remote execution when remote worker\n # supports sandbox.\n "
        }
      ]
    },
    {
      "type": "comment",
      "id": 305170374,
      "created_at": "2017-05-31T12:05:51Z",
      "user": "ulfjack",
      "body": "PTAL"
    },
    {
      "type": "review",
      "id": 41211258,
      "created_at": "2017-05-31T12:13:10Z",
      "user": "philwo",
      "state": "APPROVED",
      "body": ""
    }
  ]
}