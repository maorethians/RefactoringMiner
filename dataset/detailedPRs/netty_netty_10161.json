{
  "repo": "netty/netty",
  "number": 10161,
  "pr_number": 10161,
  "title": "SslHandler should fail handshake / close promise and notify pipeline …",
  "user": "normanmaurer",
  "created_at": "2020-04-02T13:41:51Z",
  "merged_at": "2020-04-03T06:45:48Z",
  "merged": true,
  "state": "closed",
  "labels": [],
  "base_commit": "db39b10249b9a619b14ada376ac178485cd3a328",
  "merge_commit_sha": "21f375fa9080f1ed55aedf7977568bd3a3fa2e4a",
  "commits": 1,
  "issue_events": [
    "review_requested",
    "review_requested",
    "review_requested",
    "milestoned",
    "review_requested",
    "head_ref_force_pushed",
    "referenced",
    "merged",
    "closed",
    "head_ref_deleted",
    "referenced",
    "referenced"
  ],
  "url": "https://github.com/netty/netty/pull/10161",
  "timeline": [
    {
      "type": "description",
      "created_at": "2020-04-02T13:41:51Z",
      "user": "normanmaurer",
      "body": "…on removal\r\n\r\nMotivation:\r\n\r\nIf the SslHandler is removed from the pipeline we also need to ensure we fail the handshake / close promise if it was not notified before as otherwise we may never do so.\r\n\r\nModifications:\r\n\r\n- Correctly fail promise and notify pipeline if handshake was not done yet when the SslHandler is removed\r\n- Add unit test\r\n\r\nResult:\r\n\r\nFix https://github.com/netty/netty/issues/10158"
    },
    {
      "type": "commit",
      "date": "2020-04-02T14:45:38Z",
      "sha": "ccedbe52dce84995d657b513c9fa13bc434b4f53",
      "message": "SslHandler should fail handshake / close promise and notify pipeline on removal\n\nMotivation:\n\nIf the SslHandler is removed from the pipeline we also need to ensure we fail the handshake / close promise if it was not notified before as otherwise we may never do so.\n\nModifications:\n\n- Correctly fail promise and notify pipeline if handshake was not done yet when the SslHandler is removed\n- Add unit test\n\nResult:\n\nFix https://github.com/netty/netty/issues/10158",
      "author": "Norman Maurer",
      "author_email": "norman_maurer@apple.com",
      "author_raw_date": "2020-04-02T13:28:45Z",
      "author_date": "2020-04-02T13:28:45Z",
      "committer": "Norman Maurer",
      "committer_email": "norman_maurer@apple.com",
      "raw_date": "2020-04-02T14:45:38Z",
      "diff_text": "handler/src/main/java/io/netty/handler/ssl/SslHandler.java\n@@ -68,6 +68,7 @@\n import javax.net.ssl.SSLEngineResult.HandshakeStatus;\n import javax.net.ssl.SSLEngineResult.Status;\n import javax.net.ssl.SSLException;\n+import javax.net.ssl.SSLHandshakeException;\n import javax.net.ssl.SSLSession;\n \n import static io.netty.buffer.ByteBufUtil.ensureWritableSuccess;\n@@ -691,6 +692,24 @@ public void handlerRemoved0(ChannelHandlerContext ctx) throws Exception {\n                     new ChannelException(\"Pending write on removal of SslHandler\"));\n         }\n         pendingUnencryptedWrites = null;\n+\n+        SSLHandshakeException cause = null;\n+\n+        // If the handshake is not done yet we should fail the handshake promise and notify the rest of the\n+        // pipeline.\n+        if (!handshakePromise.isDone()) {\n+            cause = new SSLHandshakeException(\"SslHandler removed before handshake completed\");\n+            if (handshakePromise.tryFailure(cause)) {\n+                ctx.fireUserEventTriggered(new SslHandshakeCompletionEvent(cause));\n+            }\n+        }\n+        if (!sslClosePromise.isDone()) {\n+            if (cause == null) {\n+                cause = new SSLHandshakeException(\"SslHandler removed before handshake completed\");\n+            }\n+            notifyClosePromise(cause);\n+        }\n+\n         if (engine instanceof ReferenceCounted) {\n             ((ReferenceCounted) engine).release();\n         }\nhandler/src/test/java/io/netty/handler/ssl/SslHandlerTest.java\n@@ -67,7 +67,6 @@\n import java.net.InetSocketAddress;\n import java.net.Socket;\n import java.nio.channels.ClosedChannelException;\n-import java.security.KeyStore;\n import java.security.NoSuchAlgorithmException;\n import java.security.cert.CertificateException;\n import java.security.cert.X509Certificate;\n@@ -85,12 +84,10 @@\n import java.util.concurrent.atomic.AtomicBoolean;\n import java.util.concurrent.atomic.AtomicReference;\n \n-import javax.net.ssl.ManagerFactoryParameters;\n import javax.net.ssl.SSLContext;\n import javax.net.ssl.SSLEngine;\n import javax.net.ssl.SSLException;\n import javax.net.ssl.SSLProtocolException;\n-import javax.net.ssl.TrustManager;\n import javax.net.ssl.X509ExtendedTrustManager;\n \n import static io.netty.buffer.Unpooled.wrappedBuffer;\n@@ -255,6 +252,43 @@ private static void testHandshakeTimeout(boolean client) throws Exception {\n         }\n     }\n \n+    @Test(timeout = 5000L)\n+    public void testHandshakeAndClosePromiseFailedOnRemoval() throws Exception {\n+        SSLEngine engine = SSLContext.getDefault().createSSLEngine();\n+        engine.setUseClientMode(true);\n+        SslHandler handler = new SslHandler(engine);\n+        final AtomicReference<Throwable> handshakeRef = new AtomicReference<Throwable>();\n+        final AtomicReference<Throwable> closeRef = new AtomicReference<Throwable>();\n+        EmbeddedChannel ch = new EmbeddedChannel(handler, new ChannelInboundHandlerAdapter() {\n+            @Override\n+            public void userEventTriggered(ChannelHandlerContext ctx, Object evt) {\n+                if (evt instanceof SslHandshakeCompletionEvent) {\n+                    handshakeRef.set(((SslHandshakeCompletionEvent) evt).cause());\n+                } else if (evt instanceof SslCloseCompletionEvent) {\n+                    closeRef.set(((SslCloseCompletionEvent) evt).cause());\n+                }\n+            }\n+        });\n+        assertFalse(handler.handshakeFuture().isDone());\n+        assertFalse(handler.sslCloseFuture().isDone());\n+\n+        ch.pipeline().remove(handler);\n+\n+        try {\n+            while (!handler.handshakeFuture().isDone() || handshakeRef.get() == null\n+                    || !handler.sslCloseFuture().isDone() || closeRef.get() == null) {\n+                Thread.sleep(10);\n+                // Continue running all pending tasks until we notified for everything.\n+                ch.runPendingTasks();\n+            }\n+\n+            assertSame(handler.handshakeFuture().cause(), handshakeRef.get());\n+            assertSame(handler.sslCloseFuture().cause(), closeRef.get());\n+        } finally {\n+            ch.finishAndReleaseAll();\n+        }\n+    }\n+\n     @Test\n     public void testTruncatedPacket() throws Exception {\n         SSLEngine engine = newServerModeSSLEngine();",
      "diff": [
        {
          "file": "handler/src/main/java/io/netty/handler/ssl/SslHandler.java",
          "patch": "@@ -68,6 +68,7 @@\n import javax.net.ssl.SSLEngineResult.HandshakeStatus;\n import javax.net.ssl.SSLEngineResult.Status;\n import javax.net.ssl.SSLException;\n+import javax.net.ssl.SSLHandshakeException;\n import javax.net.ssl.SSLSession;\n \n import static io.netty.buffer.ByteBufUtil.ensureWritableSuccess;\n@@ -691,6 +692,24 @@ public void handlerRemoved0(ChannelHandlerContext ctx) throws Exception {\n                     new ChannelException(\"Pending write on removal of SslHandler\"));\n         }\n         pendingUnencryptedWrites = null;\n+\n+        SSLHandshakeException cause = null;\n+\n+        // If the handshake is not done yet we should fail the handshake promise and notify the rest of the\n+        // pipeline.\n+        if (!handshakePromise.isDone()) {\n+            cause = new SSLHandshakeException(\"SslHandler removed before handshake completed\");\n+            if (handshakePromise.tryFailure(cause)) {\n+                ctx.fireUserEventTriggered(new SslHandshakeCompletionEvent(cause));\n+            }\n+        }\n+        if (!sslClosePromise.isDone()) {\n+            if (cause == null) {\n+                cause = new SSLHandshakeException(\"SslHandler removed before handshake completed\");\n+            }\n+            notifyClosePromise(cause);\n+        }\n+\n         if (engine instanceof ReferenceCounted) {\n             ((ReferenceCounted) engine).release();\n         }"
        },
        {
          "file": "handler/src/test/java/io/netty/handler/ssl/SslHandlerTest.java",
          "patch": "@@ -67,7 +67,6 @@\n import java.net.InetSocketAddress;\n import java.net.Socket;\n import java.nio.channels.ClosedChannelException;\n-import java.security.KeyStore;\n import java.security.NoSuchAlgorithmException;\n import java.security.cert.CertificateException;\n import java.security.cert.X509Certificate;\n@@ -85,12 +84,10 @@\n import java.util.concurrent.atomic.AtomicBoolean;\n import java.util.concurrent.atomic.AtomicReference;\n \n-import javax.net.ssl.ManagerFactoryParameters;\n import javax.net.ssl.SSLContext;\n import javax.net.ssl.SSLEngine;\n import javax.net.ssl.SSLException;\n import javax.net.ssl.SSLProtocolException;\n-import javax.net.ssl.TrustManager;\n import javax.net.ssl.X509ExtendedTrustManager;\n \n import static io.netty.buffer.Unpooled.wrappedBuffer;\n@@ -255,6 +252,43 @@ private static void testHandshakeTimeout(boolean client) throws Exception {\n         }\n     }\n \n+    @Test(timeout = 5000L)\n+    public void testHandshakeAndClosePromiseFailedOnRemoval() throws Exception {\n+        SSLEngine engine = SSLContext.getDefault().createSSLEngine();\n+        engine.setUseClientMode(true);\n+        SslHandler handler = new SslHandler(engine);\n+        final AtomicReference<Throwable> handshakeRef = new AtomicReference<Throwable>();\n+        final AtomicReference<Throwable> closeRef = new AtomicReference<Throwable>();\n+        EmbeddedChannel ch = new EmbeddedChannel(handler, new ChannelInboundHandlerAdapter() {\n+            @Override\n+            public void userEventTriggered(ChannelHandlerContext ctx, Object evt) {\n+                if (evt instanceof SslHandshakeCompletionEvent) {\n+                    handshakeRef.set(((SslHandshakeCompletionEvent) evt).cause());\n+                } else if (evt instanceof SslCloseCompletionEvent) {\n+                    closeRef.set(((SslCloseCompletionEvent) evt).cause());\n+                }\n+            }\n+        });\n+        assertFalse(handler.handshakeFuture().isDone());\n+        assertFalse(handler.sslCloseFuture().isDone());\n+\n+        ch.pipeline().remove(handler);\n+\n+        try {\n+            while (!handler.handshakeFuture().isDone() || handshakeRef.get() == null\n+                    || !handler.sslCloseFuture().isDone() || closeRef.get() == null) {\n+                Thread.sleep(10);\n+                // Continue running all pending tasks until we notified for everything.\n+                ch.runPendingTasks();\n+            }\n+\n+            assertSame(handler.handshakeFuture().cause(), handshakeRef.get());\n+            assertSame(handler.sslCloseFuture().cause(), closeRef.get());\n+        } finally {\n+            ch.finishAndReleaseAll();\n+        }\n+    }\n+\n     @Test\n     public void testTruncatedPacket() throws Exception {\n         SSLEngine engine = newServerModeSSLEngine();"
        }
      ]
    },
    {
      "type": "review_comment",
      "id": 402436436,
      "created_at": "2020-04-02T16:12:56Z",
      "user": "NiteshKant",
      "body": "Should we be considering the case when `ctx.fireUserEventTriggered(...)` above throws and we will miss calling `notifyClosePromise(cause);`",
      "path": "handler/src/main/java/io/netty/handler/ssl/SslHandler.java",
      "diff_hunk": "@@ -691,6 +692,24 @@ public void handlerRemoved0(ChannelHandlerContext ctx) throws Exception {\n                     new ChannelException(\"Pending write on removal of SslHandler\"));\n         }\n         pendingUnencryptedWrites = null;\n+\n+        SSLHandshakeException cause = null;\n+\n+        // If the handshake is not done yet we should fail the handshake promise and notify the rest of the\n+        // pipeline.\n+        if (!handshakePromise.isDone()) {\n+            cause = new SSLHandshakeException(\"SslHandler removed before handshake completed\");\n+            if (handshakePromise.tryFailure(cause)) {\n+                ctx.fireUserEventTriggered(new SslHandshakeCompletionEvent(cause));\n+            }\n+        }\n+        if (!sslClosePromise.isDone()) {",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 706,
      "original_line": 706,
      "side": "RIGHT",
      "original_position": 23,
      "position": 23,
      "subject_type": "line",
      "reply": [
        {
          "id": 402442468,
          "user": "normanmaurer",
          "body": "ctx.fireUserEventTriggered(...) will never throw by definition... ",
          "created_at": "2020-04-02T16:21:56Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 386581855,
      "created_at": "2020-04-02T16:13:18Z",
      "user": "NiteshKant",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 386589420,
      "created_at": "2020-04-02T16:21:57Z",
      "user": "normanmaurer",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "review",
      "id": 386619660,
      "created_at": "2020-04-02T16:59:17Z",
      "user": "carl-mastrangelo",
      "state": "APPROVED",
      "body": "LGTM"
    },
    {
      "type": "review",
      "id": 386625543,
      "created_at": "2020-04-02T17:06:52Z",
      "user": "NiteshKant",
      "state": "APPROVED",
      "body": ""
    },
    {
      "type": "review",
      "id": 386858028,
      "created_at": "2020-04-03T00:03:57Z",
      "user": "BenD10",
      "state": "APPROVED",
      "body": ""
    }
  ]
}