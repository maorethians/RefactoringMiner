{
  "repo": "elastic/elasticsearch",
  "number": 64876,
  "pr_number": 64876,
  "title": "Introduce listener mechanism in InternalSnapshotsInfoService to help testing",
  "user": "tlrx",
  "created_at": "2020-11-10T15:36:07Z",
  "merged_at": null,
  "merged": false,
  "state": "closed",
  "labels": [
    ">test",
    ":Distributed/CCR",
    "Team:Distributed",
    "v7.11.0",
    "v8.0.0-alpha1"
  ],
  "base_commit": "cf4054cfe5aab4c4a127fab7af0b29f3afb18b3b",
  "merge_commit_sha": "94b41eeb7de02cd12b97f2c77b4fc9e3217deb11",
  "commits": 1,
  "issue_events": [
    "labeled",
    "labeled",
    "labeled",
    "labeled",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "subscribed",
    "labeled",
    "review_requested",
    "closed",
    "head_ref_deleted",
    "unlabeled",
    "labeled"
  ],
  "url": "https://github.com/elastic/elasticsearch/pull/64876",
  "timeline": [
    {
      "type": "commit",
      "date": "2020-11-10T15:29:02Z",
      "sha": "5d9743ccbd58606acb036b59f49c122520ce1d9c",
      "message": "Introduce listener mechanism in InternalSnapshotsInfoService to help tests",
      "author": "Tanguy Leroux",
      "author_email": "tlrx.dev@gmail.com",
      "author_raw_date": "2020-11-10T15:29:02Z",
      "author_date": "2020-11-10T15:29:02Z",
      "committer": "Tanguy Leroux",
      "committer_email": "tlrx.dev@gmail.com",
      "raw_date": "2020-11-10T15:29:02Z",
      "diff_text": "server/src/main/java/org/elasticsearch/snapshots/InternalSnapshotsInfoService.java\n@@ -49,9 +49,11 @@\n import java.util.HashSet;\n import java.util.LinkedHashSet;\n import java.util.LinkedList;\n+import java.util.List;\n import java.util.Objects;\n import java.util.Queue;\n import java.util.Set;\n+import java.util.concurrent.CopyOnWriteArrayList;\n import java.util.function.Supplier;\n \n public class InternalSnapshotsInfoService implements ClusterStateListener, SnapshotsInfoService {\n@@ -67,9 +69,17 @@ public class InternalSnapshotsInfoService implements ClusterStateListener, Snaps\n         e -> logger.debug(\"reroute after snapshot shard size update failed\", e)\n     );\n \n+    public interface Listener {\n+        void onSuccess(SnapshotShard snapshotShard, long size);\n+\n+        default void onFailure(SnapshotShard snapshotShard, Exception failure) {\n+        };\n+    }\n+\n     private final ThreadPool threadPool;\n     private final Supplier<RepositoriesService> repositoriesService;\n     private final Supplier<RerouteService> rerouteService;\n+    private final List<Listener> listeners;\n \n     /** contains the snapshot shards for which the size is known **/\n     private volatile ImmutableOpenMap<SnapshotShard, Long> knownSnapshotShardSizes;\n@@ -99,6 +109,7 @@ public InternalSnapshotsInfoService(\n         this.threadPool = clusterService.getClusterApplierService().threadPool();\n         this.repositoriesService = repositoriesServiceSupplier;\n         this.rerouteService = rerouteServiceSupplier;\n+        this.listeners = new CopyOnWriteArrayList<>();\n         this.knownSnapshotShardSizes = ImmutableOpenMap.of();\n         this.unknownSnapshotShards  = new LinkedHashSet<>();\n         this.failedSnapshotShards  = new LinkedHashSet<>();\n@@ -109,6 +120,8 @@ public InternalSnapshotsInfoService(\n         final ClusterSettings clusterSettings = clusterService.getClusterSettings();\n         clusterSettings.addSettingsUpdateConsumer(INTERNAL_SNAPSHOT_INFO_MAX_CONCURRENT_FETCHES_SETTING, this::setMaxConcurrentFetches);\n         if (DiscoveryNode.isMasterNode(settings)) {\n+            addListener((snapshotShard, size) ->\n+                rerouteService.get().reroute(\"snapshot shard size updated\", Priority.HIGH, REROUTE_LISTENER));\n             clusterService.addListener(this);\n         }\n     }\n@@ -117,6 +130,11 @@ private void setMaxConcurrentFetches(Integer maxConcurrentFetches) {\n         this.maxConcurrentFetches = maxConcurrentFetches;\n     }\n \n+    public void addListener(final Listener listener) {\n+        final boolean added = listeners.add(listener);\n+        assert added : \"cannot add listener twice: \" + listener;\n+    }\n+\n     @Override\n     public SnapshotShardSizeInfo snapshotShardSizes() {\n         synchronized (mutex){\n@@ -237,24 +255,28 @@ protected void doRun() throws Exception {\n                 assert invariant();\n             }\n             if (updated) {\n-                rerouteService.get().reroute(\"snapshot shard size updated\", Priority.HIGH, REROUTE_LISTENER);\n+                listeners.forEach(listener -> listener.onSuccess(snapshotShard, snapshotShardSize));\n             }\n         }\n \n         @Override\n         public void onFailure(Exception e) {\n             logger.warn(() -> new ParameterizedMessage(\"failed to retrieve shard size for {}\", snapshotShard), e);\n+            boolean updated = false;\n             synchronized (mutex) {\n                 if (isMaster) {\n-                    final boolean added = failedSnapshotShards.add(snapshotShard);\n-                    assert added : \"snapshot shard size already failed for \" + snapshotShard;\n+                    updated = failedSnapshotShards.add(snapshotShard);\n+                    assert updated : \"snapshot shard size already failed for \" + snapshotShard;\n                 }\n                 if (removed == false) {\n                     unknownSnapshotShards.remove(snapshotShard);\n                 }\n                 activeFetches -= 1;\n                 assert invariant();\n             }\n+            if (updated) {\n+                listeners.forEach(listener -> listener.onFailure(snapshotShard, e));\n+            }\n         }\n \n         @Override\nx-pack/plugin/ccr/src/internalClusterTest/java/org/elasticsearch/xpack/ccr/CcrRepositoryIT.java\n@@ -23,12 +23,8 @@\n import org.elasticsearch.action.index.IndexRequest;\n import org.elasticsearch.action.support.IndicesOptions;\n import org.elasticsearch.action.support.PlainActionFuture;\n-import org.elasticsearch.cluster.ClusterStateListener;\n-import org.elasticsearch.cluster.RestoreInProgress;\n import org.elasticsearch.cluster.metadata.IndexMetadata;\n import org.elasticsearch.cluster.metadata.MappingMetadata;\n-import org.elasticsearch.cluster.routing.IndexRoutingTable;\n-import org.elasticsearch.cluster.routing.ShardRouting;\n import org.elasticsearch.cluster.service.ClusterService;\n import org.elasticsearch.common.settings.Settings;\n import org.elasticsearch.common.unit.ByteSizeValue;\n@@ -43,10 +39,10 @@\n import org.elasticsearch.repositories.RepositoriesService;\n import org.elasticsearch.repositories.Repository;\n import org.elasticsearch.repositories.RepositoryMissingException;\n+import org.elasticsearch.snapshots.InternalSnapshotsInfoService;\n import org.elasticsearch.snapshots.RestoreInfo;\n import org.elasticsearch.snapshots.RestoreService;\n import org.elasticsearch.snapshots.SnapshotId;\n-import org.elasticsearch.snapshots.SnapshotShardSizeInfo;\n import org.elasticsearch.snapshots.SnapshotsInfoService;\n import org.elasticsearch.test.transport.MockTransportService;\n import org.elasticsearch.transport.TransportActionProxy;\n@@ -63,13 +59,15 @@\n import java.util.List;\n import java.util.Locale;\n import java.util.Map;\n+import java.util.concurrent.ConcurrentHashMap;\n import java.util.concurrent.CountDownLatch;\n import java.util.concurrent.TimeUnit;\n import java.util.concurrent.atomic.AtomicBoolean;\n-import java.util.concurrent.atomic.AtomicInteger;\n \n import static org.elasticsearch.test.hamcrest.ElasticsearchAssertions.assertAcked;\n+import static org.elasticsearch.test.hamcrest.ElasticsearchAssertions.assertHitCount;\n import static org.hamcrest.Matchers.aMapWithSize;\n+import static org.hamcrest.Matchers.containsString;\n import static org.hamcrest.Matchers.equalTo;\n import static org.hamcrest.Matchers.greaterThan;\n import static org.hamcrest.Matchers.instanceOf;\n@@ -465,12 +463,12 @@ public void testFollowerMappingIsUpdated() throws IOException {\n \n     public void testCcrRepositoryFetchesSnapshotShardSizeFromIndexShardStoreStats() throws Exception {\n         final String leaderIndex = \"leader\";\n-        final int numberOfShards = randomIntBetween(1, 2);\n+        final int numberOfShards = randomIntBetween(1, 3);\n         assertAcked(leaderClient().admin().indices().prepareCreate(leaderIndex)\n             .setSource(getIndexSettings(numberOfShards, 0,\n                 Map.of(Store.INDEX_STORE_STATS_REFRESH_INTERVAL_SETTING.getKey(), TimeValue.ZERO.getStringRep())), XContentType.JSON));\n \n-        final int numDocs = scaledRandomIntBetween(0, 1_000);\n+        final int numDocs = scaledRandomIntBetween(0, 100);\n         if (numDocs > 0) {\n             final BulkRequestBuilder bulkRequest = leaderClient().prepareBulk(leaderIndex);\n             for (int i = 0; i < numDocs; i++) {\n@@ -513,99 +511,65 @@ public void testCcrRepositoryFetchesSnapshotShardSizeFromIndexShardStoreStats()\n                 equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n         }\n \n-        final CountDownLatch blockCcrRestore = new CountDownLatch(1);\n+        final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n+        assertThat(snapshotsInfoService, instanceOf(InternalSnapshotsInfoService.class));\n \n-        final List<MockTransportService> transportServices = new ArrayList<>();\n-        for (TransportService transportService : getFollowerCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n-            final MockTransportService mockTransportService = (MockTransportService) transportService;\n-            mockTransportService.addSendBehavior((connection, requestId, action, request, options) -> {\n-                if (action.equals(PutCcrRestoreSessionAction.NAME)) {\n-                    try {\n-                        blockCcrRestore.await();\n-                    } catch (InterruptedException e) {\n-                        throw new AssertionError(e);\n-                    }\n-                }\n-                connection.sendRequest(requestId, action, request, options);\n-            });\n-            transportServices.add(mockTransportService);\n-        }\n-\n-        try {\n-            final String followerIndex = \"follower\";\n-            final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n-            final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n-\n-            final PlainActionFuture<IndexRoutingTable> waitForRestoreInProgress = PlainActionFuture.newFuture();\n-            final ClusterStateListener listener = event -> {\n-                RestoreInProgress restoreInProgress = event.state().custom(RestoreInProgress.TYPE, RestoreInProgress.EMPTY);\n-                if (restoreInProgress != null\n-                    && restoreInProgress.isEmpty() == false\n-                    && event.state().routingTable().hasIndex(followerIndex)) {\n-                    waitForRestoreInProgress.onResponse(event.state().routingTable().index(followerIndex));\n-                }\n-            };\n-            clusterService.addListener(listener);\n+        final Map<Integer, Long> fetchedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        ((InternalSnapshotsInfoService) snapshotsInfoService).addListener((snapshotShard, size) ->\n+            fetchedSnapshotShardSizes.put(snapshotShard.shardId().id(), size));\n \n-            final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n-                .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n-                .renameReplacement(followerIndex)\n-                .masterNodeTimeout(TimeValue.MAX_VALUE)\n-                .indexSettings(Settings.builder()\n-                    .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n-                    .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n-            restoreService.restoreSnapshot(restoreRequest, PlainActionFuture.newFuture());\n+        final String followerIndex = \"follower\";\n+        final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n+        final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n \n-            final IndexRoutingTable indexRoutingTable = waitForRestoreInProgress.get(30L, TimeUnit.SECONDS);\n-            clusterService.removeListener(listener);\n+        final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n+            .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n+            .renameReplacement(followerIndex)\n+            .masterNodeTimeout(TimeValue.MAX_VALUE)\n+            .indexSettings(Settings.builder()\n+                .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n+                .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n \n-            final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n-            assertBusy(() -> {\n-                SnapshotShardSizeInfo snapshotShardSizeInfo = snapshotsInfoService.snapshotShardSizes();\n-                for (int shardId = 0; shardId < numberOfShards; shardId++) {\n-                    Long snapshotShardSize = snapshotShardSizeInfo.getShardSize(indexRoutingTable.shard(shardId).primaryShard());\n-                    assertThat(snapshotShardSize,\n-                        equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n-                }\n-            }, 60L, TimeUnit.SECONDS);\n+        final PlainActionFuture<RestoreInfo> future = PlainActionFuture.newFuture();\n+        restoreService.restoreSnapshot(restoreRequest, waitForRestore(clusterService, future));\n+        future.actionGet();\n \n-            blockCcrRestore.countDown();\n-            ensureFollowerGreen(followerIndex);\n+        ensureFollowerGreen(followerIndex);\n+        assertHitCount(followerClient().prepareSearch(followerIndex).setSize(0).get(), numDocs);\n \n-            assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n-        } finally {\n-            transportServices.forEach(MockTransportService::clearAllRules);\n+        for (int shardId = 0; shardId < numberOfShards; shardId++) {\n+            assertThat(fetchedSnapshotShardSizes.get(shardId),\n+                equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n         }\n+\n+        assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n     }\n \n     public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n         final String leaderIndex = \"leader\";\n-        final int numberOfShards = randomIntBetween(1, 2);\n+        final int numberOfShards = randomIntBetween(1, 3);\n         assertAcked(leaderClient().admin().indices().prepareCreate(leaderIndex)\n             .setSource(getIndexSettings(numberOfShards, 0,\n                 Map.of(Store.INDEX_STORE_STATS_REFRESH_INTERVAL_SETTING.getKey(), TimeValue.ZERO.getStringRep())), XContentType.JSON));\n \n-        final IndexMetadata indexMetadata = leaderClient().admin().cluster().prepareState().setIndices(leaderIndex)\n-            .clear().setMetadata(true).get().getState().metadata().index(leaderIndex);\n+        final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n+        assertThat(snapshotsInfoService, instanceOf(InternalSnapshotsInfoService.class));\n \n-        final AtomicInteger indicesStatsRequestsCount = new AtomicInteger(0);\n-        final CountDownLatch blockCcrRestore = new CountDownLatch(1);\n+        final Map<Integer, Long> fetchedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        final Map<Integer, Exception> failedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        ((InternalSnapshotsInfoService) snapshotsInfoService).addListener(new InternalSnapshotsInfoService.Listener() {\n+            @Override\n+            public void onSuccess(InternalSnapshotsInfoService.SnapshotShard snapshotShard, long size) {\n+                fetchedSnapshotShardSizes.put(snapshotShard.shardId().getId(), size);\n+            }\n+\n+            @Override\n+            public void onFailure(InternalSnapshotsInfoService.SnapshotShard snapshotShard, Exception failure) {\n+                failedSnapshotShardSizes.put(snapshotShard.shardId().getId(), failure);\n+            }\n+        });\n \n         final List<MockTransportService> transportServices = new ArrayList<>();\n-        for (TransportService transportService : getFollowerCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n-            final MockTransportService mockTransportService = (MockTransportService) transportService;\n-            mockTransportService.addSendBehavior((connection, requestId, action, request, options) -> {\n-                if (action.equals(PutCcrRestoreSessionAction.NAME)) {\n-                    try {\n-                        blockCcrRestore.await();\n-                    } catch (InterruptedException e) {\n-                        throw new AssertionError(e);\n-                    }\n-                }\n-                connection.sendRequest(requestId, action, request, options);\n-            });\n-            transportServices.add(mockTransportService);\n-        }\n         for (TransportService transportService : getLeaderCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n             final MockTransportService mockTransportService = (MockTransportService) transportService;\n             mockTransportService.addRequestHandlingBehavior(IndicesStatsAction.NAME, (handler, request, channel, task) -> {\n@@ -616,9 +580,7 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n                         && indicesStatsRequest.search() == false\n                         && indicesStatsRequest.fieldData() == false\n                     ) {\n-                        indicesStatsRequestsCount.incrementAndGet();\n                         channel.sendResponse(new ElasticsearchException(\"simulated\"));\n-                        return;\n                     }\n                 }\n                 handler.messageReceived(request, channel, task);\n@@ -634,6 +596,9 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n             assertThat(repository.getMetadata().type(), equalTo(CcrRepository.TYPE));\n             assertThat(repository.getMetadata().name(), equalTo(leaderCluster));\n \n+            final IndexMetadata indexMetadata = leaderClient().admin().cluster().prepareState().setIndices(leaderIndex)\n+                .clear().setMetadata(true).get().getState().metadata().index(leaderIndex);\n+\n             for (int i = 0; i < numberOfShards; i++) {\n                 final Index index = indexMetadata.getIndex();\n                 final int shardId = i;\n@@ -644,54 +609,40 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n                         new ShardId(index, shardId)));\n                 assertThat(exception.getMessage(), equalTo(\"simulated\"));\n             }\n-            assertThat(indicesStatsRequestsCount.getAndSet(0), equalTo(numberOfShards));\n \n             final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n             final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n \n-            final PlainActionFuture<IndexRoutingTable> waitForRestoreInProgress = PlainActionFuture.newFuture();\n-            final ClusterStateListener listener = event -> {\n-                RestoreInProgress restoreInProgress = event.state().custom(RestoreInProgress.TYPE, RestoreInProgress.EMPTY);\n-                if (restoreInProgress != null\n-                    && restoreInProgress.isEmpty() == false\n-                    && event.state().routingTable().hasIndex(followerIndex)) {\n-                    waitForRestoreInProgress.onResponse(event.state().routingTable().index(followerIndex));\n-                }\n-            };\n-            clusterService.addListener(listener);\n-\n             final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n                 .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n                 .renameReplacement(followerIndex)\n                 .masterNodeTimeout(TimeValue.MAX_VALUE)\n                 .indexSettings(Settings.builder()\n                     .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n                     .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n-            restoreService.restoreSnapshot(restoreRequest, PlainActionFuture.newFuture());\n \n-            final IndexRoutingTable indexRoutingTable = waitForRestoreInProgress.get(30L, TimeUnit.SECONDS);\n-            clusterService.removeListener(listener);\n+            final PlainActionFuture<RestoreInfo> future = PlainActionFuture.newFuture();\n+            restoreService.restoreSnapshot(restoreRequest, waitForRestore(clusterService, future));\n \n-            final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n             assertBusy(() -> {\n-                SnapshotShardSizeInfo snapshotShardSizeInfo = snapshotsInfoService.snapshotShardSizes();\n-                for (int shardId = 0; shardId < numberOfShards; shardId++) {\n-                    final ShardRouting primary = indexRoutingTable.shard(shardId).primaryShard();\n-                    assertThat(snapshotShardSizeInfo.getShardSize(primary), equalTo(ShardRouting.UNAVAILABLE_EXPECTED_SHARD_SIZE));\n-                    final long randomSize = randomNonNegativeLong();\n-                    assertThat(snapshotShardSizeInfo.getShardSize(primary, randomSize), equalTo(randomSize));\n+                for (int i = 0; i < numberOfShards; i++) {\n+                    assertThat(failedSnapshotShardSizes.get(i), notNullValue());\n+                    assertThat(failedSnapshotShardSizes.get(i).getMessage(), containsString(\"simulated\"));\n                 }\n-            }, 60L, TimeUnit.SECONDS);\n+            });\n         } finally {\n             transportServices.forEach(MockTransportService::clearAllRules);\n         }\n \n-        assertThat(indicesStatsRequestsCount.get(), equalTo(numberOfShards));\n-        blockCcrRestore.countDown();\n-\n-        followerClient().admin().cluster().prepareReroute().get();\n+        followerClient().admin().cluster().prepareReroute().setMasterNodeTimeout(TimeValue.MAX_VALUE).get();\n         ensureFollowerGreen(followerIndex);\n \n+        assertBusy(() -> {\n+            for (int i = 0; i < numberOfShards; i++) {\n+                assertThat(fetchedSnapshotShardSizes.get(i), greaterThan(0L));\n+            }\n+        });\n+\n         assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n     }\n ",
      "diff": [
        {
          "file": "server/src/main/java/org/elasticsearch/snapshots/InternalSnapshotsInfoService.java",
          "patch": "@@ -49,9 +49,11 @@\n import java.util.HashSet;\n import java.util.LinkedHashSet;\n import java.util.LinkedList;\n+import java.util.List;\n import java.util.Objects;\n import java.util.Queue;\n import java.util.Set;\n+import java.util.concurrent.CopyOnWriteArrayList;\n import java.util.function.Supplier;\n \n public class InternalSnapshotsInfoService implements ClusterStateListener, SnapshotsInfoService {\n@@ -67,9 +69,17 @@ public class InternalSnapshotsInfoService implements ClusterStateListener, Snaps\n         e -> logger.debug(\"reroute after snapshot shard size update failed\", e)\n     );\n \n+    public interface Listener {\n+        void onSuccess(SnapshotShard snapshotShard, long size);\n+\n+        default void onFailure(SnapshotShard snapshotShard, Exception failure) {\n+        };\n+    }\n+\n     private final ThreadPool threadPool;\n     private final Supplier<RepositoriesService> repositoriesService;\n     private final Supplier<RerouteService> rerouteService;\n+    private final List<Listener> listeners;\n \n     /** contains the snapshot shards for which the size is known **/\n     private volatile ImmutableOpenMap<SnapshotShard, Long> knownSnapshotShardSizes;\n@@ -99,6 +109,7 @@ public InternalSnapshotsInfoService(\n         this.threadPool = clusterService.getClusterApplierService().threadPool();\n         this.repositoriesService = repositoriesServiceSupplier;\n         this.rerouteService = rerouteServiceSupplier;\n+        this.listeners = new CopyOnWriteArrayList<>();\n         this.knownSnapshotShardSizes = ImmutableOpenMap.of();\n         this.unknownSnapshotShards  = new LinkedHashSet<>();\n         this.failedSnapshotShards  = new LinkedHashSet<>();\n@@ -109,6 +120,8 @@ public InternalSnapshotsInfoService(\n         final ClusterSettings clusterSettings = clusterService.getClusterSettings();\n         clusterSettings.addSettingsUpdateConsumer(INTERNAL_SNAPSHOT_INFO_MAX_CONCURRENT_FETCHES_SETTING, this::setMaxConcurrentFetches);\n         if (DiscoveryNode.isMasterNode(settings)) {\n+            addListener((snapshotShard, size) ->\n+                rerouteService.get().reroute(\"snapshot shard size updated\", Priority.HIGH, REROUTE_LISTENER));\n             clusterService.addListener(this);\n         }\n     }\n@@ -117,6 +130,11 @@ private void setMaxConcurrentFetches(Integer maxConcurrentFetches) {\n         this.maxConcurrentFetches = maxConcurrentFetches;\n     }\n \n+    public void addListener(final Listener listener) {\n+        final boolean added = listeners.add(listener);\n+        assert added : \"cannot add listener twice: \" + listener;\n+    }\n+\n     @Override\n     public SnapshotShardSizeInfo snapshotShardSizes() {\n         synchronized (mutex){\n@@ -237,24 +255,28 @@ protected void doRun() throws Exception {\n                 assert invariant();\n             }\n             if (updated) {\n-                rerouteService.get().reroute(\"snapshot shard size updated\", Priority.HIGH, REROUTE_LISTENER);\n+                listeners.forEach(listener -> listener.onSuccess(snapshotShard, snapshotShardSize));\n             }\n         }\n \n         @Override\n         public void onFailure(Exception e) {\n             logger.warn(() -> new ParameterizedMessage(\"failed to retrieve shard size for {}\", snapshotShard), e);\n+            boolean updated = false;\n             synchronized (mutex) {\n                 if (isMaster) {\n-                    final boolean added = failedSnapshotShards.add(snapshotShard);\n-                    assert added : \"snapshot shard size already failed for \" + snapshotShard;\n+                    updated = failedSnapshotShards.add(snapshotShard);\n+                    assert updated : \"snapshot shard size already failed for \" + snapshotShard;\n                 }\n                 if (removed == false) {\n                     unknownSnapshotShards.remove(snapshotShard);\n                 }\n                 activeFetches -= 1;\n                 assert invariant();\n             }\n+            if (updated) {\n+                listeners.forEach(listener -> listener.onFailure(snapshotShard, e));\n+            }\n         }\n \n         @Override"
        },
        {
          "file": "x-pack/plugin/ccr/src/internalClusterTest/java/org/elasticsearch/xpack/ccr/CcrRepositoryIT.java",
          "patch": "@@ -23,12 +23,8 @@\n import org.elasticsearch.action.index.IndexRequest;\n import org.elasticsearch.action.support.IndicesOptions;\n import org.elasticsearch.action.support.PlainActionFuture;\n-import org.elasticsearch.cluster.ClusterStateListener;\n-import org.elasticsearch.cluster.RestoreInProgress;\n import org.elasticsearch.cluster.metadata.IndexMetadata;\n import org.elasticsearch.cluster.metadata.MappingMetadata;\n-import org.elasticsearch.cluster.routing.IndexRoutingTable;\n-import org.elasticsearch.cluster.routing.ShardRouting;\n import org.elasticsearch.cluster.service.ClusterService;\n import org.elasticsearch.common.settings.Settings;\n import org.elasticsearch.common.unit.ByteSizeValue;\n@@ -43,10 +39,10 @@\n import org.elasticsearch.repositories.RepositoriesService;\n import org.elasticsearch.repositories.Repository;\n import org.elasticsearch.repositories.RepositoryMissingException;\n+import org.elasticsearch.snapshots.InternalSnapshotsInfoService;\n import org.elasticsearch.snapshots.RestoreInfo;\n import org.elasticsearch.snapshots.RestoreService;\n import org.elasticsearch.snapshots.SnapshotId;\n-import org.elasticsearch.snapshots.SnapshotShardSizeInfo;\n import org.elasticsearch.snapshots.SnapshotsInfoService;\n import org.elasticsearch.test.transport.MockTransportService;\n import org.elasticsearch.transport.TransportActionProxy;\n@@ -63,13 +59,15 @@\n import java.util.List;\n import java.util.Locale;\n import java.util.Map;\n+import java.util.concurrent.ConcurrentHashMap;\n import java.util.concurrent.CountDownLatch;\n import java.util.concurrent.TimeUnit;\n import java.util.concurrent.atomic.AtomicBoolean;\n-import java.util.concurrent.atomic.AtomicInteger;\n \n import static org.elasticsearch.test.hamcrest.ElasticsearchAssertions.assertAcked;\n+import static org.elasticsearch.test.hamcrest.ElasticsearchAssertions.assertHitCount;\n import static org.hamcrest.Matchers.aMapWithSize;\n+import static org.hamcrest.Matchers.containsString;\n import static org.hamcrest.Matchers.equalTo;\n import static org.hamcrest.Matchers.greaterThan;\n import static org.hamcrest.Matchers.instanceOf;\n@@ -465,12 +463,12 @@ public void testFollowerMappingIsUpdated() throws IOException {\n \n     public void testCcrRepositoryFetchesSnapshotShardSizeFromIndexShardStoreStats() throws Exception {\n         final String leaderIndex = \"leader\";\n-        final int numberOfShards = randomIntBetween(1, 2);\n+        final int numberOfShards = randomIntBetween(1, 3);\n         assertAcked(leaderClient().admin().indices().prepareCreate(leaderIndex)\n             .setSource(getIndexSettings(numberOfShards, 0,\n                 Map.of(Store.INDEX_STORE_STATS_REFRESH_INTERVAL_SETTING.getKey(), TimeValue.ZERO.getStringRep())), XContentType.JSON));\n \n-        final int numDocs = scaledRandomIntBetween(0, 1_000);\n+        final int numDocs = scaledRandomIntBetween(0, 100);\n         if (numDocs > 0) {\n             final BulkRequestBuilder bulkRequest = leaderClient().prepareBulk(leaderIndex);\n             for (int i = 0; i < numDocs; i++) {\n@@ -513,99 +511,65 @@ public void testCcrRepositoryFetchesSnapshotShardSizeFromIndexShardStoreStats()\n                 equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n         }\n \n-        final CountDownLatch blockCcrRestore = new CountDownLatch(1);\n+        final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n+        assertThat(snapshotsInfoService, instanceOf(InternalSnapshotsInfoService.class));\n \n-        final List<MockTransportService> transportServices = new ArrayList<>();\n-        for (TransportService transportService : getFollowerCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n-            final MockTransportService mockTransportService = (MockTransportService) transportService;\n-            mockTransportService.addSendBehavior((connection, requestId, action, request, options) -> {\n-                if (action.equals(PutCcrRestoreSessionAction.NAME)) {\n-                    try {\n-                        blockCcrRestore.await();\n-                    } catch (InterruptedException e) {\n-                        throw new AssertionError(e);\n-                    }\n-                }\n-                connection.sendRequest(requestId, action, request, options);\n-            });\n-            transportServices.add(mockTransportService);\n-        }\n-\n-        try {\n-            final String followerIndex = \"follower\";\n-            final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n-            final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n-\n-            final PlainActionFuture<IndexRoutingTable> waitForRestoreInProgress = PlainActionFuture.newFuture();\n-            final ClusterStateListener listener = event -> {\n-                RestoreInProgress restoreInProgress = event.state().custom(RestoreInProgress.TYPE, RestoreInProgress.EMPTY);\n-                if (restoreInProgress != null\n-                    && restoreInProgress.isEmpty() == false\n-                    && event.state().routingTable().hasIndex(followerIndex)) {\n-                    waitForRestoreInProgress.onResponse(event.state().routingTable().index(followerIndex));\n-                }\n-            };\n-            clusterService.addListener(listener);\n+        final Map<Integer, Long> fetchedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        ((InternalSnapshotsInfoService) snapshotsInfoService).addListener((snapshotShard, size) ->\n+            fetchedSnapshotShardSizes.put(snapshotShard.shardId().id(), size));\n \n-            final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n-                .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n-                .renameReplacement(followerIndex)\n-                .masterNodeTimeout(TimeValue.MAX_VALUE)\n-                .indexSettings(Settings.builder()\n-                    .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n-                    .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n-            restoreService.restoreSnapshot(restoreRequest, PlainActionFuture.newFuture());\n+        final String followerIndex = \"follower\";\n+        final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n+        final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n \n-            final IndexRoutingTable indexRoutingTable = waitForRestoreInProgress.get(30L, TimeUnit.SECONDS);\n-            clusterService.removeListener(listener);\n+        final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n+            .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n+            .renameReplacement(followerIndex)\n+            .masterNodeTimeout(TimeValue.MAX_VALUE)\n+            .indexSettings(Settings.builder()\n+                .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n+                .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n \n-            final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n-            assertBusy(() -> {\n-                SnapshotShardSizeInfo snapshotShardSizeInfo = snapshotsInfoService.snapshotShardSizes();\n-                for (int shardId = 0; shardId < numberOfShards; shardId++) {\n-                    Long snapshotShardSize = snapshotShardSizeInfo.getShardSize(indexRoutingTable.shard(shardId).primaryShard());\n-                    assertThat(snapshotShardSize,\n-                        equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n-                }\n-            }, 60L, TimeUnit.SECONDS);\n+        final PlainActionFuture<RestoreInfo> future = PlainActionFuture.newFuture();\n+        restoreService.restoreSnapshot(restoreRequest, waitForRestore(clusterService, future));\n+        future.actionGet();\n \n-            blockCcrRestore.countDown();\n-            ensureFollowerGreen(followerIndex);\n+        ensureFollowerGreen(followerIndex);\n+        assertHitCount(followerClient().prepareSearch(followerIndex).setSize(0).get(), numDocs);\n \n-            assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n-        } finally {\n-            transportServices.forEach(MockTransportService::clearAllRules);\n+        for (int shardId = 0; shardId < numberOfShards; shardId++) {\n+            assertThat(fetchedSnapshotShardSizes.get(shardId),\n+                equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n         }\n+\n+        assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n     }\n \n     public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n         final String leaderIndex = \"leader\";\n-        final int numberOfShards = randomIntBetween(1, 2);\n+        final int numberOfShards = randomIntBetween(1, 3);\n         assertAcked(leaderClient().admin().indices().prepareCreate(leaderIndex)\n             .setSource(getIndexSettings(numberOfShards, 0,\n                 Map.of(Store.INDEX_STORE_STATS_REFRESH_INTERVAL_SETTING.getKey(), TimeValue.ZERO.getStringRep())), XContentType.JSON));\n \n-        final IndexMetadata indexMetadata = leaderClient().admin().cluster().prepareState().setIndices(leaderIndex)\n-            .clear().setMetadata(true).get().getState().metadata().index(leaderIndex);\n+        final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n+        assertThat(snapshotsInfoService, instanceOf(InternalSnapshotsInfoService.class));\n \n-        final AtomicInteger indicesStatsRequestsCount = new AtomicInteger(0);\n-        final CountDownLatch blockCcrRestore = new CountDownLatch(1);\n+        final Map<Integer, Long> fetchedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        final Map<Integer, Exception> failedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        ((InternalSnapshotsInfoService) snapshotsInfoService).addListener(new InternalSnapshotsInfoService.Listener() {\n+            @Override\n+            public void onSuccess(InternalSnapshotsInfoService.SnapshotShard snapshotShard, long size) {\n+                fetchedSnapshotShardSizes.put(snapshotShard.shardId().getId(), size);\n+            }\n+\n+            @Override\n+            public void onFailure(InternalSnapshotsInfoService.SnapshotShard snapshotShard, Exception failure) {\n+                failedSnapshotShardSizes.put(snapshotShard.shardId().getId(), failure);\n+            }\n+        });\n \n         final List<MockTransportService> transportServices = new ArrayList<>();\n-        for (TransportService transportService : getFollowerCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n-            final MockTransportService mockTransportService = (MockTransportService) transportService;\n-            mockTransportService.addSendBehavior((connection, requestId, action, request, options) -> {\n-                if (action.equals(PutCcrRestoreSessionAction.NAME)) {\n-                    try {\n-                        blockCcrRestore.await();\n-                    } catch (InterruptedException e) {\n-                        throw new AssertionError(e);\n-                    }\n-                }\n-                connection.sendRequest(requestId, action, request, options);\n-            });\n-            transportServices.add(mockTransportService);\n-        }\n         for (TransportService transportService : getLeaderCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n             final MockTransportService mockTransportService = (MockTransportService) transportService;\n             mockTransportService.addRequestHandlingBehavior(IndicesStatsAction.NAME, (handler, request, channel, task) -> {\n@@ -616,9 +580,7 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n                         && indicesStatsRequest.search() == false\n                         && indicesStatsRequest.fieldData() == false\n                     ) {\n-                        indicesStatsRequestsCount.incrementAndGet();\n                         channel.sendResponse(new ElasticsearchException(\"simulated\"));\n-                        return;\n                     }\n                 }\n                 handler.messageReceived(request, channel, task);\n@@ -634,6 +596,9 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n             assertThat(repository.getMetadata().type(), equalTo(CcrRepository.TYPE));\n             assertThat(repository.getMetadata().name(), equalTo(leaderCluster));\n \n+            final IndexMetadata indexMetadata = leaderClient().admin().cluster().prepareState().setIndices(leaderIndex)\n+                .clear().setMetadata(true).get().getState().metadata().index(leaderIndex);\n+\n             for (int i = 0; i < numberOfShards; i++) {\n                 final Index index = indexMetadata.getIndex();\n                 final int shardId = i;\n@@ -644,54 +609,40 @@ public void testCcrRepositoryFailsToFetchSnapshotShardSizes() throws Exception {\n                         new ShardId(index, shardId)));\n                 assertThat(exception.getMessage(), equalTo(\"simulated\"));\n             }\n-            assertThat(indicesStatsRequestsCount.getAndSet(0), equalTo(numberOfShards));\n \n             final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n             final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n \n-            final PlainActionFuture<IndexRoutingTable> waitForRestoreInProgress = PlainActionFuture.newFuture();\n-            final ClusterStateListener listener = event -> {\n-                RestoreInProgress restoreInProgress = event.state().custom(RestoreInProgress.TYPE, RestoreInProgress.EMPTY);\n-                if (restoreInProgress != null\n-                    && restoreInProgress.isEmpty() == false\n-                    && event.state().routingTable().hasIndex(followerIndex)) {\n-                    waitForRestoreInProgress.onResponse(event.state().routingTable().index(followerIndex));\n-                }\n-            };\n-            clusterService.addListener(listener);\n-\n             final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n                 .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n                 .renameReplacement(followerIndex)\n                 .masterNodeTimeout(TimeValue.MAX_VALUE)\n                 .indexSettings(Settings.builder()\n                     .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n                     .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n-            restoreService.restoreSnapshot(restoreRequest, PlainActionFuture.newFuture());\n \n-            final IndexRoutingTable indexRoutingTable = waitForRestoreInProgress.get(30L, TimeUnit.SECONDS);\n-            clusterService.removeListener(listener);\n+            final PlainActionFuture<RestoreInfo> future = PlainActionFuture.newFuture();\n+            restoreService.restoreSnapshot(restoreRequest, waitForRestore(clusterService, future));\n \n-            final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n             assertBusy(() -> {\n-                SnapshotShardSizeInfo snapshotShardSizeInfo = snapshotsInfoService.snapshotShardSizes();\n-                for (int shardId = 0; shardId < numberOfShards; shardId++) {\n-                    final ShardRouting primary = indexRoutingTable.shard(shardId).primaryShard();\n-                    assertThat(snapshotShardSizeInfo.getShardSize(primary), equalTo(ShardRouting.UNAVAILABLE_EXPECTED_SHARD_SIZE));\n-                    final long randomSize = randomNonNegativeLong();\n-                    assertThat(snapshotShardSizeInfo.getShardSize(primary, randomSize), equalTo(randomSize));\n+                for (int i = 0; i < numberOfShards; i++) {\n+                    assertThat(failedSnapshotShardSizes.get(i), notNullValue());\n+                    assertThat(failedSnapshotShardSizes.get(i).getMessage(), containsString(\"simulated\"));\n                 }\n-            }, 60L, TimeUnit.SECONDS);\n+            });\n         } finally {\n             transportServices.forEach(MockTransportService::clearAllRules);\n         }\n \n-        assertThat(indicesStatsRequestsCount.get(), equalTo(numberOfShards));\n-        blockCcrRestore.countDown();\n-\n-        followerClient().admin().cluster().prepareReroute().get();\n+        followerClient().admin().cluster().prepareReroute().setMasterNodeTimeout(TimeValue.MAX_VALUE).get();\n         ensureFollowerGreen(followerIndex);\n \n+        assertBusy(() -> {\n+            for (int i = 0; i < numberOfShards; i++) {\n+                assertThat(fetchedSnapshotShardSizes.get(i), greaterThan(0L));\n+            }\n+        });\n+\n         assertAcked(followerClient().admin().indices().prepareDelete(followerIndex).setMasterNodeTimeout(TimeValue.MAX_VALUE));\n     }\n "
        }
      ]
    },
    {
      "type": "description",
      "created_at": "2020-11-10T15:36:07Z",
      "user": "tlrx",
      "body": "This pull request introduces a change in `InternalSnapshotsInfoService` that allows listeners to register themselves to be notified when fetching the size of a snapshotted shard is a success or a failure.\r\n\r\nThe motivation is to help and to simplify tests like `CcrRepositoryIT` which are verifying that fetching the size from the remote leader cluster is successful (or not). Because the sizes of the snapshot shards are only kept in a very short period of time the test cannot rely on analyzing the cluster state and querying the `SnapshotsInfoService`, and this can trigger failures like #64167.\r\n\r\nCloses #64167"
    },
    {
      "type": "comment",
      "id": 724780954,
      "created_at": "2020-11-10T15:36:09Z",
      "user": "elasticmachine",
      "body": "Pinging @elastic/es-distributed (:Distributed/CCR)"
    },
    {
      "type": "review_comment",
      "id": 520763926,
      "created_at": "2020-11-10T18:03:28Z",
      "user": "original-brownbear",
      "body": "Couldn't we run this busy assert on the CS thread as soon as we observe the restore/recovery there? That way the recovery is not removed concurrently and we get a consistent view of the `InternalSnapshotsInfoService` without having to complicate production code?",
      "path": "x-pack/plugin/ccr/src/internalClusterTest/java/org/elasticsearch/xpack/ccr/CcrRepositoryIT.java",
      "diff_hunk": "@@ -513,99 +511,65 @@ public void testCcrRepositoryFetchesSnapshotShardSizeFromIndexShardStoreStats()\n                 equalTo(indexStats.getIndexShards().get(shardId).getPrimary().getStore().getSizeInBytes()));\n         }\n \n-        final CountDownLatch blockCcrRestore = new CountDownLatch(1);\n+        final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n+        assertThat(snapshotsInfoService, instanceOf(InternalSnapshotsInfoService.class));\n \n-        final List<MockTransportService> transportServices = new ArrayList<>();\n-        for (TransportService transportService : getFollowerCluster().getDataOrMasterNodeInstances(TransportService.class)) {\n-            final MockTransportService mockTransportService = (MockTransportService) transportService;\n-            mockTransportService.addSendBehavior((connection, requestId, action, request, options) -> {\n-                if (action.equals(PutCcrRestoreSessionAction.NAME)) {\n-                    try {\n-                        blockCcrRestore.await();\n-                    } catch (InterruptedException e) {\n-                        throw new AssertionError(e);\n-                    }\n-                }\n-                connection.sendRequest(requestId, action, request, options);\n-            });\n-            transportServices.add(mockTransportService);\n-        }\n-\n-        try {\n-            final String followerIndex = \"follower\";\n-            final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n-            final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n-\n-            final PlainActionFuture<IndexRoutingTable> waitForRestoreInProgress = PlainActionFuture.newFuture();\n-            final ClusterStateListener listener = event -> {\n-                RestoreInProgress restoreInProgress = event.state().custom(RestoreInProgress.TYPE, RestoreInProgress.EMPTY);\n-                if (restoreInProgress != null\n-                    && restoreInProgress.isEmpty() == false\n-                    && event.state().routingTable().hasIndex(followerIndex)) {\n-                    waitForRestoreInProgress.onResponse(event.state().routingTable().index(followerIndex));\n-                }\n-            };\n-            clusterService.addListener(listener);\n+        final Map<Integer, Long> fetchedSnapshotShardSizes = new ConcurrentHashMap<>();\n+        ((InternalSnapshotsInfoService) snapshotsInfoService).addListener((snapshotShard, size) ->\n+            fetchedSnapshotShardSizes.put(snapshotShard.shardId().id(), size));\n \n-            final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n-                .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n-                .renameReplacement(followerIndex)\n-                .masterNodeTimeout(TimeValue.MAX_VALUE)\n-                .indexSettings(Settings.builder()\n-                    .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n-                    .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n-            restoreService.restoreSnapshot(restoreRequest, PlainActionFuture.newFuture());\n+        final String followerIndex = \"follower\";\n+        final RestoreService restoreService = getFollowerCluster().getCurrentMasterNodeInstance(RestoreService.class);\n+        final ClusterService clusterService = getFollowerCluster().getCurrentMasterNodeInstance(ClusterService.class);\n \n-            final IndexRoutingTable indexRoutingTable = waitForRestoreInProgress.get(30L, TimeUnit.SECONDS);\n-            clusterService.removeListener(listener);\n+        final RestoreSnapshotRequest restoreRequest = new RestoreSnapshotRequest(leaderCluster, CcrRepository.LATEST)\n+            .indices(leaderIndex).indicesOptions(indicesOptions).renamePattern(\"^(.*)$\")\n+            .renameReplacement(followerIndex)\n+            .masterNodeTimeout(TimeValue.MAX_VALUE)\n+            .indexSettings(Settings.builder()\n+                .put(IndexMetadata.SETTING_INDEX_PROVIDED_NAME, followerIndex)\n+                .put(CcrSettings.CCR_FOLLOWING_INDEX_SETTING.getKey(), true));\n \n-            final SnapshotsInfoService snapshotsInfoService = getFollowerCluster().getCurrentMasterNodeInstance(SnapshotsInfoService.class);\n-            assertBusy(() -> {\n-                SnapshotShardSizeInfo snapshotShardSizeInfo = snapshotsInfoService.snapshotShardSizes();",
      "start_line": null,
      "original_start_line": null,
      "start_side": null,
      "line": 564,
      "original_line": 564,
      "side": "LEFT",
      "original_position": 124,
      "position": 124,
      "subject_type": "line",
      "reply": [
        {
          "id": 521957015,
          "user": "tlrx",
          "body": "I should have thought about it myself, it seems obvious, there's not need to complexify production like I did. Thanks Armin!",
          "created_at": "2020-11-12T09:23:37Z"
        }
      ]
    },
    {
      "type": "review",
      "id": 527468878,
      "created_at": "2020-11-10T18:04:22Z",
      "user": "original-brownbear",
      "state": "COMMENTED",
      "body": "Thanks for tracking this down Tanguy!\r\n\r\nOne question/suggestion about an alternative solution that might be simpler :)\r\n"
    },
    {
      "type": "review",
      "id": 528894278,
      "created_at": "2020-11-12T09:23:37Z",
      "user": "tlrx",
      "state": "COMMENTED",
      "body": ""
    },
    {
      "type": "comment",
      "id": 725954700,
      "created_at": "2020-11-12T09:23:53Z",
      "user": "tlrx",
      "body": "Closed in favor of #64976"
    }
  ]
}